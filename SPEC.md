# Pre-Launch Refactor Spec

> **Status:** Implemented — data layer + extraction shipped 2026-04-29; module
> carve, strict types, Vite, and production cutover (Cloudflare Pages)
> completed 2026-05-02. § 13 lives on as the project-wide decisions log.
> **Author:** Adam Denney (AI-assisted)
> **Date:** written 2026-04-28 · body reconciled with shipped reality 2026-06-11

This spec covered the structural work done **before launch**: moving app
data off localStorage into Postgres, splitting the god file into modules, and
adding a build step. §§ 1–12 are the historical contract, now annotated with
what actually shipped (including deviations). It does not cover post-launch
features — those live in `PLAN.md` and accrue entries in § 13.

Companion docs:
- `PLAN.md` — central planning doc (current work, sub-steps, status)
- `SCHEMA.md` — full database schema (drafted 2026-04-27; source of truth)
- `index.html` — now an ~840-line markup shell; the ~11,400-line god file this
  spec describes was split into `src/*.js` (see § 10 / § 13)

---

## 1. Problem

ProCabinet works as a prototype but is structured for throwaway code, not for
launch:

- **Data is split between Supabase and localStorage**, with most app data
  (sheets, pieces, edge bands, cabinets, quote lines, catalogs, biz info) in
  localStorage. This means: no multi-device sync, no backups, no real reports,
  no path to teams.
- **The codebase is one 11,430-line `index.html`** with 408 functions, 369
  inline event handlers, and ~30 top-level mutable globals. Every change
  touches the same file; every typo can break unrelated features.
- **Two parallel subsystems exist** (Cabinet Builder + Cabinet Quote) with
  overlapping schemas — clarity debt that will only compound.

> *(Historical — describes the app as of 2026-04-28. All three problems were
> resolved: data lives in Postgres, the god file is split into `src/*.js`, and
> the two cabinet subsystems were unified post-launch — § 13, 2026-05-05.)*

---

## 2. Why now

- **Pre-launch is the only free window for schema changes.** Once real users
  have real data, every column rename is a migration with risk.
- **The god file's pain compounds per feature.** Every new feature added now
  is one more thing to detangle later.
- **AI-assisted iteration is materially faster on modular code** — the
  workflow you already use benefits directly from this refactor.

> *(As it played out: the refactor ran 2026-04-28 → 2026-05-02, immediately
> before go-live, and the "only free window" bet held — every schema change
> since launch has been an additive migration.)*

---

## 3. Goals (in priority order)

1. **All app data persists in Postgres** — zero functional data in
   localStorage (UI prefs only).
2. **Code is split into feature modules** under ~1000 lines each, no god file.
3. **A new feature can be prototyped in one file** without touching the rest
   of the app.
4. **The app supports cross-project queries** ("most-used cabinet sizes",
   "edge banding consumed YTD") via real SQL.
5. **Deployment works end-to-end** on a real domain with auth, with no manual
   localStorage seeding.

> *(All five delivered — see § 7 for verification. #2's "~1000 lines each"
> was relaxed in practice; see the file-size note in § 7.)*

---

## 4. Non-goals (explicit)

These are tempting but **out of scope**:

- Switching to React / Vue / Svelte (vanilla JS stays)
- Adding new features during the refactor
- Writing tests retroactively (smoke testing only)
- Multi-user / team support (single-user-per-account stays)
- Mobile native app
- Rewriting cut-list optimization algorithm
- Public API or third-party integrations
- Perfect code — "good enough to launch" is the bar

> *(All held through the refactor and launch. Post-launch, several were
> deliberately revisited as features, not scope creep: Builder/Quote
> unification (2026-05-05), nested CNC cut optimization (2026-05-25),
> QuickBooks/Xero + Stripe integrations (2026-05-30 / 2026-06-09) — § 13.
> Still standing: vanilla JS, no tests, single-user accounts, no native app.)*

---

## 5. Constraints

| Constraint | Value (as shipped) |
|---|---|
| Time budget | Actual: ~5 days — 2026-04-28 → 2026-05-02 production cutover |
| Tech stack | Vanilla JS, Supabase Postgres, no framework |
| Build step | Yes — Vite (added 2026-04-30, reversing the initial defer; § 13) |
| Downtime tolerance | Was pre-launch/unlimited; moot since the 2026-05-02 cutover |
| Solo or assisted | Solo + AI-assisted |
| Browser support | Modern evergreen; mobile Safari via the responsive pass (2026-05-23) |
| Hosting | Cloudflare Pages (`procabinet.app`), auto-deploy from `main` — not the Netlify option PLAN.md originally floated |

---

## 6. Scope

### In scope

- [x] Database schema redesign (per `SCHEMA.md`) — 2026-04-28
- [x] Migrate all localStorage data to Postgres tables — 2026-04-28
- [x] Extract CSS to `styles.css` — 2026-04-28
- [x] Split JS under `src/` — completed 2026-05-01 as a **classic-script
      carve**, not ES modules (deliberate deviation, § 13 2026-04-30; only
      `main.js` is a module)
- [x] `window.X` exports for inline handlers — moot under the classic-script
      split: functions stay in the shared global lexical environment, typed
      via `src/globals.d.ts`
- [x] ~~Convert global mutable state to a shared `state` object~~ — dropped;
      strict-mode JSDoc typing over cross-file globals took its place
      (Item 3 phases F/G, § 13)
- [x] Smoke test every feature — per-phase, `docs/SMOKE_TESTS.md`
- [x] Deploy to production domain — 2026-05-02, `https://procabinet.app`

### Optional / stretch

- [x] Add Vite (build step, HMR) — 2026-04-30 (Item 3 phases A/B/D)
- [x] Supabase row types + JSDoc type-checking — full `strict: true` across
      all src files by 2026-05-02 (Item 3 phases F/G)
- [x] Move logo from base64 to Supabase Storage — `business-assets/` bucket,
      2026-04-28 (base64 localStorage copy kept as legacy fallback)
- [x] Unify Cabinet Builder + Cabinet Quote — post-launch, 2026-05-03 → 05-05
      (Item 2; `cq*` → `cb*` rename 2026-05-02)

### Explicitly deferred

- Tests — still deferred; `npm run typecheck` is the only automated gate
- Performance optimization — picked up 2026-06-11 once boot size mattered
  (boot bundle 1.24 MB → 652 KB; § 13)
- Real-time collaboration — still deferred; Supabase realtime is used for
  live-link status sync since 2026-06-09, which is not collaboration

---

## 7. Success criteria

The refactor is done when **all of these are true** (re-verified 2026-06-11):

- [x] `grep -r "localStorage.setItem" src/` returns only UI-pref keys — *with
      known exceptions*: the deliberate dual-write / legacy-fallback net still
      writes `pc_cl_pieces`/`pc_cl_sheets` (`_saveCutList`),
      `pc_order_prodstarts`, `pc_stock_libraries`, and `pc_biz_logo`.
      Postgres is the source of truth; these are write-only safety copies
- [x] `index.html` contains no inline `<script>` block — as shipped it's one
      `<script type="module">` bridge + ~40 `<script defer src="/src/*.js">`
      classic tags (deliberate deviation from "ES modules only", § 13
      2026-04-30)
- [x] `index.html` contains no `<style>` block (only `<link rel="stylesheet">`)
- [ ] ~~No single source file exceeds 1500 lines~~ — **not met, consciously
      relaxed**: `cutlist.js` 5,167 · `app.js` 2,194 · `quotes.js` 1,848 ·
      `cabinet.js` 1,825 (counts 2026-06-11; remaining files are under)
- [x] Manual smoke test passes (see § 11)
- [x] A fresh browser (incognito) can sign up, create a client, project, cut
      list, edge band, quote, and order — and refreshing preserves everything
      (in production use since 2026-05-02, including real customer signups)
- [x] Opening the app on a second device shows the same data — DB-backed,
      verified in daily cross-device use
- [x] At least one cross-project SQL query runs successfully — verified
      2026-06-11: `quotes × quote_lines` per-status aggregate with distinct
      client counts runs in Supabase SQL
- [x] Production deploy is reachable at the public URL with HTTPS —
      `https://procabinet.app` since 2026-05-02

---

## 8. Failure modes

What could go wrong, and how you'd know:

| Failure mode | Signal | Mitigation |
|---|---|---|
| Scope creep — refactor becomes a rewrite | More than 2× over time budget | Re-read § 4; cut features |
| Data loss migrating localStorage to DB | Smoke test reveals missing data | Export-to-JSON tool first; keep localStorage as fallback for one release |
| Module split breaks inline handlers silently | Buttons do nothing in production | Smoke test every clickable element; grep `onclick=` and verify each function is on `window` |
| State migration introduces stale-reference bugs | UI shows old data after edits | Audit reads of mutated state; rely on render-after-write pattern |
| Half-done state — half migrated, app inconsistent | App works in dev, breaks under real use | Land each phase as its own commit; never merge a half-finished phase |
| Hidden cross-feature coupling discovered mid-refactor | Module A fails because of Module B | Keep `app.js` checkpoint commit so you can bail to single-file if needed |
| Vite setup eats a day | Build won't deploy | Time-box Vite to 4 hours; if not working, defer |
| Burnout / abandonment | You stop after phase 1 | Front-load DB normalization (the irreversible one); accept that "modules later" is a valid stop |
| Schema design has gaps revealed by code | Need to add columns mid-build | Keep migrations additive; never destructive until every feature uses the new shape |
| Logo / image data too large for jsonb | Insert fails or quota errors | Move to Supabase Storage from day one |

> *(None of these materialized as launch blockers. The closest calls: the
> dual-write safety net was kept rather than removed — see § 7 — and schema
> gaps did surface post-launch, handled as additive migrations per the
> mitigation, e.g. the `order_lines` parity fix, § 13 2026-06-10.)*

---

## 9. Open questions (resolve before starting)

All resolved — outcomes below, details in § 13 under the dates given.

- [x] **Cabinet systems**: deferred at refactor time (2026-04-28), then
      unified post-launch as Item 2 (2026-05-03 → 05-05); `cq*` → `cb*`
      rename 2026-05-02
- [x] **Catalog storage**: ~~Per-user tables (`materials_catalog`) or single
      jsonb column on `business_info`?~~ **Resolved** — single unified
      `catalog_items` table with a `type` column (`material` | `handle` |
      `finish` | `hardware`). Type-specific extras live in a `specs jsonb`
      field on the row. See `SCHEMA.md` § 3.2.
- [x] **Logo**: Supabase Storage — `business-assets/` bucket, URL on
      `business_info.logo_url` (2026-04-28); base64 copy kept as fallback
- [x] **Quote sources**: `cqSavedQuotes` dropped in the Phase 7 destructive
      cleanup (2026-04-29)
- [x] **Vite**: deferred 2026-04-28, reversed 2026-04-30 — in (dev server,
      build, env injection)
- [x] **Types**: JSDoc + `checkJs` under full `strict: true`, no `.ts` rename
      (Item 3 phases F/G, 2026-05-01 → 05-02)
- [x] **Migration approach**: phased per-feature, with DB + localStorage dual
      writes as the safety net (2026-04-28)
- [x] **Existing data**: exported — Phase 0 snapshot
      `Building Docs/snapshots/2026-04-28-prelaunch.json` + `_exportLocal()`
- [x] **Smoke test list**: written — `docs/SMOKE_TESTS.md` (Phase 0,
      2026-04-28)

---

## 10. Phases

High-level only — granular task list goes in `PLAN.md` or a new `REFACTOR_PLAN.md`.

| # | Phase | Estimate | Description | Done |
|---|---|---|---|---|
| 0 | Prep | ~2 hr | Export current localStorage to JSON. Snapshot the DB. Write the smoke test list. | ✅ 2026-04-28 |
| 1 | Schema | ~1 day | Apply migrations for new tables. RLS policies. Verify in Supabase SQL editor. | ✅ 2026-04-28 |
| 2 | Data migration code | ~1 day | Write `migrateLocalToDB()` one-shot in current `index.html`. Run it; verify all data lands in tables. | ✅ 2026-04-28 |
| 3 | Code reads from DB | ~1 day | Replace localStorage reads with Supabase queries throughout. App still in one file. Smoke test. | ✅ 2026-04-28 |
| 4 | CSS extraction | ~1–2 hr | Pull all `<style>` blocks into `styles.css`. | ✅ 2026-04-28 |
| 5 | JS extraction | ~30 min | Pull `<script>` content to `src/app.js`. Verify. | ✅ 2026-04-28 |
| 6 | Module split | ~1 day | Split `app.js` into feature modules with imports/exports and `window.X` bindings. Smoke test. | ✅ 2026-05-01 — as a classic-script carve (Item 3 phase E), not ES modules |
| 7 | Cleanup | ~2 hr | Delete dead code surfaced by the split. | ✅ 2026-04-29 → 04-30 — destructive column cleanup + dead-code sweep |
| 8 | Optional | varies | Vite, types, etc. — only if Phases 1–7 stayed under budget. | ✅ 2026-04-30 → 05-02 — Vite + full strict types (Item 3) |
| 9 | Launch | varies | Production deploy. Real-domain click-through. Done. | ✅ 2026-05-02 — Cloudflare Pages cutover, `procabinet.app` |

> Each phase landed as its own commit(s), per the original rule.

---

## 11. Smoke test checklist

Written before Phase 1 as `docs/SMOKE_TESTS.md` and run per-phase; all passing
at the 2026-05-02 cutover. Superseded by daily production use since.

- [x] Sign up new user, sign in, sign out
- [x] Create / edit / delete a client
- [x] Create / edit / delete a project
- [x] Add stock items; edit category, supplier, variant
- [x] Cut list: add panels, add pieces, generate layout
- [x] Add edge bands; assign to piece sides
- [x] Cabinet Quote: add cabinet line, edit dimensions/materials, save quote
- [x] Convert quote to order
- [x] Update business info; upload logo; verify on PDF export
- [x] Export PDF, export CSV, import CSV
- [x] Toggle dark mode, change units, change currency
- [x] Refresh page → all data persists
- [x] Sign in on a second browser → same data appears

---

## 12. Definition of done

- [x] All success criteria (§ 7) ticked — two consciously relaxed (file-size
      cap; localStorage dual-write residue), annotated inline in § 7
- [x] All in-scope items (§ 6) shipped — one swapped (shared `state` object →
      strict types over globals)
- [x] No regressions found in smoke test (§ 11)
- [x] Production URL works — `https://procabinet.app`
- [x] `SPEC.md` updated with anything learned, marked **Status: Implemented**
- [x] Outstanding bugs tracked in `PLAN.md` (review backlog), not blockers

---

## How to use this spec

This was the contract for the pre-launch refactor (written 2026-04-28, closed
out 2026-05-02). §§ 1–12 are kept as the historical record, annotated with
what actually shipped — including the deviations (classic-script carve instead
of ES modules, relaxed file-size cap, localStorage dual-write residue).

What stays live:

- **§ 13 (Decisions log)** — the ongoing project-wide log. One entry per
  non-obvious decision, oldest first. **Append new entries at the very end of
  this file** (per `CLAUDE.md`); § 13 is deliberately the last section so
  plain appends land in the right place.
- **`PLAN.md`** — current work, sub-steps, and status.

---

## 13. Decisions log

- 2026-07-03 — **Both wiki-session follow-ups done (same day).** (1) *Realtime/local insert race — fixed everywhere.* New `_mergeLocalRow(arr, row)` in `src/livelink.js` (beside `_applyRealtimeRow`, window-exported): drops the realtime channel's bare copy if it arrived first and keeps the locally-inserted object canonical (call sites hold its reference and attach `_lines`/notes, which the later realtime UPDATE merges in place). All 13 insert-then-unshift sites on the `quotes`/`orders` arrays converted (quotes.js ×4 incl. the earlier inline convert fix, orders.js ×4, cabinet.js ×3, quote-editor.js ×2). Verified live: converted a quote on the recording account — every order card renders exactly once. (2) *`business-assets` direct uploads unlocked.* Added the missing `biz_assets_owner_select` SELECT policy on `storage.objects` (the storage API uploads with `INSERT … RETURNING`, so SELECT is required alongside INSERT/UPDATE). Verified: a plain user-JWT upload to the owner folder now succeeds, and owner delete works too (test object uploaded + removed, nothing orphaned). The `image-upload` edge-function path still works and is untouched — retiring it in `_uploadLogoAsset` is now optional cleanup, not a necessity.
- 2026-07-03 — **Public wiki + automated workflow clips (Phases 1–3 complete).** Public how-to wiki at `/wiki` (index + 9 guide pages) generated at build time by `scripts/build-wiki.mjs` from `wiki/guides.mjs` + `wiki/clips.json`, styled by `wiki/wiki.css` (landing tokens, dark bar, amber steps), wired via `buildWikiPlugin` in vite.config.mjs (dev middleware renders per request; static imports on purpose — dynamic `import('…?t=')` can't resolve inside Vite's bundled config). SEO: wiki URLs appended to sitemap.xml + llms.txt by seoFilesPlugin; per-page HowTo/VideoObject/Breadcrumb JSON-LD; canonical + OG; `/wiki*` + `/wiki.css` rules in `_headers`; "Guides" links on landing footer + blog chrome; in-app Help → "Guides & Videos" deep-links the active tab's guide (`_openGuides()` in help.js — slug map duplicated from guides.mjs, keep in sync). Clips are REAL screen recordings: Playwright drives the app (dedicated `adamdenney33+wiki@` account, seeded by `scripts/seed_wiki_account.sql` incl. permanent Pro grant + walkthrough suppression + metric `unit_format` pin) with a synthetic walkthrough-style cursor (`wiki/recordings/_driver.mjs` + 9 drive scripts), encoded to faststart H.264 via Remotion's bundled ffmpeg (`DYLD_FALLBACK_LIBRARY_PATH` required), published to the public `wiki-clips` Storage bucket by the account's own JWT (`npm run wiki:record/encode/publish/clips`). Video binaries stay out of git — only `wiki/clips.json` is committed. **Two production bugs found & fixed along the way:** (1) converted orders appeared twice in the list until reload — realtime INSERT raced `convertQuoteToOrder`'s local `orders.unshift`; guarded by id in quotes.js (other unshift sites flagged for audit). (2) Storage RLS: the storage API uploads with `INSERT … RETURNING`, so user-JWT writes need a **SELECT policy** too — INSERT/UPDATE policies alone 403 ("new row violates row-level security policy"). This is why `business-assets` user-JWT writes never worked; adding the missing SELECT policy is the likely fix there as well.
One entry per non-obvious decision — originally mid-refactor only, now the
ongoing project-wide log (per `CLAUDE.md`). Chronological, oldest first.
(Re-sorted and re-homed as the final section on 2026-06-11; five entries had
been stranded after the old closing section.)

- 2026-04-27 — **Catalog design**: unified `catalog_items` table with `type` column instead of separate `materials_catalog` / `handles_catalog` / `finishes_catalog` tables. Rationale: identical row shape across types; one place to manage prices; new categories become rows, not tables. See `SCHEMA.md` § 3.2.
- 2026-04-27 — **`SCHEMA.md` drafted** — full schema with DDL, RLS patterns, migration order. Marked Status: Draft pending review.
- 2026-04-28 — **Refactor scoped to Phases 0-3** (data layer only). Phases 4-7 (CSS/JS extraction, module split, destructive migrations, deploy) deferred to a follow-up refactor session, with prototyping in between. Plan at `~/.claude/plans/lets-plan-the-refactor-wobbly-lighthouse.md`.
- 2026-04-28 — **Vite deferred** — vanilla ES modules with no build step remain the target for the eventual module split.
- 2026-04-28 — **Cabinet systems unification deferred** to post-launch. `cqLines` migrates to `quote_lines`; `cabItems` migrates to `cabinet_templates`. Both subsystems coexist during refactor.
- 2026-04-28 — **Logo storage**: Supabase Storage bucket `business-assets/`, URL on `business_info.logo_url`. Replaces base64 in `pc_biz_logo`.
- 2026-04-28 — **Migration approach**: phased per-feature (not big-bang). Dual writes (DB + localStorage) maintained through Phase 3 and during prototyping pause as safety net.
- 2026-04-28 — **Orphan localStorage keys resolved**: `pc_stock_libraries` dropped (no migration target — `stock_items` is source of truth); `pc_order_prodstarts` → new `orders.production_start_date` column; `pc_cq_settings` rates → new columns on `business_info`; `pc_cq_projects` → migrate to `projects` + `quote_lines` rows.
- 2026-04-28 — **Phase 0 (Prep) complete**: `docs/SMOKE_TESTS.md` created at project root; Supabase JSON snapshot at `Building Docs/snapshots/2026-04-28-prelaunch.json` (28 rows across 5 tables); `_exportLocal()` button added to Settings panel (`index.html` lines 1233-1241, function at line 3028).
- 2026-04-28 — **Phase 1 (Schema additive) complete**: 9 migrations applied to Supabase project `mhzneruvlfmhnsohfrdo`. Created 11 new tables (`business_info`, `catalog_items`, `cabinet_templates`, `cabinets`, `cabinet_hardware`, `sheets`, `edge_bands`, `pieces`, `piece_edges`, `quote_lines`, `order_lines`); added 9 columns to `stock_items`, 4 to `orders`, 2 to `quotes`. All new tables have RLS enabled with full owner CRUD policies (4 policies each), except `piece_edges` which uses chained-ownership Pattern B (1 ALL policy). All 28 existing rows across the original 5 tables verified intact post-migration. No security advisor warnings related to the new schema.
- 2026-04-28 — **Phase 1.5 (Unify project-save) complete**: new `_saveProjectScoped({ name, scope, payload })` at `index.html` line 8087 ensures one canonical `projects` row per `(user_id, name)`. Both `_clSaveProjectByName` (line 8119) and `cqSaveProject` (line 9039) now call it — Cut List with `scope: 'cutlist'`, Cabinet Quote with `scope: 'quote'`. Scope payload is merged into `projects.data` jsonb under `data.cutlist` / `data.quote` for now (Phase 3 will switch to writing into child tables). cqSaveProject still ALSO writes to `pc_cq_projects` localStorage for backward compatibility through the prototyping window.
- 2026-04-28 — **Phase 2 (Migration) complete**: Storage bucket `business-assets` created (public, 512KB max, image MIME types only) with owner-folder RLS policies on `storage.objects`. New `migrateLocalToDB()` orchestrator at `index.html` line 3475 runs 9 idempotent subroutines: `_migrateBizInfo`, `_migrateCatalog`, `_migrateStock`, `_migrateCabinets`, `_migrateCutListProjects`, `_migrateCQProjects`, `_migrateSavedQuotes`, `_migrateOrderRefs`, `_dropStockLibraries`. Idempotency via tag-based notes (`[CQMIG:id]`, `[SAVEDMIG:id]`) on `quotes` and check-then-insert pattern on `catalog_items` / `cabinet_templates`. Logo upload migrates `pc_biz_logo` base64 → Supabase Storage `business-assets/{user_id}/logo.{ext}`. UI: "Run Migration to Database" button in Settings → Backup & Migration; results shown in `_showMigrationLog()` modal with summary line and per-subroutine status. JS parses cleanly (519KB, 12,030 lines total).
- 2026-04-28 — **Phase 3 (all 8 features) complete** with **fallback-safe pattern**: each helper checks DB column / array first, falls back to localStorage if empty. Safe to land before/regardless of when migration runs. Done: **3.1 Stock metadata** — `_scGet/_ssGet/_svGet` prefer `stockItems[i].category/supplier/...` over localStorage maps; setters dual-write via new `_stockUpdateCols(id, updates, lsKey, lsValue)` helper. **3.2 Catalogs** — `loadAllData` extended to load `catalog_items` and `business_info` in parallel; new `_applyCatalogFromDB(rows)` overlays material/handle/finish arrays onto `cabSettings`/`cqSettings`. **3.3 Business info** — `_applyBizInfoFromDB(rows)` updates form inputs and rate defaults from DB; `saveBizInfo` now triggers debounced `_syncBizInfoToDB(payload)` (800ms); `handleLogoUpload` uploads to Storage `business-assets/{user_id}/logo.{ext}` and writes URL to `business_info.logo_url`. **3.4 Cabinets** — new `_loadCabItemsFromDB()` overlays `cabinet_templates` rows into `cabItems` on auth; `saveCabItems` triggers debounced `_syncCabItemsToDB()` (800ms) which INSERTs new and UPDATEs existing (tracked by `cab.db_id` field); `removeCabinetItem` also DELETEs the corresponding `cabinet_templates` row. **3.5 Cut list** — `loadProject(id)` queries `sheets`/`pieces`/`edge_bands` tables for the project; falls back to `projects.data` jsonb if DB child rows are empty; `_saveProjectScoped` for `scope: 'cutlist'` now calls `_replaceCutListChildTables(projectId, payload)` (REPLACE semantics). `_clSaveProjectByName` payload extended to include `edgeBands`. **3.6 Cabinet Quote** — `_saveProjectScoped` for `scope: 'quote'` calls `_replaceQuoteLinesChildTable(projectId, payload)`; finds-or-creates a tagged `[CQ_DEFAULT]` quote per project, deletes old `quote_lines` and inserts current `cqLines` via shared `_cqLineToRow()` helper. **3.7 Quotes UI** — Quote popup now has a "Quote Number" input (`pq-quote-number`); `_saveQuotePopup` writes `quote_number` to the `quotes` table. **3.8 Order helpers** — `_oqGet/_oqSet`, `_onGet/_onSet`, `setOrderProdStart`, `_restoreProdStarts` all prefer `orders.quote_id`/`notes`/`production_start_date` columns; setters dual-write to DB. JS parses cleanly throughout.
- 2026-04-28 — **Phase 4 (CSS extraction) complete**: 1,125-line stylesheet extracted from `index.html` `<style>` block to `styles.css`. HTML now references via `<link rel="stylesheet" href="styles.css">`. Five other `<style>` tags in the file are intentionally left in place — they live inside JS template literals for PDF/print output, not part of the page CSS.
- 2026-04-28 — **Phase 5 (JS extraction) complete**: entire `<script>` block (10,500+ lines) extracted from `index.html` to `src/app.js`. HTML now references via `<script src="src/app.js"></script>`. `index.html` shrunk from 12,467 → 798 lines (pure HTML markup). `src/app.js` parses cleanly.
- 2026-04-28 — **Phase 6 (PARTIAL — module split): migrate code extracted to `src/migrate.js`**. The 489-line migration block (`_migReadLS`, `_migLog`, 9 subroutines, `migrateLocalToDB`, `_runMigration`, `_showMigrationLog`, `_cqLineToRow`) was lifted out of `app.js` and loaded via a second classic `<script>` tag after `app.js`. Both files share the global namespace, so no `window.X` exports needed for inline handlers. **Deeper ES-module split deferred** — switching to `<script type="module">` would require explicit `window.X = X` for ~80–100 functions called from 369 inline event handlers, plus per-feature state-object refactor; this is best done as a focused session with manual click-testing after each module is extracted. File state at end: `index.html` 798 lines · `styles.css` 1,126 lines · `src/app.js` 10,057 lines · `src/migrate.js` 494 lines.
- 2026-04-28 — **Phase 7 (destructive migrations) deferred**: dropping legacy columns (`quotes.client/project/materials/labour`, `orders.client/project/value`) and renaming `projects.data → ui_prefs` requires that no code path still reads them. Today the Quote popup still writes/reads `materials` and `labour` (the legacy totals fields), and the legacy text columns are still used as fallbacks where `client_id`/`project_id` FKs are null. Deferred until: (a) Quote totals are derived from `quote_lines` aggregation, and (b) all `client`/`project` text fallbacks are replaced with FK lookups.
- 2026-04-29 — **Phase 7 (destructive cleanup) complete**, in seven committed steps. (1) Added FK-resolving helpers `quoteClient/quoteProject/orderClient/orderProject` and a `quoteTotalsFromLines` aggregator that fills `q._totals = {materials, labour}` for every quote on load; `quoteTotal()` reads the cache. (2) Mechanical perl swap of every bare `q.client`/`q.project`/`o.client`/`o.project` for the helpers across `src/app.js`, plus dropping the same keys from row inserts in `convertQuoteToOrder`, `_saveQuotePopup`, `_saveOrderPopup`, `createQuote`, `duplicateQuote`, `duplicateOrder`, the CSV importers, and migrate.js. (3) Manual-totals quote UX writes a single `quote_lines` row (`name='Manual Quote'`, `material_cost_override` + `labour_hours` + `labour_override=true`); the popup's per-quote "Rate /hr" input is gone — labour now uses the global `cqSettings.labourRate`. Backfilled all 12 surviving quotes via `_writeManualTotalsLine(q.id, materials, labour/rate)`. (4) `convertQuoteToOrder` copies `quote_lines → order_lines`; `duplicateOrder` does the same. (5) `_saveProjectScoped` no longer merges scope payloads into the projects row — child tables (sheets/pieces/edge_bands/quote_lines) are the source of truth; `loadProject`'s `else { /* legacy data jsonb */ }` block deleted. (6) Snapshot saved at `Building Docs/snapshots/2026-04-29-pre-phase7.json`; orphan rows deleted (11 quotes + 3 orders, all with NULL FKs and prototyping garbage like "njcncsd"/"sdcsdv"); destructive migrations applied in three commits (`phase7_quotes_drop_legacy_columns`, `phase7_orders_drop_legacy_text_columns`, `phase7_projects_rename_data_to_ui_prefs`). (7) Status flipped above. **Deviation from SCHEMA.md § 3.15:** kept `orders.value`. Reason: `order_lines` aggregation produces materials+labour only; the customer-paid total snapshotted at quote→order conversion includes markup+tax that cannot be reconstructed from lines alone (orders have no markup/tax columns and the parent quote may be edited or deleted later). Adding markup/tax to orders was out of scope. The plain-English schema doc and code comment in `orderTotal()` flag this.
- 2026-04-30 — **Dead-code sweep complete**: deleted v0.11 library-system stub functions (`_renderLibUI`, `renderCLLibraries`, `renderCabLibraries`, `renderStockLibrariesUI`, `toggleProjectsPanel`) and ~10 no-op call sites across `switchSection`, `saveStockLibrary`/`loadStockLibrary`/`deleteStockLibrary`/`importStockLibrary`, `_clLoadProjectList`, `deleteProject`, and the auth-state-change handler; removed the 49-line `_renderLibUI_OLD` commented-out block; dropped the unused `saveData()` no-op; simplified Phase 7 transition fallbacks in `_openQuotePopup` (the `q.materials || 0` branches were unreachable post-step-6 since the columns are gone) and reworded the `quoteTotalsFromLines` docstring. Also refreshed `CLAUDE.md` Stack section to reflect the post-Phase-5/6 file layout (`index.html` shell + `styles.css` + `src/app.js` + `src/{db,ui,migrate}.js`). Net: `src/app.js` 10,090 → 10,022 lines; six commits, no behavior change, no schema/DB changes.
- 2026-04-30 — **Cabinet Builder ghost removal + cloud library** (Path B of post-launch item 2). Deleted 564 lines of orphan parallel cabinet code that had no UI surface but was syncing to `cabinet_templates` on every sign-in: state (`cabItems`, `cabSettings`, `cabActiveIdx`, `_cabId`), constants (`CAB_DEFAULT_MATERIALS`, `CAB_DEFAULT_HANDLES`, `SHEET_AREA`), localStorage keys (`pc_cab_items`, `pc_cab_settings`), every `cab*`/`_cab*` function (`defaultCabinet`, `addCabinetItem`, `removeCabinetItem`, `duplicateCabinetItem`, `selectCabinet`, `matPrice`, `handlePrice`, `pricePerM2`, `calcCabinet`, `populateCabDropdowns`, `loadCabForm`, `renderCabMaterialsList`, `renderCabHandlesList`, `renderCabItemsList`, `renderCabResults`, `renderCabinetQuote`, `loadCabItems`, `saveCabItems`, `loadCabSettings`, `saveCabSettings`, `_loadCabItemsFromDB`, `_syncCabItemsToDB`, `importCabBuilderToCQ`), the ghost auth-time call in `loadAllData`, and the `cabSettings` branches in `_applyCatalogFromDB` (kept `cqSettings`). The render targets it expected (`#cab-results`, `#cab-cab-list`, `cab-name`/`cab-w`/`cab-h`/`cab-d`) never existed in `index.html`. Repurposed the now-unused `cabinet_templates` DB table to back the saved-cabinet library (`cqLibrary`), which previously lived only in `pc_cq_library` localStorage. New helpers `_saveCabinetToDB`/`_deleteCabinetFromDB`/`_loadCabinetTemplatesFromDB`; auth handler loads from DB after `loadAllData`. Repointed `migrate.js`'s `_migrateCabinets` from the dead `pc_cab_items` to the live `pc_cq_library`. Dropped `loadCQLibrary`/`saveCQLibrary` and 6 callers — DB is sole source of truth for the library. Net: `src/app.js` 10,022 → 9,498 lines (≈−524); five commits; no schema changes (table was already in place from Phase 1); the `cqLines`/`cqSettings`/`cq*` symbol surface stays — renaming deferred to item 3's module split. Path C (templates-vs-instances product redesign) parked as a separate roadmap item.
- 2026-04-30 — **Item 3 phases A/B/D landed** (Vite scaffold + npm deps + TS toolchain), reversing the earlier 2026-04-28 Vite-deferred decision. (A) `package.json` + `vite.config.mjs` add Vite as dev server (`npm run dev`, port 3000) and build tool (`npm run build` → `dist/`). A `copy-classic-scripts` plugin in vite config copies `src/*.js` into `dist/src/` so the existing classic `<script src="src/*.js">` tags resolve from the build output without any source-file changes. `.claude/launch.json` adds a "Vite dev" config alongside the existing npx-serve config. (B) Replaced the three CDN `<script>` tags (`@supabase/supabase-js`, `jspdf`, `jspdf-autotable`) with npm packages bundled by Vite. The actual swap uses a tiny module bridge (`src/main.js`) that imports the npm packages and re-exposes them on `window` under the same names the CDN scripts provided (`window.supabase.createClient`, `window.jspdf.jsPDF`); this lets `db.js`/`app.js`/`migrate.js` stay as classic scripts unchanged. The four classic source `<script>` tags got `defer` added so they execute after the deferred-by-default module bridge sets up the globals. (D) Added `typescript` + `@types/node` devDeps and a permissive `tsconfig.json` (`allowJs:true`, `checkJs:false`, `strict:false`, `noEmit:true`); `npm run typecheck` passes clean. **Deviated from plan #3:** skipped what the plan called phase C (bulk `<script type="module">` swap with a 150-name window-shim) — the bridge module already lets npm packages flow into classic source files, so the bulk conversion brings little value before phase E (module split) actually needs it. Phase E will introduce ESM into source files incrementally as functions get carved out, rather than as a big-bang conversion. Phases E (module split, 8–12 commits), F (`.ts` everywhere), G (strict mode + DB types), and H (deploy cutover) remain TODO.
- 2026-04-30 — **Item 3 phase E partial** (3 carves landed). Began carving `src/app.js`'s functional sections into separate files using a **classic-script split** pattern rather than ES module conversion: each new file is a `<script defer>` and top-level `function`/`let`/`const` declarations stay globally accessible via classic-script semantics. Cross-file references (e.g. `orders` array, `_escHtml`, `_db`) work through the global lexical environment, no `window.X` shim required. (1) `src/schedule.js` (157 lines) — `renderSchedule`, `_scrollToSchedBar`, `setOrderProdStart`, `_restoreProdStarts`. (2) `src/dashboard.js` (307 lines) — `renderDashboard` + `drawRevenueChart`. (3) `src/orders.js` (216 lines) — `orders` state array + `orderNextId`/`ORDER_STATUSES`/`STATUS_LABELS`/`STATUS_COLORS`/`STATUS_BADGES` constants + `addOrder`/`removeOrder`/`duplicateOrder`/`advanceOrder`/`renderOrdersMain`. State-bearing carves like `orders.js` need to load BEFORE `app.js` (so the bindings exist when app.js's body references them); script tag ordering in index.html follows that constraint. Net: `src/app.js` 9,495 → 8,841 lines (≈−654); 3 commits. ES module syntax (import/export) deferred to a later pass — the classic-script split pattern is decoupling the file structure without forcing rewrites. Phase E continues with QUOTES, QUOTE HELPERS, STOCK, CUTLIST, CABINET BUILDER, and others when work resumes.
- 2026-04-30 — **Item 3 phase E continued** (4 more carves + 1 dissolve, 8 commits). Continued the classic-script split with: (4) `src/quotes.js` 328 lines — `quotes`/`quoteNextId` state + FK resolvers (`quoteClient`/`quoteProject`/`orderClient`/`orderProject`) + totals (`quoteTotalsFromLines`/`_hydrateQuoteTotals`/`_refreshQuoteTotals`/`quoteTotal`/`orderTotal`) + mutations (`createQuote`/`removeQuote`/`convertQuoteToOrder`/`approveQuote`/`revertQuoteToDraft`) + `renderQuoteMain` + `exportQuotesCSV`/`importQuotesCSV` (the CSV pair was moved here in a follow-on commit, see below). (5) `src/backup.js` 48 lines — `_exportLocal` (carved out of LOCAL BACKUP). (6) `src/auth.js` 94 lines — `dismissAuth`/`showAuthFromPaywall`/`_showShortcutsHelp` plus two top-level `document.addEventListener('keydown')` blocks for keyboard shortcuts (Ctrl/Cmd+1–9 / N / / / Escape) and Escape-overlay-close. (7) `src/business.js` 116 lines — `saveBizInfo`/`_syncBizInfoToDB`/`loadBizInfo`/`handleLogoUpload`/`removeLogo`/`loadLogoPreview`/`getBizLogo`/`getBizInfo` + `_bizInfoSyncTimer` debounce state. **Dissolved ORDER HELPERS** (4 commits): the section was named misleadingly — its contents were a mix of order helpers, CSV import/export for three entity types, and dead stubs. Investigation showed each function had one obvious home, so instead of creating a new "csv.js" or "order-helpers.js" file: deleted 4 dead functions (`updateOrderField`/`_noOp`/`renderQuoteLibraries`/`renderOrderLibraries` — all 0 callers or empty stub-only callers, plus the dangling `try/catch` calls in switchSection); moved 6 functions to their carved-file callers (`exportQuotesCSV`/`importQuotesCSV` → quotes.js; `setOrderFilter`/`setOrderStatus`/`exportOrdersCSV`/`importOrdersCSV` → orders.js); left `exportClientsCSV`/`importClientsCSV` in app.js with a TODO (will move to clients.js when CLIENTS section is carved); removed the now-empty section banner. Net for the whole batch: `src/app.js` 8,841 → 8,237 lines (≈−604); 12 commits. After every commit: `switchSection` iterated over all 7 nav tabs without errors; for the auth carve specifically, dispatched a synthetic Cmd+1 keydown to confirm the side-effect listener still attached. Phase E continues with QUOTE HELPERS (542 lines, the smart-suggest helpers), STOCK HELPERS (255), STOCK (478, state), PROJECTS PANEL (261), CABINET BUILDER (~2,200, biggest), CUTLIST (~2,780, biggest), plus a settings-cluster batch (THEME/UNITS/CURRENCY/SETTINGS/SECTION-NAV ~150 lines combined) and tail sections (FORM DEFAULTS, INIT, CLIENTS & PROJECTS).
- 2026-05-01 — **Item 3 phase E continued v2** (4 more carves, 4 commits). (8) `src/stock.js` 267 lines — `stockLibraries` state + `removeUsedSheets`/`useStockInCutList` plus the stock-library subsystem (load/save/saveStockLibrary/loadStockLibrary etc.). State-bearing, loads BEFORE app.js. Sed range was off-by-one initially (pulled in the FREE TIER top banner) — caught by checking the boundary post-delete and manually restoring the line in app.js / dropping it from stock.js before commit. The file currently holds *only* helpers; the STOCK section proper (`stockItems` state + main render + CRUD, ~478 lines) remains in app.js for now, will join this file when carved. (9) `src/projects.js` 275 lines — entire PROJECTS PANEL: `_clLoadProjectList`/`_saveProjectScoped`/`_replaceQuoteLinesChildTable`/`_replaceCutListChildTables` + project save/load/delete UI helpers. No state. References `_clProjectCache` declared with `let` in app.js's CABINET BUILDER section — runtime resolution through global env handles this fine. Loads AFTER app.js. (10) `src/forms.js` 85 lines — FORM DEFAULTS section: a top-level IIFE wiring localStorage persistence + input listeners on the quote-form fields, plus date helpers `_orderDateToISO`/`_relativeDate`, plus `_updateQuotePreview`. The IIFE runs at script-load time but is safe under `<script defer>` (DOM parsed first, no-ops when the quote panel hasn't rendered yet). Loaded AFTER app.js. (11) `src/clients.js` 315 lines — entire CLIENTS & PROJECTS section, the file's last functional block: foundational helpers `_dbInsertSafe`/`resolveClient`/`resolveProject` (used app-wide), client CRUD (`createClient`/`removeClient`/etc.), project CRUD, `renderClientsMain`/`renderProjectsMain`, plus the trailing `try { renderDashboard(); … }` script-load init that was sitting after `renderProjectsMain` at the very bottom. No own-state — `clients`/`projects` arrays are declared at the top of app.js's STOCK section and resolve through global env. Loaded AFTER app.js, after dashboard.js (because of the trailing init). Net for v2 batch: `src/app.js` 8,237 → 7,354 lines (≈−883); 4 commits, all green per the per-tab smoke. Cumulative phase E so far: app.js 9,495 → 7,354 (−2,141, ~22%) over 11 carves + 1 dissolve + 23 commits. Remaining sections to handle: settings cluster (GLOBALS/SETTINGS DROPDOWN/THEME/UNITS/CURRENCY/SECTION-NAV/FREE TIER/UTILS, ~190 lines combined — likely batched into one `src/settings.js`), STOCK proper (478, state), QUOTE HELPERS (563, contains the shared smart-suggest infrastructure — likely splits across quotes.js + a new src/smart-input.js), CABINET BUILDER (~2,200, state), CUTLIST (~2,780, state), INIT (~36, top-level init code).
- 2026-05-01 — **Item 3 phase E essentially complete** (5 more carves, 5 commits). (12) `src/settings.js` 190 lines — bundle of 6 small adjacent sections (GLOBALS / SETTINGS DROPDOWN / THEME TOGGLE / UNITS / CURRENCY / SECTION NAVIGATION). State: `let darkMode` plus 3 init IIFEs that restore theme/units/currency from localStorage at script-load time. Loaded BEFORE app.js. switchSection lives here too — slightly out of theme but small enough to not justify a separate file. (13) STOCK section (478 lines) appended to existing `src/stock.js` — state declarations: `stockItems`, `clients`, `projects`, `stockNextId`, `STOCK_CATS`, plus _stockUpdateCols and the full stock CRUD + renderStockMain. File now 747 lines combining helpers (carve 8) and main section. The `clients` / `projects` arrays lived inside STOCK's banner range historically and came along — relocation to clients.js deferred (cosmetic). (14) **QUOTE HELPERS dissolved** (single commit) — 540 lines appended to `src/quotes.js` (smart-suggest, popup creators, print pipeline, quote mutations) and 20 lines appended to `src/clients.js` (exportClientsCSV / importClientsCSV — finally moving the orphans that carve 11 left under a TODO comment). Section banner removed; 563 lines off app.js. (15) `src/cabinet.js` 2,296 lines — CABINET BUILDER, the second-largest section. State: `cqSettings`/`cqLibrary`/`cqLines`/`cqSavedQuotes`/`_clProjectCache`. Trailing init block calls `loadStockLibraries()` (defined in stock.js), so cabinet.js loads AFTER stock.js + BEFORE app.js. The historical `cq*` symbol-name prefix kept verbatim — renaming would touch ~150 inline-handler call sites and is parked as a separate cosmetic pass. (16) `src/cutlist.js` 2,805 lines — CUTLIST, the largest single section in the codebase. State: 15+ top-level `let` bindings (`sheets`, `pieces`, `edgeBands`, `results`, `activeSheetIdx`, `activeTab`, `_sheetId`, `_pieceId`, `layoutZoom`/`layoutColor`/`layoutGrain`/`layoutFontScale`/`layoutCutOrder`/`layoutSheetCutList`, etc.). `addSheet`/`addPiece`/`renderSheets`/`renderPieces`/`_loadCutList` are called from app.js's INIT block; cutlist.js loads BEFORE app.js (and BEFORE settings.js, whose setUnits IIFE may touch sheets/pieces). After this carve the demo data seed at INIT still loaded correctly (`sheets` 1, `pieces` 5 post-reload). Net for v3 batch: `src/app.js` 7,354 → 1,103 lines (≈−6,251); 5 commits, all green per the per-tab smoke. **Cumulative phase E final**: app.js 9,495 → 1,103 (−8,392, ~88%) over 16 carves + 1 dissolve across two sessions; 16 new files in `src/` (one per domain plus a settings bundle). Remaining in app.js after phase E: only INIT-style code (FREE TIER ~17 lines, UTILS ~9 lines, INIT block ~36 lines, plus a ~200-line click-outside-handler block at the top, plus an auth-state-change handler at the very top of GLOBALS). The file is now effectively the bootstrap entry — phase F (TS conversion) and phase G (strict mode) start from this clean baseline. The `cq*` rename and any `_clProjectCache` / `clients` / `projects` relocation to their conceptual homes (clients.js) are deferred as cosmetic cleanup, not blockers.
- 2026-05-01 — **Item 3 phase F partial** (15/19 src files type-checked, 7 commits). Phase F kicks off the TypeScript-coverage push. **Deviated from plan #3**: the plan said "rename *.js → *.ts and add @ts-nocheck", but the codebase is classic-script-loaded (each src/*.js gets its own `<script defer>` tag, not ES modules), so a bulk rename would break dev/build until Vite is taught to compile .ts back to classic-loadable .js. Instead, took the pragmatic first step: enabled `checkJs: true` in `tsconfig.json` and added `// @ts-nocheck` to every src/*.js, then started peeling per-file. Files .js stay as-is; type-checking happens via JSDoc + a new `src/globals.d.ts` ambient-declarations file (Window stash slots for inline-handler render state, the npm-bridge globals from main.js). Files now type-checking clean (no `@ts-nocheck`): `main.js`, `backup.js`, `db.js`, `ui.js`, `forms.js`, `auth.js`, `business.js`, `schedule.js`, `settings.js`, `orders.js`, `projects.js`, `dashboard.js`, `migrate.js`, `clients.js`, `app.js`. Common patterns introduced: per-file `_xInput(id)` helpers casting `getElementById` to `HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement`; `+date` coercion for `isNaN(Date)` / `Date - Date`; `String(...)` coercion for `.textContent =` and `localStorage.setItem(...)` numeric values; `/** @type {HTMLImageElement|HTMLCanvasElement|HTMLButtonElement} */` casts where elements have specific properties (`src` / `width` / `disabled` / etc.). One key cross-cutting fix: `src/db.js`'s `_DBBuilder.then()` got a JSDoc `@returns {Promise<...>}` so every `await _db(...)` chain elsewhere typechecks; the `return this` line is `// @ts-expect-error`'d because the builder is structurally a fake-thenable that recurses (it returns `this`, not a real Promise, but `this` is itself thenable). Files **still carrying `@ts-nocheck`**: `stock.js` (745 lines, 75 errors), `quotes.js` (872, 37 errors), `cabinet.js` (2,296), `cutlist.js` (2,805) — all the big domain files. Each has dozens of identical-shape `.value`-on-`HTMLElement` errors scattered across many functions; grinding them one-by-one is a poor effort/value match. They're deferred to a future session, possibly with a sed-replace pass once the `_xInput`-helper pattern is consistent across all already-peeled files. Phase F's plan-3 step "rename *.js → *.ts" remains future work — meaningful only after the remaining big files have peeled their nocheck or once Vite gets a build pipeline that compiles `.ts` for classic-script tags.
- 2026-05-01 — **Item 3 phase F complete** (19/19 src files type-checked, 8 commits total). Closed out the four big domain files (stock / quotes / cabinet / cutlist, ~6.7k lines combined) using a sed-based bulk approach instead of per-call casts. Strategy: (a) added a typed `_byId(id)` helper to `src/ui.js` whose JSDoc lies that the return is `HTMLInputElement` — `.value` / `.disabled` / `.src` / `.checked` / `.files` / etc. all typecheck at any call site, runtime semantics unchanged. (b) bulk sed-replaced `document.getElementById(` → `_byId(` across the four files (77+30+112+40 = 259 replacements). (c) augmented `Element` and `EventTarget` interfaces in `src/globals.d.ts` to permit common form-input properties at querySelector / event.target call sites (style / value / focus / select / dataset / files / src / etc.) — hacky but contained to the transition d.ts file; tighten back when individual files get a strict pass. (d) added many more Window stash slots to globals.d.ts (_editingStockId, _stockSearch, _stockCatFilter, _ratesOpen, _cqBlankLine, _ebDraft, _ebPieceId, _ebBuildSVG, _ebBuildTable, _currentProjectId, saveCurrentProject, saveStockItems, stockItems, _saveStock, results, clipboardData). (e) targeted residual fixes: `cqSettings.labourTimes` cast to `any` in cabinet.js (TS narrowed initial `{}` too aggressively); `cqDefaultLine(type)` gained the param it always took at runtime; `_clLoadCabinetParts` function-reassignment got a `@ts-expect-error`; `pdf.autoTable` cast through any (jspdf-autotable extends jsPDF via plugin without declaration merging); FileReader.result narrowed to string; localStorage.setItem numeric values wrapped in String(); click-outside handler EventTarget cast to Node + Element. Verified: `npm run typecheck` clean; reload + 7 nav tabs render; demo data loads; no console errors. The `_DBBuilder.then()` JSDoc was also tightened from `PromiseLike<T>` to `Promise<T>` (the prior type lacked `.catch`, breaking `_db(...).catch(...)` fallback patterns in `loadAllData`). End-state: every file in src/ is typecheck-clean. Strict mode (`noImplicitAny: true`, `strictNullChecks: true`) remains future work — phase G. The plan-3 step "rename *.js → *.ts" still deferred since classic-script loading would need a vite-plugin to compile .ts for classic tags; cheaper to rename later (or skip entirely if the project sticks with JSDoc indefinitely).
- 2026-05-01 — **Item 3 phase G.1 sub-pass A complete** (10 commits, ~95 errors fixed across 9 source files). G.1 flips `strictNullChecks: true` in `tsconfig.json` (other strict knobs stay off — G.2/G.3/G.4 are explicit follow-ups for the Element/EventTarget augs, noImplicitAny, and Supabase row types). Plan at `~/.claude/plans/continue-procabinet-app-http-procabinet-warm-falcon.md`. Initial flag flip surfaced ~350 errors codebase-wide; sub-pass A targets the non-_byId callers (sub-pass B narrows `_byId` and fixes the cascade in cabinet/stock/cutlist/quotes). **Triage forecast vs. actuals**: clients.js + orders.js + backup.js + main.js + migrate.js were already strict-clean from Phase F's existing patterns (`?.value`, typed-cast nullable, per-file `_xInput` helpers); the sub-A queue collapsed to 9 files plus 1 zero-source-change flag-flip commit. **Files closed (in commit order)**: `db.js` (1 — `_dbHeaders` got a `Record<string,string>` JSDoc to widen the union of its two return paths past `HeadersInit`'s strictness), `business.js` (1 — FileReader.onload's `e.target.result` got an inline JSDoc cast on `e.target` to FileReader), `projects.js` (1 — #results-area cast + if-el), `ui.js` non-_byId (2 — `_confirm`'s two onclick assignments JSDoc-cast to `HTMLElement`; `_byId` itself untouched, sub-pass B's job), `auth.js` (3 — #auth-screen / #paywall-modal casts; activeElement.tagName `?? ''` for `Array.includes`), `settings.js` (4 — toggleSettings/toggleAccount dropdown casts), `forms.js` (12 — inputVal got `?? ''` for parseFloat compatibility, the seven qfp-* writes got a local `out` helper that JSDoc-casts to HTMLElement since they're descendants of #quote-form-preview asserted on line 73), `app.js` (12 — auth screen / toggleAuthMode / loadAllData orders-badge / onAuthStateChange account views, plus `String(orders.filter(...).length)` for the implicit number→string coercion that strict caught and `session.user.email ?? ''` for the `string | undefined` → `string | null` mismatch on textContent), `dashboard.js` (24 → 0 with one line — `if (!ctx) return;` after `canvas.getContext('2d')` was the entire fix), `schedule.js` (35 → 0 — added a `SchedEvent` typedef and cast the `.filter(Boolean)` result since checkJs doesn't narrow Boolean-as-predicate, plus JSDoc-cast the local `s`/`d` to Date at the two forEach sites whose map-invariant guarantees one of e.start/e.due is non-null). **Common patterns introduced**: (1) `/** @type {HTMLElement} */ (document.getElementById('id'))` inline cast — the JSDoc equivalent of TS's `!` non-null assertion (which JS files can't use). Used wherever the targeted element is a permanent index.html element or a just-rendered descendant. (2) `?? ''` to coerce optional-chain `?.value` results from `string | undefined` to `string` for parseFloat / Array.includes / String comparisons. (3) Early-return null guard for canvas `getContext('2d')` (collapses dozens of downstream null-errors to one line). (4) `.filter(Boolean)` typedef-then-cast pattern for map-may-return-null array builds. **Anti-patterns avoided**: no silent fallback (returning empty strings when the original threw TypeError); no globals.d.ts changes (those are sub-G.2 work). Verified: `npm run typecheck` reports exactly 226 remaining errors, all in the four `_byId`-heavy files (cutlist 216 / stock 4 / cabinet 4 / quotes 2) — exactly what sub-pass B tackles next; reload + 7 nav tabs all show panelActive=true with no console errors.
- 2026-05-01 — **Item 3 phase G.1 sub-pass B complete — phase G.1 closed** (6 commits + 1 SPEC entry, ~480 errors fixed). All 19 src/*.js files now type-check clean under `strictNullChecks: true`. **Strategy reordered mid-phase**: the plan's per-file order (quotes → cutlist → stock → cabinet) assumed `_byId` narrowing would be the first sub-step. Actual run separated the work cleanly by error category: (B.1) shared `e.target.files[0]` pattern across stock/cabinet/quotes — five sites, one inline `/** @type {HTMLInputElement} */ (e.target).files?.[0]` cast each, runtime guarantee (`e.target` is the freshly-created `<input type="file">` element). (B.2) cutlist's 216 non-_byId errors closed in one commit before _byId was narrowed: parseFloat(localStorage.getItem(...)) coerced via `?? ''` (lines 38/41); edge-band sides object given a `Record<string, {id,trim}|null>` typedef so the conditional re-assigns past initial-null narrowing (859-864); paste handler's `e.target` JSDoc-cast and explicit dataset null guards added (1106-1115); CSV import FileReader.onload `e.target` cast (1167); print-iframe `contentDocument` / `contentWindow` cached into local cdoc/cwin vars JSDoc-cast to Document/Window (1351-1357); ONE `if (!ctx) return;` after `canvas.getContext('2d')` collapsed 164 downstream ctx-null errors in `drawSheet` (line 2214); and the `never[]` array-literal-narrowing problem in `buildGuillotinePlan`'s local `out = { cuts: [], offcuts: [] }` plus `cutLines` were JSDoc-typed to `any[]` (the structures are heterogeneous and partially-typed across the function). (B.3) `_byId`'s JSDoc return type narrowed from `HTMLInputElement` to `HTMLInputElement | null` in `src/ui.js` — the contained-lie about subtype stays so `.value`/`.disabled`/`.checked`/`.files` chains keep working after a null guard or non-null cast; the lie is now ONLY about subtype, not nullability. Updated docstring to reflect the change. **Cascade after narrow** — actual error count came in much smaller than the plan's "~245" forecast: cutlist 8, quotes 16, cabinet 28, stock 65 (total 117). Many `_byId` calls were already null-safe via `?.value` chains or `if (el)` checks; the bare-chain `_byId('x').value = ...` pattern was concentrated in form-population functions. (B.4-B.6) per-function `inp` helper pattern — `/** @param {string} id */ const inp = id => /** @type {HTMLInputElement} */ (_byId(id));` — adopted as the standard for any function with 4+ form-field accesses (`createQuote`, `cqAddToNewQuote`, `loadCQQuote`, `newCQQuote`, `cancelStockEdit`, `addStockItem`, `editStockItem`, `saveStockEdit`, plus the analogous quote-pre-fill in cabinet.js's `convertToQuote`). Smaller functions used inline JSDoc casts (3 onclick assignments in cabinet.js's `_clLoadCabinetParts` merge popup; 4 `_byId(targetInputId).value = name` sites in quotes.js). One additional `if (!el) return;` early guard added to `renderStockMain` (line 636 — covers 100+ uses of `el` in the function body). **Patterns NOT introduced**: still no globals.d.ts changes (sub-G.2's job); no _byId signature change beyond nullability (subtype lie kept until the codebase decides whether per-call-site `HTMLButtonElement | HTMLImageElement | etc.` discrimination is worth the diff cost); no silent-fallback runtime behavior changes. **End-state**: tsconfig has `strictNullChecks: true`, other strict knobs unchanged. `npm run typecheck` clean. `npm run build` produces working dist/. Comprehensive smoke verified: reload + 8 nav tabs (dashboard/stock/cabinet/quote/orders/schedule/clients/cutlist) all panel-active, demo data persists (1 sheet, 5 pieces), cutlist `optimize()` runs and produces 3 canvases with non-empty results-area, no console errors. Sub-pass B alone: 6 commits across 4 files (cutlist counted twice — once for non-_byId fixes in B.2, once for _byId in B.3 alongside the ui.js narrow; quotes/cabinet/stock once each). Cumulative G.1 push: 17 commits, ~480 errors fixed. Phase G.2 (tighten globals.d.ts Element/EventTarget augs), G.3 (`noImplicitAny: true`), and G.4 (Supabase row types via `generate_typescript_types` MCP + `_DBBuilder<T>` wiring) remain future work — separate plans.
- 2026-05-02 — **Item 3 phase G.4 complete — Supabase row types + `_DBBuilder<K, Single>` generic** (1 commit). G.4 was promoted ahead of G.2/G.3 in the same session because it has the highest bug-finding value of the three remaining strict sub-phases. Generated `src/database.types.ts` via the Supabase MCP `generate_typescript_types` tool against project `mhzneruvlfmhnsohfrdo` — 16 tables (`business_info`, `cabinet_hardware`, `cabinet_templates`, `cabinets`, `catalog_items`, `clients`, `edge_bands`, `order_lines`, `orders`, `piece_edges`, `pieces`, `projects`, `quote_lines`, `quotes`, `sheets`, `stock_items`) plus the standard `Tables<T>` / `TablesInsert<T>` / `TablesUpdate<T>` helpers. The generated file is committed as-is so types are reproducible without an MCP round-trip; regenerate after schema migrations. **`_DBBuilder` rewritten as `_DBBuilder<K extends _TableName, Single extends boolean = false>`**: every chainable method (`select` / `insert` / `update` / `delete` / `eq` / `order` / `limit`) preserves both K and Single; `.single()` flips Single to true. The `.then()` resolver type uses a conditional — `{data: Single extends true ? Row | null : Row[] | null}` — so callers get the right shape based on what builder methods they chained. `_db()` is generic on K so `_db('clients').select()` returns `Promise<{data: Tables<'clients'>[] | null}>`. `eq` / `order`'s `col` parameter typed as `keyof Row` so column-name typos error out. `update` body typed as `Tables[K]['Update']`, `insert` body as `Insert | Insert[]`. The fake-thenable `return this` pattern is preserved (still uses `@ts-expect-error`); the conditional-return-type narrowing inside the `.then` handler casts through `any` at the resolve site since generic-conditional-narrowing-inside-handler is a TS limitation. **Schema-divergence bugs surfaced and TODO'd** (NOT fixed in this commit — they're behavior-affecting and need their own audit): `stock.js addStockItem`'s in-memory `data.thickness/width/length` shadow the DB columns `thickness_mm/width_mm/length_m` (desync after page reload); `quotes.js _openNewClientPopup` / `_openNewProjectPopup` pass `id: Date.now()` to Supabase Insert (the DB has serial id and silently overrides; the local `newProject` also has extra `client/desc/status` fields not in the projects schema); `app.js`'s update-project popup writes `status` and `description` columns that don't exist on `projects`; `app.js loadAllData` adds an in-memory `status: 'active'` to project rows. All four cast through `any` with TODO(schema-divergence) comments. Mechanical fixes (no behavior change): `.single()` callers got `if (error || !data) return;` null guards (data is now `Row | null`, was `any`); `delete nl.id` / `delete nl.quote_id` patterns in orders/quotes cast through `any` (delete operator forbids non-optional fields under strict); `ui_prefs` Json access in projects.js + migrate.js cast through `any`; `cabinet_templates.default_specs` Json spread cast. Verified: typecheck clean across all 19 src files; reload + 8 nav tabs render; demo data persists; cutlist `optimize()` produces 3 canvases; no console errors.
- 2026-05-02 — **Item 3 phase G.2 complete — globals.d.ts Element/EventTarget tightening** (1 commit). The Phase F transition augmentations on `Element` + `EventTarget` (value, style, focus, select, dataset, src, width, height, getContext, toDataURL, files, checked, disabled, closest, placeholder etc.) were a hack to let peeled .js files avoid per-call casts; with G.1 (strictNullChecks) and G.4 (DB types) in, those properties belong on the proper subtypes. Removed the entire augs block from `src/globals.d.ts` (kept the Window-stash-slots block — those are legitimate cross-file globals + npm-bridge globals). **Surprisingly small cascade**: 17 errors total across cabinet.js (4), cutlist.js (10), quotes.js (3) — all `querySelector` / `querySelectorAll` call sites that didn't narrow. Standard fix pattern adopted: cast the iterator type, not each call site. `/** @type {NodeListOf<HTMLElement>} */ (qSAll(...)).forEach(...)` applied once and the forEach callback gets `b: HTMLElement` for free (used 9 times across the three files). For single-element results: cast inline (`/** @type {HTMLElement | null} */ (qS(...))`) — used twice in cutlist for `.cl-left` and the table-cell focus target. Specific subtypes for narrower types: `NodeListOf<HTMLCanvasElement>` for canvas spread→toDataURL; `HTMLInputElement | null` for the input that receives focus+select. Plus an ad-hoc `Array.from(sidebar.children).filter(...)` cast to `HTMLElement[]` once for the cabinet builder/rates tab toggle. The Window declarations block in globals.d.ts is untouched. Verified: typecheck clean; reload + cutlist optimize() → 3 canvases; toggleCol('grain') and toggleEdgeBandCol() execute without console errors (those were among the rewired call sites).
- 2026-05-02 — **Item 3 phase G.3 complete — noImplicitAny + full strict mode** (10 commits, ~1260 errors fixed across all 17 non-clean src files). Closed out the strict-mode push: tsconfig now has `strict: true` (which expands to `noImplicitAny + strictNullChecks + useUnknownInCatchVariables + strictFunctionTypes + alwaysStrict + strictPropertyInitialization + noImplicitThis + strictBindCallApply`). All 19 src files type-check clean. **Initial flag flip**: 1247 errors total — 627 TS7006 (untyped params), 480 TS7005/7034 (untyped vars/declarators including cross-file `let` globals), 53 TS7053 (untyped object indexing), plus a few others. **G.3.1 (cross-file global annotations, 1 commit, ~290 errors closed)**: each src/*.js file's top-level `let`-declared global got a JSDoc type annotation right at the declaration site — `_userId: string | null`, `orders: Tables<'orders'>[]`, `quotes: Tables<'quotes'>[]`, `stockItems: Tables<'stock_items'>[]`, `clients: Tables<'clients'>[]`, `projects: (Tables<'projects'> & {status?: string})[]`, plus `any[]` for the in-memory cut-list/cabinet-quote state (sheets / pieces / edgeBands / cqLines / cqLibrary / etc.). Source-side annotation chosen over `globals.d.ts` `var` declarations because TS treats classic-script files as scripts (not modules) and `var` in a d.ts conflicts with `let` in source under TS2451 redeclare. **G.3.2 (per-file annotation, 9 commits, ~970 errors closed)**: smallest-first attack order — auth/forms/ui/settings/business/schedule/orders cleared in batches of 1-30 errors each, then projects/dashboard/clients/migrate/app/quotes/stock/cabinet/cutlist as the larger files. Patterns adopted: `/** @param {string} foo */` JSDoc above every untyped function (mostly mechanical `string`/`number`/`any` annotations); `/** @type {Record<string, T>} */` on lookup-table object literals (STATUS_LABELS, GRAIN_ICONS, CL_COLS, statusColMap, etc.) so dynamic-key indexing works; `i.qty ?? 0` / `o.value ?? 0` / `q.markup ?? 0` everywhere arithmetic uses nullable schema columns; row-literal cast-through-any at insert/update boundaries where the local in-memory shape has fields beyond the DB schema. The `cqSettings` declaration in cabinet.js cast `any` because the codebase mutates it dynamically (adds edgeBanding sub-keys, labourTimes, materials lists, etc.) — stricter typing would force a giant union. For the deepest nested callbacks (drawCanvas's buildGuillotinePlan + 30+ inner arrow callbacks in cutlist.js), a small Python script auto-injected JSDoc by parsing the typecheck output and inserting `/** @param {any} name */ (name) =>` rewrites; verified by re-running typecheck. **G.3.3 (final commit + strict:true)**: flipped `strict: true` in tsconfig as a sanity check — surfaced exactly 13 TS18046 errors, all `useUnknownInCatchVariables` (catch `e` is `unknown` instead of `any`). All same shape: `(/** @type {any} */ (e)).message || e` cast at the access site, batch-fixed with another small Python script. Other strict flags (`strictFunctionTypes`, `strictPropertyInitialization`, `noImplicitThis`, `strictBindCallApply`) surfaced zero new errors — the codebase was already conformant. **Anti-patterns avoided**: no `// @ts-nocheck` reintroductions; no silent runtime changes (every `??` substitution preserves the original `||` semantics where the field is nullable; the `any` casts at row boundaries match what the runtime already does). Verified: `npm run typecheck` clean under full `strict: true`; reload + 8 nav tabs render; demo data persists; cutlist optimize() runs without errors; no console errors. Phase G is now closed in full — tsconfig is strict, DB row types thread through `_DBBuilder<K, Single>`, and the Element/EventTarget augs are gone. Plan-3's remaining items (rename `*.js` → `*.ts`, deploy cutover) are independent of strict mode and tracked separately.
- 2026-05-02 — **Phase H1 follow-up — GitHub Actions auto-deploy wired** (1 workflow file, 2 commits + 1 SPEC commit). After confirming Cloudflare's dashboard doesn't allow converting a direct-upload Pages project to git-connected (Settings exposes only Variables/Bindings/Runtime/General — no Source/Builds-and-deployments tab on direct-upload projects), pivoted to GitHub Actions. New `.github/workflows/deploy.yml` runs on push to `main` and on `workflow_dispatch`: checkout → setup-node@v4 (Node 20) → `npm ci` → `npm run typecheck` (gate) → `npm run build` (with VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY hardcoded inline — they're publishable values, already in every deployed bundle, so not actually secrets) → `cloudflare/wrangler-action@v3` deploys `dist/` to the cabinetry-app project. Two repo secrets used: `CLOUDFLARE_API_TOKEN` (custom token scoped to Account / Cloudflare Pages / Edit, restricted to user's account) and `CLOUDFLARE_ACCOUNT_ID` (`cf6a335f2b8b6fded3b921c9e318ba03`). **First run validation**: pushed all 100 unpushed local commits + the new workflow → workflow ran in 40s end-to-end → all steps green → procabinet.app served the same bundle hash (deterministic build). **Subtle gotchas hit and resolved**: (a) `gh push` rejected with `OAuth App ... without 'workflow' scope`; gh's default scopes are `gist, read:org, repo`, so `gh auth refresh -s workflow` was required to add the scope. (b) Local branch was 100+ commits ahead of remote (the entire Phase E/F/G/H sequence had been worked locally without pushing) and remote had 1 ahead (`Delete CNAME`, auto-committed by GitHub when the custom domain was removed from Pages settings); first attempted `git pull --rebase` half-applied 50 of 100 commits before failing on a working-tree-overwrite conflict in db.js/ui.js; recovered with `git rebase --abort` (which re-popped the autostash automatically) then `git merge origin/main --no-edit` for a clean merge commit. (c) macOS iCloud sync produced ~25 untracked `* 2.*` duplicate files in the working tree; left untracked, ignored. **Heads-up logged**: GitHub annotation warns Node 20 actions deprecate June 2026; bump to Node 24 / actions@v5 ahead of that date. Local-dev escape hatch (manual `npm run build && npx wrangler pages deploy dist --project-name=cabinetry-app --branch=main`) still works for emergencies.
- 2026-05-02 — **Phase H1 — production cutover to Cloudflare Pages complete** (Phase H closed; plan-3 fully done). Pivoted from the Cloudflare Pages dashboard's GitHub-OAuth flow (which silently fails to register the GitHub App authorization on the Cloudflare side; standard "click Connect GitHub, get bounced back to a blank repo list" flake) to a wrangler-CLI direct-upload deploy. Added `wrangler@4.87.0` as a devDependency, ran `npx wrangler login` (one-click OAuth in browser), `npx wrangler pages project create cabinetry-app --production-branch main`, then `npx wrangler pages deploy dist --project-name cabinetry-app --branch main` — first production build deployed to `https://cabinetry-app.pages.dev` with hashed bundles inlined-env-var-correctly (`grep mhzneruvlfmhnsohfrdo dist/assets/*.js` returns 1, the `import.meta.env` replacement). DNS migration: domain registered at IONOS (nameservers `ns10xx.ui-dns.*`); user added procabinet.app to Cloudflare via the standard "add a domain" flow which auto-imported the existing IONOS records (4 GitHub Pages A records + email MX/SPF/DMARC + www CNAME); switched IONOS nameservers from `ui-dns.*` (4) to `raegan.ns.cloudflare.com` + `weston.ns.cloudflare.com` (2); propagation was instant. Cloudflare auto-detected the nameserver flip and marked the domain Active immediately. Adding `procabinet.app` as a Pages custom domain triggered Cloudflare to swap the four GitHub Pages A records (`185.199.x.153`) for a CNAME-flatten pointing at the Pages worker; SSL cert provisioned in <1 min. **Verification**: `dig +short A procabinet.app @1.1.1.1` returns `172.67.146.51` / `104.21.28.131` (Cloudflare edge, was GitHub `185.199.x.153`); `curl -I https://procabinet.app` returns `HTTP/2 200 server: cloudflare`; bundle hash matches the *.pages.dev deploy; user-side smoke (sign in, dashboard data, all 8 nav tabs, PDF export, project edit + reload + persisted, clean console) passed on `https://procabinet.app`. **GitHub Pages disabled**: removed the procabinet.app custom domain claim, then "Unpublish site" in repo settings. **CNAME file kept** in repo for now (Cloudflare ignores it; useful as a quick rollback signal). **Auto-deploy on push to main NOT yet wired**: current setup is direct-upload via wrangler — to deploy a code change, run `npm run build && npx wrangler pages deploy dist --project-name cabinetry-app --branch main`. Auto-deploy can be added later either by re-attempting the GitHub App OAuth in the Cloudflare dashboard now that the project exists, or by adding a GitHub Actions workflow that runs wrangler on each push (would use a Cloudflare API token stored as a repo secret). **Bot Fight Mode + Leaked credentials mitigation toggled ON** in the procabinet.app Cloudflare zone — defense-in-depth for the auth surface; client-side-security toggle left OFF (no third-party scripts in the bundle). The hardcoded `sb-mhzneruvlfmhnsohfrdo-auth-token` localStorage key in db.js still ties auth sessions to the project ref — fine for now since the URL is stable.
- 2026-05-02 — **Phase H0.4 — preflight for deploy cutover** (3 commits, no production effect). Extracts the hardcoded Supabase URL + publishable key from `src/db.js:11-18` to `import.meta.env.VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`. Constraint: `import.meta.env` is module-only and `db.js` is a classic script — solution is to read the env vars in `src/main.js` (the existing ES module bridge), assert they're set with an actionable error, and stash them on `window._SBURL` / `window._SBKEY`; `db.js` reads from window. HTML spec guarantees module scripts run before classic-defer scripts, so the order is correct. `.env.example` committed as a template; `.env.local` gitignored (already excluded) holds dev values. `src/globals.d.ts` adds `_SBURL: string; _SBKEY: string;` to the Window stash slots block. Dead dev-server cleanup: deleted `server.py` (Python http.server with a hardcoded path that was already broken — missing `Claude/Projects/`), deleted `.claude/server.js` (the npx-serve port-8081 helper), removed the "Static File Server (npx serve)" entry from `.claude/launch.json` (only "Vite dev" remains). `CLAUDE.md` Stack section refreshed — was wildly out of date (said "main app logic ~10k lines" when post-Phase-E it's ~1.1k across 16 carves; said "No build step" when Vite is the build tool); new "Build & deploy" section documents `npm run {dev,build,typecheck}` and the Cloudflare Pages target. **Verification**: `npm run typecheck` clean; `npm run build` clean; bundle inlines the URL once via Vite's `import.meta.env` build-time replacement (verified via `grep -c mhzneruvlfmhnsohfrdo dist/assets/*.js`); preview reload confirms `window._SBURL` / `window._SBKEY` populate correctly, `window.supabase.createClient` is ready, console clean. **The localStorage auth-token key (`sb-mhzneruvlfmhnsohfrdo-auth-token` in db.js:23) still hardcodes the project ref** — fine for now since the URL doesn't change; would need to be derived from URL if we ever switch projects. H1 (the actual production cutover — Cloudflare Pages connection + DNS) lands in a follow-up session per the two-pass split decision in `~/.claude/plans/phase-h-deploy-cutover.md`.
- 2026-05-02 — **Schema-divergence H0.3 — quotes popup inserts: Date.now() ids dropped, schema-mismatch fields removed** (1 commit, addresses bug #2 from G.4's TODO audit). Both popup-creation flows in `quotes.js` were creating in-memory rows with `id: Date.now()`, pushing optimistically, then firing-and-forgetting an insert. The DB has serial PKs that silently override; the in-memory array ended up with phantom Date.now() ids that didn't match the DB rows — every subsequent `.find(p => p.id === ...)` mismatched. **`_saveNewClientPopup`**: drops `id: Date.now()`, awaits `_db('clients').insert(insertBody).select().single()`, errors-and-returns on failure (toast + console.error), pushes the DB-returned `data` (with the real serial id). Pattern matches `stock.js:523` already-correct insert flow. **`_saveNewProjectPopup`**: same pattern, plus drops the schema-mismatch fields — `client: clientName` (string) is replaced by `client_id: cli?.id ?? null` via a `clients.find(c => c.name === clientName)` lookup; `desc` becomes `description` (the H0.1 column); the Insert type is now `Tables<'projects'>['Insert']`-compatible without the `/** @type {any} */` cast. **`_autoFillClientFromProject` rewritten** (the only consumer of the legacy `proj.client` shadow string): now reads `proj.client_id`, looks up the matching client in the `clients` array, fills the input with `cli.name`. This fixes a latent bug — projects loaded from the DB previously had no `client` shadow field, so the autofill silently failed for every existing project; now it works for both newly-created and DB-loaded projects. **All 5 TODO(schema-divergence) sites from G.4 are now resolved.** **Verification**: `npm run typecheck` clean; `npm run build` clean; reload console clean; preview eval probe injected a fake project (`client_id: 888`) + matching client (`id: 888, name: "Probe Client"`) and confirmed `_autoFillClientFromProject` correctly resolved and filled `"Probe Client"`. End-to-end UI smoke deferred — preview unauthenticated this session.
- 2026-05-02 — **Schema-divergence H0.2 — stock_items shadow field hydration on load** (1 commit, addresses bug #1 from G.4's TODO audit). The codebase pervasively reads stock-item edge-band metadata via short shadow names (`thickness`/`width`/`length`/`glue`) in cut-list dropdowns and edge-band UI; the DB columns added in Phase 7 are `thickness_mm`/`width_mm`/`length_m`/`glue`. Insert-time path at `stock.js:528-535` set the shadow fields directly on the post-insert row, so they were correct for the immediate session but lost after reload — `loadAllData` selected `*` from `stock_items` and the short names came back undefined. **Fix**: replaced `stockItems = stk || []` in `app.js loadAllData` with a load-time map that copies `thickness_mm → thickness`, `width_mm → width`, `length_m → length` whenever the source value is non-null. The `& { thickness?: number, width?: number, length?: number, thick?: number }` intersection on the `stockItems` declaration in `stock.js:303` already permits the shadow fields, so no type changes needed. The in-place mutations at `stock.js:530-535` stay (immediate-session correctness without waiting for reload); the TODO(schema-divergence) comment is replaced with a one-liner pointing at the load map. **Why not rename consumers**: the short names appear in template-literal HTML inside cut-list and edge-band code at ~30+ sites — separate rename pass, deferred. **Verification**: `npm run typecheck` clean; `npm run build` clean; reload console clean; SQL confirms 3 stock_items rows have the *_mm/*_m columns populated (id 20 Oak Veneer 2/22/50/Pre-glued is the canonical test case). UI smoke deferred — preview unauthenticated.
- 2026-05-02 — **Schema-divergence H0.1 — projects.status + projects.description promoted to real columns** (1 commit, addresses bugs #3 and #4 from G.4's TODO audit). Migration `h01_projects_add_status_description` adds `status text not null default 'active'` and `description text` to `public.projects`; the default backfilled all 4 existing rows to `status='active', description=null` (matches the in-memory shadow the codebase had been injecting at load time). Regenerated `src/database.types.ts` via the Supabase MCP `generate_typescript_types` tool — `Tables<'projects'>` now exposes `status: string` (NOT NULL) and `description: string | null`. Code changes: `src/app.js _saveProjectPopup` dropped the `/** @type {any} */` cast on the update body — `{ name, status, description }` is now a valid `Tables<'projects'>['Update']` shape; `loadAllData` dropped the in-memory `.map(p => ({ ...p, status: p.status || 'active' }))` and is now plain `projects = prj || []` since defaults come from the DB; the `(Tables<'projects'> & {status?: string})[]` intersection on the `projects` declaration in `stock.js:307` simplified to `Tables<'projects'>[]` (the intersection became redundant once `status` was a real column). `SCHEMA.md § 3.6` updated to document the new columns. Both TODO(schema-divergence) comments at app.js:491 and app.js:985 removed. `quotes.js _saveNewProjectPopup` (Date.now() ids + extra `client/desc` fields) NOT touched yet — that's H0.3's job. **Verification**: `npm run typecheck` clean; `npm run build` clean; SQL `SELECT id, name, status, description FROM projects` confirms backfill (4 rows, status='active', description=null). UI smoke deferred — preview was unauthenticated this session, no signed-in flow available.
- 2026-05-02 — **Post-H cleanup: `cq*` → `cb*` symbol rename** (1 commit, 10 files, 770 symmetric line changes). Renamed all `cq`/`CQ`-prefixed symbols to `cb`/`CB` (Cabinet Builder) across the entire codebase — state vars, functions, HTML IDs, CSS classes, and comment headers. Zero collisions with existing `cb*` symbols. localStorage key strings (`pc_cq_*`) intentionally kept unchanged to preserve existing user data. Typecheck + build clean; dev server verified all 9 tabs render, `cbLines`/`cbSettings`/`calcCBLine` load correctly, old `cqLines`/`cqSettings` globals are `undefined`.
- 2026-05-03 — **Item 2 phase 1.1 + 1.2 — draft-quote helpers + DB dual-write** (3 commits). Plan at `~/.claude/plans/item-2-cabinet-quote-unification.md` with 4 decisions resolved: draft tag in `quotes.notes` (`[CB_DRAFT]`), drop guest mode for Cabinet Builder, full cbSettings move to business_info, edit-in-place + Duplicate button. **1.1**: added `_isDraftQuote` and `_findOrCreateDraftQuote` to `src/quotes.js` (notes-prefix convention, find-or-create per project). Filtered drafts out of Quotes tab list, dashboard counts, CSV export, client-card and project-card quote chips. No callers yet — pure plumbing; no DB rows tagged, behavior unchanged. **1.2**: `saveCBLines()` now debounces (800ms) a fire-and-forget DB sync to the project's `[CB_DRAFT]` quote — REPLACE semantics on `quote_lines`. No-op without auth or when `cb-project` input doesn't match a real `projects` row. Harmonized `_replaceQuoteLinesChildTable` (projects.js) onto the same helper — was using `[CB_DEFAULT]` tag, now `[CB_DRAFT]`; both code paths target one row per project. LocalStorage stays source of truth until phase 1.4. Verified: typecheck + build clean; dev server reload — all helpers loaded, `saveCBLines()` no-throw in guest mode. End-to-end DB-write verification deferred — preview unauthenticated, will test on live site post-deploy.
- 2026-05-04 — **Item 2 phase 1 complete — cbLines lives in DB, auth required** (sub-steps 1.3 + 1.4 + 1.5, 4 commits + 1 dev-infra commit). **1.3** added `_loadCBLinesFromDB` in cabinet.js, called from `loadAllData` after auth + projects + quotes are populated; finds the saved-project-name's draft quote and replaces in-memory cbLines with the converted `quote_lines` rows. No-op when LS lacks a saved project name, when no draft exists, or when the draft has zero lines. **1.4** added `_renderCBAuthGate` — `renderCBPanel` early-returns to a sign-in prompt overlay (lock icon, "Sign in to use Cabinet Builder" CTA opening the existing auth screen) when `_userId` is null. The builder DOM stays in place but `display:none`'d. Sign-out hook in app.js's auth state handler clears in-memory cbLines and re-renders so the gate appears immediately. Per Item 2 plan: cabinet workflow is now sign-in-required; cut-list optimization remains guest-friendly. **1.5** removed `localStorage.setItem('pc_cq_lines', ...)` from `saveCBLines` — debounced 800ms sync to `quote_lines` is now the only cabinet-data persistence path. Navigation state (project name, client name) stays in LS as a "what was last selected" pointer for the next session's `_loadCBLinesFromDB` resolution. After successful DB load, the legacy `pc_cq_lines` key is removed so it can't shadow a future session. **Race guard** added: `_loadCBLinesFromDB` bails when `_cbLinesSyncTimer` is non-null — `loadAllData` re-runs on every auth event including hourly TOKEN_REFRESHED, so without this guard a token refresh during the 800ms debounce window could clobber unsaved edits with stale DB state. **Dev infrastructure** (separate commit, dev-only): added `window._signInForTesting()` in `db.js`, gated on `import.meta.env.DEV` + `VITE_TEST_EMAIL` / `VITE_TEST_PASSWORD` (in `.env.local`, gitignored). Lets the assistant authenticate the local Vite preview to verify auth-gated behavior. Production builds skip the gate entirely. **Verification**: `npm run typecheck` + `npm run build` clean at every commit. End-to-end signed-in test in dev preview: clear `pc_cq_lines`, sign in, confirm cbLines hydrates from DB only. Edit cabinet name to a unique marker, wait for sync, verify in DB via SQL. Reload, confirm marker survives without LS involvement. Sign out → gate appears, cbLines cleared. Sign in → builder + cabinets restored from DB. SPEC.md § 13 entry below covers 1.1 + 1.2; this entry covers 1.3 + 1.4 + 1.5.
- 2026-05-05 — **Item 2 phase 4 complete — cleanup**. Closes Item 2; the Cabinet Builder ↔ Quote unification is fully shipped. **4.1 — catalog_items CRUD wiring**: new `_syncCatalogToDB()` in business.js — debounced 800ms REPLACE-semantics sync of `cbSettings.materials` / `cbSettings.hardware` / `cbSettings.finishes` to the `catalog_items` table (delete all rows for the user filtered to the three managed types, then re-insert from in-memory state). Wired into `saveCBSettings()` so inline edits in the My Rates panel persist past reload — fixes the prior bug where `_applyCatalogFromDB` re-applied stale rows after a TOKEN_REFRESHED. Race guard added to `_applyCatalogFromDB` (`if (_catalogSyncTimer) return`) matching the patterns from phase 1.5 / phase 3. `_DBBuilder` got a new `.in(col, vals)` method (PostgREST `column=in.(v1,v2,v3)` syntax) for the multi-type delete filter — kept the existing _DBBuilder shape, added one method. The 'handle' catalog type rows are not touched by the sync (cbSettings has no `handles` field after the cq→cb rename), so any rows of that type from old migrations are preserved. **4.2 — cbSavedQuotes removed**: state vars (`cbSavedQuotes`, `cbActiveQuoteIdx`), helpers (`loadCBSaved`, `saveCBSaved`), CRUD functions (`saveCBQuote`, `loadCBQuote`, `newCBQuote`, `deleteCBQuote`, `dupCBSavedQuote`), and the `renderCBSavedShelf` pill UI all deleted. The `pc_cq_saved` localStorage key is no longer written; `migrate.js _migrateSavedQuotes` still reads it for one-time legacy migration into the `quotes`/`quote_lines` tables. Init block at end of cabinet.js dropped the `loadCBSaved()` call. **4.3 — cbProjectLibrary removed**: state var, `loadCBProjectLibrary` / `saveCBProjectLibrary`, full CRUD (`cbSaveProject`, `cbLoadProject`, `cbDeleteProject`, `renderCBProjects`, `cbExportProjects`, `cbImportProjects`), and the `_cbSaveProjectByName` flow all deleted. `toggleCabPanel` simplified — used to handle 'projects' AND 'library' branches; now only the 'library' branch (cabinet template library, a separate concept). `pc_cq_projects` no longer written; `pc_cq_project_name` / `pc_cq_client_name` kept as nav-state pointers per the plan. Init block dropped the `loadCBProjectLibrary()` call. Migration helper still references `pc_cq_projects` for one-time read. **4.4 — converters slimmed (not dropped)**: discovered the plan's "drop entirely" framing was overstated. The converters (`_quoteLineRowToCB` / `_cbLineToRow`) ARE load-bearing, not dead weight: cb-only fields `backMat` / `doorMat` exist in the in-memory shape AND in renderCBEditor's UI inputs but have no matching schema columns (DB only stores `material`), so the round-trip silently collapses both back to the carcass material. Additionally, the full rename from camelCase → snake_case would touch ~250 cabinet.js sites and `.w` / `.h` / `.d` collide with `stockItems` (which has its own `w`/`h` columns) AND cutlist `pieces` (which has `p.w`/`p.h`). Slimmer outcome: kept the converter pair, added explicit `// ── cbLines ↔ quote_lines boundary converters ──` heading with a "Round-trip caveat" doc block listing the cb-only fields (`id`, `backMat`, `doorMat`) that don't survive a save → reload cycle, and a TODO note that the proper fix is `back_material` + `door_material` schema columns (deferred). The full rename is parked behind a future `cb*` → `qb*` symbol rename pass. **4.5 — file rename skipped**: `src/cabinet.js` → `src/quote-builder.js` rejected as a half-measure — the internal symbols still all start with `cb*` (`cbLines`, `cbSettings`, `calcCBLine`, `renderCBPanel`, etc.), and a 2,499-line pure-cosmetic git diff would muddy history without delivering enough mental-model gain on its own. Bundle with a future symbol-rename pass if it ever happens. **File deltas**: cabinet.js 2,705 → 2,499 lines (−206); business.js 164 → 199 lines (+35, the new sync helper); db.js +1 method (`.in()`). **Verification**: `npm run typecheck` clean; preview eval confirmed all 20 removed legacy symbols are now `undefined` and all 9 kept/new functions are `function`; reload + 4-tab smoke (cabinet/quote/stock/cutlist) all render; `toggleCabLibrary()` round-trips clean; no console errors. End-to-end DB-write verification of the catalog sync deferred — preview was unauthenticated this session, will smoke on procabinet.app.
- 2026-05-05 — **Item 2 phase 2 complete — UI unification**. Brings Cabinet Builder and Quotes onto one editing surface. **2.1 — project gate**: new `_ensureCBProject()` in cabinet.js auto-creates the `projects` row on first cabinet add when the typed name doesn't match an existing project (uses existing `resolveClient`/`resolveProject` from clients.js); `addCBLine` and `addCBLineFromPreset` are async and bail with a toast when no name is entered; new `_renderProjectStatus()` shows a "⚠ Select or create a project to save" hint under the project input when `_getCBProjectId()` returns null. **2.2 — Edit button**: new `editQuoteInCB(quoteId)` loads a quote's `quote_lines` via `_quoteLineRowToCB`, sets a new `cbEditingQuoteId` global (number = editing specific quote, null = editing project draft), persists to `localStorage('pc_cb_editing_quote_id')`, and switches to the cabinet tab. While set, `_syncCBLinesToDB()` routes through new `_syncCBLinesToQuote(id)` instead of the project draft — same delete-then-insert REPLACE semantics, plus `_refreshQuoteTotals(id)` so the quotes-tab `_totals` cache updates. `renderCBResults()` shows an "Editing: <project>" banner with Done/Discard buttons (replacing the normal-mode "Create Quote" button). `finishEditingQuote()` flushes the pending debounced sync and reloads the project draft via `_loadCBLinesFromDB()`. `discardQuoteEdits()` re-writes the snapshot in `cbEditingOriginalLines` to the DB before exiting. `_loadCBLinesFromDB()` extended to check the localStorage key and restore from the editing-quote's lines first, with an additional guard `if (cbEditingQuoteId && cbLines.length > 0) return` to prevent TOKEN_REFRESHED from clobbering an active edit. `removeQuote()` in quotes.js clears the editing localStorage when the deleted id matches. Edit button added to the quote-card footer in `renderQuoteMain`. **2.3 — Duplicate**: already shipped — the existing "Copy" button on quote cards calls `duplicateQuote(id)` (quotes.js:949), which already copies the quote row plus all `quote_lines` rows. No code change. **2.4 — Create Quote**: new `cbCreateQuoteFromDraft()` snapshots the current cbLines into a brand-new customer-facing quote (no `[CB_DRAFT]` tag) — calls `_ensureCBProject` for the project_id, builds a notes string from line specs, inserts the `quotes` row, then inserts the converted `quote_lines` via `_cbLineToRow`, then `_refreshQuoteTotals`. The draft stays as the live workspace, so the user can iterate and create additional quotes from the same project. Single "Create Quote" button replaces the old "Add to Existing Quote" / "+ New Quote" pair. Legacy `cbAddToNewQuote` / `cbAddToExistingQuote` kept as thin stubs that delegate to `cbCreateQuoteFromDraft()`; `_cbApplyToQuote` is a no-op stub. The legacy internals (form pre-fill, inline picker dropdown, append-to-existing path) were removed via an awk-based delete after the Edit tool struggled to match the unicode `×` / `—` characters in the source. **Verification**: `npm run typecheck` clean; preview eval confirmed all new functions defined, Edit button present on quote cards (`["Mark Sent","→ Order","Edit","Copy","Print","PDF"]`), normal-mode buttons (`["Create Quote","↓ PDF","⊕ Print","↓ Export","↑ Import"]`), editing-mode swaps to `["Done","Discard"]` with the banner shown, `cb-project-status` div in DOM. End-to-end DB-write smoke deferred — preview was unauthenticated this session.
- 2026-05-05 — **Item 2 phase 3 complete — cbSettings lives in business_info** (1 commit). Migration `phase3_business_info_add_cb_settings_columns` adds 6 columns to `business_info`: `default_deposit_pct numeric default 50`, `default_edging_per_m numeric default 0`, plus jsonb columns for `default_labour_times`, `default_base_types`, `default_constructions`, `default_edge_banding` (all default `'{}'` / `'[]'`). Regenerated `src/database.types.ts`. **`_applyBizInfoFromDB` rewritten** — hard overlay (DB always wins) replaces the prior soft overlay (only-if-defaults). All 6 new fields plus the 3 existing (labour_rate / markup / tax) overlay onto cbSettings. **Race guard** (mirrors phase 1.5's _loadCBLinesFromDB): bails when `_cbSettingsSyncTimer` is pending — prevents hourly TOKEN_REFRESHED loadAllData from clobbering unsaved edits. **`_syncCBSettingsToDB` added in business.js**: debounced 800ms upsert of all cbSettings → business_info, mirroring `_syncBizInfoToDB`'s pattern. Both helpers update the same row but write disjoint column sets. **`saveCBSettings` rewired**: drops `localStorage.setItem('pc_cq_settings', ...)`, calls `_syncCBSettingsToDB()` instead. `loadCBSettings` keeps its single LS read as a one-time backwards-compat hydrate before auth fires. After successful DB overlay, `_applyBizInfoFromDB` clears the legacy `pc_cq_settings` LS key so it can't shadow on a future session. **Out of scope (defer)**: `materials`, `hardware`, `finishes` — already in `catalog_items`, but their inline-edit handlers in renderCBRates write to in-memory cbSettings only (no catalog_items CRUD). Edits don't persist past reload. Separate concern; tackle in phase 4 cleanup or its own pass. **Verification**: `npm run typecheck` + `npm run build` clean; signed-in dev preview test set markup=33.3 + deposit=42, confirmed via SQL the DB row updated and LS `pc_cq_settings` stayed null. Restore-to-defaults round-tripped cleanly.
- 2026-05-05 — **Door / drawer labour: per-count linear → m²/m³ power-law + multi-type**. Extended the carcass power-law to the rest of the labour model. New `cbSettings` arrays: `carcassTypes` / `doorTypes` / `drawerFrontTypes` / `drawerBoxTypes`, each with `{name, refHours}` rows; cabinet lines pick one type per category (`line.carcassType` / `doorType` / `drawerFrontType` / `drawerBoxType`). Power-law formulas `hours = refHours × (actual / reference)^0.7` use four hardcoded reference geometries derived from a 600×720×580 cabinet at 50% door / 30% drawer pct: `CARCASS_REF_VOLUME=0.25 m³`, `DOOR_REF_AREA=0.216 m²`, `DRAWER_FRONT_REF_AREA=0.1296 m²`, `DRAWER_BOX_REF_VOLUME=0.07517 m³` (constants in `src/cabinet-calc.js`). **Geometry semantic shift**: `doorPct` and `drawerPct` are now "% of full front face area" (silently reinterpreted from old "% of cabinet height"). New default `drawerPct=0`; door + drawer pcts clamp to ≤100 in the editor (`cbUpdatePct` helper in `src/cabinet.js` trims the partner pct), with an "Open: X%" readout under each slider. **Material formulas updated** to use `pct/100 × W × H` for total areas; drawer box height derived as `frontArea / drawers / innerW`. **Multi-type seeds**: Carcass:Standard / Doors:Slab+Shaker+Vinyl-Wrapped+Integrated Handle / Drawer Fronts:Slab+Shaker / Drawer Boxes:Standard+Dovetail (user can edit/delete). **DB**: migration `add_cb_labour_type_columns_to_business_info` adds 4 jsonb columns to `business_info` (`default_carcass_types`, `default_door_types`, `default_drawer_front_types`, `default_drawer_box_types`, all default `'[]'`). Regenerated `src/database.types.ts`. `_syncCBSettingsToDB` extended to upsert the 4 arrays; `_applyBizInfoFromDB` extended to overlay them on auth (only when DB row has non-empty arrays). **UI**: My Rates panel got 4 new collapsible "Types" sections (each with reference geometry in the title, e.g. "Door Types (per 0.22 m²)") and a renamed "Other Labour Times" section for the remaining fixed-rate components (Fixed Shelf, Adj. Holes, Loose Shelf, Partition, End Panel, Finish). Cabinet editor gained 4 type dropdowns (Carcass type in Cabinet section; Door type in Doors section; Front type + Box type in Drawers section). **Back-compat**: legacy `lt.door` / `lt.drawer` / `lt.carcass` fields ignored when type lists are present; old `doorPct=95` cabinets render visually similar under new semantics; no migration script needed.
- 2026-05-05 — **Carcass labour: linear → power-law scaling**. Replaced `autoLabour += (lt.carcass || 1.5) * volume` with `refHours × (volume / refVolume)^exponent` in both `calcCBLine` and `calcCBSections` (`src/cabinet-calc.js`). Three new `cbSettings.labourTimes` fields drive the curve: `carcassRefVolume` (default 0.25 m³ — typical 600×720×580 base cabinet), `carcassRefHours` (default 0.4 hrs), `carcassExponent` (default 0.7). Rationale: a cabinet twice as big does not take twice as long — most cabinet labour is surface work (cuts, banding, drilling, sanding), and surface area scales as V^(2/3) for similar shapes, so n in the 0.6-0.8 range matches reality. Old `lt.carcass: 1.5` field kept in defaults as a no-op for back-compat with persisted settings (the new fields take precedence). My Rates UI in `src/cabinet-render.js` swapped the single "Carcass (volume)" row for three rows ("Carcass: ref volume / ref hours / scaling (n)"). Calibration: at the reference volume the new formula yields exactly `refHours`; at 4× volume (1.0 m³) it yields ~1.06 hrs vs the old 1.5 hrs (linear) — large cabinets stop being penalized for size. No migration script needed; the legacy field is just ignored.
- 2026-05-05 — **Stripe pricing pivot to USD-only + Adaptive Pricing; preferred_currency dropped + F.1 limits module landed**. Same-day reversal of the multi-currency Stripe Prices decision: switched to USD-only ($35/mo, $299/yr) with Stripe Adaptive Pricing handling local-currency conversion at Checkout. UK Stripe account still settles in GBP via the FX spread. Migration `drop_business_info_preferred_currency` removed the column added earlier in the session — zero rows had non-null values, no code referenced it. Regenerated `database.types.ts`. **F.1 — limits module shipped**: new `src/limits.js` (loaded between `ui.js` and `orders.js` in index.html) declares `FREE_LIMITS` (frozen 5-each for clients/projects/quotes/orders/cabinet_templates/stock), `_subscription` global, `loadSubscription()`, `isPro()`, `getLimit()`, `isAtLimit()`, `isApproachingLimit()`. `loadAllData` kicks `loadSubscription()` off in parallel with the existing data fetches via `subPromise` (its return value isn't destructured — side effect updates the global). `signOut()` and the auth-state-change handler both reset `_subscription = null`. Returns false / cap=5 for everyone until S.6 webhook starts populating subscription rows. Docs updated: PLAN.md S.2/S.4/S.5/S.8 simplified for USD-only; PLAN.md F.1 marked done; SCHEMA.md § 3.1 reverted to original; SCHEMA.md § 3.17 (subscriptions) kept; § 5 modified-tables list cleaned. `npm run typecheck` clean.
- 2026-05-05 — **Stripe S.3 — subscriptions table + multi-currency prep**. Migration `create_subscriptions_and_preferred_currency` applied to project `mhzneruvlfmhnsohfrdo`. New `subscriptions` table tracks Pro state (`stripe_customer_id` unique-not-null, `stripe_subscription_id` unique, `stripe_price_id` cache for currency+cadence, `plan` text cache 'pro_monthly'/'pro_annual', `status`, `current_period_start/end`, `cancel_at_period_end`, `canceled_at`, timestamps). Free users have **no row** — null/missing = free. RLS deviates from Pattern A: SELECT-only policy for `user_id = auth.uid()`; no INSERT/UPDATE/DELETE policies (Stripe webhooks write via service role, bypassing RLS). Prevents users from forging subscription rows for themselves. Also added `business_info.preferred_currency text check (in 'GBP','USD','EUR','AUD')` for the multi-currency Checkout flow — distinct from existing `default_currency` (which is the symbol shown on quote/invoice PDFs, not a Stripe currency). Detected from Cloudflare `CF-IPCountry` on first upgrade visit, user can override in Settings. **Decisions locked this session**: pricing model = manual multi-currency Stripe Prices (4 currencies × 2 cadences = 8 Prices, GBP base £29/£249, USD/EUR/AUD TBD by user); no free trial (free tier IS the trial); no row for free users (simpler than seeding everyone with a 'free' status). Regenerated `src/database.types.ts` via Supabase MCP. SCHEMA.md § 3.1 + new § 3.17 updated. `npm run typecheck` clean.
- 2026-05-06 — **Multi-unit format system** (new file `src/units.js`, ~10 files changed, 1 DB migration). Added `formatDim()` / `parseDim()` / `convertDim()` / `unitLabel()` in a new `src/units.js` loaded before `cutlist.js`. `window.unitFormat = { mode, decimals, denominator }` alongside existing `window.units`. Imperial modes: decimal, fractional (GCD-reduced), feet-inches. Metric modes: mm, cm. Settings UI: format pills, decimal places (0/1), fraction precision (1/4–1/64) in the gear dropdown between Units and Currency. **Key decisions**: (1) internal storage keeps full precision — `convertDim()` never rounds, `formatDim()` rounds for display only, enabling lossless round-trips (96.375" → 2447.925mm → 96.375"). (2) Auto-reset format mode on unit-system switch (imperial fractional → metric resets to mm; metric cm → imperial resets to decimal). (3) CSV export keeps raw numbers for machine readability. (4) `parseDim()` supersedes `parseVal()` for dimension inputs — handles feet-inches (`1' 3 3/8`), mixed fractions, unit suffixes. **Bug fixes during implementation**: `_toFeetInches` floating-point edge case (95.999... displayed as `7' 12"` instead of `8' 0"` — added carry normalization); `parseDim` feet-inches regex missed ASCII apostrophe (only had Unicode prime/curly); parsing order fixed to check feet-inches before stripping quotes. **Persistence**: `business_info.unit_format` jsonb column (migration applied), synced via `_saveUnitFormat()` → `saveBizInfo()`, loaded in `_applyBizInfoFromDB()`. **Verification**: typecheck clean, all format modes tested in dev preview, round-trip conversion verified lossless.
- 2026-05-06 — **Quotes & Orders — real line items**. Migration `add_line_kind_unit_price_orders_markup_tax` adds `line_kind text not null default 'cabinet' check (in 'cabinet','item','labour')` and `unit_price numeric` to both `quote_lines` and `order_lines`, plus `markup numeric not null default 0` and `tax numeric not null default 0` to `orders` (resolves the 2026-04-29 `orders.value` workaround). Quote popup (`_openQuotePopup`/`_renderQuotePopup` in `src/app.js`) and order popup (`_openOrderPopup`/`_renderOrderPopup`) replace the old single-textarea + materials/labour-rate/hours block with a structured **Line Items** section: one row per line, kind icon, inline-editable for `item` (qty × unit_price) / `labour` (hours × rate). Cabinet rows are read-only here — the ✎ button routes to `editQuoteInCB` for full builder editing. New shared helper `_lineSubtotal(row)` in `src/quotes.js` covers all three kinds; `quoteTotalsFromLines` and the new `orderTotalsFromLines` aggregate via it. `_syncCBLinesToQuote` and `_syncCBLinesToDB` filter their delete to `line_kind = 'cabinet'` so item/labour lines survive a builder round-trip. `cbCreateQuoteFromDraft` no longer auto-populates `quotes.notes` with em-dash specs (rows are real now). `convertQuoteToOrder` copies markup/tax onto the order; `_saveOrderPopup` recomputes `orders.value` from `order_lines × markup × tax` on every save. Quote-form sidebar stripped of Materials Cost / Labour Rate / Labour Hours / preview block — only client/project/notes/markup/tax remain. Created quotes immediately open the popup so the user can add line items. Order-form Value input replaced with markup/tax inputs (computed total); `addOrder` opens the order popup post-create. PDF (`_buildQuotePDF` in `src/cutlist.js`) and print-HTML (`printQuote` in `src/quotes.js`) replace the em-dash notes parser with real `quote_lines` iteration; new `_lineDisplay(row)` helper formats per-kind. PDF groups by kind (CABINETS / ITEMS / LABOUR). One-shot `_migrateManualStubLines` in `src/migrate.js` converts pre-rewrite "Manual Quote" stub rows (a single `quote_lines` row with `material_cost_override` + `labour_hours`) into proper Item + Labour lines. Orphan `cbSendToQuickQuote` and the legacy `_writeManualTotalsLine` removed. SCHEMA.md § 3.14, § 3.15, § 3.16 updated. `npm run typecheck` clean.
- 2026-05-06 — **Stripe S.5/S.7 (Customer Portal) + F.2 (free-tier write-time enforcement) shipped**. **S.5**: new `supabase/functions/stripe-portal/index.ts` deployed; client `openCustomerPortal()` + `_portalAction()` redirect helper; state-aware Manage popup `_handleManageSubscription` branches into four states (Active / Cancelling / PastDue / Free) with tailored copy and action rows that all redirect through the Customer Portal for actual mutations. `handlePortalReturn()` toasts + reloads subscription on `?portal=returned`. Customer Portal configured in Stripe Dashboard (cancellation at period end, payment method updates, plan switching, invoice history). End-to-end verified with Pro · Annual user. S.7 (invoices) auto-resolved — Customer Portal surfaces invoice history natively, no extra build. **F.2**: `_enforceFreeLimit(library, count)` helper in `src/limits.js` returns false + opens `_openLimitHitModal(library)` (in `src/stripe.js`) when at cap; tailored copy per library ("You've used all 5 of your free clients..."). 13 gates wired across 6 libraries: createClient/resolveClient, createProject/resolveProject, createQuote/duplicateQuote/cbCreateQuoteFromDraft (count excludes [CB_DRAFT] tagged quotes), addOrder/duplicateOrder/convertQuoteToOrder, addStockItem, cbSaveToLibrary/_confirmSaveCLToCabLib/cbImportLibrary (bulk CSV refuses if would bust cap). Pro users skip every check via Infinity getLimit. Bulk CSV imports for clients/quotes/orders/stock not yet gated — flagged as follow-up since CSV-import users are typically Pro candidates. `npm run typecheck` clean.
- 2026-05-06 — **U.9 — cabinet & cut-list counts wired (follow-up to action strip)**. Cabinet count per project = `cabinet`-kind rows summed across all of the project's quotes (drafts included), read from the `q._lines` cache that `_hydrateQuoteTotals` (`src/quotes.js:142`) already populates — no extra fetch. Cut-list count per project = membership in a new `window._projectsWithCutLists: Set<number>` populated by `_loadCutListProjectIds()` in `src/clients.js`, which runs `SELECT project_id FROM pieces / sheets WHERE user_id = me` in parallel and dedupes via Set. Wired in two places: (a) `loadAllData` in `src/app.js:1437` fires it alongside the existing parallel queries, and the post-`_hydrateQuoteTotals` then-handler now also calls `renderProjectsMain` so cabinet counts repaint after lines hydrate; (b) `switchSection('projects')` in `src/settings.js:294` re-fires `_loadCutListProjectIds` on each Projects-tab visit so the count stays fresh after a Cut List save/clear elsewhere in the session. The render-after-load path in `_loadCutListProjectIds` itself is gated on `panel-projects` being `.active` to avoid spurious renders. New `globals.d.ts` slot for `_projectsWithCutLists`. `npm run typecheck` clean. Live verified: dev session shows "House" / "Utility" = 3 cabinets each (3 cabinet lines in their quotes), "New 5" = 1 cut list (only project with `pieces` rows). Both counts use the existing `.proj-act` empty/populated visual states with no extra CSS.
- 2026-05-06 — **U.9 — Projects tab cross-tab action strip shipped**. Replaced the inline-styled thin project card in `renderProjectsMain` (`src/clients.js:274`) with a `.proj-card` + 4-button action strip (Cabinets · Cut Lists · Quotes · Orders) per approved mockup `mockups/library/row-interactions/option-3-row-action-strip.html`. Each button renders icon + label + count + a separated `+` segment; counts are wired for Quotes/Orders from the in-memory arrays (Cabinets / Cut Lists deferred — would need per-project `quote_lines`/`sheets` queries). Empty actions (count = 0) glow teal via `var(--accent2)` to invite a first-touch; populated actions hover gold via `var(--accent)`. Six new helpers in `src/clients.js`: `_prefillSmartInput(inputId, value)` (sets value + hides the matching `*-suggest` dropdown so the prefill doesn't pop the autocomplete), `_drillQuotesForProject(name)` / `_drillOrdersForProject(name)` (mirrors the existing `_quoteSearch`/`_orderSearch` filter pattern from `_openProjectPopup` at `src/app.js:838`), and `_newCabinetForProject(id)` / `_newCutListForProject(id)` / `_newQuoteForProject(id)` / `_newOrderForProject(id)` — each calls `switchSection()` then prefills the destination tab's `cb-project` / `cl-project` / `q-project` / `o-project` smart-input. The quote and order bridges also cross-fill the client smart-input from `projects.client_id` and focus the next form field. New CSS classes added to `styles.css` (`.proj-card`, `.proj-strip`, `.proj-act` + `.empty` variant, `.proj-act-main`, `.proj-act-add`, `.proj-act-count`, `.proj-act-money`, `.proj-name`, `.proj-client`, `.proj-desc`, `.proj-total`) themed via existing `var(--surface)` / `var(--accent)` / `var(--accent2)` / `var(--muted)` tokens so dark mode works without extra rules; one explicit `html.dark` override for the count chip background. `npm run typecheck` clean. Verified live in Vite preview at `http://localhost:3000/`: 8 projects render with action strips; clicking `+ Quote` on the "House · Adam Denney" project switches to Quote tab with both `q-project='House'` and `q-client='Adam Denney'` prefilled and no stray suggest dropdowns. Mockup files (`mockups/library/row-interactions/option-1-row-hover-toolbar.html` through `option-5-master-detail.html`) kept in repo as reference for the rejected directions.
- 2026-05-07 — **Cabinet Builder UX & pricing refactor (batch 1, 8 items)**. Single pass across `src/cabinet.js` / `src/cabinet-calc.js` / `src/cabinet-render.js` / `src/cabinet-library.js` / `src/migrate.js` / `src/scheduler.js` / `src/app.js` / `src/orders.js` / `src/business.js` / `index.html`, plus 2 schema migrations. (1) **Contingency → % of labour time**: `cbSettings.contingencyHours` replaced by `cbSettings.contingencyPct` (default 5); `calcCBLine` and `calcCBSections` multiply `autoLabour` by `(1 + pct/100)` before converting to cost — both labour hours and labour cost reflect contingency at the per-cabinet level. The old per-order `orders.contingency_hours` plumbing is no longer read in scheduler.js / app.js's `_orderHoursBreakdown`; the order popup loses its standalone Contingency input (replaced with an info note pointing at My Rates) and the breakdown's "Cabinet labour" row now shows an "incl. N% contingency" tag. New columns added but not yet read for per-order overrides: `orders.contingency_pct`, `business_info.default_contingency_pct`. (2) **Packaging moved**: from Core Rates to Other Labour Times in My Rates (Core: 5 rates / Other: 6 rates). (3) **Power-law UI parity**: the existing per-type refHours math (live since 2026-05-05) is now exposed via two distinct sections — the Drawers section split into adjacent **Drawer Fronts** + **Drawer Boxes** collapsibles, each with their own type select / material / live cost. (4) **Open library/project on add**: `cbSaveToLibrary` now calls `switchCBMainView('library')` after the toast; `cbCommitToProject` calls `switchCBMainView('results')` so the user sees where their work landed. (5) **"Results" tab → "Project"**: label-only change in `index.html:261`; internal view key stays `'results'` to avoid churn across the carved files. (6) **Per-component finish**: standalone Finish section removed; new `doorFinish` / `drawerFrontFinish` / `drawerBoxFinish` fields on cabinet lines, each with its own picker inside the Cabinet/Doors/Drawer Fronts/Drawer Boxes sections. Pricing applies per-component surface area: carcass finish to `2HD + 2innerW·D + W·H`, door finish to `doorTotalArea`, drawer-front finish to `drawerFrontTotalArea`, drawer-box finish to per-box surface area. (7) **Per-component hardware (3 lists) + auto-hw removed**: standalone Hardware section gone; new `doorHwItems` / `drawerHwItems` arrays alongside the repurposed `hwItems` (now cabinet-only). The hardcoded `line.doors * 2 * 6` hinges and `line.drawers * 24` slides in cabinet-calc.js are deleted — hardware totals strictly from the three manual lists. `_smartCBHwSuggest` / `_smartCBHwAddSuggest` / `_addCBHwByName` / `_openNewCBHardwarePopup` / `_saveNewCBHardware` / `updateCBHw` / `removeCBHw` all gained an optional `scope` parameter (`'cabinet'` | `'door'` | `'drawer'`, default `'cabinet'` for back-compat); per-section DOM ids namespaced (`cb-hw-${scope}-${lineId}-${idx}`). New `_hwListKey` / `_hwList` helpers in cabinet.js. (8) **Cabinet qty stepper removed from sidebar**: deleted from the Cabinet section's first form-row in `renderCBEditor`; the existing qty stepper in the Project view's cabinet card remains the only place to edit qty post-add. **Schema**: migrations `cabinet_per_component_finish_hardware_and_contingency_pct` (5 columns on `quote_lines`: `door_finish text`, `drawer_front_finish text`, `drawer_box_finish text`, `door_hardware jsonb default '[]'`, `drawer_hardware jsonb default '[]'`; 1 column on `orders`: `contingency_pct numeric`) and `business_info_default_contingency_pct` (1 column: `default_contingency_pct numeric`). `migrate.js` `_quoteLineRowToCB` and `_cbLineToRow` extended to round-trip the new fields. `database.types.ts` regenerated. **Verification** (live in dev preview, signed in): typecheck clean; tab reads "Project"; sidebar Cabinet section row 1 shows Width/Height/Depth (no Qty); Core Rates 5 rows incl. Contingency at unit `%` default 5; Other Labour Times 6 rows incl. Packaging; calcCBLine ratio test = 1.20 hours and 1.20 cost when pct flips 0→20; per-component finish independent (cabinet £241.50 vs doors £57.64 with door-only Oil finish at £12/m²); hardware totals = `cabinet:10 + door:24 + drawer:96 = 130` with empty auto-hw; `cbSaveToLibrary` source contains `switchCBMainView('library')`; `cbCommitToProject` source contains `switchCBMainView('results')`; refHours per type intact (Carcass:Standard, Doors:Slab/Shaker/Vinyl-Wrapped/Integrated Handle, Drawer Fronts:Slab/Shaker, Drawer Boxes:Standard/Dovetail).
- 2026-05-07 — **Schedule polish bundle (7 items)**. Single pass across `src/schedule.js`, `src/orders.js`, `styles.css`, `index.html`. (1) Job bars now slice into runs of consecutive working days so 0-hour days / holidays leave a visible gap — `weekEvents.forEach` builds `runs[]` and renders one segment per run. (2) Each sidebar order card now shows `Due {o.due}` next to the status label (skipped when due is `'TBD'` / empty); separated with `·` from status and slack chip. (3) `duplicateOrder` (`src/orders.js`) now copies `markup`, `tax`, `priority`, `auto_schedule`, `packaging_hours`, `contingency_hours` from the source row and calls `renderSchedule()` after the insert so the duplicate's bar appears immediately under the auto-scheduler — fixed the long-standing "duplicates don't auto-schedule until edited" bug. (4) When a week-row has `maxLane >= 3` (4+ stacked bars), the calendar switches to a compact lane stride (5px instead of 20px) so all bars fit inside the 90px cell with the top of each visible. Click any bar in compact mode → `_schedBarClick` sets `window._expandedSchedWeek` and re-renders that week with full 20px stride; a `document` click capture-listener (`_collapseSchedExpand`) restores compact mode on any click outside `.sched-bar`. Click a bar in expanded mode opens the order popup as before. (5) New `<select>` sort selector under the Jobs section header with five modes — start date (default), due date, priority (desc, tiebreak start), manual (drag-handles, persisted), order placed. Mode persists to `localStorage['pc_sched_sort']`; manual order persists to a new `orders.sidebar_order_index` column (gap-spaced `i*10`, indexed by `(user_id, sidebar_order_index)`). Drag handlers reuse the cutlist `cl-drag-over` class plus a local `SCHED_DRAG_HANDLE` SVG. (6) Sidebar restructured into `.sched-sidebar` flex-column with `.sched-sidebar-head` / `.sched-sidebar-body` (scrollable) / `.sched-sidebar-foot` (fixed); ⚙ Hours and Today buttons moved to the footer at 50/50 width via `flex:1`. (7) Edit pencil (✎) removed from each card — double-click still opens the popup. Schema migration `add_orders_sidebar_order_index` applied to project `mhzneruvlfmhnsohfrdo`; `database.types.ts` regenerated; SCHEMA.md § 3.15 updated. `npm run typecheck` clean.
- 2026-05-09 — **Cut List multi-cutlist + 3-tab refactor (code-side complete; schema migration pending manual apply)**. Adds support for multiple named cutlists per project (was 1-per-project) and reorganises the Cut List main view into 3 tabs: **Cut Layout / Project Cut Lists / Cabinet Library**. **Schema (planned):** new `cutlists` table (`id, user_id, project_id, name, position, ui_prefs jsonb, created_at, updated_at`) with Pattern A RLS (4 policies); nullable `cutlist_id` column added to `sheets / pieces / edge_bands` with FK + index. Backfill creates a "Main" cutlist per project that has any child rows and back-references existing rows. `cutlist_id` stays nullable until a follow-up migration sets it NOT NULL after monitoring confirms zero NULL inserts (avoids deploy-ordering deadlock). **Migration SQL staged** at `~/.claude/plans/currently-i-can-only-abstract-raccoon.md` Phase 1 — Supabase MCP `apply_migration` was blocked by harness permissions, user to apply manually via SQL editor. **Code (committed):** sidebar — button label `Save Project` → `Save cut list to project` (`index.html:502`); Cabinet Library smart-search dropdown removed and replaced by a single button `Save selected parts to cabinet library` wired to existing `_clSaveToCabinetLibrary()` (which requires checked piece IDs in `_clSelectedIds`). Save flow — `_saveProjectScoped` (`src/projects.js`) now find-or-creates a cutlist via new `_findOrCreateCutlist()` keyed on `(user, project, lower(trim(name)))`; `_replaceCutListChildTables(cutlistId, projectId, payload)` rescoped to delete-and-replace by `cutlist_id` (other cutlists in the same project untouched); save popup (`_openSaveProjectPopup` in `src/cutlist.js`) gains a Cut List Name field (default `_clCurrentCutlistName || 'Main'`). New helpers in `src/projects.js`: `_clLoadCutlist(id)` (loads sheets/pieces/edge_bands by `cutlist_id`), `_clDuplicateCutlist(id)` (clones row + children), `_clDeleteCutlist(id)` (cascade delete), `_clRenameCutlist(id)`. `loadProject(id)` no longer auto-loads child rows — just sets project state, clears in-memory arrays, and switches to the Project Cut Lists tab; user picks a cutlist to load. **Tabs (`index.html` `.cl-right` block):** wrapped existing toolbars + `#results-area` in `#cl-view-layout`; added tab strip with `cl-tab-layout` / `cl-tab-cutlists` / `cl-tab-library` and sibling containers `#cl-view-cutlists` / `#cl-view-library`. Default view = Project Cut Lists. New `switchCLMainView(view)`, `renderCLCutListsView()`, `renderCLCabinetLibraryView()` in `src/cutlist.js`. Project Cut Lists shows ALL cutlists across projects when none is loaded; filters to current project (with `View all projects` button to clear). Cabinet Library tab is a card-grid of `cbLibrary` (shared with Cabinet Builder); click loads parts via existing `_clLoadCabinetParts()` — Duplicate/Delete also exposed; click-to-edit suppressed (editing only in Cabinet Builder). **Auto-tab-switching:** `optimize()` (`src/cutlist.js:2690`) calls `switchCLMainView('layout')` on success; `_confirmSaveCLToCabLib` (`src/cabinet-library.js`) calls `switchCLMainView('library')` after the cabinet save. **Cleanup:** removed orphaned `_smartCLCabinetSuggest` (was the only consumer of the deleted smart-search input); reset `_clCurrentCutlistId/Name` in `_doClearAll`. **Types:** `database.types.ts` hand-updated to add `cutlists` table + nullable `cutlist_id` columns on sheets/pieces/edge_bands plus FK Relationships entries — matches what `generate_typescript_types` will return after migration. `npm run typecheck` clean. **Browser-verified pre-migration:** all 3 tabs render, Cut Layout shows the original toolbars + empty state, Cabinet Library shows the existing 4 cabinet entries with Load/Duplicate/Delete actions, save popup shows "Save cut list to project" title + Cut List Name field defaulting to "Main". Project Cut Lists tab correctly errors with `Could not find the table 'public.cutlists' in the schema cache` until the migration is applied. Pending: apply migration; smoke (`select count(*) from pieces where cutlist_id is null` = 0); E2E browser walkthrough; final NOT-NULL follow-up migration after a week. Detail in PLAN.md "Cut List multi-cutlist + 3-tab refactor".
- 2026-05-09 — **Orders / Quotes sidebar redesign (code-side complete; `hours_allocated` migration pending manual apply)**. Eight-point overhaul of the editor sidebars driven by user feedback that line-item inputs read as a "jumble" and the schedule block was visually heavy. (1) **Line-item rows redesigned** — item + labour rows in `_orderLineRowHtml()` (`src/app.js`) and `_lineRowHtml()` switched from a single horizontal strip of unlabelled 56-px inputs to a two-line `.li-row-stacked` layout: row 1 has kind badge + name + amount + remove-button, row 2 has labelled fields (`Qty / Price / Hrs` for items, `Hours / Rate /hr` for labour) using new `.li-fields` / `.li-field` / `.li-num` classes. Cabinet rows unchanged (read-only desc). Applied to both order and quote editors — line items work the same in both. (2) **Compact inline rows** — Pricing and Schedule meta now use new `.pf-row-inline` + `.pf-inline` + `.pf-input-compact` utilities (`styles.css`): `Markup [20] %  Tax [13] %` sits on one line; same pattern for `Auto schedule` / `Priority` / `Run-over` / `Override hours` / `Allocated`. Replaces the chunky two-column `.pf-row` (label-above) layout. Date inputs get explicit `min-width: 130px; height: 24px;` so they match text-input rhythm. (3) **Manual start/end inputs removed** — the `<div id="po-manual-dates">` block (with `po-manual-start` + `po-manual-end`) deleted. Production Start (`po-start`) is now the single editable date when auto-schedule is off (disabled when on, picks up greyed `:disabled` styling). `saveOrderEditor` mirrors `production_start_date` into `manual_start_date` when `auto_schedule=false` (legacy column kept for back-compat) and writes `manual_end_date = null`. **Scheduler change** in `src/scheduler.js` manual-orders branch: when `manual_end_date` is null, walks the calendar from `manual_start_date || production_start_date` consuming `hoursRequired` per workday — same algorithm as auto-orders — to compute `endISO`. Existing rows with explicit `manual_end_date` still honour it. (4) **Disabled-date colour** — new `.pf-input:disabled` rule (`color: var(--muted); cursor: not-allowed`) plus `:disabled::-webkit-calendar-picker-indicator { opacity: 0.35 }` so the auto-driven date renders visibly greyed-out. (5) **Hours-allocated override** — new nullable column `orders.hours_allocated` (numeric). NULL = use the existing line-item-summed `orderHoursRequired()`; non-null = the scheduler reserves exactly that value. UI: new "Override hours" checkbox + Allocated input inside Schedule (visible only when checked); auto breakdown panel hides while override is on. `_orderHoursOverrideToggle()` in `src/orders.js` shows/hides the input + breakdown and seeds the input with the current computed total on enable. `orderHoursRequired()` (`src/scheduler.js`) early-returns the override value before any line-item computation; `_orderHoursBreakdown()` (`src/app.js`) does the same for the readout consistency. (6) **Schedule section collapsible** — wrapped in `<details class="editor-section editor-section--collapsible">` with `<summary>` showing title + 1-line state readout (`Auto · Start 12 May · 12.5 h` or `Manual · Start 14 May · 8.0 h`); open/closed state persisted in `localStorage['pc_order_sched_open']`, default closed. New `_renderOrderSchedSummary()` in `src/orders.js` reads live values from inputs and runs on every change. CSS hides the native `::-webkit-details-marker` and renders a custom `▾` chevron that rotates on open. (7) **Totals above schedule** — `<div class="pf-totals" id="po-totals">` moved from the bottom (under Notes) to immediately after Pricing inputs and before the Schedule `<details>`. Same reorder in quotes (totals between Pricing and Notes — quotes have no schedule section). (8) **Status + order # removed from project header** — calls to `_renderProjectHeader()` (`src/ui.js:178`) in both `renderOrderEditor()` and `renderQuoteEditor()` no longer pass `status` / `summary` fields. The `metaRow` (status pill + summary line) is omitted; project name + back button + client line remain. The Status select, Order #/Quote # input, status pipeline, and overdue badge stay in the editor section directly below — still fully editable, no longer duplicated in the title row. **Migration SQL staged** in plan file `~/.claude/plans/orders-quotes-sidebar-1-line-glimmering-kay.md` — Supabase MCP `apply_migration` was blocked by harness permissions, user to apply manually via SQL editor. **Code is migration-tolerant**: `o.hours_allocated` reads return `undefined` until the column exists, override checkbox stays unchecked, save handler still includes the field but a write to a missing column will fail at the DB level (the user is expected to apply the migration before exercising the override). `npm run typecheck` clean. SCHEMA.md § 3.15 updated. PLAN.md sub-step pending until the migration lands.
- 2026-05-09 — **Orders auto-numbering shipped**. Migration `add_order_number` adds `order_number text` (nullable) to `public.orders` and backfills existing rows per-user in id-ascending order via `lpad(row_number() over (partition by user_id order by id), 4, '0')` — 7 existing orders backfilled `0001`–`0007`. New `_nextOrderNumber()` helper in `src/orders.js` mirrors `_nextQuoteNumber()`: scans trailing digits of `order_number` and `id` across the in-memory `orders` array, returns `String(max+1).padStart(4,'0')` (plain 4-digit, no prefix per user request). Order editor (`renderOrderEditor`) gains an Order # input next to Status; create / save / quote→order convert paths persist the value (`createOrderFromEditor`, `saveOrderEditor`, `convertQuoteToOrder` in `src/quotes.js`). Quote→order conversion intentionally generates a fresh O-NNNN — independent series from `quote_number`. Order cards prepend `#NNNN ·` to the title; editor header summary uses `#NNNN` over `Order #<id>`. CSV export adds an `Order #` column. `database.types.ts` regenerated. SCHEMA.md § 3.15 updated.
- 2026-05-10 — **Unified content-area header**. Cabinet Library content header (icon + bold title, no count) extracted into a reusable `_renderContentHeader({ iconSvg, title, clientName? })` helper in `src/ui.js`, plus seven nav-tab icon constants (`_CH_ICON_CABINET` / `_CH_ICON_PROJECT` / `_CH_ICON_CUTLIST` / `_CH_ICON_STOCK` / `_CH_ICON_QUOTE` / `_CH_ICON_ORDER` / `_CH_ICON_CLIENT`) and one new `.content-header` CSS block. Applied to: Cabinet Library (replaces inline `<h2>` + `5 templates` count — count dropped per user choice); Cabinet Builder Project view (`renderCBResults` — project icon + `project name — client`, falls back to `_cbCurrentProjectName` since `cb-project` input was removed in a previous pass; header reordered to sit above the Send-to / Export-Import buttons row); Cut List Project sub-tab (`renderCLCutListsView` — cabinet icon + cabinet name when `_clCurrentCabinetId` is set, project icon + `project name — client` otherwise); Cut List Library (cut-list icon + `Cut List Library`); Stock (`renderStockMain` — stock icon + `Stock Library`, both empty and populated states); Quotes (`renderQuoteMain` — quote icon + `Quotes`); Orders (`renderOrdersMain` — clipboard icon + `Orders`); Projects (`renderProjectsMain` — folder icon + `Projects`, replaces the inline `Projects (X)` heading; search input moved into the filter-pills row to free the title row for the icon header); Clients (`renderClientsMain` — person icon + `Clients`, replaces inline `Clients (X)` heading + drops the count; Export / Import buttons restyled from `class="btn"` to `class="btn btn-outline"` with the smaller font-size/padding that the Stock / Quotes / Orders tabs already use). The sidebar `_renderProjectHeader` (back button + name) is untouched — both headers coexist when a project is active. `npm run typecheck` clean. Browser-verified live: every tab renders the new header with the right icon + title, project-context headers carry the client suffix, Cabinet Library is visually identical to the pre-change reference minus the dropped count. Plan at `~/.claude/plans/i-want-to-use-rosy-manatee.md`.
- 2026-05-10 — **Cutlists & Cabinets library-pattern refresh (8 items)**. Single pass across `src/cutlist.js` / `src/cabinet.js` / `src/cabinet-render.js` / `src/cabinet-library.js` / `src/ui.js` / `src/db.js` / `index.html` plus 2 schema migrations. Plan at `~/.claude/plans/in-cutlists-and-cabinets-cheeky-glade.md`. (1) **Auto-named entries** — new helpers `_clNextCutlistName(projectId)` (cutlist.js) and `_cbNextCabinetName(libraryMode)` (cabinet.js) parse trailing `\d+` from existing rows and return `Cutlist N+1` / `Cabinet N+1`. Wired into `_clNewCutlistFromInput` (defaults empty input to next sequential name) and `addCBLine` / `addCBLineFromPreset` / `cbAddFromLibrary` / `cbLoadFromLibrary` / `cbSaveToLibrary` (each new cabinet auto-named). (2) **Scratchpad dropped + autosave** — `cbScratchpad` now references the active live row (cbLines[i] or cbLibrary[i]) instead of an ephemeral copy; `addCBLine()` immediately pushes a fresh `cbDefaultLine()` named "Cabinet N" onto cbLines and points the editor at it. `cbCommitToProject` and `cbCancelEdit` deleted. New `_cbScheduleAutosave()` routes 800 ms debounced writes to either the existing `saveCBLines()` path (project mode) or `_updateCabinetInDB`/`_saveCabinetToDB` (library mode); wired into `cbUpdateField`/`cbUpdatePct`/`cbStepField`/`cbAddExtra`/`cbUpdateExtra`/`updateCBHw`/`removeCBHw` and `_cbCabinetSearchInput`. `cbEditLibraryEntry(idx)` now points scratchpad at `cbLibrary[idx]` directly (no JSON copy); `cbSaveLibraryChanges` and `cbCancelLibraryEdit` deleted. New `_cbConfirmDeleteLine(idx)` resets `cbScratchpad`/`cbEditingLineIdx` correctly when the deleted row was the active one. Cutlist side: new `_clScheduleAutosave()` invoked from `_setClDirty(true)` — fires `_clSaveProjectByName` for project cutlists or a direct `_db('cutlists').update(...)` + delete-and-insert children sync for library cutlists. The `Save cut list to project` button (`index.html:509`) replaced with `Add to Library`. (3) **Header cleanup** — `_renderProjectHeader(_domain, opts)` (`src/ui.js:178`) now renders only row-1 (back button + icon + name); `metaRow` (status badge + parts/sheets summary + `cl-unsaved-pill[data-save-pill]`) and `clientLine` blocks deleted. New optional `iconSvg` parameter overrides the default folder icon. Status / summary / clientName / `_setSaveStatus` calls in `_clRenderContext` and `_cbRenderContext` simplified — params no longer passed (function ignores them). (4) **"Project Cut Lists" → "Project"** — tab label `Project Cut Lists` → `Project` (`index.html:522`). `renderCLCutListsView` rewritten: viewer header + `View all projects` button + `+ New cut list` button removed (entire title block deleted, leaving only the cards grid). Always filters by `_clCurrentProjectId`; non-project state shows "Pick a project to see its cut lists" empty message. `_clViewAllCutlists` and `_clNewCutlistFromHere` deleted. (5) **Cut List Library tab** — third cutlist tab label `Cabinet Library` → `Cut List Library` (`index.html:523`). `renderCLCabinetLibraryView` replaced with `renderCLCutListLibraryView` — loads via `_db('cutlists').select(...).is('project_id', null)`. New `is()` method added to `_DBBuilder` (`src/db.js`) for PostgREST `is.null` operator support. Card grid shows name + part count + date + optional `Linked: <cabinet name>` chip; per-row buttons: Open / Link to Cabinet / Duplicate / Delete. New cutlist-library functions: `_clOpenLibraryCutlist(id)` (loads pieces/sheets into editor with `_clCurrentProjectId = null`), `_clAddToCutlistLibrary` (inserts project-less cutlist row + copies in-memory pieces/sheets), `_clLinkToCabinet(cutlistId)` (popup mirroring `cbSendToQuote` listing `cbLibrary` entries; on pick writes `cutlists.cabinet_id`), `_clDuplicateLibraryCutlist`, `_clDeleteLibraryCutlist`. `switchCLMainView('library')` now calls `renderCLCutListLibraryView()`. (6) **Import/Export moved to project viewer** — three `cl-csv-btn` buttons (Import / Export / Clear) deleted from sidebar `Cut Parts` header (`index.html:413-415`). Top toolbar in `renderCLCutListsView` adds Import/Export at the right edge mirroring `cabinet-render.js:599-600` pattern. Clear dropped (per-row Delete remains). (7) **Cabinet tab `+ Cut List` per row** — second cabinet tab label `Library` → `Cabinet Library` (`index.html:251`). `_renderLibraryCards` (cabinet-render.js) prepends a `+ Cut List` button before `+ Project` on each row. New `_cbAddCutListForLibrary(libIdx)` (cabinet-library.js): saves cabinet to DB if needed, inserts blank library cutlist with `cabinet_id` FK, switches to Cutlist → Cut List Library tab. (8) **Library editing headers** — new SVG constants `_CB_LIBRARY_ICON` (cabinet.js — rect + drawer rail) and `_CL_LIBRARY_ICON` (cutlist.js — multi-pointed star) for the project-style headers. `_cbRenderContext` library-edit branch now renders `_renderProjectHeader('cabinet', { name: 'Cabinet Library', exitFn: '_cbExitLibraryEdit', iconSvg: _CB_LIBRARY_ICON })`; `_clRenderContext` adds a new branch when `_clCurrentProjectId` is null AND `_clCurrentCutlistId` is set, rendering `'Cut List Library'` with `_clExitLibraryEdit`. New `_cbExitLibraryEdit` (cabinet-library.js) and `_clExitLibraryEdit` (cutlist.js) clear the active library reference and switch the right pane back to the library tab. **Schema** — migrations `cutlists_library_support` (`cutlists.project_id` drop NOT NULL; new nullable `cutlists.cabinet_id bigint references cabinets(id) on delete set null` + `cutlists_cabinet_id_idx`) and `cutlist_children_project_nullable` (`pieces / sheets / edge_bands` all drop `project_id NOT NULL` so library-cutlist children can have NULL project_id; `cutlist_id` remains the primary ownership relation). RLS policies on `cutlists` were already keyed on `user_id = auth.uid()` directly (no project join), so library cutlists with NULL project_id work without policy changes. `database.types.ts` regenerated. **Verification** (live in dev preview at port 3000): typecheck clean across all 19 src files (only pre-existing dashboard.js errors remain, unrelated to this work); cutlist tabs read **Project** / **Cut List Library**; cabinet tab reads **Cabinet Library**; project header for both shows back-arrow + icon + name only (no status badge, no summary, no client line); empty `+` on cutlist smart-input creates `Cutlist 1` (auto-named); `+` on cabinet creates `Cabinet N+1` referencing the live cbLines row; library row click opens editor with header reading `Cabinet Library` + cabinet icon, edits autosave (verified via SQL: `cabinet_templates.default_w_mm = 750` after `cbUpdateField('w', 750)` + 1.1s wait); cabinet sidebar editor has only `Save to Library` (no Add to Project / Save Changes / Cancel / Save Library Changes); 5 library rows show `+ Cut List` button; `Add to Library` button under Optimize replaces the old `Save cut list to project`. Plan at `~/.claude/plans/in-cutlists-and-cabinets-cheeky-glade.md`.
- 2026-05-10 — **Orders / Quotes editor cleanup pass (4-item port from mockup B)**. Visual port of `mockups/orders/orders-redesign-B-zebra-cutlist.html` into the live editor for both Orders and Quotes. (1) **Pricing chips** — `Markup [20]% · Tax [13]% · Disc [0]%` rendered as three equal-flex `.rate-chip` elements with a label / number-input / unit-suffix segmented layout (`styles.css` adds `.rates-chips` + `.rate-chip` + `.chip-label` + `.chip-unit`). Replaces the chunky two-row stacked `Markup` / `Tax` block in `renderOrderEditor` (`src/orders.js:440-446`) and `renderQuoteEditor` (`src/quotes.js:1183-1189`). New per-editor input ids `po-discount` / `pq-discount` wired to `_renderOrderLineTotals()` / `_renderQuoteLineTotals()`. (2) **Schedule chevron** — `<span class="ed-chev">` with a 16×16 inline SVG chevron prepended LEFT of the "Schedule" label (was a 9px `▾` glyph on the right via `::after`). New 28×28 click target with `hover { background: var(--accent-dim) }` and `details[open] { transform: rotate(180deg) }`; `summary` switched from `align-items: baseline; justify-content: space-between` to `align-items: center; gap: 10px`. (3) **Line items table** — `_renderOrderLines()` / `_orderLineRowHtml()` (`src/app.js:49-130`) and `_renderQuoteLines()` / `_lineRowHtml()` (`src/app.js:362-460`) rewritten to render a single zebra-striped `<table class="editor-li-table">` instead of stacked `<div class="li-row">` blocks. Columns: handle / kind-dot / description / Qty / Price / Hrs / Disc% / Total / remove. Cabinet rows show qty editable + read-only Price + read-only auto-computed Hrs (from `calcCBLine().labourHrs × qty`, cached on `row._hrs`); item rows are fully editable. Description cell uses `white-space: normal; word-break: break-word` so long names wrap onto a second line without crowding the numeric columns. Zebra striping via `tbody tr:nth-child(even) td { background: rgba(0,0,0,0.025) }` (dark mode flips to `rgba(255,255,255,0.03)`); hover overlays `var(--accent-dim)`. (4) **Discount column + whole-order discount** — new `discount` column (numeric, 0-100 percentage) on all four tables (`quotes`, `orders`, `quote_lines`, `order_lines`); also added `schedule_hours` to `quote_lines` for parity with `order_lines` (the new Hrs column on quote-side rows needs somewhere to write). `_lineSubtotal` (`src/quotes.js:130`) multiplies materials+labour by `(1 - discount/100)` so all downstream sums stay simple. `_renderOrderLineTotals` / `_renderQuoteLineTotals` add `(after-tax) × order_discount/100` as a separate red row between Tax and the final total, hidden when zero. (5) **Labour tile dropped from UI** — `_O_ICON_LABOUR` add-tile and `_qAddLine('labour')` add-tile removed from both editors; `.editor-add-tiles` grid switched from `repeat(3, ...)` to `repeat(2, ...)`. Existing `line_kind='labour'` rows in the DB still render in-place (mapped to item-style with `labour_hours` → Hrs cell; `_lineSubtotal`'s labour branch still computes `hrs × rate` for back-compat). `_orderLineAdd` / `_lineAdd` keep their `labour` branch defensively but no UI calls them now. (6) **PDFs** — quote print/PDF (`src/quotes.js:953-973` HTML, `src/cutlist.js:2147-2320` jspdf) + order PDFs (`src/cutlist.js:2512-2712` for order_confirmation/proforma/invoice) gain a `Disc%` column when `rows.some(r => r.discount > 0)` and a red `Discount (N%)` totals row when `q.discount > 0` / `o.discount > 0`. Both conditional — when no discounts exist, PDFs look exactly like before. (7) **CSV** — Quote CSV export header gains `Discount %`; importer reads `r[6]` as discount, shifts `status`/`date`/`notes` to `r[7]`/`r[8]`/`r[9]`. `updateQuoteField`'s `numFields` list adds `discount`. (8) **Migration** — `supabase/migrations/20260510233952_add_discount_columns.sql` adds the four `discount numeric default 0` columns plus `quote_lines.schedule_hours numeric default 0`. **MCP `apply_migration` was blocked by harness permissions; user to apply via Supabase SQL editor** before save paths can persist the new fields. **Code is migration-tolerant**: reads use `(o.discount ?? 0)`, the chip inputs default to 0, the totals row stays hidden, the per-line Disc% cell stays as `—`; save handlers always include the field but a write to a missing column will fail at the DB level (same pattern as the 2026-05-09 `hours_allocated` rollout). `npm run typecheck` clean. SCHEMA.md updated under §§ 3.14 / 3.15 / 3.16. Plan at `~/.claude/plans/clean-up-orders-dapper-yao.md`.
- 2026-05-11 — **Orders / Quotes editor port from mockup J (column toggles + stock library + per-order stock markup)**. Second-pass redesign of the order/quote editor sidebar, replacing the bridge from the 2026-05-10 pass with the J-mockup design the user iterated on extensively (`mockups/orders/orders-redesign-J-column-toggles.html` for orders, `quotes-redesign-J-column-toggles.html` for quotes). Major changes: (1) **New `.ed-head` editor header** with back-arrow + tab-icon + editable order/quote-number prefix (`#NNNN` / `#Q-NNNN`) + project name on row 1, client name + clickable status `<select>` styled as a coloured badge on row 2 (`data-status` attribute drives the CSS background colour per status). Replaces the prior `_renderProjectHeader` call + the separate Status/Order# editor-section + the status pipeline visualization. Pipeline is gone; status is changed inline via the badge. Order number input dropped the `#` prefix into a static `<span>` next to the input so the input value stays bare digits; quote number similarly dropped the `Q-` prefix and re-prepends it on save. (2) **Column-toggle pills** (`Discount` / `Hours` / `Stock`) added to a new `.cl-section-header` row above the line-items table, replacing the old `editor-section-title`. State persisted in `localStorage['pc_order_col_{disc,hrs,stock}']` and `pc_quote_col_*`. Toggling Disc/Hrs adds `.hide-disc`/`.hide-hrs` to the table so the corresponding columns hide via CSS; toggling Stock reveals the stock smart-library below the add buttons (no column). (3) **Line items table** — `_orderLineRowHtml` / `_lineRowHtml` (`src/app.js`) refactored: descriptions are now `<textarea class="cl-input desc">` with `_autoGrowTextarea` so they wrap onto multiple lines and the row height adapts; cells use the universal `.cl-input` class; the row template emits `col-handle / col-dot / col-desc / col-qty / col-price / col-hrs / col-disc / col-total / col-x`. Hrs column header + cells render in muted grey (`var(--muted)`, opacity 0.7 on the header) to telegraph that workshop hours are PDF-hidden; cabinet rows seed the Hrs cell with `calcCBLine().labourHrs × qty` and render it italic via `.is-computed` until the user edits it. Cabinet description prepopulates with `row.name` (just the name; users add detail via the editable textarea — dims no longer auto-appended on every render). Stock-kind rows render via the same item-style branch (`is-stock` dot colour `var(--success)`). (4) **Split add buttons** — `+ Cabinet` / `+ Item` replace the `editor-add-tiles` grid (`.cl-add-row` / `.cl-add-btn`). Plain text, no colour dots in the buttons. (5) **Stock smart-library** — when the Stock pill is on, a search input with a `+` button reveals below the add buttons. The `+` button calls `_openNewStockPopup()` (the canonical app.js helper — see (8) for disambiguation). Typing in the search renders the `.stock-suggest` dropdown grouped by stock category with sticky section labels; clicking a row pushes a new `line_kind='stock'` line via `_oAddStockLineFromLibrary` / `_qAddStockLineFromLibrary`. Implemented in new `_stockSearchRender(q, suggestId, onPick)` helper (`src/app.js`). Below the search: a single `Stock Markup [N]%` input persisted on the order/quote row's `stock_markup` column. (6) **Pricing chips refactored to Tax + Disc only** — the order/quote-level `markup` chip is removed from the UI. The legacy `markup` column on `quotes` / `orders` stays in the DB for back-compat; saves preserve whatever value was already there (rather than zero it out via the missing input), and the legacy markup still applies in totals math when non-zero on a pre-existing row. New `stock_markup` is the only user-facing markup field. Divider line moved from below the Pricing chips to ABOVE the Pricing header (`border-top: 1px solid var(--border)` on the section, `border-bottom: none`), giving a clear separation between the line-items / stock-library block and the pricing block. (7) **Totals math** — `_renderOrderLineTotals` / `_renderQuoteLineTotals` and `quoteTotal` updated: `_lineSubtotal` returns the same `{materials, labour}` shape per line, but the order/quote totals reducer now splits `stockMat` out so the stock_markup can be applied to stock-line materials only before the legacy markup → tax → discount chain. `orderTotalsFromLines` / `quoteTotalsFromLines` extended to return `stockMat` alongside `materials` / `labour`. New "Stock markup (N%)" row appears in the totals breakdown only when stock_markup × stockMat > 0. (8) **Footer buttons removed** from the editor — Delete / Work Order / Invoice / PDF / → Order live on the order/quote CARD in the main list, so the editor sidebar is editing-focused. A single `+ Create Order` / `+ Create Quote` button remains for the new-row flow. (9) **`_openNewStockPopup` disambiguation fix** — cabinet-library.js had a function named `_openNewStockPopup(fieldName)` that actually opened a "New Finish" popup, shadowing app.js's canonical new-stock popup due to script load order. Renamed it to `_openNewCBFinishPopup`; updated its 3 legitimate finish-popup callers (cabinet-render.js's `finishSmart`, cabinet-library.js `_smartRatesFinishSuggest` and `_smartCBFinishSuggest`). Side-effect fix: the cabinet builder's "Add new material" / "Add new edge banding to stock" buttons in `_smartRatesSheetSuggest` / `_smartRatesEdgeSuggest` / `_smartCBMaterialSuggest` (which were calling the misnamed function and erroneously opening a finish popup) now correctly invoke the real stock popup. (10) **Schema migration staged** at `supabase/migrations/20260511015625_stock_kind_and_markup.sql`: drops + re-creates the `line_kind` check constraint on `quote_lines` and `order_lines` to allow `'stock'` (existing values still valid); adds `stock_markup numeric not null default 0` to `quotes` and `orders`. MCP `apply_migration` blocked by harness permissions — user to apply manually via Supabase SQL editor; reads / saves are migration-tolerant until then (defaults to 0, no rows visible as kind=stock yet). (11) **PDFs** — `_lineDisplay` (`src/quotes.js`) extended to handle kind=stock the same as kind=item with a fallback name `'Stock item'`. The HTML print path in `printQuote` and the jspdf builders `_buildQuotePDF` + `_buildOrderDocPDF` (`src/cutlist.js`) all add a "Stock markup (N%)" row in the totals block, between Subtotal and the legacy Markup row, only when `stock_markup × stockMat > 0`. `npm run typecheck` clean. Mockups: `mockups/orders/orders-redesign-J-column-toggles.html` and `mockups/quotes/quotes-redesign-J-column-toggles.html` (the user iteratively refined the J variant across ~15 turns). Plan at `~/.claude/plans/clean-up-orders-dapper-yao.md` (Phase 2 — first phase shipped 2026-05-10 entry below).
- 2026-05-12 — **Bugfix: `cutlists.cabinet_id` FK retargeted to `cabinet_templates`.** The cabinet-library Cut List pill (`_cbCutListProjActHtml` in `src/cabinet-library.js:698`) and the link-to-cabinet picker (`_clLinkToCabinet` in `src/cutlist.js:4367`) both treat `cutlists.cabinet_id` as referring to a `cabinet_templates.id` — that's what `_saveCabinetToDB` persists (`src/cabinet.js:69`) and what `cbLibrary` is loaded from (`src/cabinet.js:113`). But the FK constraint on `cutlists.cabinet_id` pointed at `cabinets(id)` (the per-project instance table), so every link attempt failed with Supabase error 23503 "Key is not present in table 'cabinets'." surfacing as the "Link failed" toast at `src/cutlist.js:4400`. Single migration `supabase/migrations/20260512081913_cutlists_cabinet_id_fk_to_templates.sql` drops `cutlists_cabinet_id_fkey` and recreates it `references public.cabinet_templates(id) on delete cascade`. Applied via Supabase MCP `apply_migration`. No data migration: column stayed nullable and was NULL on every row. No code changes — every reader/writer already assumed the templates table. **SCHEMA.md § 3.12a** updated to document the `cabinet_id` column (added 2026-05-12) and the FK target. Verified live by repeating the prior-failing update with a `cabinet_templates.id` — succeeded, re-read confirmed link persisted. **Tangential**: while diagnosing, swapped the bullet-list SVG in `_CB_CUTLIST_ICON` (`src/cabinet-library.js:692`) for the saw-blade glyph that the sidebar nav-tab uses (`index.html:171`), so the Cut List pill on cabinet cards now matches the top-level Cut List tab visually.
- 2026-05-12 — **Quote / Order / Invoice number formats unified to 3-letter prefixes.** Quote `Q-NNNN` → `QUO-NNNN` (`quotes.quote_number` text col); order `NNNN` (no prefix) → `ORD-NNNN` (`orders.order_number` text col); order-PDF per-doc prefixes promoted from 2-letter `OC` / `PF` to 3-letter `ORC` / `PRO` (Tax Invoice stays `INV`). Work Order PDF keeps its `WO-` prefix but now derives the digits from `o.order_number` instead of the raw DB row id, so screen and paper agree on the number. (1) **Generation** — `_nextQuoteNumber()` (`src/quotes.js:115`) now returns `'QUO-' + ...`; `_nextOrderNumber()` (`src/orders.js:44`) now returns `'ORD-' + ...`. Both still scan trailing-digit `match(/(\d+)/)` of existing values, so they keep working through the legacy→new transition. (2) **PDF builders** — `src/cutlist.js:2226` (Quote PDF header) now uses `q.quote_number || ('QUO-' + id.padStart(4,'0'))`; `_buildOrderDocPDF` `refNum` (`src/cutlist.js:2554`) strips `ORD-` so the per-doc 3-letter prefix replaces it; `cfg.order_confirmation.prefix` `'OC'` → `'ORC'`; `cfg.proforma.prefix` `'PF'` → `'PRO'`; `cfg.invoice.prefix` unchanged. Work Order PDF (`src/cutlist.js:2469`) derives `woRef` from `o.order_number`. The Tax Invoice closing-line `'Please reference #INV-' + refNum + ' on remittance.'` is unchanged because INV is unchanged. (3) **Display + editor** — every fallback site that previously synthesised `'Q-' + id.padStart(4,'0')` now produces `'QUO-' + ...` (quotes.js card title fallback, dashboard.js quote pill, quotes.js HTML print view title, suggest list filter regex, "Start new quote" suggest text); same pattern for orders (orders.js card / editor / suggest, dashboard.js order pill, dashboard.js schedule numberLabel, schedule.js numberLabel, cabinet-render.js editing-order banner). Quote editor input strip regex broadened from `/^Q-/i` to `/^(QUO|Q)-/i` so legacy `Q-NNNN` rows still display as bare digits in the input until the migration runs. Order editor input gains a strip-on-display + prepend-on-save pair mirroring how quotes already worked (`po-order-number` value sanitised through `String(orderNumFull).replace(/^ORD-/i, '')` for display; both `createOrderFromEditor` and `saveOrderEditor` write `'ORD-' + onRaw.replace(/^ORD-/i,'')`). The "From Quote" chip on the order editor (`src/orders.js:392`) now uses `fromQuote.quote_number` with `QUO-` fallback. (4) **Save normalization** — `createQuoteFromEditor` and `saveQuoteEditor` (quotes.js:1561 + 1606) now normalize via `'QUO-' + qnRaw.replace(/^(QUO|Q)-/i, '')` so saving an existing legacy `Q-NNNN` quote migrates that single row to `QUO-NNNN`; orders side mirrors this. (5) **Migrate.js** — legacy LS quote-import (`migrate.js:444`) also normalizes incoming `quoteNum` to the new prefix. (6) **Migration** — `supabase/migrations/20260512175008_renumber_prefixes.sql` rewrites stored values: `UPDATE quotes SET quote_number = 'QUO-' || regexp_replace(quote_number, '^Q-', '', 'i') WHERE quote_number ~* '^Q-';` and `UPDATE orders SET order_number = 'ORD-' || order_number WHERE order_number IS NOT NULL AND order_number !~* '^ORD-';`. **MCP `apply_migration` was blocked by harness permissions**; user to apply via Supabase SQL editor. Code is migration-tolerant: pre-migration the display paths show whatever's stored (legacy `Q-` / bare digits) but every new entry, every save, and every PDF render uses the new format. `npm run typecheck` clean. Plan at `~/.claude/plans/change-order-and-quote-clever-castle.md`.
- 2026-05-12 — **cutlists ↔ cabinet_templates promoted to M:N via `cutlist_cabinets` join table.** Replaces the single-link `cutlists.cabinet_id` column (added earlier the same day, see entry above). Migration `supabase/migrations/20260512083426_cutlist_cabinets_join_table.sql` creates `cutlist_cabinets(cutlist_id, cabinet_id, user_id, created_at)` with composite PK + RLS Pattern A + indexes on `cabinet_id` and `user_id`, backfills the one existing single-link row, then drops `cutlists.cabinet_id`. **Picker UX rewrite (`src/cutlist.js`):** the cabinet pill on each library cut-list row now has two distinct actions — clicking the main button calls new `_clOpenLinkedCabinets(cutlistId)` which shows ONLY currently-linked cabinets and on pick navigates to that cabinet's cut-list view (via new helper `_clOpenCabinetFromPicker`); clicking + still calls `_clLinkToCabinet(cutlistId)` but it's been rewritten as a multi-toggle picker that lists ALL persisted cabinets in `cbLibrary`, marks linked ones with a green "Linked" pill (`tone: 'approved'`), and on pick calls new `_clTogglePickedCabinet(cutlistId, cabinetDbId)` which inserts or deletes a `cutlist_cabinets` row and re-opens the picker so the user keeps toggling. The old `_clConfirmLinkToCabinet` helper is gone. **Reader updates**: `renderCLCutListLibraryView` switched its select to embed `cutlist_cabinets(cabinet_id, cabinet_templates(name))` and shows the count of linked rows in the pill (count only, no name list — names are visible in the picker); the cabinet-scope branch of `_clRenderContext` switched to a `cutlist_cabinets!inner(cabinet_id)` filtered embed; `_cbApplyCutListCounts` in `src/cabinet-library.js` now group-counts from `cutlist_cabinets`. **Writer update**: `_clNewCabinetLinkedCutlist` no longer sets `cabinet_id` in its cutlists INSERT; after the cutlist is created it inserts a `cutlist_cabinets` row with the active cabinet id (best-effort — link is tolerated to fail). **Types**: `src/database.types.ts` regenerated via Supabase MCP — `cutlist_cabinets` table now present with relationships to both `cutlists` and `cabinet_templates`; `cutlists` row type no longer has `cabinet_id`. The PostgREST filtered-embed `eq('cutlist_cabinets.cabinet_id', ...)` doesn't fit the generated `keyof Row` type signature on `eq`, so that one call site casts the column name through `any`. Verified end-to-end: typecheck clean, picker round-trip works (link a cabinet → "Linked" pill appears in re-rendered picker; library count goes 0→1; unlinking goes 1→0); multi-link works (cutlist 21 → cabinets 1 + 4 visible as count "2" in row pill and shown by main-button picker as 2 entries); cabinet-card pill counts via `_cbApplyCutListCounts` reflect the new schema (cabinet 4 = 2 cut lists, cabinet 1 = 1, others 0).
- 2026-05-13 — **F7: Cabinet Library sub-tab gets its own sidebar gate — standalone, out-of-client template workflow.** `#panel-cabinet` previously had two main-area sub-tabs (Quote Builder, Cabinet Library) but a single shared sidebar gated on `_cbCurrentClientId`, forcing the user to pick a client before they could touch the library. `renderCBPanel()` worked around this by auto-flipping to the Library sub-tab whenever no client was selected (`src/cabinet-render.js:211-213`), but the sidebar still showed the client-picker empty state — the Library was effectively gated. This refactor splits the sidebar into two siblings inside the same `<aside>`: `#cb-sidebar-builder` (existing client-picker + `#cb-context` + `#cb-tabs-wrap` + `#cb-cab-editor`) and **new** `#cb-sidebar-library` containing a `#cb-lib-gate` div. `switchCBMainView()` toggles which wrapper is visible and exits the OTHER sub-tab's scratchpad on switch (drops in-quote cabinet scratchpad on switch-to-library; drops template-edit scratchpad on switch-to-builder + calls `_cbRenderContext()` so the sidebar header refreshes from "Cabinet Library" to "Pick a client"). The cabinet editor itself (`#cb-cab-editor`) stays inside the builder wrapper and is reused for both in-quote AND template edits — sidebar visibility is driven by `showBuilder = view === 'results' || (cbEditingLibraryIdx >= 0 && cbScratchpad)`, so the editor stays visible while editing a template even though the user is technically on the Library sub-tab. Refactored `switchCBMainView` extracted the chrome-sync into a new pure helper `_syncCBMainViewChrome()` so `renderCBPanel` can re-sync without recursing through `switchCBMainView`'s state-cleanup branches. **Removed `renderCBPanel`'s auto-flip-to-Library** at lines 211–213 — the Quote Builder sub-tab now correctly shows its own "Pick a client" empty state when no client is selected. **New file** `src/cabinet-library-sidebar.js` (~60 lines) provides `_renderCBLibSidebarGate()` (renders the gate via `_renderListEmpty()` mirroring Clients/Stock pattern, with Recent templates sorted by `updated_at` and a "+ Add Template" button) and `cbStartNewLibraryEntry()` (creates `cbDefaultLine()`, sets `cbEditingLibraryIdx = cbLibrary.length - 1`, points `cbScratchpad` at the new entry, calls `renderCBPanel` + `_cbScheduleAutosave`). The autosave route already branches on `cbEditingLibraryIdx >= 0` to write to `cabinet_templates` instead of `quote_lines` (existing logic at `src/cabinet.js:631-647`), so the first scheduled save fires `_saveCabinetToDB` and back-fills `db_id` on the in-memory entry — no further plumbing needed. `index.html` script tag added between `cabinet-library.js` and `cabinet-render.js`. **Import/Export buttons moved** from the Quote Builder results header (`renderCBResults` at ~line 614) to the Library main-grid filter bar (`renderCBLibraryView` at ~line 714) per `CLAUDE.md:101` ("Import/Export buttons belong in the main content area filter bars, not in sidebars"). Empty-state Library view also gains an Import CSV button (replacing the now-stale "Use Save to Library in the editor" instruction). **`type` column hardcode relaxed** in `_saveCabinetToDB` (line 75) and `_updateCabinetInDB` (added to update payload at lines 94-105): writes `entry.cabType || entry.type || 'base'` instead of always `'base'`, so future cabinet-type metadata propagates without another DB-helper edit. No schema change — `cabinet_templates.type` is already a text column with default `'base'`. **Verified end-to-end** in the Vite dev preview (`npm run dev`, http://localhost:3000): (1) clean reload with no client lands on Quote Builder sub-tab showing the "Pick a client" empty state — confirmed no auto-flip. (2) Click Cabinet Library tab → sidebar swaps to the new gate; no client required. (3) Click "+ Add Template" → drills into the cabinet editor with a blank template; eval confirms `cbEditingLibraryIdx=4` and `cbScratchpad.db_id=12` (autosave fired and back-filled). (4) Network panel shows POST to `cabinet_templates` (201 Created), NOT to `quote_lines`. (5) Switching to Quote Builder while editing a template → state cleared and sidebar refreshes to "Pick a client" empty state. (6) `_cbExitLibraryEdit` returns to library gate with the new template visible in the Recent list. (7) Library grid shows the new template in the main area alongside existing templates. `npm run typecheck` clean. Plan at `~/.claude/plans/edit-staged-brook.md`. PLAN.md "Remove Projects entity · adopt library-first / Cabinet-IS-Quote" entry extended with F7 bullet.
- 2026-05-13 — **Phases F5/F6 of the "remove Projects entity" refactor — destructive sweep complete.** Two commits land the changes: `bb4e5b1` **F5** (drop `project_id` reads/writes + columns) and `2b909c6` **F6** (drop the `projects` table + `renderProjectsMain` + `#panel-projects`). **Decisions locked at plan exit** (clarifying questions answered before execution): (1) Client card layout: flat sections — ▾ Quotes / ▾ Orders / ▾ Cut Lists — directly under each Client card, replacing the inline project cards F1 introduced. (2) Cut List tab structure: drop the middle "Project Cut Lists" tab; Cut List goes from 3 tabs to 2 (Cut Layout + Cabinet Library, with Library as the new default landing view). (3) Copy-project flow: drop entirely; the per-entity duplicate flows (`duplicateQuote`, `_clDuplicateCutlist`) cover the pragmatic need. **F5 workspace re-key**: Cabinet Builder's `_findOrCreateDraftQuote(projectId)` rewritten to `_findOrCreateDraftQuote(clientId)` with "most recent designing-status quote per client" semantic (sorts by `updated_at` desc); state vars `_cbCurrentProjectId/Name` renamed to `_cbCurrentClientId/Name`; DOM input `cb-project` → `cb-client`; localStorage key `pc_cq_project_name` migrated to `pc_cq_client_name` with one-shot legacy read; helper functions renamed (`_getCBClientId`, `_ensureCBClient`, `_cbPickClient`, `_smartCBEmptyClientSuggest`, `_loadCBClientById`, `_cbSaveClientByName`, `_exitClient_cabinet`); empty state shows a client picker + Recent Clients list. Quote/Order sidebar editors collapse `_qpState.projectId` / `_opState.projectId` into `clientId`; empty-state pickers swap to client smart-inputs (`_smartQClientSuggest`, `_smartOClientSuggest`, `_qPickClient`, `_oPickClient`, `_qChangeClient`, `_oChangeClient`). `quoteProject` / `orderProject` helpers repurposed to read `quotes.name` / `orders.name` (the F2 column) so the ~25 cross-file display sites stay valid without churn. CSV import drops the `resolveProject` mapping step (col r[1] now writes to `name`). **F5 schema**: `supabase/migrations/20260513160000_f5_drop_project_id.sql` drops `project_id` from quotes, orders, cutlists, pieces, sheets, edge_bands, cabinets via `alter table … drop column if exists` (cascading FKs were already `on delete cascade`). `database.types.ts` regenerated. **F6 destructive sweep**: deletes `renderProjectsMain`, `_renderProjectInlineCard`, `_PROJ_ICON_*`, `_projFmtShort`, `_highlightProject`, `_prefillSmartInput`, all `_pj*` autosave/state machinery (`_pjLoadProject`, `_pjStartNewProject`, `_pjFormChanged`, `_pjScheduleAutosave`, `_pjRunAutosave`, `_pjExitProject`, `_pjRenderProjectSubGate`, `_pjRenderDrillHeader`, `_pjNextProjectName`, `_pjClientSuggest`), the projects-tab gate (`_renderProjectsSidebarGate`, `_pickClientForProjects`, `_exitClient_projects`, `_smartProjectsClientSuggest`, `_setProjectsActiveClient`, `_gotoProjectFromClient`, `_projectsRevealForm`, `_projectsMaybeResetFormFlag`, `_projectsActiveClientId`), per-project drill helpers (`_drill*ForProject`, `_new*ForProject`), and project CRUD (`updateProject`, `removeProject`, `_projectName`, `duplicateProject`, `resolveProject`) all from `src/clients.js`. `renderClientsMain.clientCard` rewritten to emit three flat collapsible sections — Quotes / Orders / Cut Lists — each with clickable rows that jump straight into the entity's editor. New `window._cutListsByClient` cache + `_loadCutListsByClient` boot hydrator powers the Cut Lists section (fetches a slim id/name/client_id projection at boot, grouped by client_id). Cross-file dead chains deleted: `_openNewProjectPopup` / `_saveNewProjectPopup` / `_setClLoadedProject` / `_setCbLoadedProject` / `_smartProjectSuggest` / `_autoFillClientFromProject` (`src/quotes.js`), `_smartCLProjectSuggest` / `_smartCBProjectSuggest` (`src/cabinet-library.js`), `_clProjectCache` (`src/cabinet.js`), `_renderProjectEmpty` (`src/ui.js`), `_clSaveProject` / `_openSaveProjectPopup` / `_doSaveProject` / `_clPickProjectByIdSafe` / `_smartCLEmptyProjectSuggest` / `_clNewProject` (`src/cutlist.js`), `_openProjectPopup` (`src/app.js`). Settings.js `switchSection` 'projects' branch removed. `migrate.js` legacy LS migrations (`_migrateCutListProjects`, `_migrateCBProjects`) neutered to SKIP. `src/projects.js` collapsed from ~480 to ~165 lines — keeps only the per-cutlist helpers (`_clLoadCutlist`, `_clDuplicateCutlist`, `_clDeleteCutlist`, `_clRenameCutlist`, `_clConfirmRenameCutlist`). `index.html` deletes the entire `#panel-projects` div (sidebar + drill section + pj-name/pj-client/pj-desc/pj-status form fields + `#projects-main`) plus the Cut List middle tab button + its container; the cut-list tab strip is now 2 buttons. `stock.js`'s `projects` declaration retyped as `any[]` for null-state safety. **F6 schema**: `supabase/migrations/20260513170000_f6_drop_projects_table.sql` runs `drop table if exists public.projects;` — no FK references remained after F5. `database.types.ts` regenerated without `projects`. **Net code delta**: F5 +569/-982 across 14 files; F6 +139/-1607 across 13 files. `npm run typecheck` clean at both commit boundaries. Browser smoke confirms every tab loads with no console errors at both stages. The "Remove Projects entity" build-plan entry flipped from 🚧 In Progress to ✅ Done.
- 2026-05-13 — **Foundation phases F1–F4 of the "remove Projects entity / library-first / Cabinet-IS-Quote" refactor.** Architecture designed across six mockups (`mockups/ux-explorations/ux-review.html`, `mockups/architecture/projects-merge-flow.html` Option A, `mockups/architecture/option-c-files-under-client-flow.html`, `mockups/architecture/option-d-flat-files-flow.html`, `mockups/architecture/option-e-cabinet-is-quote-flow.html`, `mockups/architecture/top-level-architecture-flow.html`, `mockups/architecture/library-to-quote-add-flow.html`) — see PLAN.md Active Work entry for the executive summary. Four commits landed (F5/F6 deferred to a dedicated follow-up session because they require a 94-call-site sweep of `project_id` references across 9 source files). **F1 (`69ff6d4` refactor(clients): collapse Projects tab into Client cards):** removed the Projects nav-tab from `index.html`; lifted `projectCard` from `renderProjectsMain` into module-level `_renderProjectInlineCard(p, opts?)` in `src/clients.js` (with `showClient?: boolean` to drop the client-name field when nested inside a client card); `renderClientsMain.clientCard` now renders each client's projects inline below the header using the existing 4-cell `.proj-strip` pattern, with a `+ New project` button as fallback when the client has no projects yet; the 1-cell "Projects" action strip on Client cards is dropped (replaced by the per-project full strip). `src/settings.js` `switchSection` sections array drops `'projects'` (8 nav tabs now) so the index-based active-tab toggle continues to match the live DOM. The `#panel-projects` element and `renderProjectsMain` stay in place so `_openProjectPopup(id)` (called from project-row click in inline cards, plus dashboard/schedule callers) still routes correctly until F6 deletes them. No schema change. **F2 (`e77282a` feat(schema): add name to quotes/orders, tags jsonb to library tables):** migration `supabase/migrations/20260513120000_f2_add_name_and_tags_columns.sql` applied via Supabase MCP. Adds `quotes.name text` + `orders.name text` (backfilled from associated `projects.name` — all 3 existing quotes and 1 existing order named successfully); adds `tags jsonb not null default '[]'::jsonb` to `cabinet_templates`, `stock_items`, and `cutlists` with GIN indexes on each. **Three tables, one UX**: storage stays purpose-built per kind (decided against unifying into one `library_items` table — stock decrements when used while templates don't; cutlists have FK children; stock has cost/qty fields cabinets don't need); tags are a UX-layer cross-cutting filter convention only. `src/database.types.ts` updated by hand (full regenerate happened but only the relevant 5 tables were diff'd into the existing file to minimise churn). `npm run typecheck` clean. No code reads of `name` / `tags` columns yet — they're available for F3+. **F3 (`b5d8990` refactor(quotes): replace [CB_DRAFT] notes-tag with status='designing'):** migration `supabase/migrations/20260513140000_f3_designing_status.sql` applied via Supabase MCP. `UPDATE quotes SET status='designing' WHERE notes like '[CB_DRAFT]%'` then `UPDATE ... SET notes = nullif(trim(regexp_replace(notes, '^\[CB_DRAFT\]\s*', '')), '')` to strip the prefix preserving any user-added text. 1 quote migrated (id 42); other 2 customer-facing quotes untouched. **Code change** (`src/quotes.js:66-110`): `_isDraftQuote(q)` now checks `q.status === 'designing'` first; falls back to the legacy notes-prefix check as a belt-and-braces guard for any unmigrated in-memory rows (should be zero in production after migration ran). `_findOrCreateDraftQuote` inserts with `status: 'designing'` instead of `notes: CB_DRAFT_TAG + status: 'draft'`. `CB_DRAFT_TAG` constant retained only as the legacy detector. All 10+ filter call sites (Quotes tab, Order tab, Dashboard, Clients-inline-projects, CSV exports, schedule, cabinet-builder) continue working unchanged — only the detection mechanism moved. **F4 (`8cb87cb` feat(schema): add cutlists.quote_id bookmark FK):** migration `supabase/migrations/20260513150000_f4_cutlists_quote_id.sql` applied via Supabase MCP. Adds `cutlists.quote_id bigint references public.quotes(id) on delete set null` plus index. **Pure bookmark, no part data copied** — confirmed: cabinets don't export parts to cut lists; the link is just an FK pointer for UI navigation. Originally specced as a rename of `cutlists.cabinet_id`, but inspection showed the live schema never had that column — the cutlist↔cabinet relationship was always via the `cutlist_cabinets` many-to-many join table (added 2026-05-12, see entry above). Reframed F4 as additive: introduce the per-quote bookmark without disturbing the existing many-to-many. No existing rows backfilled; `quote_id` stays NULL for all 3 current cutlists. Future cut lists created from a Quote's Cabinet view will set it. `src/database.types.ts` updated by hand. **F5/F6 deferred**: dropping `project_id` from leaf tables (quotes, orders, cutlists, pieces, sheets, edge_bands, cabinets) and dropping the `projects` table itself requires a coordinated sweep of 94 `project_id` references across 9 src files (cutlist.js 22, clients.js 19, quotes.js 18, orders.js 10, projects.js 9, migrate.js 6, cabinet.js 6, cabinet-library.js 2, app.js 1) — many are filter ops (`q.project_id === pid`) that need replacement with `client_id`-based or `quote_id`-based logic, plus Cabinet Builder's `_findOrCreateDraftQuote` re-keying. A handoff prompt for the F5/F6 session is in the chat transcript that landed F1-F4. The 4 commits represent a clean intermediate state — nothing is broken, the app is functionally complete with the architecture clearly heading in the new direction. `npm run typecheck` clean throughout. Migrations all applied to dev project `mhzneruvlfmhnsohfrdo`.
- 2026-05-14 — **Business Details popup + logos on PDFs + bank details + free-vs-Pro branding.** Plan: `~/.claude/plans/i-want-the-business-rustling-aurora.md`. (1) **Popup** — the 5-field inline form in the account dropdown (`index.html:141-153`) collapsed to a single "Edit business details" button that opens `_openBusinessDetailsPopup` (new fn in `src/business.js`). Popup reuses `_openPopup` / `_closePopup` / `.pf` form classes and the existing `data-save-pill="business"` indicator on the sidebar button so dirty/saving/saved transitions still work. Pop now hosts: logo upload, name, address, phone+email row, ABN, and a 5-row **bank details textarea** (new). Logo upload still goes through `handleLogoUpload` / `removeLogo` — no logic change, just a different DOM mount point. (2) **Schema** — `business_info` gets a single new column `bank_details text` (freeform; region-agnostic so users can format BSB / Sort Code / IBAN / Routing # as they need). Migration `add_bank_details_to_business_info` applied via Supabase MCP `apply_migration` (initially blocked by the harness permission classifier; user authorized on second attempt). `src/database.types.ts` regenerated — `business_info.bank_details: string | null` now visible in Row / Insert / Update. `get_advisors` clean. `saveBizInfo` rewritten to **merge** payloads (read prior `pc_biz`, only overwrite keys whose input element is currently mounted) so a popup that renders some fields can't blank-out fields edited elsewhere — important because logo upload happens in the same popup but writes through a different path. (3) **Logo + caption on PDFs** — `getBizLogo()` was fetched in `_buildQuotePDF` (`src/cutlist.js:2084`) and never rendered. Now wired through a shared `_drawBizHeader(pdf, biz, logoDataUrl, x, y, opts?)` helper at top of `cutlist.js`. When a logo data URL is present, it occupies the top-left (max 35×18mm, aspect-preserved via `pdf.getImageProperties`) and the business name moves into a small bold caption beneath it. **Caption order reordered per user spec**: name → **address → phone → email** (was phone → email → address). ABN line stays below. When no logo, the old big-bold name layout renders unchanged but with the same reordered subline. All 5 PDF entry points routed through the helper: `_buildQuotePDF`, `_buildStockPDF`, `_buildWorkOrderPDF`, `_buildOrderDocPDF`, `_buildCutListPDF` `pageHeader()`. The cutlist compact `titleBar()` stays text-only (12mm strip too tight for a logo) but gets the same address/phone/email reorder. (4) **Bank details on PDFs** — `_buildOrderDocPDF`'s hardcoded `[Bank details — configure in Business settings]` placeholder at line 2670 replaced with real `biz.bank_details` (line-wrapped via `splitTextToSize`, with a softer "Add them in account → business details" prompt when empty). Same block added to `_buildQuotePDF` between Validity and Acceptance, since clients often pay a deposit on quote acceptance. Stock / work order / cut list PDFs do not get the block — internal docs. (5) **Free-vs-Pro footer branding** — new `_drawPdfFooter(pdf, biz, dateStr, PW, PH, M)` helper replaces inline 6.5pt footer in all 6 PDF call sites (`_buildQuotePDF`, `_buildStockPDF`, `_buildWorkOrderPDF`, `_buildOrderDocPDF`, cutlist combined-page, and elsewhere). Pro users (`isPro()` from `src/limits.js` → subscription status 'active' / 'trialing') get a clean footer with business name + date only. Free users get an accent-coloured band along the bottom of every page with `"Made with ProCabinet.app"` (variant 2 default — mockup at `mockups/pdf/pdf-branding-options.html` shows all three intensities). Variant toggle is the module-level `_PROCAB_FOOTER_VARIANT` constant; flip it 1/2/3 without touching any call site. Variant 3's optional `_drawPdfBrandingStrip(pdf, PW)` corner badge is also wired into every header call site but only paints when the variant constant is 3. (6) **Boot hydration** — `_applyBizInfoFromDB` in `src/app.js` extended to set `biz-bank-details` element (no-op when popup closed) and to persist `bank_details` back to `pc_biz` localStorage so `getBizInfo()` picks it up on subsequent reads. **Verification**: branding mockup screenshot + popup smoke + at least one regenerated quote PDF (logo top-left, address/phone/email reorder, bank-details block) before commit. `npm run typecheck` clean.
- 2026-05-15 — **O.2 — Guided walkthrough complete** (8 milestones, ~10 commits). Spotlight tour built into the real app: CSS `clip-path` mask cuts a highlight around live DOM elements; 15 steps spanning all 8 tabs (Dashboard → Settings dropdown → Clients → Cabinet → My Rates → Library → Quote → Order → Schedule → Stock → Cut List × 2 → Dashboard → Pro CTA). **Architecture**: new `src/walkthrough.js` classic-defer script (loaded last); `onboarding_state` jsonb column on `business_info` (migration `20260515130000_o2_add_onboarding_state.sql`); `_wtPersistState` UPSERTs via bare `_db`; `_wtMaybeAutoStart` called from `app.js onAuthStateChange` after `_loadCabinetTemplatesFromDB`. **Sample-data seeder** (`_wtSeedSampleProject`) inserts 1 client + 3 cabinet templates + 3 stock items + 1 quote + 3 quote_lines + 1 order + 3 order_lines + 1 cutlist — all under the 5-item free cap; IDs captured in `onboarding_state.sample_ids` for clean teardown via `_wtClearSampleData`. Seeder marks `sample_seeded:true` BEFORE inserts (interruption guard). **Key decisions**: (1) `requestAnimationFrame` swapped for synchronous draw + `setTimeout` retry because the preview tool's page is treated as hidden by the browser and rAF callbacks are paused; (2) PostgREST bulk insert requires all array items to have identical keys — labour line gets `type:null, w_mm:null, h_mm:null, d_mm:null` so the shape matches cabinet lines; (3) Cabinet Builder steps 4–5 fall back to a centred tooltip (no active builder project in the seeder, so `#cb-sidebar`/`#cab-view-rates` remain `display:none`); (4) Settings dropdown step uses `openSettings:true` + `e.stopPropagation()` on the overlay click handler to block the document outside-click handler in settings.js from closing it; (5) overlay z-index 9750 (above popups 8000 / suggest 9000, below confirm 9999 / toast 99999). **Help menu**: `_openUserGuide()` in help.js now calls `_wtStart({force:true})`; conditional "Clear sample data" item in the Help dropdown. **Persistence shape**: `{version, dismissed_at, completed, sample_seeded, sample_ids:{clients,stock_items,cabinet_templates,quotes,orders,cutlists}}`. `WT_VERSION=1` — bump triggers version-gated re-show without re-seeding. **Verification**: typecheck clean (0 errors); Quote tab renders cleanly with `isEditing` badge working; seeder round-trip confirmed via DB queries; tour walks all steps with keyboard + mouse.
- 2026-05-15 — **Sentry error tracking wired — P.3 (DSN-gated, inert until a Sentry account exists).** Implements PLAN.md P.3 / locked decision D.5. Plan: `~/.claude/plans/set-up-sentry-dynamic-boot.md`. Installed `@sentry/browser` (^10.53.1, dependency) + `@sentry/vite-plugin` (^5.3.0, devDependency). **(1) Init** — `src/main.js` imports `* as Sentry from '@sentry/browser'` and calls `Sentry.init({ dsn, environment: import.meta.env.MODE })` at the very top, before the existing Supabase env-var check, so a missing-key `throw` is also captured. Init is gated on `import.meta.env.VITE_SENTRY_DSN` — with no DSN the SDK stays uninitialized, `Sentry.init` tree-shakes out of the build (Vite inlines the env read as `undefined`), and every `Sentry.*` call is a safe no-op, so dev and pre-account builds run untouched. `window.Sentry = Sentry` is assigned unconditionally, mirroring the existing `window.supabase` / `window.jspdf` bridge so the 27 classic `<script defer>` files can call `window.Sentry.captureException(...)` directly. **Error tracking only** — no Tracing/Replay/Profiling integrations added (none are default in `@sentry/browser`), keeping Sentry's footprint ~30 KB gz; the default `globalHandlers` integration auto-hooks `window.onerror` + unhandledrejection, so uncaught errors from every classic script are captured without per-file wiring. **(2) User context** — `src/app.js` `onAuthStateChange` listener calls `window.Sentry.setUser({ id: session.user.id, email: session.user.email })` on session and `setUser(null)` on sign-out. **Decision**: user context = id + email (chosen 2026-05-15); Sentry's `sendDefaultPii` stays at its default `false`, so nothing beyond id+email is attached. **(3) Types** — `src/globals.d.ts` declares `Window.Sentry: typeof import('@sentry/browser')` beside the supabase/jspdf bridge globals so `tsc` stays clean. **(4) Source maps / releases** — `vite.config.mjs` adds `sentryVitePlugin({ org, project, authToken, disable: !process.env.SENTRY_AUTH_TOKEN })`; `build.sourcemap` was already `true`. `disable` makes the plugin a no-op whenever `SENTRY_AUTH_TOKEN` is absent, so local `npm run build` and pre-secret CI runs never fail or attempt an upload. Note: only `main.js` is bundled/minified by Vite — the 27 domain scripts are copied verbatim to `dist/src/` (unminified, traces already readable), so the plugin's main value is release/commit grouping plus de-minified `main.js` traces. **(5) Build env** — the build runs in GitHub Actions, not Cloudflare (`wrangler pages deploy dist` only uploads the prebuilt `dist/`), so build-time env lives in `.github/workflows/deploy.yml` alongside `VITE_SUPABASE_*`. Added a commented scaffold there for `VITE_SENTRY_DSN` / `SENTRY_ORG` / `SENTRY_PROJECT` / `SENTRY_AUTH_TOKEN`; `.env.example` documents all four (the DSN is publishable; the three `SENTRY_*` build vars are not `VITE_`-prefixed and must never reach the client). **Pending user actions** (P.3 sub-steps left ⬜): create the Sentry project (Browser JavaScript platform), fill the DSN/org/project into `deploy.yml`, add `SENTRY_AUTH_TOKEN` as a GitHub Actions repo secret, set up email alerts. **Verification**: `npm run typecheck` clean; `npm run build` succeeds with the vite-plugin silently no-op'd (no token) and source maps emitted, Sentry confirmed in the bundle; `npm run dev` boots with zero console warnings/errors; in the page `window.Sentry` is an object exposing `captureException`/`setUser`/`init`, `getClient()` is undefined (init correctly skipped with no DSN), and `captureException`/`setUser` no-op without throwing.
- 2026-05-15 — **PostHog product analytics wired — C.5 (key-gated, inert until a PostHog account exists).** Implements the PostHog half of PLAN.md C.5 / locked decision D.4. Plan: `~/.claude/plans/set-up-posthog-typed-grove.md`. Installed `posthog-js` (^1.373.4, dependency). **(1) Init** — `src/main.js` imports the `posthog-js` default export and calls `posthog.init(key, {...})` **only when `import.meta.env.VITE_POSTHOG_KEY` is set**; with no key the SDK never initialises and `window.posthog` stays `undefined`. The key-presence check IS the dev gate — a dev `.env.local` simply omits the key, so dev traffic never reaches prod analytics. Unlike the Supabase keys, a missing PostHog key never `throw`s (the block is a plain `if (key) {…}`, no erroring `else`). Config: `api_host` from `VITE_POSTHOG_HOST` (default `https://eu.i.posthog.com` — **EU region**, chosen this session), `person_profiles: 'identified_only'`, autocapture + pageview on, session replay enabled with **no masking** (`maskAllInputs: false`, chosen this session — password inputs are still force-masked by PostHog); autocapture + replay both stay server-toggleable from the PostHog dashboard. `window.posthog = posthog` is assigned only inside the `if` block, so the wrapper guards below genuinely gate. **(2) Wrapper module** — new classic file `src/analytics.js` (19th `<script defer>`, loaded right after `src/stripe.js`) exposes no-op-safe globals `_track(event, props)`, `_identifyUser(session)`, `_resetAnalytics()`, each guarding `window.posthog` so call sites are unconditional. `_track` auto-attaches a `plan` property (`free`/`pro`, from `isPro()`) to every event. **(3) Event model — generic, not per-entity** (decided this session for funnel flexibility): one `library_item_created` event covers all six libraries (`library` ∈ clients/quotes/orders/cutlists/stock/cabinet_templates, the `FREE_LIMITS` keys) with `library` + `item_id` + `source` props; one `pdf_created` event covers all five PDF types (`type` ∈ quote/stock_list/work_order/order_document/cutlist). A PostHog funnel can show "any library item → any PDF" directly, or filter by property for the entity-specific funnel — no event proliferation, no separate `first_*` events (PostHog funnels are first-occurrence per user natively). Other events: `user_signed_up`, `user_logged_in`, `free_tier_limit_hit` (`library`, `current_count`), `upgrade_clicked` (`billing_cycle`), `section_viewed` (`section`); plus `identify` on auth-state-change and `reset` on sign-out. PLAN.md C.5's original "first project created" funnel step is obsolete — the `projects` entity was retired (F6); cut-list creation is the analog and flows through `library_item_created` with `library:'cutlists'`. **(4) Call sites** — `library_item_created` fires from the real create path of each library (`createClient`, `createQuoteFromEditor`, `createOrderFromEditor` + `convertQuoteToOrder` + `cbCreateOrderFromDraft`, `_clStartNewCutlist` + `_clNewCabinetLinkedCutlist`, `addStockItem`, `_saveCabinetToDB`); the empty draft-placeholder paths (`_qStartNewQuote`/`_oStartNewOrder`) are deliberately NOT wired so abandoned drafts don't inflate the metric. `pdf_created` fires inside each `_build*PDF` chokepoint in `cutlist.js` (covers every caller). `free_tier_limit_hit` fires in `_enforceFreeLimit` (one edit covers all libraries); `upgrade_clicked` in `_handleUpgradeClick`; `section_viewed` in `switchSection`. **`identify` runs in `onAuthStateChange` AFTER `await loadAllData()`** so `_subscription`/`isPro()` is loaded — otherwise the person is permanently mis-tagged `plan:'free'`. **(5) Types** — `src/globals.d.ts` declares `Window.posthog?: typeof import('posthog-js').default` (mirrors the Sentry bridge line). `.env.example` documents `VITE_POSTHOG_KEY` + `VITE_POSTHOG_HOST`. **Decision: npm + bridge, not a CDN snippet** — overrides C.5's loose "install snippet" wording, for consistency with the Supabase/jsPDF/Sentry bridge and real TS types. **Out of scope**: Cloudflare Web Analytics + Google Search Console (rest of C.5, still ⬜). **Pending user actions**: create the PostHog project (EU region), put `VITE_POSTHOG_KEY` into `.github/workflows/deploy.yml` + Cloudflare Pages env, build the funnels in the PostHog dashboard. **Verification**: `npm run typecheck` clean; `npm run build` succeeds with the key absent (proves no throw) — `dist/src/analytics.js` copied, `dist/index.html` carries the tag, `posthog-js` in the bundle; `npm run dev` reload — app boots with zero console errors, `window.posthog` is `undefined` (dev gate), `_track`/`_identifyUser`/`_resetAnalytics` are defined and no-op without throwing. Live-event confirmation deferred — needs the user's real PostHog key.
- 2026-05-16 — **Per-order priority stepper in the Schedule sidebar.** Each order row in `renderSchedule()`'s sidebar (`src/schedule.js`) now carries a compact stepper — the `orders.priority` value flanked by stacked up/down chevrons (`.sched-pri*` styles in `styles.css`; `SCHED_CHEV_UP`/`SCHED_CHEV_DOWN` icon constants). New `_schedStepPriority(orderId, dir)` mirrors `setOrderProdStart`'s persistence: updates `o.priority` in memory, fires a fire-and-forget `_db('orders').update(...)`, and re-renders — which re-runs `computeSchedule`, so the calendar re-lays-out. Floored at 0; semantics match the order editor's existing Priority field (`.sched-stepper`, "1 = highest"). **Persists immediately** — unlike the editor field there is no save button in the sidebar. Arrow buttons call `event.stopPropagation()` so they don't trigger the row's scroll/open handlers; the container is `draggable="false"` so it can't start a row drag in manual-sort mode. **Knowingly left as-is**: the sidebar's "Sort by: Priority" still orders highest-number-first, which disagrees with the scheduler's 1-is-highest ordering — a pre-existing inconsistency, out of scope per the user. **Verification**: `npm run typecheck` clean; dev-preview — stepper on all 5 orders, up/down adjust + persist, amber when ≥1, down disabled at 0, clicks don't open the order, works in manual-sort mode, no console errors.
- 2026-05-16 — **Read-only demo mode + walkthrough hardening.** Plan at `~/.claude/plans/the-walkthrough-is-still-composed-floyd.md`. A logged-out visitor now sees the whole app pre-seeded with a realistic dataset (5 clients / quotes / orders / cutlists / cabinet templates, 10 stock items) and can explore every tab; trying to *save* opens a sign-in prompt. **Mechanism**: new `src/demo.js` holds the static seed + `_demoSelect` (resolves a `_DBBuilder` against the seed) + `_demoBlockWrite`; `src/db.js`'s `_DBBuilder.then()` branches on `window._demoMode` — reads served from the seed, writes blocked. The no-session branch of `onAuthStateChange` sets `_demoMode=true` and runs the full `loadAllData` boot path, so every panel renders populated like a signed-in free user over an in-memory DB. `_userId` stays `null` for guests, so the existing autosave `if(!_userId)return` guards keep in-builder editing non-persistent (exploration, not saving). **Supersedes the 2026-05-03 Item-2 locked decision "Cabinet Builder requires sign-in"** — `_renderCBAuthGate` and the read-loaders (`_loadCabinetTemplatesFromDB`, `_loadCBLinesFromDB`, `_loadCutListsByClient`, `loadDayOverrides`, the cut-list-library render gate) now honour `_demoMode`; explicit save/create actions route through the existing `_requireAuth()` (the cabinet `_toast('Sign in to…')` guards + `cbSaveToLibrary`/`cbAddLineToLibrary` upgraded to it). **Walkthrough rebuilt** (`src/walkthrough.js`): the DB seeder (`_wtSeedSampleProject` / `_wtClearSampleData` / `_wtSyncHelpItem`, ~180 lines) deleted — the tour always runs over the demo seed, and a signed-in user's run flips `_demoMode` on temporarily via async `_wtStart` / `_wtClose` (real data restored on exit, behind an unsaved-edit confirm). Target resolution replaced with a single `requestAnimationFrame` `_wtWaitFor` waiter; an unreachable target now skips the step (`_wtSkipUnreachable`) instead of drawing a broken centered tooltip; teardown is idempotent and lands on the Dashboard. Dismissal persists to `localStorage['pc_wt_state']` (mirrored to `business_info.onboarding_state` when signed in) so a first-time visitor sees the tour once. **Optimizer paywall removed** — the `#paywall-modal` + `pcOptCount` 5-free-optimizations gate is gone; the cut-list optimizer is a free demo feature and the 5-item library cap is the sole free-tier limit. **Boot-race fix**: `onAuthStateChange` awaits `DOMContentLoaded` so `loadAllData`'s hydrate helpers see globals from late-loading defer scripts (`_quoteLineRowToCB` in `migrate.js`) — without it the guest's `INITIAL_SESSION` raced ahead and quote/order totals failed to hydrate. No schema migration. **Verification**: `npm run typecheck` clean; dev-preview confirmed the seeded Dashboard, a guest-usable Cabinet Builder with priced cabinets, and the walkthrough auto-starting and stepping over the seed.
- 2026-05-17 — **Walkthrough spotlights extended to enclose their sub-tab row.** Four `_wtSteps` entries (`src/walkthrough.js`) spotlit a content panel sitting *below* a sub-tab nav row, so the highlight read as detached from the tab the tour had just "clicked". Re-pointed each step's `target` at the wrapper ancestor that holds both the row and the panel: step 20 "Cabinets & live pricing" `#cb-results`→`#cab-view-builder .main-content`; step 24 "Rate inputs" `#cb-lib-rates-wrap`→`#cb-sidebar-library`; step 26 "Cut List Library" `#cl-view-library`→`.cl-right`; step 30 "Visual layout" `#results-area`→`.cl-right`. No engine change — `_wtDrawSpot` already draws the ring from one element's `getBoundingClientRect()`, so a wrapper target Just Works. Step 30's `.cl-right` necessarily also encloses the two layout toolbars between the sub-tab row and the diagram (no single rectangle can contain the sub-tabs + diagram while skipping the toolbars between them); chosen deliberately — step 30 now spotlights the whole Cut Layout pane as one block.
- 2026-05-17 — **Working Hours config inlined into the Schedule sidebar; priority `0` retired from the UI.** The `⚙ Hours` footer button + the `_openScheduleSettingsPopup` "Working Hours & Holidays" popup are gone — the same fields (default workday hours, the per-weekday grid, the queue-start anchor, the holidays/overrides list) now render as a collapsible `<details class="sched">` "Working Hours" section at the bottom of the Schedule sidebar body, via new `_schedHoursSectionHTML` / `_schedHoursToggle` (open-state in `localStorage['pc_sched_hours_open']`), mirroring the Orders editor's collapsible Schedule section. **`renderSchedule(opts)`** gained a `{sidebar:false}` mode that re-renders only the calendar — it skips the sidebar-DOM rewrite and the scroll-to-today; the inline hours/holiday handlers (`_updateSchedDefault`, `_updateWeekdayHour`, `_holidayAdd/EditDate/EditHours/Delete`) use it so editing an inline input doesn't destroy that input or yank the calendar viewport (`_quickOverrideDate` keeps the full re-render — it fires from a calendar cell, not a sidebar input). **Priority**: `0`/`null` stays the internal "no priority" sentinel but is never shown as `0` — the Schedule sidebar stepper and the Orders priority field render it as a dash (`—`)/blank, and both steppers floor at `1`. The Schedule sidebar "Sort by: Priority" now sorts **ascending** (1 on top, unset last), matching `scheduler.js`'s auto-scheduler which was already ascending — the old descending sidebar sort contradicted both it and the `1 = highest` label. No schema change (`orders.priority` already permits `0`/`null`).
- 2026-05-17 — **Branded Supabase auth emails + custom-sender plumbing (PLAN.md O.3).** The default Supabase signup email (generic copy, "Supabase Auth" sender, "powered by Supabase" footer) is replaced with four branded HTML templates in a new `supabase/templates/` dir — `confirmation.html`, `recovery.html`, `magic-link.html`, `email-change.html` — paste-ready for Supabase → Authentication → Emails. Shared shell: 600px table layout, inline styles, no SVG/flexbox (email-client-safe); dark `#111111` header carrying the `logo-colour-on-dark` wordmark as a hosted `<img>`, white body card on `#f2f2f2`, one amber `#e8a838` CTA button (dark `#111111` button text for contrast — white-on-amber fails AA) plus a paste-able fallback link, muted reassurance + "Workshop OS for cabinetmakers" footer. Copy is British-English trade voice per `marketing/specs/brand-voice.md`; all four use `{{ .ConfirmationURL }}`. **Invite + Reauthentication templates skipped** — no admin-invite or step-up-auth flow exists. **Logo hosting**: email clients strip SVG/data-URIs, so the logo must be a hosted PNG — `vite.config.mjs` gains `copyEmailLogoPlugin` (mirrors the existing `copyClassicScriptsPlugin` `closeBundle` pattern; `publicDir` stays `false`) copying `brand/logo/logo-colour-on-dark.png` (48 KB) into `dist/`, so it deploys to `https://procabinet.app/logo-colour-on-dark.png` — the absolute URL the templates reference. **Code**: `src/app.js` `authSubmit`'s `signUp` call gains `options.emailRedirectTo: window.location.origin` so the confirm link returns the user to the environment they signed up from (prod or localhost). **Decision — no `supabase/config.toml`**: a fresh config.toml + `supabase config push` would clobber other dashboard auth settings (Site URL, redirect allow-list, OAuth, JWT) since push is whole-config; templates kept as standalone files, pasted via the dashboard instead. **Pending user actions** (O.3 sub-steps left ⬜): create a Resend account + verify `procabinet.app` (DNS in Cloudflare), set Supabase custom SMTP (`smtp.resend.com`, sender `noreply@procabinet.app` / `ProCabinet.App`) — also what drops the Supabase footer and lifts the built-in email rate limit — paste the four templates + subjects, set Site URL + redirect allow-list, test deliverability (SPF/DKIM/DMARC, spam scoring). **Verification**: `npm run build` → `dist/logo-colour-on-dark.png` present; `npm run typecheck` clean; templates eyeballed in a browser preview.
- 2026-05-17 — **Features menu — suggest-by-email + upvotable leaderboard.** Plan: `~/.claude/plans/add-a-new-features-vectorized-breeze.md`. New Features button in the top toolbar (left of Help) runs `_openFeaturesBoard()` — a popup with a "Suggest a feature" button and an upvotable leaderboard of feature ideas. "Make a Suggestion" removed from the Help dropdown (now three items). **Suggestions stay email-only** (user decision): the suggest button keeps the existing `mailto:` flow; the leaderboard is owner-curated, not user-submitted — the owner adds entries in the Supabase dashboard from emailed suggestions, so there is no in-app submission or approval workflow. **Schema** (migration `20260517120000_feature_suggestions.sql`, applied via Supabase MCP): `feature_suggestions` — a **shared/global table with NO `user_id`** (deviation from Pattern A; every signed-in user reads the whole board), select-only RLS `using(true)`, no insert/update/delete policies so only the dashboard / service role writes (mirrors the `subscriptions` deviation); `feature_suggestion_votes` (`suggestion_id` + `user_id` composite PK = one vote per user), Pattern A select/insert/delete, no update. A `SECURITY DEFINER` trigger `feature_suggestion_vote_count()` maintains `feature_suggestions.vote_count` on vote insert/delete (definer rights needed — the voter has no UPDATE on `feature_suggestions`); `EXECUTE` revoked from `public`/`anon`/`authenticated` to clear the advisor's "SECURITY DEFINER callable as RPC" warning — the trigger still fires regardless. `status` (`open`/`planned`/`in_progress`/`shipped`) drives an optional progress badge. **Code**: new `src/features.js` (`<script defer>` after `help.js`) holds `_openFeaturesBoard` + optimistic `_featureToggleVote` + the relocated `_openSuggestion`; `_helpContext` / `_mailtoHref` / `SUPPORT_EMAIL` stay in `help.js` (Bug Report + Support still use them). The board reads `feature_suggestions` ordered by `vote_count` plus the user's own vote rows, renders rows with an arrow-up vote button, and voting is optimistic with revert-on-error. Signed-out / demo visitors get a sign-in prompt and zero DB calls. **Verification**: `npm run typecheck` clean; `get_advisors` clean bar the pre-existing leaked-password warning; dev-preview — board opens, seeded entries sort by votes with status badge, upvote/un-upvote persists and the trigger-maintained count survives reload, Help no longer shows "Make a Suggestion". SCHEMA.md § 3.19–3.20 added.
- 2026-05-17 — **Import/export gated to Pro; tour-skip routes to the CTA; demo replays the tour each reload.** Plan: `~/.claude/plans/remove-all-import-replicated-quokka.md`. **(1) Import/export = Pro-only.** New `_enforceProFeature()` in `src/limits.js` returns true for logged-out demo visitors (`!_userId`) and Pro users, false for signed-in free users — for whom it fires `_track('pro_feature_blocked')` and opens `_openProFeatureModal()` (new, `src/stripe.js`): a lock-icon popup mirroring `_openLimitHitModal`'s structure + upgrade CTAs. Guards prepended to all 10 CSV entry points (`export/importClientsCSV`, `…QuotesCSV`, `…OrdersCSV`, `…StockCSV`, `cbExport/ImportLibrary`). **Decision — CSV data only**: PDF generation (`printQuote`, `printLayout('pdf')`, `_buildStockPDF`) stays free as core trade documents; orphaned `_exportLocal` / cutlist `exportCSV`+`triggerImportCSV` / `exportStockLibrary` have no live buttons and were left ungated. **Decision — demo keeps import/export**: the gate keys on `_userId`, not `isPro()` alone, so the logged-out marketing demo stays fully usable. **(2) Skip → CTA.** New `_wtSkip()` (`src/walkthrough.js`): a non-Pro user skipping the tour jumps to its existing final CTA step (`_wtRender(lastIdx)`) rather than closing; Pro users still close immediately; skip-on-CTA closes (no loop). Reuses the tour's existing pricing step — no new CTA UI. Both skip paths (`_wtOverlayClick`, `_wtKeydown` Escape) reroute through `_wtSkip()`. **(3) Demo tour every reload.** `_wtMaybeAutoStart()` gains an early `if (!_userId) { _wtStart({force:true}); return; }` that bypasses the `pc_wt_state` dismissal gate for logged-out visitors; signed-in users keep the version-gated once-only behaviour. No schema change. **Verification**: `npm run typecheck` clean; dev preview — lock modal screenshotted for a (simulated-free) export click, Pro export gate passes, free skip jumps to the CTA pricing step, Pro skip closes immediately, `_wtMaybeAutoStart` with `_userId` nulled starts the tour.
- 2026-05-17 — **Dashboard card headers made clickable; per-card nav buttons removed.** All six `renderDashboard()` cards (`src/dashboard.js`) dropped their header nav button — the two "View all" (Active Orders / Recent Quotes), Stock Alerts' "View stock", and Schedule's "Open" — and the `.card-header` div itself is now the navigation target, carrying `class="card-header-nav"` + an `onclick="switchSection(...)"`. Active Orders → orders, Recent Quotes → quote, Stock Alerts → stock, Schedule → schedule; the two previously button-less cards, Monthly Revenue and Pipeline, also became clickable and both route to orders (they summarise order data and there is no dedicated revenue/pipeline section). New `.card-header-nav` rule in `styles.css` adds `cursor:pointer` + a hover tint (`var(--accent-dim)` background, accent-coloured `.card-title`) — the same treatment as `.client-suggest-item:hover`; no chevron, hover-only affordance per user choice. Header clicks and per-record `.dash-row` clicks don't collide (separate sibling elements, no bubbling overlap). `npm run typecheck` clean; dev-preview verified all six headers navigate correctly, row clicks still drill into records, no console errors.
- 2026-05-17 — **Walkthrough final step → four-tier ProCabinet plan CTA + Founder plan.** Plan: `~/.claude/plans/show-me-15-new-sorted-cupcake.md`. The tour's final `showPricing` step (`src/walkthrough.js`) swaps its plain 2-card Monthly/Annual block for a four-tier picker — Free / Monthly / Annual / Founder — ported from `mockups/walkthrough/walkthrough-cta-redesigns.html` variant A. New `_wtCtaHTML()` renders it (theme-variable `.wt-cta-*` CSS in styles.css; `.wt-center.wt-has-cta` widens the modal to 700px); per-tier buttons dispatch via `data-wt-act` — `cta-free` closes the tour, `cta-monthly/annual/founder` route through new `_wtCtaCheckout(plan)` (signed-in → `_handleUpgradeClick`; guest → close + `_showAuth()`, since a guest needs an account before paying). `WT_VERSION` bumped 2→3 so the updated CTA re-shows. **Founder plan** — a one-off $299 lifetime purchase capped at 50 accounts. **Decision — Founder is a `mode:'payment'` Checkout recorded as a lifetime `subscriptions` row**: `stripe-checkout` branches on `plan`; founder creates a one-time-payment session with `metadata.plan='founder'`; `stripe-webhook`'s `checkout.session.completed` now also handles `mode:'payment'` → `recordFounderPurchase()` upserts `{plan:'founder', status:'active', stripe_subscription_id:null, current_period_*:null}` — `isPro()` already treats status `active` as Pro, so no expiry = permanent access and no schema change (the subscription columns were already nullable). The 50-seat cap is enforced at Checkout-session creation (`founderSeatsTaken() >= 50` → 409); a brief two-buyer race could let one extra through — accepted for a launch promo. **Live seat counter**: new migration `20260517200000_founder_seat_counter.sql` adds a `SECURITY DEFINER` `founder_seats_taken()` RPC granted to `anon`+`authenticated` — the tour runs for guests and `subscriptions` is SELECT-RLS'd to the owner, so only the aggregate count is exposed; `_wtFetchFounderSeats()` calls it and the CTA shows "N of 50 left" (graceful "Limited to 50" fallback on failure / before the migration is applied). **Launch pricing**: Monthly $35/mo ($25/mo first 6 months), Annual $300/yr ($180 first year), applied via Stripe coupons auto-attached with `discounts:[{coupon}]` (mutually exclusive with `allow_promotion_codes`, so the latter is dropped when a coupon is configured). Stale "$299/yr · save 29%" copy across `src/stripe.js` upgrade modals refreshed to the launch prices; `renderSubscriptionSection` + `_handleManageSubscription` gain a Founder branch ("Founder · Lifetime", nothing to manage). `FOUNDER_CAP=50` added to `src/limits.js`. **Pending user actions** (Stripe is partly dashboard work): create the Founder one-off Price + the two launch coupons (recreate Annual at $300), `supabase secrets set STRIPE_PRICE_FOUNDER / STRIPE_COUPON_MONTHLY_LAUNCH / STRIPE_COUPON_ANNUAL_LAUNCH / STRIPE_PRICE_ANNUAL`, apply the migration, `supabase functions deploy stripe-checkout stripe-webhook`. **Verification**: `npm run typecheck` clean; dev preview — CTA renders in dark + light, "Continue free" closes the tour, a guest paid-button closes the tour + opens sign-up, the seat counter falls back gracefully pre-migration.
- 2026-05-17 — **Returning free users get the Pro CTA once per browser session.** A signed-in free user who has already completed the first-run guided tour now sees just the final Pro CTA — not the whole tour — once per browser session; the full tour stays first-login-only. **Mechanism** (`src/walkthrough.js`): new `_wtStartCta()` opens the overlay directly on the final `showPricing` step, and a new module flag `_wtCtaOnly` marks the mode so it behaves as a standalone modal rather than a tour — `_wtApplyContext` skips the `switchSection` / `_wtGateSection` calls, `_wtClose` skips the dismissal persistence and the "land on Dashboard" `switchSection`, and `_wtNext` / `_wtBack` collapse to single-step so Back and ← can't step out of the CTA into the middle of the tour. Unlike `_wtRunStart` it never borrows demo mode — the CTA is a centred modal over the user's own live data. New `_wtMaybeShowSessionCta()` is the gate: skipped for Pro users (`isPro()`) and when `sessionStorage['pc_wt_session_cta']` is already `'1'`; otherwise it sets the flag and calls `_wtStartCta()`. `_wtMaybeAutoStart`'s "dismissed at the current version" branch now calls it instead of returning silently. `_wtClose` for the *full* tour also sets `pc_wt_session_cta`, so the standalone CTA doesn't double up on a same-session reload. sessionStorage (not localStorage) — the flag clears when the browser session ends, so the CTA returns next session. First-run tour, the `WT_VERSION` re-show and the logged-out-demo every-reload replay are untouched. No schema migration. **Verification**: `npm run typecheck` clean; dev preview — `_wtStartCta()` renders the four-tier CTA as a centred modal with no tour spotlight, Back / ← are no-ops, "Continue free" tears it down leaving the app in place, and `_wtMaybeShowSessionCta()` skips while the flag is set and shows + re-sets it once cleared.
- 2026-05-17 — **"Upgrade to Pro" (account dropdown) opens the plan-picker CTA.** `renderSubscriptionSection`'s free-tier block (`src/stripe.js`) — the Upgrade button now calls `_wtStartCta()` (newly added to `walkthrough.js`'s public surface) to open the four-tier CTA overlay, instead of `_handleUpgradeClick('annual')` which jumped straight to annual Stripe checkout. The two price caption links beneath the button ("$15 / mo · billed yearly", "$25 / mo") are removed — the CTA's own per-tier buttons now drive plan selection. The four popup upgrade CTAs (`_openLimitHitModal`, `_openProFeatureModal`, etc.) keep their direct-checkout buttons + price captions — out of scope, unchanged. `npm run typecheck` clean; verified in the dev preview (free-tier block renders the Upgrade button with no captions, clicking it opens the CTA overlay).
- 2026-05-18 — **Two post-launch bug fixes: client-delete cascade, and Cabinet Builder quotes are no longer hidden.** **(1) Client delete cascade** (`src/clients.js`): `removeClient` deleted only the `clients` row, orphaning that client's quotes, orders and cut lists. It now removes all of them — cut lists first (`cutlists.quote_id` → `quotes` is `ON DELETE SET NULL`, so `removeClient` deletes `cutlists` by the client's quote ids before the quotes go), then `orders` and `quotes` (`quote_lines` / `order_lines` and cut-list child rows cascade at the DB level), then the client row — pruning the in-memory `quotes` / `orders` arrays and `_cutListsByClient`; on a DB error it toasts and aborts. New `_confirmRemoveClient(id)` builds the confirm warning ("Delete <b>Name</b> and permanently remove N quotes, M orders and K cut lists? This cannot be undone." — quote/order counts from memory, cut-list count via a live query). Pre-existing bug noted but left untouched: `_loadCutListsByClient` still selects a dropped `cutlists.client_id` column, so `_cutListsByClient` is always empty and the Clients-card cut-list section never renders. **(2) Cabinet Builder hidden-draft model removed** (`src/cabinet.js`, `src/quotes.js`): the builder autosaved into a `status:'designing'` quote that `_isDraftQuote` hid from the Quotes tab, client cards, dashboard and CSV export — so a quote built in the Cabinet Builder appeared nowhere except via "Go to Quote". Per user decision the hidden state is removed: `_findOrCreateDraftQuote` now creates a normal `status:'draft'` quote (find step dropped); `_loadCBClientById` creates it eagerly when a client is picked for a new quote, enforces the free-tier quote cap, and adopts it via `cbEditingQuoteId` (persisted to `pc_cb_editing_quote_id`); the `_syncCBLinesToDB` else-branch does the same as a self-healing fallback, routing through `_syncCBLinesToQuote`. A Cabinet Builder quote is now a first-class draft — visible everywhere immediately and counted toward the 5-quote free cap. `_isDraftQuote` is kept only to hide any pre-existing `'designing'` / `[CB_DRAFT]` rows. **Diverges from** `item-2-cabinet-quote-unification.md` Phase 2 ("Quotes tab shows formal non-draft quotes only") — the draft/snapshot split is collapsed. **Verification**: `npm run typecheck` + `npm run build` clean; dev-preview end-to-end — the new-quote flow creates a `status:'draft'` quote that renders in the Quotes tab ("Draft (1)") and on the client card ("1 quote · QUO-0065 Draft"); deleting the client shows the cascade warning and removes client + quote from the in-memory arrays and the DB (re-queried — zero orphans).
- 2026-05-18 — **Walkthrough tour: current-section tab stays lit, Help + New-features steps added, final Dashboard steps reordered** (`src/walkthrough.js`). (1) New `_wtDrawTabHighlight(step)` clones the active `.nav-tab` and floats the copy inside `#wt-overlay`, above the dim mask, so the section being toured reads as highlighted on every spot step (skipped when the step already spotlights the nav bar itself). The clone keeps the `.nav-tab` class for its lit styling, so its box geometry (`position`/`left`/`top`/`width`/`height`/`margin`) is pinned with `setProperty(..., 'important')` — the responsive `.nav-tab { width: auto !important }` rule would otherwise collapse it to icon width. `switchSection`'s index-based tab loop is unaffected: the clone sorts last in document order so `sections[8]` is `undefined` and the eight real tabs keep their 0-7 mapping. (2) Two header-button spot steps added after the Account step — **Help** (`#help-dropdown`, new `openHelp` step flag) and **New features** (`#features-dropdown`, `openFeatures`) — mirroring the existing Settings/Account dropdown-step pattern: `_wtApplyContext` toggles the dropdown's `.open` class, `_wtGetPreClickTarget` walks the cursor to `#help-btn` / `#features-btn`, and `_wtClose` clears both on teardown; the tour is now 40 steps. (3) Swapped the final two Dashboard spot steps — order before the CTA is now Dashboard tab → Live overview → Quick actions → CTA. `WT_VERSION` deliberately left at 3 — a bump re-shows the whole tour to everyone who already completed it, so it is deferred until this batch of walkthrough changes is finalised rather than fired on each iteration. **Verification**: `npm run typecheck` clean; dev preview — cloned tab lit above the dim and pixel-matches the real tab (937,64,146×35); the Help and New-features steps render with their dropdowns spotlit (steps 5 and 6 of 40); the final Dashboard steps confirmed as Live overview → Quick actions before the CTA.
- 2026-05-18 — **Quote Builder bug fixes — new quote lands on the builder; library "Add to Quote" no longer mints a headless quote.** Direct follow-up to the visible-draft change above: making Cabinet Builder drafts visible everywhere surfaced two latent bugs. **(1)** `_loadCBClientById` (`src/cabinet.js`) closed the new-quote flow with `switchCBMainView('library')`, dropping the user on the Cabinet Library tab instead of the Quote Builder — changed to `'results'` so creating a quote lands on the builder, mirroring `_cbPickQuote` opening an existing quote. **(2)** `cbAddFromLibrary` (`src/cabinet-library.js`), the library-card "Add to Quote" button, pushed onto `cbLines` and let the `saveCBLines` → `_syncCBLinesToDB` 800 ms debounce pick the target; with no quote or order open, the else-branch's `_findOrCreateDraftQuote` (which always *creates* now) minted a brand-new draft on every click — the "headless quote". Added a guard: when `!cbEditingQuoteId && !cbEditingOrderId` it switches to the Quote Builder and toasts "Open or start a quote first, then add from the library" instead of adding. Stale success-toast wording fixed ("added to project" → "added to quote"). **Verification**: `npm run typecheck` clean; dev-preview end-to-end — the guard blocks an add with nothing loaded (no quote created, redirected to the builder), creating a new quote lands on the builder with `cbEditingQuoteId` set, and "Add to Quote" with that quote loaded writes the cabinet into its `quote_lines` (DB re-queried: exactly the loaded quote got the row, no third quote).
- 2026-05-18 — **Manage Subscription popup shows the live offer price + price-increase date.** The active-subscription popup (`_openManagePopupActive`, `src/stripe.js`) hard-coded `$35/mo` / `$300/yr`, so every subscriber on the launch coupon ($25/mo for the first 6 months then $35/mo, auto-applied at checkout via `STRIPE_COUPON_MONTHLY_LAUNCH`) saw the standard price, not what they actually pay. The discount lives only in Stripe — the `subscriptions` table mirrors plan/status/period, never the coupon. **Decision — fetch live on popup-open, not mirror via the webhook**: the alternative (add discount columns + have `stripe-webhook` write them) needs a migration and only populates on a subscriber's *next* Stripe event, so existing subscribers would show stale/empty data until then (a backfill); a live read is accurate for everyone immediately, needs no migration, and can't go stale. **New `stripe-subscription` edge function** (`supabase/functions/stripe-subscription/`) — authenticates the caller's Supabase JWT, looks up their `stripe_subscription_id`, retrieves the subscription with `expand:['discounts']`, and returns `{currency, interval, standardAmount, currentAmount, discountEnd}` (minor units; `currentAmount` = standard with the coupon's `percent_off`/`amount_off` applied; `discountEnd` from `discount.end`). Mirrors `stripe-portal`'s auth/CORS conventions; no new secrets. **Client** — `_openManagePopupActive` renders the price card with a "Loading…" placeholder (`#ms-price-line`) plus a hidden `#ms-increase-line`, then fires `_fillManageSubscriptionPricing`: on success it shows the discounted price and, when a dated discount is active, an "Increases to $35/mo on &lt;date&gt;" line; on failure or no active discount it falls back to the static plan price. New helpers `_loadSubscriptionPricing` (authed fetch) and `_fmtSubscriptionPrice` (forces the en-US locale so USD renders `$` not `US$`; shows 2 decimals only for non-whole amounts). No schema change. **Deployed** to the live project (version 1, `verify_jwt: true`, no new secrets) and smoke-tested (OPTIONS preflight 204 + CORS, unauthenticated POST 401); the popup reads real pricing once the frontend ships. **Verification**: `npm run typecheck` clean; dev preview — formatter ($25/mo, $300/yr, $35.50/mo, £25/mo), fallback ("$35/mo", no increase line), stubbed-discount render ("$25/mo" + "Increases to $35/mo on 18/11/2026"), and no-discount render (increase line hidden) all confirmed.
- 2026-05-18 — **Security + load review — five fixes (items 1, 2, 5, 6, 10 of 12).** A security/load review flagged 12 issues; this batch actions five. Plan: `~/.claude/plans/do-1-2-5-only-mask-lovely-snail.md`. **(#1 N+1)** `_hydrateQuoteTotals` / `_hydrateOrderLines` (`src/quotes.js`) fetched `quote_lines` / `order_lines` once per quote/order on every boot (and hourly token refresh). Both rewritten to one batched `.in()` query each, grouped by parent id in memory; `q._lines` / `q._totals` / `o._lines` populated identically to before (`_lineSubtotal` reused for the totals math; `quoteTotalsFromLines` / `orderTotalsFromLines` / `_refreshQuoteTotals` kept unchanged for single-record post-edit refreshes). Dev-preview verified — a 4-order account now issues one `order_lines?order_id=in.(…)` request, was four. **(#2 DB performance)** migration `20260518150000_perf_indexes_and_rls_initplan.sql` (applied via Supabase MCP): adds 10 missing FK covering indexes (`user_id` on cabinet_hardware/clients/edge_bands/order_lines/pieces/quote_lines/quotes/sheets; `client_id` on orders/quotes) and rewrites all 61 RLS policies that called `auth.uid()` per row to `(select auth.uid())` — the InitPlan optimisation, semantically identical (same rows), evaluated once per query. `get_advisors` performance: the `auth_rls_initplan` ×57 and `unindexed_foreign_keys` ×10 lints all cleared; only INFO `unused_index` notices remain (expected on a near-empty DB — do not drop). SCHEMA.md § 4 updated to the `(select auth.uid())` form. **(#5 PostHog privacy)** `src/main.js` session-recording config gains `maskTextSelector` for the six Business Info fields (`#biz-name/#biz-phone/#biz-email/#biz-address/#biz-abn/#biz-bank-details`) — the owner's own company PII + bank/payment details. **Decision — business-info fields only**: per user scope choice, client data and pricing are deliberately NOT masked. **(#10 build)** `vite.config.mjs` — `copyClassicScriptsPlugin` now minifies each `src/*.js` through `esbuild.transformSync` before writing to `dist/src/` (production previously served the full commented source). **Decision — `minifyIdentifiers: false`**: the classic scripts share state through the global lexical environment, so identifier mangling would rename cross-file globals and break the app; only `minifyWhitespace` + `minifySyntax` are applied — strips all comments, ~35% smaller, global names intact. **Decision — classic-script source maps dropped (not uploaded to Sentry)**: the hand-copied `src/*.js` bypass Rollup, so they never receive the Sentry plugin's debug-ID injection — feeding their maps to Sentry is not a clean config change. They ship minified without maps; identifiers are preserved so Sentry frames keep real function names (only line numbers are lost). `build.sourcemap` set to `'hidden'`; a new `stripSourceMapsPlugin` deletes every `dist/**/*.map` in `closeBundle` (after the Sentry plugin's `writeBundle` upload), so the Vite-bundle maps still upload to Sentry but no `.map` ever ships — and it is independent of `SENTRY_AUTH_TOKEN`, so maps are stripped even when Sentry is disabled. **(#6 leaked-password protection)** — pending manual step: enable it in the Supabase dashboard (Authentication → password settings); no MCP tool exposes the auth config. **Out of scope** (the other 7 review items — XSS escaping, CSP/security headers, list pagination, DB-side IDs, server-side free-tier enforcement) deferred. **Verification**: `npm run typecheck` + `npm run build` clean; dev preview — batched `.in()` requests confirmed, signed-in hydration correct; the built/minified app booted on `npm run preview` and navigated every tab with zero console errors; `find dist -name '*.map'` empty.
- 2026-05-18 — **Landscape-mode usability — pane scroll chain + touch-resizable sidebars.** Plan: `~/.claude/plans/optimize-for-mobile-streamed-bonbon.md`. Scoped by the user to making the existing desktop layout *usable on a phone/tablet in landscape* with no visual redesign — two behaviour-only fixes, both inert when the viewport is tall/wide enough. **(1) Flex scroll chain** — `body` is a bounded `height:100vh` flex column and each pane nests an `overflow:auto` scroll container (`.sidebar-scroll` / `.main-scroll` / `.cl-scroll`), but no layout element set `min-height:0`. A flex item defaults to `min-height:auto`, so on a short landscape viewport the scroll container expanded to content height instead of staying bounded — its `overflow:auto` never engaged and the parent's `overflow:hidden` simply clipped the bottom, leaving content unreachable. Added one consolidated `styles.css` rule setting `min-height:0; min-width:0` on the whole pane chain — `.app-body`, `.section-panel`, `.sidebar`, `.sidebar-scroll`, `.main-content`, `.main-scroll`, `.cl-left`, `.cl-right`, `.cl-scroll`, `.sched-sidebar`, `.sched-sidebar-body`, plus the inline-styled `flex:1` wrappers `#cab-view-builder` / `#cb-sidebar-builder` / `#cb-sidebar-library` / `#schedule-main` (their inline `style` never declares `min-height`, so the rule applies without a specificity conflict). Invisible when content fits; only turns a clipped pane into a scrollable one. One related fix surfaced in verification — the Cabinet Builder's empty-state client picker renders into `#cb-context`, a plain block *outside* any scroll container, so on a short viewport it clipped (~25px at 844×390) with no way to scroll; `#cb-sidebar-builder`'s inline `overflow:hidden` (`index.html`) was changed to `overflow-y:auto;overflow-x:hidden` so the wrapper itself scrolls when the picker overflows. In-project the `flex:1` `#cb-sidebar` scroller absorbs all overflow, so the wrapper never shows a scrollbar there. **(2) Touch-resizable sidebars** — `_initSidebarResize` (`src/ui.js`) already used Pointer Events, but the handle was blocked by CSS: `@media (max-width:768px){ .resize-handle{display:none} }` removed it on smaller landscape phones, and with no `touch-action` a touch-drag on a visible handle was hijacked as a page pan. Deleted the 768px hide rule, added `touch-action:none` to `.resize-handle`, and widened the invisible `::after` hit area under `@media (pointer:coarse)` for fingers (no visual change — the handle itself is a 4px transparent strip). The `pointermove` handler now also clamps the max width to `window.innerWidth − 140` so the sidebar can't be dragged wide enough to hide the main viewer on a narrow screen. **Out of scope** (deliberate): no layout redesign — no drill-in / pane-stacking / bottom nav, the two-pane layout is unchanged per "visually stay the same"; iOS zoom-on-focus left alone (inputs are 12–13px, fixing it means a visible bump to 16px). A landscape-usability precursor to the full PLAN.md M.1 responsive pass, which stays open. **Verification**: `npm run typecheck` clean; dev preview at a landscape phone size (844×390) — every tab's sidebar and main viewer scroll to the bottom with nothing clipped, the resize handle drags via touch and clamps; re-checked at 1440×900 for zero visual change.
- 2026-05-18 — **`100vh` → `100dvh` for mobile viewport accuracy.** Follow-up to the landscape entry above. Three `styles.css` rules used `vh` — `body { height }`, `.account-dropdown { max-height: calc(100vh - 80px) }`, `.popup-modal { max-height: 90vh }` — and `vh` on mobile is the *bar-hidden* viewport (taller than what's visible), so the app shell, the header dropdown, and popups could extend behind the browser URL bar. All three switched to `dvh` (dynamic viewport height) so they track the actually-visible area as the URL bar shows/hides. `vw` (popup widths) left alone — horizontal chrome doesn't move. Identical to `vh` on desktop and in the fixed-viewport dev preview (`body` computed 390px = viewport, no regression); the gain only shows on a real mobile browser. `dvh` is supported everywhere current (iOS Safari 15.4+, Chrome 108+), so no `vh` fallback line.
- 2026-05-18 — **Mobile/tablet opening notice — once-per-visit desktop-first advisory.** Plan: `~/.claude/plans/add-an-opening-message-staged-flute.md`. A touch-device visitor now gets a one-time dismissible notice on app load — "Best viewed on a computer": the app isn't fully mobile-optimised yet, use a desktop/laptop for the full experience, limited use is possible on a phone/tablet in landscape. **Informational, not a hard block** — the app stays usable behind it. **New `src/mobile-notice.js`** (`<script defer>` before `walkthrough.js`): `_pcIsTouchDevice()` returns `window.matchMedia('(hover: none) and (pointer: coarse)').matches` — true for phones + tablets, false for desktops/laptops including touchscreen laptops (their primary pointer is `fine`); `_pcMaybeShowMobileNotice()` no-ops on desktop or when `sessionStorage['pc_mobile_notice_seen']` is set, otherwise sets the flag and appends a self-contained inline-styled overlay (modelled on `ui.js`'s `_confirm()` — reuses the `popupFadeIn`/`popupSlideIn` keyframes + theme vars, **no new CSS**) at **z-index 10000**, above the walkthrough overlay (~9760) and below toasts (99999). Backdrop-click and the "Continue anyway" button both dismiss. **Decisions (confirmed with the user before execution)**: (1) frequency = once per *visit* — a `sessionStorage` gate, re-shows next session, not once-ever; (2) the guided walkthrough still runs — the notice layers *above* the auto-started tour and the walkthrough is revealed underneath on dismiss (not suppressed on mobile); (3) scope = phones *and* tablets. **Hook**: a single line at the top of `_wtMaybeAutoStart()` (`src/walkthrough.js`) calls `window._pcMaybeShowMobileNotice()` — the existing onboarding entry gate, run after `_showApp()` in both boot branches — with no early return, so the tour proceeds. `src/globals.d.ts` declares the two `window` globals. No schema change. **Verification**: `npm run typecheck` clean; dev preview — `_pcIsTouchDevice()` returns `false` on desktop (notice no-ops, desktop users never interrupted); with the touch media query stubbed, the overlay renders centred at 375px wide with the correct copy + amber button at z-index 10000, the button and a backdrop click both dismiss it, and a second `_pcMaybeShowMobileNotice()` call no-ops while the session flag is set but shows again once cleared.
- 2026-05-18 — **Guided walkthrough — mobile landscape support.** The onboarding spotlight tour (`src/walkthrough.js`) is desktop-first: it anchors a 336px tooltip *beside* each highlighted element and centres the welcome / pricing modals. On a phone both broke — in portrait there is no room beside a target so `_wtPlaceTooltip` clamped the tooltip on top of what it pointed at, and `.wt-center` (`overflow:hidden`, no `max-height`) overflowed a short viewport, clipping the pricing CTA's "Continue free" exit button off-screen (no arrow keys on touch → the user could be trapped). **Decisions (confirmed with the user)**: (1) scope = the walkthrough overlay only — the app's non-reflowing section panels are a separate, larger effort and stay out of scope; (2) on a phone the tour runs **landscape only**, visually identical to desktop (no bottom-sheet redesign — a landscape phone ≈844px+ is wide enough for the existing placement); (3) touch navigation = tap the **right/left half of the screen** to step forward/back, replacing the arrow keys touch devices lack. **(1) Landscape gate** — new `_wtIsPortraitBlocked()` (`(orientation:portrait) and (max-width:767px) and (pointer:coarse)` — one `matchMedia`, gated on a coarse pointer so a narrow desktop window is never affected) and `_wtDrawRotatePrompt()` (a `.wt-center`-based "Rotate your device" card — circular rotate icon, heading, body, "Skip tour" button). `_wtRender` shows the rotate prompt instead of a step when blocked, keeping `_wtCurrent` so turning the device re-renders the same step; the standalone session CTA (`_wtCtaOnly`) is exempt — a centred scrollable modal that works in portrait. New `orientationchange` listeners registered alongside the existing `resize` in `_wtRunStart`/`_wtStartCta` and removed in `_wtClose`; both drive the rotate-prompt ↔ step swap. **(2) Centred modals scroll** — `styles.css` `.wt-center` swaps `overflow:hidden` for `overflow:hidden auto` and adds `max-height:calc(100dvh - 24px)` (with a `100vh` fallback line). Universal — only engages when content exceeds the viewport height (short landscape phones), so desktop is visually unchanged; the welcome and pricing modals now cap to the viewport and scroll internally, so the CTA's exit button is always reachable. New `.wt-rotate-icon` rule (circular accent badge). **(3) Touch navigation** — `_wtOverlayClick` gains tap-to-step after the existing `data-wt-act` button handling: on a coarse pointer, a tap on the dimmed backdrop (not on the tooltip / centre card / rotate prompt) advances when `clientX > innerWidth/2`, else goes back. Desktop (fine pointer) is unaffected — arrow keys + buttons only. **(4) Device-aware copy** — the welcome step's body dropped its hard-coded "use the arrow keys on your keyboard" clause; `_wtCenterPreviewHTML` now appends a `matchMedia('(pointer: coarse)')`-chosen hint — tap-zone wording on touch, arrow-keys wording otherwise. Complements `mobile-notice.js`'s once-per-session "rotate to landscape" advisory (which advises; the walkthrough enforces landscape for the tour itself). No schema change. **Verification**: `npm run typecheck` clean; dev preview — desktop: welcome + 4-column CTA render unchanged, no clipping (`scrollHeight===clientHeight`), arrow-keys hint shown; landscape phone (844×390): welcome and CTA modals cap at `calc(100dvh-24px)`=366px and scroll internally with "Continue free" reachable after scroll, spot-step tooltip places below the spotlit nav bar fully in-viewport; portrait (390×844): `_wtDrawRotatePrompt()` renders the rotate card centred and fully in-viewport (`_wtIsPortraitBlocked()`'s portrait + max-width terms confirmed true — the `pointer:coarse` term, and so the auto-gate and tap navigation, needs a real touch device the desktop preview can't emulate).
- 2026-05-18 — **Portrait phones — horizontal-scroll the desktop layout instead of crushing it.** Follow-up to the landscape entries above. In portrait the two-pane layout didn't overflow, it *crushed*: the fixed-width sidebar held its width and `.main-content` (`flex:1; min-width:0`) squeezed to ~80px (the Cut List was worse — `.cl-left`'s 400px `min-width` overflowed the panel and `.cl-right` collapsed to 0). New `styles.css` media query `@media (orientation: portrait) and (max-width: 600px)` — phones held upright only; tablets (≥744px in portrait) and every landscape viewport are excluded — sets `.app-body { overflow-x: auto; justify-content: flex-start }` (`flex-start` so the panel's left edge stays scroll-reachable; `center` would strand it) and `.section-panel.active { min-width: 840px }`, so the full desktop two-pane layout holds at 840px and the body pans horizontally. Dashboard (single-pane) pans too — user's choice, for one consistent behaviour across tabs. 840px leaves every section a usable main pane (cabinet 400 / cutlist 360 / quote+order 480 / clients+stock 530 / schedule 620 / dashboard 840). `_initSidebarResize`'s `pointermove` (`src/ui.js`) now skips the `innerWidth − 140` viewport clamp when that same media query matches — in portrait-scroll mode a wide sidebar stays reachable by panning, so the clamp (which exists to stop the sidebar hiding the viewer on a *non-scrolling* landscape screen) is counterproductive there. **Verification**: `npm run typecheck` clean; dev preview at 390×844 portrait — all 8 tabs hold the 840px panel and `.app-body` scrolls 450px to reveal it, the resize handle drags to its full `data-max` (cutlist 680, not viewport-capped); re-checked at 844×390 landscape — panel back to 844, no horizontal scroll, `.app-body` overflow/justify untouched.
- 2026-05-18 — **Landscape chrome auto-collapse — demo banner + nav-tab bar slide away on scroll.** Follow-up to the mobile walkthrough work. On a short landscape phone (~390px tall) the `body` flex column's fixed chrome — `header` (56px) + `#demo-banner` (~36px, demo/guest only) + `.nav-tabs-wrap` (~45px) — left only ~250px for content. The demo banner + nav-tab bar now collapse when a content pane is scrolled down and restore on scroll up, handing the freed height to `.app-body` (flex:1). The top header stays put — per the user, "just the tab bar and demo bar". **CSS** (`styles.css`): `#demo-banner, .nav-tabs-wrap` get `max-height: 80px` (an inert cap — real content is ~36/45px) plus a `max-height`/`padding`/`opacity` transition; `body.chrome-collapsed #demo-banner, body.chrome-collapsed .nav-tabs-wrap` sets `max-height: 0; padding: 0; opacity: 0; overflow: hidden`. **Decision — explicit `padding: 0` in the collapsed rule**: the demo banner's `padding: 7px 16px` survived `max-height: 0` (content-box — the cap zeroes content height, not padding), leaving a 14px residue confirmed in the preview; zeroing the padding (and transitioning it) collapses it fully. The nav-tab bar has no padding, so it is a no-op there. **JS** (`src/ui.js`): new `_initChromeCollapse()` registers one capture-phase `scroll` listener on `document` — scroll events don't bubble, so the capture phase catches scrolling in any pane (`.sidebar-scroll` / `.main-scroll` / `.cl-scroll`) with a single listener; it tracks each scroller's last `scrollTop` in a `WeakMap` and toggles `body.chrome-collapsed` by direction (down → collapse; up, or `scrollTop ≤ 6` → restore), rAF-throttled. **Gating**: the handler early-returns unless `matchMedia('(pointer: coarse)').matches`, so desktop (fine pointer) is never affected — and the CSS only reacts to the class, so it is inert on desktop regardless. It also early-returns while `window._wtActive` (the guided tour spotlights the nav tabs — keep them shown), and an `orientationchange` listener resets to the expanded state. No schema change. **Verification**: `npm run typecheck` clean; dev preview at a landscape phone size (844×390) — with transitions momentarily disabled for an exact read, toggling `body.chrome-collapsed` collapses `#demo-banner` 36→0 and `.nav-tabs-wrap` 45→0, `.app-body` grows 254→334 (≈80px, ~31% more content height) and starts flush under the 56px header; collapsed-state screenshot confirmed. The scroll-direction trigger is JS-gated to coarse pointers, which the desktop preview can't emulate — verified by code; live confirmation needs a real touch device.
- 2026-05-18 — **Chrome auto-collapse — re-gated on viewport height, rAF dropped, verified end-to-end.** Corrects the entry above, which shipped unverified — on a phone the headers still didn't collapse. The collapse-on-scroll was gated on `(pointer: coarse)` and `requestAnimationFrame`-throttled; neither is exercisable in the dev preview (no coarse-pointer emulation; rAF is paused in a backgrounded tab — the same reason `_wtWaitFor` avoids it), so the scroll trigger had never actually been run. Two corrections, both also making it testable: (1) the gate moved from `(pointer: coarse)` to `matchMedia('(max-height: 540px)')` — the real problem is a *short* viewport (a landscape phone is ≤~450px tall; portrait phones are ≥568px and desktops far taller), so height is the correct signal and, unlike pointer type, is reliable and emulable; (2) the rAF throttle was dropped — the `scroll` handler now runs inline, since the browser already coalesces scroll events to one per frame and the handler's work (a `WeakMap` read, a compare, an occasional `classList.toggle`) is trivial. **Verification** (now possible): the capture-phase `document` scroll listener was first confirmed to receive element-scroll events — a synthetic `scroll` dispatched on `.main-scroll` is caught at `document` in the capture phase (programmatic `scrollTop` changes, by contrast, fire no `scroll` event in this preview, which had masked the pipeline). Then end-to-end at 844×390: a simulated content-pane scroll-down (`scrollTop` set + `scroll` dispatched) adds `body.chrome-collapsed`, a scroll-up removes it, a second scroll-down re-adds it; at 1280×900 the `max-height` gate is `false` and the same simulated scroll leaves the chrome shown. `npm run typecheck` clean.
- 2026-05-18 — **Chrome auto-collapse — collapse the top header, not the nav-tab bar.** Per the user's clarification ("hide the top bar and the demo bar, keep the tab bar"), the collapse set changed: `body.chrome-collapsed` now collapses the `<header>` (56px) + `#demo-banner`, and `.nav-tabs-wrap` is left in place so section navigation stays reachable while the user is scrolled into content. CSS-only swap in `styles.css` — the rule pair is now `header, #demo-banner` / `body.chrome-collapsed header, body.chrome-collapsed #demo-banner`; `_initChromeCollapse` is element-agnostic (it only toggles the class), so the JS is unchanged. Reclaims ~90px (header 56 + demo banner ~36) rather than ~80px. **Verification**: dev preview at 844×390 — toggling `body.chrome-collapsed` collapses `header` 56→0 and `#demo-banner` 36→0 while `.nav-tabs-wrap` holds at 45px and `.app-body` grows accordingly; `npm run typecheck` clean.
- 2026-05-18 — **Guided tour — skipped on touch devices; mobile-tour scaffolding removed.** Per the user, adapting the spotlight tour for mobile wasn't worth it — the tour is now desktop-only and simply not shown on phones/tablets (the "Best viewed on a computer" notice already steers them to a desktop). **Skip**: `_wtStart()` (`src/walkthrough.js`) early-returns when `typeof window._pcIsTouchDevice === 'function' && window._pcIsTouchDevice()` — so the full tour never runs on a touch device, whether via the `_wtMaybeAutoStart` auto-start or a Help re-trigger. Gating `_wtStart` itself (not just the auto-start) means there is no path to the tour on mobile, so the now-unhandled tour can't be reached. The standalone Pro CTA (`_wtStartCta`, e.g. the account dropdown's "Upgrade to Pro") is unaffected — it is a centred scrollable modal that works on any viewport. **Removed** (now-dead mobile-tour code from the superseded "mobile landscape support" work): `_wtIsPortraitBlocked()`, `_wtDrawRotatePrompt()` and its `.wt-rotate-icon` CSS, the `_wtRender` portrait branch, the tap-to-navigate block in `_wtOverlayClick`, the `orientationchange` listeners in `_wtRunStart`/`_wtStartCta`/`_wtClose` (back to `resize`-only), and the device-aware welcome-copy split in `_wtCenterPreviewHTML` (the welcome step's keyboard hint is inline again). **Kept**: the `.wt-center` `max-height: calc(100dvh - 24px)` + `overflow: hidden auto` modal fix — the Pro CTA can still appear on a short viewport and benefits from it. **Mobile notice** (`src/mobile-notice.js`): dropped the "— rotate to landscape for the best mobile view" clause; it now ends "You can still use it with limited functionality on a phone or tablet." **Verification**: `npm run typecheck` clean; dev preview — on desktop the tour still auto-runs and the welcome step shows the restored inline "use the arrow keys" copy; with `window._pcIsTouchDevice` stubbed to return `true`, `_wtStart({force:true})` creates no overlay and leaves `_wtActive` false; the mobile notice renders with no "landscape"/"rotate" text.
- 2026-05-19 — **Paid-ads tracking foundation — first-touch UTM attribution + Meta Pixel + Google Ads + GA4, all env-gated.** Laid before any paid-ads spend so signups carry permanent ad-campaign attribution. **(1) Attribution capture** (`src/main.js`): on every page load, if `utm_source`/`utm_medium`/`utm_campaign`/`gclid`/`fbclid` are in the URL, snapshot the params + `document.referrer` + `location.pathname+search` + `first_seen_at` into `localStorage.pc_attribution`. **First-touch wins** (industry-standard for SaaS — the campaign that first introduced a user gets credit, not whatever last-touched them before signup), so the block early-returns if a row already exists. Wrapped in `try/catch` for private-mode browsers where localStorage throws. `window._getAttribution()` reads the blob, returning `{}` for organic. **(2) Pixel inits** (`src/main.js`): three independent env gates — `VITE_GA4_ID` (G-XXXXXXXXXX) and `VITE_GOOGLE_ADS_ID` (AW-XXXXXXXXX) share a single gtag.js load (the script tag is created only when either is set; `gtag('config', ...)` runs per-ID), and `VITE_META_PIXEL_ID` separately loads fbevents.js + fires a `PageView`. With no env vars set, neither script loads and `window.gtag`/`window.fbq` stay undefined; the analytics.js helpers below feature-detect those globals and no-op. The Google Ads conversion `send_to` string (`AW-XXX/LABEL`, generated in the Google Ads dashboard) lives in a separate `VITE_GOOGLE_ADS_CONVERSION_SEND_TO` env var so the conversion action can be configured (or rotated) without touching the account ID; main.js exposes it to classic scripts as `window._GADS_CONV`. **(3) Signup wiring** (`src/app.js`): `authSubmit()`'s signup branch reads `window._getAttribution()` and passes the blob into `_sb.auth.signUp({ options: { data: { marketing_opt_in, attribution } } })`, so it lands in `auth.users.raw_user_meta_data.attribution` permanently. Queryable later via `select raw_user_meta_data->'attribution' from auth.users`. On success, calls new `_trackSignupConversion()` in analytics.js which fires Meta Pixel `CompleteRegistration`, GA4 `sign_up` (method:'email'), and Google Ads `conversion` with the `send_to` from `window._GADS_CONV`. The conversion event fires on signup form success — **not on email confirm** — because Google/Meta attribution chains rely on the ad-click → form-submit referrer chain, which the email-confirm redirect breaks. **(4) Person enrichment** (`src/analytics.js`): `_identifyUser()` now lifts `session.user.user_metadata.attribution` into PostHog person properties as `initial_utm_source`/`initial_utm_medium`/etc., so funnel reports can break down by ad campaign without a backend join. **(5) No migration** — `auth.users.raw_user_meta_data` is a JSONB column already; a SQL view to flatten it can be added later once campaigns produce data worth querying. **(6) Types** (`src/globals.d.ts`): declared `Window.gtag?`/`fbq?`/`dataLayer?`/`_fbq?`/`_getAttribution?`/`_GADS_CONV?` plus the `_track`/`_identifyUser`/`_resetAnalytics`/`_trackSignupConversion` function globals so strict-mode typecheck stays clean. **(7) `.env.example`** documents all four new vars with where to find each ID and what each enables. **Decision: first-touch over last-touch** — locked this session. Last-touch would attribute to whatever organic or direct source the user happened to arrive from on their final visit before signing up, which would undercount paid-ads credit and make campaign optimisation impossible. First-touch matches how Google Ads / Meta Ads Manager think about conversions, so cross-platform reports line up. **Decision: store in `auth.users.user_metadata` JSON, no dedicated table** — chosen for shipping speed; a `signup_attribution` table would require a migration, RLS policies, and an extra row write at signup. JSON works for any query we need short-term; if we ever want per-campaign joins from the app, a SQL view over `raw_user_meta_data->'attribution'` is a 10-minute add. **Pending user actions**: create the GA4 property, Google Ads account + conversion action, and Meta Pixel (via Meta Events Manager); fill the four `VITE_*` env vars into Cloudflare Pages env vars; verify the Pixel via Meta's Test Events tool after the first prod signup. **Verification**: `npm run typecheck` clean; with no env vars set, page load produces zero network calls to `googletagmanager.com` or `facebook.net`, `window.gtag`/`window.fbq` stay undefined, `_trackSignupConversion` no-ops without throwing. Live pixel verification deferred — needs the user's real GA4/Ads/Pixel IDs.
- 2026-05-19 — **Quote Builder gate — My Rates tab now reachable without an open quote.** The `#cb-sidebar-builder` Builder / My Rates tab bar (`#cb-tabs-wrap`) was hidden by `_cbRenderContext` whenever no quote/client was open, so rates could only be edited from inside a quote — unlike the Cabinet Library sidebar, whose equivalent tab bar is always shown. The gated empty state now shows the tab bar. **Mechanics**: the rates panel (`#cab-view-rates`) is nested inside `#cb-sidebar` while the gate empty-state lives in the sibling `#cb-context`, so a single display toggle can't pick which is "active". Added a `cbActiveTab` state global (`'builder'|'rates'`, in `cabinet-render.js` next to `cbMainView`); `switchCabTab` now sets it and, when gated (`!_cbCurrentClientId && !cbEditingQuoteId && !(cbEditingLibraryIdx>=0)`), toggles `#cb-context` (gate) against `#cb-sidebar` (rates) — the Builder tab restores the gate, My Rates reveals the rates panel. `_cbRenderContext` resets `#cb-context` display at the top of every call (the gated rates tab hides it) and syncs the tab per branch: the gate branch calls `switchCabTab(cbActiveTab)` (so a rate edit's `renderCBPanel` re-render doesn't kick you off My Rates), the library-edit branch forces `switchCabTab('builder')` (fixes a latent bug where a stale rates panel could stack over the template editor). In-project behaviour is unchanged. **Verification**: `npm run typecheck` clean; dev preview — on the gate the tab bar shows, My Rates renders the rates panel and hides the quote picker, Builder restores the picker, editing a rate stays on My Rates, and gate↔in-project↔exit transitions all render the correct header/editor/rates; no new console errors.
- 2026-05-21 — **Bugfix: `editQuoteInCB` / `editOrderInCB` now populate `_cbCurrentClientId` + `_cbCurrentClientName`.** Both functions wrote the project name into the `#cb-client` DOM input and `localStorage.pc_cq_client_name` but skipped the module-scope `_cbCurrentClientName` binding (`src/cabinet.js:155`, initial `''`). The sidebar header in `renderCBPanel` (`cabinet.js:1337-1341`) reads from that global, so opening a quote/order via "Edit in Cabinet Builder" rendered the header as `"QUO-1042 · "` — quote number followed by a dangling separator and empty name. Real users hit this whenever they drilled into a quote/order from the entity tab card and on first cabinet-builder paint; navigating in through the quote-picker (`_cbPickQuote`, `cabinet.js:1577-1583`) was already correct, which is why the bug had been masked in some session flows. Fix: both functions now mirror `_cbPickQuote`'s globals-population block — resolve the client row, set `_cbCurrentClientId`, build `_cbCurrentClientName` as `[projName, cliName].filter(Boolean).join(' · ') || refNum`, write the same value into the now-unified `pc_cq_client_name` localStorage key (replacing the prior `projName`-only write). Headless screenshot script (`brand/_src/shoot-sidebars.mjs`) reverted from its workaround call to `_cbPickQuote` back to `editQuoteInCB(1)` to act as a regression guard — re-running `node brand/_src/shoot-sidebars.mjs` now produces `brand/screenshots/sidebar-editor.png` with `← QUO-1042 · Mitchell Kitchen Renovation · Sarah Mitchell [SAVED]` in the breadcrumb, where it had previously rendered as `← QUO-1042 ·`. **Verification**: `npm run typecheck` clean (the 3 pre-existing `dashboard.js` warnings predate this change); screenshot regenerated and visually confirms the breadcrumb populates correctly via the `editQuoteInCB` entry point.
- 2026-05-22 — **Onboarding tour trimmed — removed the 8 per-section "X tab" intro steps.** Per the user ("the tour is too long, cut some steps — remove the tab steps"), dropped the dedicated nav-tab spotlight step that opened each section in `_wtSteps` (`src/walkthrough.js`): Clients tab / Quotes tab / Orders tab / Stock tab / Cabinet tab / Cut List tab / Schedule tab / Dashboard. **Kept** (per the clarifying questions): the early "Navigate between sections" overview step (step 1, spotlights `.nav-tabs`), and **all** export-related steps — Cut List's "Export & deduct stock" and Quote's "Send & convert" (user answered "none" when asked which export steps to cut). **Why it still works without the tab steps**: section navigation is driven by each step's `section` field, not by the tab step — when the first *remaining* step of a section renders, `_wtApplyContext` calls `switchSection` and `_wtGetPreClickTarget` animates the cursor to that section's nav tab, so the tab still lights up and the view still switches; the `cbView: 'results'` / `clView: 'library'` context that the removed Cabinet/Cut List tab steps carried is also present on the following step, so nothing is lost. No hardcoded step indices exist (`_wtLastIdx`/`findIndex`/relative `i±1` are all count-agnostic), so the cut needed no other edits. **Decision — `WT_VERSION` deliberately NOT bumped** (left at 3): the version gate exists to re-show the tour when there's something *new* worth seeing; removing steps shows returning users nothing new, so forcing a re-show isn't warranted. Bump only if the shortened tour should be re-displayed to people who already saw the long one. **Net**: tour 40 → 32 steps. **Verification**: `npm run typecheck` clean; dev preview (guest/demo) — `_wtStart()` opens the overlay, advancing 6× lands on "Client cards" (phase "Clients · Step 7 of 32") with the spotlight drawn and the Clients nav tab active; live `_wtSteps` confirms every section now starts on its content step and `anyTabTitle` is false; no console errors/warnings.
- 2026-05-22 — **Onboarding tour restructured into a tighter, uniform shape (follow-up to the trim above).** Per the user: two steps per nav tab, except Cabinet (four — one per sub-tab) and Cut List (three — sidebar + two sub-tabs); all top-toolbar steps moved to the end in right-to-left header order; `WT_VERSION` bumped 3→4 so users who completed the old tour are re-shown the new one once. **Final 26-step order**: Welcome, Navigate between sections, Clients (Client cards, Client profile), Quotes (Quote list, Quote editor), Orders (Order cards, Order detail), Stock (Material list, Adding materials), Cabinet (Quote Builder → Cabinet Builder → My Rates → Cabinet Library), Cut List (Sheets & pieces → Cut List Library → Cut Layout), Schedule (Order queue, Gantt calendar), Dashboard (Live overview, Quick actions), Toolbar (Account, Settings, Help, New features), Pro CTA. **Decisions**: (1) Quotes keeps Quote list + Quote editor and drops 'Send & convert' — that removes the last 'export' step the user had asked to keep the turn before, but the new 2-per-tab rule supersedes it; the Quote editor step gained `preClickCard: '.quote-card'` so it still opens a populated quote now that the card-opening 'Send & convert' step is gone. (2) Cut List drops the per-item Sheet stock / Piece list / Optimise / Visual layout / Export steps for one combined sidebar step ('Sheets & pieces', `clView: 'layout'`, target `.cl-left`) plus the two `.cl-right` sub-tabs (`#cl-tab-library`, `#cl-tab-layout`). (3) Cabinet's four steps spotlight the four sub-tab elements — `#cb-main-tab-results` (Quote Builder), `#cab-tab-builder` (Cabinet Builder), `#cab-tab-rates` (My Rates, via `subtab: 'rates'`), `#cb-main-tab-library` (Cabinet Library); a browser probe confirmed `switchCBMainView('results')` does NOT reset the active sidebar sub-tab, so the My Rates step shows its rates content. (4) Toolbar order is the visual header reversed (DOM/visual L→R is New features, Help, Settings, Account ⇒ R→L is Account, Settings, Help, New features), confirmed by measuring the four buttons' x-positions in the preview; phase relabelled 'Toolbar'; the Settings step title lost the now-misplaced word 'first'. **No hardcoded step indices** exist (`_wtLastIdx`/`findIndex`/relative nav are count-agnostic), so the re-order needed no other edits. **Verification**: `npm run typecheck` clean; dev preview reloaded, a full keyboard drive-through rendered all 26 steps in order with a spotlight on each (Cabinet + Cut List sub-tabs included) and completed into the CTA; no console warnings (an unreachable/skipped step logs one) or errors; screenshot confirmed the live counter reads 'Step 7 of 26'.
- 2026-05-22 — **Tour sub-tab steps now spotlight the content, not the tab chip (refinement of the restructure above).** Per the user, the Cabinet and Cut List sub-tab steps were retargeted from the tab buttons to the content each tab reveals: Quote Builder → `#cb-results`, Cabinet Builder → `#cb-sidebar-builder` (`#cb-cab-editor` only renders once a cabinet is open, so the always-present builder-sidebar container is used as the workspace target), My Rates → `#cab-view-rates`, Cabinet Library → `#cb-library-view`, Cut List Library → `#cl-view-library`, Cut Layout → `#results-area` (the diagram). The cursor still pre-clicks the sub-tab — the view/subtab switch resolves to the tab button inside `_wtGetPreClickTarget`, so the motion reads as click-tab-then-highlight-content. Tooltip sides flipped to suit each target (left-sidebar targets → `position: 'right'`, main-content targets → `position: 'left'`). Content-container visibility was probed per view before swapping (all sized: `#cb-results`/`#cb-library-view` 847×867, `#cb-sidebar-builder` 440×904, `#cab-view-rates` 436×1249, `#cl-view-library` 807×867, `#results-area` 807×775). **Verification**: `npm run typecheck` clean; dev preview — driving to the My Rates step spotlights the rate-inputs panel with the spotlight rect matching the content container (screenshot, header 'CABINET · STEP 13 OF 26'); no console warnings/errors.
- 2026-05-22 — **Tour Cabinet Builder / My Rates steps: open a cabinet + anchor the spotlight to the sub-tab bar (fix on the content-focus change above).** Two issues the user hit: (1) the Cabinet Builder step showed no editor because `#cb-cab-editor` only renders once a cabinet is open — fixed by giving the Quote Builder step `preClickCard: '#cb-results .quote-card'` (no-ops when a quote is already auto-loaded, which the demo does) and the Cabinet Builder step `preClickCard: '#cb-results .cb-cab-card'` so a cabinet opens and the editor renders. (2) the My Rates spotlight bled to the top of the page: `#cab-view-rates` is ~1249px tall, so `_wtResolveAndDraw`'s `scrollIntoView({block:'center'})` centred it and pushed its rect top to y≈−19 → the spotlight clamped to y0. Both the Cabinet Builder and My Rates steps now target `#cb-sidebar-builder` (stable top at the sub-tab bar `#cb-tabs-wrap`, y≈137; it is NOT inside the scrollable `#cb-sidebar`, so `scrollIntoView` can't push it negative). The box is identical for the two steps by design — they are two sub-tabs of the same sidebar — with the active sub-tab and its content (editor form vs rate inputs) swapping between them. **Verification**: `npm run typecheck` clean; dev preview — Cabinet Builder spotlight top=130 (=137−7 pad) with the editor rendered (h=2109), My Rates spotlight top=130 with the rate inputs shown and the header/nav above it dimmed (screenshot, 'CABINET · STEP 13 OF 26'); no console warnings/errors.
- 2026-05-23 — **PostHog wired into the marketing landing page (build-time key injection).** The landing (`landing.html`, served at `/`) loads none of the app bundle and is copied verbatim into `dist/` by `copyLandingPlugin` (vite.config.mjs), so it can't read `import.meta.env` the way `src/main.js` does. Added the official PostHog browser snippet to `landing.html`'s `<head>` (placed *after* the auth/Stripe redirect so pass-through visitors don't fire a stray landing pageview) using `__VITE_POSTHOG_KEY__` / `__VITE_POSTHOG_HOST__` placeholders. `copyLandingPlugin` now reads the source, `split`/`join`-replaces both placeholders from `loadEnv(mode, cwd, 'VITE_')` (`.env.local` locally, `process.env` on Cloudflare), and writes `dist/index.html` + `dist/landing.html` (previously a verbatim `copyFileSync`). A guard (`if (!k || k.charAt(0)==='_') return;`) makes the snippet a no-op in dev — the plugin only runs at build, so in dev the placeholders survive and PostHog never inits, keeping dev traffic out of prod analytics (matches the existing `VITE_POSTHOG_KEY` philosophy). Same PostHog project + same origin as `/os`, so a visitor's anonymous landing activity stitches to their identified profile after signup. Init opts: `capture_pageview` + `autocapture` on (CTA clicks feed the conversion funnel), `disable_session_recording: true` to conserve the free-tier replay quota for in-app sessions (flip to record landing replays). Config converted to function form to read `mode`; placeholder is deliberately NOT `%VITE_%` so Vite's dev html-env transform can't half-replace it (only the build plugin does). **Verification**: `npm run typecheck` + `npm run build` clean; built `dist/index.html` has 0 leftover placeholders, the real `phc_…` key + EU host injected, `posthog.init` present, and `dist/os/index.html` (the app) untouched (0 inline `phc_`, still uses the bundled SDK). Dev preview via the raw-source "Landing" server: `window.posthog` undefined, no `array.js` injected, placeholder intact, 0 console errors. Isolated active-path test (throwaway file, fake token): snippet runs → `window.posthog` defined, `array.js` loaded from `eu-assets.i.posthog.com`, `init` wired, 0 console errors.
- 2026-05-23 — **Mobile-native responsive pass (7 phases, 7 commits).** Replaced the May-18 portrait horizontal-scroll hack (`styles.css` `min-width:840px` + body x-scroll) with a true single-column "one pane at a time" layout. **Core mechanism:** `body[data-mv]` ("list" | "editor") + a two-class marker contract (`.mv-pane-editor` on each section's sidebar/editor, `.mv-pane-list` on its main/list pane) added to `index.html`, so one `@media (max-width:760px)` block stacks every heterogeneous panel (`.sidebar`/`.main-content`, `.cl-left`/`.cl-right`, inline-width Cabinet/Schedule sidebars) uniformly and shows one pane at a time — chosen over pure-CSS because the drill-in needs JS state. New `src/mobile-nav.js` exposes `_mvSet`/`_mvIsMobile` (matchMedia ≤760px)/`_mvShowEditor`/`_mvShowList` + injects a global sticky "← Back to list" bar; `data-mv` defaults to `list` synchronously to avoid a flash. Drill-in wired by appending one guarded line to the *existing* card→editor / back→list fns (`loadQuoteIntoSidebar`, `loadOrderIntoSidebar`, `editStockItem`/`_stockRevealForm`, `editClient`/`_clientsRevealForm`, `_clLoadCutlist`/`_clDoOpenLibraryCutlist`, `cbEditCabinetFromOutput`/`cbEditLibraryEntry`/`cbStartNewLibraryEntry`, the `_q*`/`_o*` new/exit/clear fns) plus a `switchSection` reset and a catch-all in `_renderProjectHeader`'s back button. Cabinet/Cut-List creation lives in the sidebar (hidden in list view), so `.mv-only` (`display:none` desktop, shown ≤760px) "+ Add" / "Open builder" buttons were added to those list panes for reachability. **Decisions:** (1) two breakpoints — `@media (max-width:760px)` for the single-column *layout*, `@media (pointer:coarse), (max-width:760px)` (OR) for *touch sizing* — the OR arm makes touch sizing render in a desktop preview window (pointer:fine) so it stays verifiable, and a narrow mouse window getting fat targets is harmless. (2) Phase 4 line-item cards are **pure CSS** — the stable column classes (`.col-qty` etc.) carry their labels via `::before`, so no markup/JS change and the `hide-hrs`/`hide-disc` toggles still work. (3) Phase 5 canvas pinch-zoom transforms the canvas via CSS (bitmap untouched → `toDataURL` PDF export unaffected); gated to phones/coarse so desktop hover/toolbar is unchanged; `touch-action` flips to `none` only while zoomed so the results list still scrolls at fit; toolbar re-renders replace the canvas (free reset). (4) Phase 6 schedule agenda is a `_mvIsMobile()` render-branch reusing the sidebar's existing `sortedEvents`, with sort/filter/hours relocated into the agenda header and the redundant sidebar hidden. **Bugs fixed in polish:** `input[type="tel"]` was missing from the base field rule (phone field rendered browser-default narrow); `.oc-info` lacked `min-width:0` (long quote/order card titles pushed the value off-screen). Mobile notice (`_pcMaybeShowMobileNotice`) reframed from the desktop-first "Best viewed on a computer" warning to a positive once-per-session "Optimised for mobile" welcome (tap-to-open / Back / pinch-zoom tips); kept the `_pcIsTouchDevice` gate + the `_wtMaybeAutoStart` call (tour still skips touch). **Gotchas confirmed:** chrome-collapse is `max-height:540px` (landscape) so it never fights the portrait layout; resize-handle is `display:none` on mobile so its inline-width persistence can't leak; `optimize()` now also `_mvShowList()`s so the freshly-rendered cut layout (in the list pane) is visible. Verified at 360/390px across all 8 sections + desktop regression at 1440px (two-pane, table, 7-col grid all intact); `npm run typecheck` + `npm run build` clean. Deferred: cut-list `.cl-table` inline inputs stay sub-16px (iOS may zoom); agenda "Sort: Manual" has no drag-reorder.
- 2026-05-23 — **Paid-ads tracking — landing-page coverage fix (the May-19 attribution foundation only fired on `/os`).** The first-touch attribution capture + GA4 + Google Ads + Meta Pixel from the 2026-05-19 work all live in `src/main.js`, which only loads on the app at `/os`. But the marketing landing page (`landing.html`) is the home page at `/` — where every paid-ad click actually lands — and it loads PostHog only, not `main.js`. Its CTAs are bare `href="/os"` that drop the query string. **Net:** an ad click arriving at `/?gclid=…&utm_source=…` never had its params captured (the snapshot code wasn't on the page), and the `_gcl_aw`/`_fbc` first-party cookies were never set, so by the time `main.js` ran on `/os` (params now stripped) every paid signup looked organic — Google Ads/Meta couldn't attribute conversions and smart bidding was blind. A regression from when the landing page was split out to `/` (the attribution code predates the split, when the app was at root). **Fix (3 edits, no schema):** (1) `landing.html` — added an inline tracking block after the PostHog snippet (placed after the auth/Stripe redirect so pass-through visitors don't fire stray events) that mirrors `main.js` exactly: same `pc_attribution` localStorage snapshot (first-touch, never overwrite once set), the GA4 + Google Ads gtag.js loader, and the Meta Pixel init + PageView. (2) `vite.config.mjs` `copyLandingPlugin` — extended the build-time placeholder injection (already used for PostHog) to swap `__VITE_GA4_ID__` / `__VITE_GOOGLE_ADS_ID__` / `__VITE_META_PIXEL_ID__` from the build env; `landing.html` is copied verbatim so `import.meta.env` isn't available inside it. (3) `landing.js` — forwards the marketing params (utm_*/gclid/fbclid) onto every `a[href^="/os"]` so the app page sees them directly too. **Why this fully fixes it:** the pixels set `_gcl_aw`/`_fbc` and the snapshot writes `pc_attribution` on `/`, all same-origin, so `/os` reads them at signup; the param-forwarding is belt-and-braces. **Decisions:** (1) duplicate the tracking inline in `landing.html` rather than share a module — the landing is copied verbatim and loads none of the app bundle by design; both copies carry `mirrors src/main.js` comments to stay in sync. (2) only the three pixel IDs are injected on the landing — the Google Ads conversion `send_to` is not, because the conversion event fires at `/os` signup, not on the landing. (3) consent-mode / GDPR banner not added here (matches existing `main.js` behaviour; deferred as a separate effort). **Env-gated identically to main.js:** with no IDs set the injected value is `''`, the inline `val()` guard returns `''`, and every block no-ops — zero network calls. **Verification:** `npm run typecheck` clean; a build with dummy IDs injects all three into `dist/index.html` (the home page) with zero leftover `__VITE_*__` placeholders; a clean build renders `val('')` ×3 (no-op) with the attribution snapshot + block structure intact. **Still pending (user-side):** create the GA4 / Google Ads / Meta accounts and set the four `VITE_*` vars in Cloudflare Pages (GA4 ID `G-HXN3J8V1YX` captured this session), then verify pixels live after the first prod signup.
- 2026-05-23 — **Mobile header refinements (follow-up to the responsive pass, from on-device testing).** Six fixes. (A) **Per-tab create "+"**: extended the shared `_renderContentHeader` (`src/ui.js`) with an optional `addOnclick` → a mobile-only `.ch-add` button that `.ch-title`'s `flex:1` pushes to the header's right; wired at every list header (`_clientsRevealForm`/`_qNewQuote`/`_oNewOrder`/`_stockRevealForm`/`cbStartNewLibraryEntry`/`_clStartNewCutlist`; Cabinet Quote-Builder "+" = `window._mvShowEditor()`). Replaces the ad-hoc `.mv-only` "+ Add"/"Open builder" buttons from the first pass. **Decision:** mobile-only (`.mv-only`) — desktop keeps its always-visible sidebar create. **Bug found+fixed:** the "+" can be tapped with a stale edit still loaded (e.g. after tab-switching), so `_clientsRevealForm`/`_stockRevealForm` now call their `cancel*` reset first (then `_mvShowEditor` overrides the reset's `_mvShowList`), and `_qNewQuote`/`_oNewOrder` clear `quoteId/orderId/lines/clientId` before `startingNew` — guarantees "+" = a fresh record. (B) **Quote Builder back arrow**: `_renderContentHeader` also got an optional `backOnclick` (left `.ch-back`, mobile-only); the Cabinet results header passes `_exitClient_cabinet()` (the same exit the desktop cabinet sidebar's back button uses) when `cbEditingQuoteId` is set — **not** `finishEditingQuote()`, which force-syncs to the DB and so popped the sign-in screen for demo/guest users (fixed after on-device report). Quote/Order *list* drill-down headers use `_renderProjectHeader` (always-visible back), so that fn got an optional `addOnclick` too (so the client-scoped list still has a "+" on mobile). (C) **Cut Layout back-to-editor**: a mobile-only `.cl-layout-mobilebar` (back arrow + cut-list name from `_clCurrentCutlistName`, set in `renderResults`) prepended to `#cl-view-layout`, `onclick=_mvShowEditor()` — the layout lives in the list pane, so this is the only route back to the parts editor. (D) **Removed the global `#mv-backbar`** (`_mvBuildBackbar` + CSS) — every editor already has a header back arrow that returns to the list (Stock/Clients `cancel*`, Quote/Order `_q/_oChangeClient`→`_clearEditor`, Cabinet/Cut-List `_renderProjectHeader`). (E) **Line-item cards** reworked from one-field-per-row to a wrapping CSS grid (`tr{display:grid;grid-template-columns:repeat(auto-fit,minmax(84px,1fr))}`, desc spans `1/-1`, numeric cells label-on-top) so columns stay aligned and wrap to a 2nd row (~3 across at 360px) — per user feedback "maintain columns, possibly over multiple rows". (F) **Order-schedule steppers were spilling**: Phase 2's generic `input{min-height:42px;font-size:16px}` was hitting `.pf-input-compact` (base 24px) while the flanking `.step-btn`s stayed 24px → misaligned + number overflow. Fixed by excluding `.pf-input-compact` from that bump and giving the stepper a uniform 36px height under a **`.sched-body`-prefixed** selector (needed to beat the base `.sched-stepper .step-btn` rule defined later in the file — same specificity otherwise loses on source order); `.sched-fields` now `flex-wrap` so each stepper has room. Verified at 390px via live-DOM checks (the demo tour keeps stealing the active section between eval and screenshot, so eval is authoritative) + screenshots of the line-item grid and cut-layout back bar; `npm run typecheck` + `npm run build` clean.
- 2026-05-23 — **Mobile round 3 (more on-device feedback).** (1) **Schedule "Orders" / "Calendar" sub-tabs**: the Phase-6 agenda had *hidden* the week grid entirely on mobile, leaving no way to see the calendar. Added a sub-tab strip (matching the cut-list/cabinet style) atop `#schedule-main`; `_renderScheduleAgenda` now *returns* its HTML (was setting innerHTML) so `renderSchedule` composes `tabs + (agenda | cal)`; new `_schedMobileView` state (localStorage `pc_sched_mobile_view`) + `_setSchedMobileView`. The 7-col grid is too wide for a phone, so the "Calendar" view wraps it in `.sched-mobile-cal { overflow-x:auto }` with `> div { min-width:600px }` (swipe horizontally; no page-level overflow). The early-return mobile branch was removed so `cal` is always built. (2) **Cut layouts default to portrait on mobile** — landscape sheets (2440×1220) render as a tiny strip on a phone, so `renderResults` sets `layoutRotate = true` when `_mvIsMobile()` and the user hasn't touched Rotate (new `_clRotateTouched` flag, set in `toggleLayoutRotate`); the `lt-rotate` button active-state now syncs in `renderResults` too. Respects a manual toggle and is desktop-inert. (3) **Cut-layout back bar now matches the other headers**: it reused `.ph-back`/`.ph-title` but those are scoped `.project-header .ph-*`, so the bar's button rendered as a default border-box. Extended those selectors to `.cl-layout-mobilebar .ph-*`, added the saw-blade `.ph-icon`, and set padding to `10px 14px` (= `.project-header`); now identical to `_renderProjectHeader` (24px borderless back + icon + 16px/800 title). Verified at 390px (Orders+Calendar tabs reachable, calendar scrolls; cut layout renders portrait with Rotate active; header matches) + desktop regression at 1440 (schedule keeps 7-col grid + sidebar, no sub-tabs; cut layout landscape; bar hidden); `npm run typecheck` + `npm run build` clean.
- 2026-05-23 — **Demo writes nudge instead of walling (guest UX).** A logged-out demo visitor who tried to save/add anything got the full-screen sign-in wall (`_requireAuth` → `_showAuth`), and the 6 create flows that reach `_enforceFreeLimit` without a `_requireAuth` guard first (`resolveClient`, `_loadCBClientById`, `cbStartNewLibraryEntry`, `cbDuplicateLibraryEntry`, `_clDuplicateLibraryCutlist`, `_clDuplicateCutlist`) got the Stripe "Upgrade to Pro" modal — both jarring for someone just exploring (the demo seeds every library at the 5-item cap, so a guest is *always* at the limit). Now all three chokepoints route to a new debounced **`_demoNudge()`** (`src/demo.js`): an `info`-style toast "You're exploring the demo — sign in to save your work." that leaves them exactly where they are. **Wiring:** `_requireAuth` (app.js) — `if (window._demoMode) { _demoNudge(); return false; }` before `_showAuth()`; `_enforceFreeLimit` (limits.js) — guest branch `if (window._demoMode && !_userId)` skips the upgrade modal *and* the `free_tier_limit_hit` analytics event (a seeded-cap hit isn't a real limit signal); `_demoBlockWrite` (demo.js) — the `_db()`-level backstop now calls `_demoNudge()` instead of `_requireAuth()`, so unguarded fire-and-forget inline edits (e.g. `updateClient`) nudge too. **Decisions:** (1) **guest-only** — `_demoNudge` early-returns when `_userId` is set, so a signed-in user mid-walkthrough (also `_demoMode === true`) sees zero behaviour change (telling them to "sign in" would be nonsense). (2) **debounced 4000ms** (> the 3500ms toast lifetime) so rapid inline edits / sliders can't spam the toast stack. (3) explicit sign-in entry points (demo banner, account menu, pricing deep-link, walkthrough CTA) still call `_showAuth()` directly — the user can always *choose* to sign in; we just don't *force* it on a blocked write. (4) signed-in free user at cap still gets the upgrade modal unchanged (monetization path intact). **Verification:** `npm run typecheck` clean; dev preview (signed-in session) — source-inspected the live bundle to confirm all three demo branches present; `_demoBlockWrite({_isSingle:true})` returns the benign `{data:null, error:{_demo:true,…}}` shape; two back-to-back backstop calls produced exactly **one** `toast info` with the right copy (debounce works); after adding the guest-only guard, the same call while signed in produced **zero** toasts.
- 2026-05-23 — **Unified list/library filter bars (Cabinet Library as the reference).** The search/filter input + Import/Export buttons differed in shape, height, and layout across every list tab (Clients/Quotes/Orders/Stock/Cut List Library) — Quotes/Orders crammed search + status tabs + sort `<select>` + Import/Export onto one wrapping line; inputs were pill-shaped 20px-radius `.order-search-input` (5px 10px) in some tabs vs rounded-rect inline-styled in others. Standardised on the Cabinet Library layout via four shared classes in `styles.css` (`.lib-filter-row` / `input.lib-filter-input` / `.lib-filter-btn` / `.lib-toggle-row`) + `.lib-sort-select`: **row 1** is the search/filter input (`flex:1`, 13px, 8px 12px pad, `var(--radius)` 10px, `surface2`) followed by `↓ Export` / `↑ Import` (and Stock's count + `PDF`) all the same height on one line; **row 2** (`.lib-toggle-row`) carries any toggles — status pills (`.ofilter-tab`) + the sort dropdown — moved off the search line per the user ask. `cabinet-render.js` (the reference) and `cutlist.js` converted to the classes (visually identical); `quotes.js`/`orders.js` split into the two rows; `clients.js` search-row reclassed + its sort moved to a toggle row; `stock.js` search-row reclassed (its category pills already were a separate line). **Two gotchas:** (a) the input class needs an `input.` prefix — the global `input[type="text"]` rule (`styles.css:701`, specificity 0,1,1) otherwise out-specifies a bare `.lib-filter-input` (0,1,0) and forced text-type inputs to 7px-10px pad / 7px radius while search-type inputs got the class values, *recreating* the inconsistency; `input.lib-filter-input` ties at 0,1,1 and wins on source order. (b) the same global rule sets `select{width:100%}`, which (combined with the inline `margin-left:auto`) made the sort `<select>` blow out to full width and wrap to its own line — `.lib-sort-select { width:auto; flex:0 0 auto }` (class 0,1,0 beats bare `select` 0,0,1) fixes it. Retired `.order-search-input` / `.order-filter-tabs` (now unused; kept `.ofilter-tab`); updated the print-hide rule and the `/`-focus selector in `auth.js` to the new class. Removed the old per-tab inline styles. **Verification:** `npm run typecheck` clean; dev preview at 1600px — Orders/Stock/Clients/Cut-List bars screenshotted (search+Import/Export one line, toggles below); computed styles confirmed identical across text-type (Clients/Cut List) and search-type (Orders/Stock) inputs (33px tall, 8px 12px pad, 10px radius, 13px font); 0 console errors. Quotes/Cabinet Library bars are empty-state in the demo data but use the same templates as Orders/the reference.
- 2026-05-23 — **Cache-busting for the classic `/src/*.js` scripts (stale-deploy fix).** User reported the live app showing an old version in a normal Safari window but the current build in a Private window, with a "hard refresh" not helping. Root cause: the 34 classic `<script defer src="/src/*.js">` tags (which hold ~all app logic) keep stable filenames across deploys, and Cloudflare Pages' default browser cache (`Cache-Control: public, max-age=14400, must-revalidate` — confirmed via `curl -I https://procabinet.app/src/cutlist.js`) let the browser reuse them for 4h without revalidating. Only the Vite-hashed `/assets/index-*.js` bundle (the `main.js` bridge) busts on deploy; the carved domain files don't. **No service worker involved** — verified absent in source, git history, and at the origin (probed `/sw.js` etc., all return Cloudflare's catch-all HTML). The misleading clue was "hard refresh doesn't fix it": the user pressed Cmd+Shift+R, which is Chrome's shortcut and a no-op in Safari (whose reload-from-origin is Cmd+Opt+R), so the cache was never actually bypassed; Private windows worked because they start with an empty cache. **Fix in `vite.config.mjs`:** (1) `versionClassicScriptsPlugin` (runs after `copyLandingPlugin`, once the app HTML is at `dist/os/index.html`) sha256-hashes each `dist/src/*.js` and appends `?v=<8hex>` to its `<script src>` so the URL changes whenever the file changes. (2) New root `_headers` file copied to `dist/_headers` by `copyHeadersPlugin`: `/assets/*` and `/src/*` → `max-age=31536000, immutable` (safe now that both URL kinds change on content change), and `/`, `/os`, `/os/` → `max-age=0, must-revalidate` (the lynchpin — HTML always revalidates so the browser always reads the latest `?v=` URLs). Self-healing on the next load after deploy (HTML was already must-revalidate, so no Cloudflare purge needed); old `/src/*.js` cache entries are simply orphaned by the new query string. **Verification:** `npm run build` clean; `dist/_headers` present; all 34 `/src` tags carry distinct `?v=` hashes (grep: stamped 34 / total 34 / unstamped 0). Deployed to production (GitHub Actions run 26345856691, green in 49s) and verified live — `/os` serves the stamped `?v=` URLs, `/src/*` returns `max-age=31536000, immutable` (was `max-age=14400`), and `/os` HTML stays `max-age=0, must-revalidate`. Landing-page `/landing.js` + `/landing.css` are still unhashed at root (same latent 4h-cache bug, lower stakes) — left as an optional follow-up.
- 2026-05-23 — **Cache-busting follow-ups: landing assets + CI Node 24.** Two follow-ups to the `/src` cache fix above. (1) **Landing page**: `versionClassicScriptsPlugin` now also stamps the unhashed root assets `/landing.js` + `/landing.css` (the only refs the marketing home carried without a hash) with `?v=<contenthash>` in both `dist/index.html` and the `dist/landing.html` back-compat alias; `_headers` gains `immutable` rules for both plus a `must-revalidate` rule for `/landing.html`. Closes the same latent 4h-stale-cache bug on the landing page that the previous entry flagged. (2) **CI Node 24**: bumped the deprecated GitHub Actions to Node-24-native majors — `actions/checkout` v4→v6, `actions/setup-node` v4→v6, `cloudflare/wrangler-action` v3→v4 (verified v4's `action.yml` is `using: node24` and keeps the `apiToken`/`accountId`/`command` inputs). With all three now native Node 24, the `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` workaround env var in `deploy.yml` is redundant and was removed. **Verification:** `npm run typecheck` + `npm run build` clean; both landing HTML files carry stamped `?v=` URLs (0 unstamped), app `/src` still 34/34, `dist/_headers` has the landing rules.
- 2026-05-24 — **Landing-page PostHog: session recording turned ON (follow-up to the wiring above).** User reported landing-page visits never appear in PostHog session replays. Root cause: the original snippet set `disable_session_recording: true` (a deliberate quota-saving choice), so the landing page captured `$pageview`/autocapture events but was never recorded — and replays of signups began at `/os` with the landing entry missing. Flipped to `disable_session_recording: false` in `landing.html`. Because the landing (`/`) and app (`/os`) share one PostHog project + origin, a visitor's replay is now one continuous session spanning landing → app. Quota note: replays count toward the free-tier 5k/mo; add a `session_recording: { sampleRate: '0.5' }` block to sample if landing traffic grows. **Events side was already fine** (the "not picking up" complaint was specifically about replays, not events): served the production `dist/` build via `vite preview` and watched real traffic — all SDK assets 200 incl. `/array/<token>/config.js` (proves the key is valid for EU), project remote-config permissive (`autocapture_opt_out:false`), and a real `$pageview` POST to `eu.i.posthog.com/i/v0/e/` → 200. The automatic initial `$pageview` only fired after forcing `document.visibilityState='visible'`: posthog-js defers the first pageview until the tab is visible and the preview browser renders hidden — a test-harness artifact, not a production bug. **Verification of the recording fix**: rebuilt (`disable_session_recording: false` present in `dist/index.html`); in the preview `posthog.sessionRecordingStarted()` → true, `posthog-recorder.js` loaded (200 — it is NOT fetched when recording is disabled), and `get_session_replay_url()` returns a valid replay link. `npm run build` clean.
- 2026-05-24 — **PDF outputs — spacing/layout cleanup pass** (all 5 jsPDF builders in `src/cutlist.js`). Added an offline QA harness `scripts/render-pdf-samples.mjs` that loads the real PDF-drawing slice from `cutlist.js` into a Node `vm` sandbox (stubbed app globals + seed data) and writes one sample per template to `pdf-samples/` (gitignored) — lets the builders be reviewed/diffed without clicking through the app. Fixes found and applied: **(1) garbled minus sign** — the Discount totals row used U+2212 (not in jsPDF's WinAnsi Helvetica), which rendered as `"` with broken letter-spacing; switched to ASCII `-` in `_buildQuotePDF` + `_buildOrderDocPDF`. **(2) logo-header collision** — `_drawBizHeader` returns the height it consumes but every caller hard-advanced `y` by a fixed amount sized for the *no-logo* header, so a logo's name/address/ABN caption overprinted the divider + client row; all callers now do `y += Math.max(<fixed>, hdrH)` and the cut-list `pageHeader()` returns its divider Y so summary content flows below it (no-logo output unchanged — `Math.max` floors to the old constant). **(3) stock table column collisions** — fixed-x columns were too cramped (Size ran into Supplier, Material/Supplier truncated mid-word); rebuilt with proper geometry, width-aware ellipsis truncation (`fit()` via `getTextWidth`), right-aligned Qty/Alert/Value, and a `—` fallback for hardware with no dimensions. **(4) quote total tie-out** — total box used `fmt0` (rounded to whole pounds) while the breakdown showed pence; switched to `fmt` (2dp) so it matches the line items and the order-doc totals; removed now-unused `fmt0`. **Polish:** added `_pdfNiceDate()` so due dates render "15 July 2026" (was raw ISO `2026-07-15`) on the work order + order docs; stripped a doubled full stop in the quote acceptance line for businesses ending in "Co.". `npm run typecheck` clean; all 10 samples re-rendered + visually verified.
- 2026-05-25 — **DXF / CNC export of the nested cut layout** (`src/cutlist.js`, new `exportLayoutDXF` + `_buildLayoutDXF`/`_dxfSheetBlock`/`_dxfRect`/`_dxfText`/`_dxfTextLeft`/`_dxfNum`/`_dxfFilenameSafe`; `DXF` button in `#layout-toolbar-top` beside PDF). Exports the optimiser's nested layout as **one** DXF for import into CAM / CNC nesting software — every unique sheet packing (`results.uniqueLayouts`) tiled left-to-right into a single drawing, each block translated by a running X offset (gap 100 mm / 4 in) and topped with an identifying caption (`Sheet i/N: <name> [WxH] xQty`). `$EXTMAX` spans the full tiled extent. **Decisions:** (1) **DXF, not G-code** — G-code is machine-specific (post-processor, tool dia, feeds/speeds, controller dialect); the correct handoff is a DXF the user drops into their own CAM, which posts for their machine. (2) **R12 (`AC1009`) + old-style closed `POLYLINE`** for maximum CAM compatibility (Vectric/Fusion/AlphaCAM/Cabinet Vision); LWPOLYLINE needs R14+ and some minimal readers reject it. (3) **Origin flip** — the packer uses a top-left/y-down canvas origin; DXF + CNC beds are bottom-left/y-up, so every y maps through `sheetH - y` (skip it and every part mirrors vertically). (4) **Cut size, not finished** — draws `placed.w/h` (edge-band trim already subtracted by `_trimmedDims` in `optimize`), i.e. what the machine actually cuts. (5) **Pro-gated** via `_enforceProFeature()` — a data export, mirroring the CSV gating (PDF stays free). (6) **One combined file** (`<name>-nested.dxf`) — chosen over one-file-per-sheet (the original cut), so a single click → single download, no zip dep and no browser multi-download prompt. Geometry on a `PARTS` layer, sheet outline on `SHEET`, labels (part name + cut dims, ASCII-only, unit-aware text height) + sheet captions on `LABELS`; `$INSUNITS` = 4 (mm) / 1 (inch) from `window.units`. **Verification** (dev preview, demo mode): `_buildLayoutDXF` over a synthetic 2-sheet (2440×1220 + 3050×1300) layout — asserted single HEADER/ENTITIES/EOF, polyline/seqend counts, tiling offset (sheet 2 outline at x=2540 = 2440+100; its part at x=2540, y=1300−500=800), `$EXTMAX` 5590×1370, both captions; per-sheet y-flip (1220−720=500, 1220−300=920) + imperial branch (`$INSUNITS` 1, 48−30=18) from the earlier per-sheet build; real `optimize()` → `exportLayoutDXF()` → single `Westside-12B-Cut-List-nested.dxf`, "Exported nested DXF (3 sheets)", zero console errors. `npm run typecheck` clean.
- 2026-05-25 — **Cut type toggle: panel saw (guillotine) vs CNC router (nested)** (`src/cutlist.js` `cutMethod` state + `packSheetNested`/`_pruneFreeRects` + `setCutMethod`/`_syncCutMethodToggle`; `optimize()` picks the packer; toggle in `#cl-action-bar` in `index.html`; `.cut-method*` styles in `styles.css`; init sync in `src/app.js`). The optimiser was guillotine-only (`packSheetRecGuillotine`, edge-to-edge cuts for a panel saw). Added a second non-guillotine packer for CNC routers, where the tool follows each part outline so parts can be placed freely and packed denser. **Decisions:** (1) **MaxRects (Best-Short-Side-Fit)** for the nested packer — free placement, good rectangle density, ~120 lines, no deps. (2) **Gap = the existing per-sheet kerf** (user choice) — in router mode the kerf value is the bit-diameter clearance; no new field. Implemented by inflating the bin by `gap` and every part's footprint by `gap` (place real `w×h` at the footprint origin), which guarantees ≥`gap` between neighbours while parts can still reach the real sheet edge. (3) **Identical output shape** — `packSheetNested` returns `{ placed:[{x,y,w,h,item,rotated}], leftover }` exactly like the guillotine packer, so the layout viewer, stats, PDF and DXF export all consume it unchanged. Grain constraints reuse the same `orientOf` rule. Default stays guillotine; choice persists to `localStorage.pc_cut_method`; switching re-runs `optimize()` when a layout is on screen. A `/** @type {{placed:any[],leftover:any[]}} */` annotation on the ternary result avoids a TS7022 circular-inference error from unioning the two packer return types. **Verification** (dev preview): direct packer test on a 28-part pool — nested gave 0 overlaps, 0 out-of-bounds, 0 gap violations and higher utilisation (91.4% vs guillotine 87.9% on sheet 1); end-to-end toggle flips `localStorage` + active button + re-optimises; real layouts rendered at 94% (nested) vs 92% (guillotine) with the expected free-vs-edge-to-edge structure. `npm run typecheck` clean.
- 2026-05-30 — **QuickBooks Online + Xero invoice integration — one-way push (code complete, DB live, 4 edge functions deployed).** Adds the ability to turn an order into a **draft** invoice in the user's connected QuickBooks/Xero account from the order card. Pro-only. Full plan: `~/.claude/plans/splendid-gliding-flute.md`. **Locked decisions (confirmed with the user up front):** (1) **one-way push only** — app → accounting; pulling paid/unpaid status back is out of scope (future). (2) **both providers** ship together — the OAuth scaffold, token store, push flow and UI are provider-agnostic, with QBO-vs-Xero specifics isolated to `supabase/functions/_shared/providers.ts`. (3) **draft** invoices (Xero `Status:"DRAFT"`; QBO created-but-not-emailed) so the user reviews before sending. (4) **OAuth tokens encrypted at rest** (AES-GCM, key `ACCOUNTING_TOKEN_KEY` in Supabase secrets) — refresh tokens grant ongoing access to a financial system. **Schema** (migration `accounting_integration`, applied via MCP; SCHEMA.md § 3.21/3.22): `accounting_connections` (one row per user+provider; `access_token_enc`/`refresh_token_enc` + `expires_at`/`realm_id`/`tenant_id`/`org_name`/`default_tax_code`/`status`) and `accounting_invoice_links` (order ↔ external invoice for the "Synced" chip + deep link + re-push dedupe). **Token columns are hidden from the client by column-level grants** — `revoke all … from authenticated` then `grant select (safe cols only)`, so a client `select=*` is denied at the column level (verified live: `permission denied` on `*`/`refresh_token_enc`, success on the safe projection); RLS is SELECT-only (owner), writes go via the service role in the functions (mirrors `subscriptions` § 3.17). **Edge functions** (Deno; shared `_shared/{cors,auth,crypto,providers,connection}.ts`): `accounting-oauth-start` (verify_jwt true → signed-`state` authorize URL), `accounting-oauth-callback` (**verify_jwt false** — provider redirect, secured by HMAC `state` + one-time code; exchanges code→tokens, captures Xero tenant / QBO realm, picks a default tax code, encrypted upsert, 302 → `/os?accounting=connected`), `accounting-push-invoice` (verify_jwt true), `accounting-disconnect` (verify_jwt true; provider revoke + row delete). Token refresh in `connection.ts` re-encrypts + **persists the rotated refresh token** (mandatory for Xero, which rotates every refresh). **Two design deviations worth recording:** (a) **invoice line amounts are computed client-side** (`_accountingBuildLines` in `src/accounting.js`) and passed in the push body — because cabinet costing (`calcCBLine`) lives in the frontend and the figures must match the Invoice PDF exactly. This is safe: it's the user's own data going into their own accounting system as a reviewable draft; the function still re-checks order ownership server-side and sources the customer/contact from the DB (never the body). (b) **Tax**: the app carries a single order-level `tax %` but QB/Xero compute tax from their own configured rate codes, so on push every line is tax-exclusive and gets the connection's auto-picked `default_tax_code` (or the provider no-tax code when order tax = 0); order-level markup/stock-markup are pushed as their own lines and the order discount as a pre-tax negative line. Line items + markup tie out to the cent; the grand total matches when the chosen provider rate equals the order's tax %. The QBO `ItemRef`/Xero `AccountCode` requirements are satisfied by resolving a default Service item / REVENUE account per push — the area most likely to need a tweak during live sandbox testing. **Frontend** (`src/accounting.js` + account-dropdown entry, order-card "Sync ▾" button + "Synced ✓" chip, `?accounting=` return handler, boot hydrate, Pro gate via `_enforceProFeature`). **Verification:** `npm run typecheck` clean; `get_advisors` clean (no new lints); 4 functions deployed ACTIVE; smoke tests green (oauth-start OPTIONS→204+CORS, unauth POST→401 on all JWT fns, callback no-params→302 `?accounting=error` confirming verify_jwt=false); column-grant token-hiding confirmed on the live DB; dev preview — Sync button renders on order cards, Pro gate fires for free users, connect popup (both provider cards) renders for Pro, missing-data handled with zero console errors. **Committed** `1b1eab9` (code; migration file staged). **Pending (credentials only):** register the Intuit + Xero developer apps (redirect URI `https://mhzneruvlfmhnsohfrdo.supabase.co/functions/v1/accounting-oauth-callback`) and set the 6 secrets (`QBO_CLIENT_ID/SECRET`, `QBO_ENVIRONMENT`, `XERO_CLIENT_ID/SECRET`, `ACCOUNTING_TOKEN_KEY`, `ACCOUNTING_STATE_SECRET`), then run the live sandbox connect → push → disconnect + token-refresh check. PLAN.md entry stays 🚧 until that live check passes.
- 2026-05-31 — **PostHog `pro_subscription_started` event + saved acquisition funnel.** Closed the last gap in the landing→app→signup→pro funnel. The Pro conversion (`handleCheckoutReturn` on `?upgrade=success`, `src/stripe.js`) only fired the ad-platform pixels (`_trackPurchaseConversion` → Meta `Subscribe`/`Purchase`, GA4 `purchase`, Google Ads) and **never a PostHog `capture`**, so PostHog could see `upgrade_clicked` (intent) but not the actual paid conversion — the funnel had no real final step. Added `_track('pro_subscription_started', { plan })` next to the pixel call. **Decision: fire on the Stripe-success return (client-side), not from the Stripe webhook** — mirrors the existing pixel placement and keeps the event on the same PostHog person/session (distinct_id persists across the Stripe round-trip, same origin), where a webhook would need server-side identify plumbing. Best-effort/no-op-safe via the existing `_track` guard. **PostHog-side (no code):** built + pinned the saved insight **"Acquisition funnel: Landing → App → Signup → Pro"** (`short_id e1bs6UMd`) to the app dashboard (683581) — 5 ordered steps, 90-day window: landing `$pageview` (`$pathname` `^/(index\.html)?$`), app `$pageview` (`$pathname` `^/os`), `user_signed_up`, `upgrade_clicked` (intent), `pro_subscription_started` (paid). The insight has `filterTestAccounts: true`. **Test-account filter:** intended to exclude `$host` = `localhost`/`127.0.0.1` (local dev pollutes counts because `.env.local` carries the live `VITE_POSTHOG_KEY`) — the PostHog MCP `project-settings-update` rejected the structured `test_account_filters` value (serialised the list to a string), so this remains a 2-click manual step in PostHog → Settings → *Internal & test accounts*; until set, "Filter test accounts" has no rules to apply. **Live funnel at writing** (tracking live since 2026-05-15): landing 425 → app 58 → signup 5 → upgrade-click 1; `pro_subscription_started` reads 0 until this deploys + the first prod purchase. No schema change. `npm run typecheck` clean; committed (not pushed).
- 2026-06-04 — **Reverted the card-upfront 14-day trial + read-only lock; restored the pre-trial 5-item freemium.** Product decision to roll back the billing model that shipped 2026-06-03/04. Billing-only files restored to their pre-trial state (commit `2662402`): `src/limits.js` (back to `isPro`/`_enforceProFeature`/`FREE_LIMITS`; no `canEdit`/`_subState`/`isGrandfathered`/trial gating), `src/stripe.js`, `src/db.js` (read-only write-block removed), `src/walkthrough.js`, `styles.css`, `supabase/functions/stripe-checkout/index.ts` (no `trial_period_days`), `landing.html`/`landing.css` (pre-trial pricing), and the export functions in `cabinet-library`/`clients`/`stock`/`cutlist`/`quotes` (re-gated to `_enforceProFeature`). Surgically removed **only** the trial bits from the 3 files that also carry kept work: `src/app.js` (dropped `_syncTrialBanner` + the `_userCreatedAt` capture), `index.html` (dropped `#trial-banner`; demo copy back to "sign in to save your work"), `src/orders.js` (export gate). **Kept** (the unrelated work landed alongside the trial): legal pages, Xero/QBO integration, PDF line-items, signup name capture, the order value-cache fix. **Done on `main` only** (= `origin/main` 0773735 + this revert). The concurrent `feat(db)` quote/order schema + `feat(photos)` line-photos module that briefly landed on `main` mid-revert were moved to `feat/quote-order-overhaul-mockups` (their real home) via cherry-pick; `main` carries neither. The day-15 `stripe-webhook` conversion event is also `feat`-only and never reached production. Backups: `backup/main-pre-revert` (`0773735`), `backup/before-trial-revert` (`9e01cac`), `backup/trial-revert-commit` (the original tangled revert). **Follow-ups:** redeploy the `stripe-checkout` edge function to drop the trial server-side; the trial code still lives on `feat/quote-order-overhaul-mockups`. `npm run typecheck` clean.
- 2026-06-05 — **Order cards gain a "Live order" link + an inline messages thread (Orders tab = post-sale hub).** Carries the live-page + customer chat onto order cards so the order is where the customer keeps communicating (and later sees schedule/production updates). **Decisions:** (1) the order's live page **reuses the originating quote's `/q` share link** — `_openLiveOrderPage` (`src/share.js`) resolves `o.quote_id`→`quotes.find().share_token`→`window.open(_shareLink(token))`, falling back to `_openSharePanel(o.quote_id)` to mint it, else an info toast for quote-less orders — i.e. one link per deal that evolves quote→order, rather than a separate `/o` page (deferred). (2) Chat is an **expandable in-card thread** (the chosen Option D), not a popup — `_toggleOrderThread`/`_orderThreadInner`/`_sendOrderMessage` (`src/clients-chat.js`) reuse the client-scoped `customer_messages` cache + `_cmTable` insert (tagging `order_id` as context), with **per-order element IDs** (`oc-thread-{body,input}-${id}`) so multiple cards expand independently; mark-read + unread-badge clears happen **in place** (no `renderOrdersMain()`, which would collapse the open thread). (3) Buttons relabelled **"Live quote" / "Live order"** (the quote-card "Share" became "Live quote"). `.oc-thread` styles in `styles.css`; type decls in `globals.d.ts`. **Out of scope (future):** rendering order production/schedule status on the live page (needs `/q` or a dedicated `/o` to show order state; `orders.share_token`/`viewed_at` already migrated) and realtime message push. **Verification:** `npm run typecheck` clean; dev preview Orders tab — all 4 sample cards show Live order + 💬 Messages, the QUO-SAMPLE-linked order shows "🔗 Live order", the thread expands inline with a composer, and an end-to-end send persisted to `customer_messages` tagged `order_id:46` then was deleted (net-zero). On `feat/quote-order-overhaul-mockups`; not pushed.
- 2026-06-06 — **Live-link sidebar tab (replaces the Share popup) + per-spec customer editability.** The quote/order sidebar editors gained **Quote builder | Live link** sub-tabs (mirroring the cabinet sidebar's `switchCabTab` pattern). **Decisions:** (1) **controls in the sidebar, preview in the main pane** — the Live-link tab body holds link + Send-live-link + permission toggles + deposit + per-line controls, while `#quote-main`/`#orders-main` render a live `<iframe src="/q.html?t=…">` of the customer page with a floating **💬 Message** launcher (`_openClientChat`, two-sided via the shared `customer_messages` thread; the business posts through the authenticated app, never the public widget). (2) **Orders reuse the originating quote's link** — `_llShareQuote('order')` resolves `order.quote_id`→quote, so the order's Live-link tab manages that quote (one link per deal). (3) **Per-spec editability** replaces the blanket "Editable" checkbox — each cabinet line shows an editable-specs dropdown (Dimensions/Finish/Material/Doors/Drawers, only those it actually has), stored in a new additive **`editable_specs jsonb`** column (migration `20260605120000`); `customer_editable` stays the master gate (= length>0). (4) **Cards**: "Live quote/order" → **✉ Send live link** (`_sendLiveLink` → pre-filled `mailto` with the client email + link + business name; mints the link first if unshared); the order card's four PDF buttons → one **PDF ▾** popup (`_orderPdfMenu`). Kept the in-card Messages thread too. New `src/livelink.js`; `_openSharePanel` now redirects to the tab; `_generateShareLink` writes `editable_specs` (graceful fallback if unmigrated) + re-renders the panel/preview instead of the popup; styled the previously-bare `.mini-toggle` pills. `quote-public.js` `specEditor` renders one input per unlocked spec; `quote-public-get` returns `editable_specs`+`finishes`+`materials`; `quote-public-update` accepts + range-validates height/depth/material/doors/drawers gated by `editable_specs`. **Prod actions (user-authorized):** migration applied + both edge functions deployed. **Verified end-to-end on prod**: business unlocks `dims` on QUO-SAMPLE line 318 → `/q` shows Edit with Width/Height/Depth only → a customer Height change persists and marks the line "price to confirm". `npm run typecheck` clean. Commits `6774d05` + `352c072` on `feat/quote-order-overhaul-mockups`; **not pushed**.
- 2026-06-07 — **Account now required (guest demo removed) + automatic 14-day no-card Pro trial → free.** Two funnel changes, landed directly on `main` as a standalone hotfix (independent of `feat/quote-order-overhaul-mockups`). **(A) Require account:** the no-session branch of `onAuthStateChange` (`src/app.js`) now calls `_showAuth()` and loads nothing, instead of entering guest demo mode — it no longer sets `window._demoMode`, skips `loadAllData`/walkthrough/restore. The `#demo-banner` (HTML in `index.html`, CSS in `styles.css`, `_syncDemoBanner()` + its call sites in `app.js`) is deleted. Landing CTAs reworded "Try the demo" → "Get started free" → `/os?signup`, and the guest branch opens the auth card in sign-up mode when `?signup` is present. **Demo seed kept** (`src/demo.js`, `db.js` routing, the per-module `window._demoMode` write-guards) — it now serves *only* the in-app walkthrough, which a signed-in user borrows via `_wtRunStart(tempDemo)`. **(B) 14-day trial:** unlike the reverted 2026-06-03/04 model (card-upfront Stripe trial → read-only), this is **no-card, client-derived, and falls back to the 5-item free tier** (no one loses read access). `src/limits.js` gains `TRIAL_DAYS=14`, `let _userCreatedAt` (assigned from `session.user.created_at` in `app.js` on sign-in, cleared on sign-out — server-set, tamper-proof), and `_trialActive()` / `_trialDaysLeft()` / `_hasProAccess()` (= `isPro() || _trialActive()`). `getLimit` / `isApproachingLimit` / `_enforceProFeature` route through `_hasProAccess`, as do the PDF-branding gates in `cutlist.js` (`_drawPdfFooter`, `_drawPdfBrandingStrip`) and the CSV-import cap in `cabinet-library.js` — so the trial is genuinely "the Pro plan". `isPro()` stays subscription-only so `renderSubscriptionSection` (`src/stripe.js`) can show a distinct "Pro Trial — N days left" row (paid → "Pro · Active", lapsed → "Free Plan"). No schema / Stripe / edge-function changes. Accounts older than 14 days compute `_trialActive()=false` → free, as before. **Verified** in dev preview: typecheck clean, no console errors; logged-out `/os` → full-screen auth gate (no demo, no banner, no data); `/os?signup` → sign-up mode; signed-in paid user → app + "Pro · Annual Active"; simulated free accounts at 0/13 days → unlimited + "Pro Trial" UI, at 20 days → 5-item cap + "Free Plan".
- 2026-06-08 — **Base types: flat price → labour hours**. The My Rates "Base" section now stores `{name, refHours}` instead of `{name, price}`, matching the Carcass/Door/Drawer type lists. Base/plinth fabrication is now priced as labour (refHours × labour rate, flat — no power-law, since a plinth's effort doesn't scale with cabinet volume), not a fixed material add-on. **Calc** (`src/cabinet-calc.js`): removed `baseType.price` from `cabinetMat`/`matCost` in both `calcCBSections` and `calcCBLine`; added `baseHrs` to `cabinetLabour` and `autoLabour` respectively. Uses an **exact-match** lookup (`?.refHours || 0`) rather than `_typeRefHours` — base is optional, so an unset/unknown base must contribute 0 hours instead of falling back to the first list item. **UI** (`src/cabinet-render.js`): Base section switched from `listItems`(price/currency) to `typeListItems`(refHours/"hrs"); editor dropdown label shows `(Xhrs)` instead of `(+£price)`. **Defaults** (`src/cabinet.js`): None 0 / Plinth 0.3 / Feet·Legs 0.4 / Castors 0.3 / Frame 0.5. **Migration**: legacy `{name, price}` rows are converted on load — both the localStorage-init path (`src/cabinet.js`) and the DB-overlay path (`src/app.js` `_applyBizInfoFromDB`) drop `price` and default `refHours` to 0 (price≠hours, so no auto-conversion — users re-enter hours). `default_base_types` jsonb column unchanged (shape is `{name, ...}`, only the second key changed). Quote prices for cabinets with a base change as a result. Typecheck clean; verified Base section renders "hrs" in dev preview.
- 2026-06-08 — **Finishing stock items measured in litres + coverage rate that prices quotes by area.** Paint/oil/lacquer items (category `Finishing`) now carry a volume and a coverage figure instead of a generic unit count. **Decisions (confirmed with the user):** (1) quote behaviour = **price by surface area** — adding a finish to a quote/order creates a stock line with `unit_price = cost-per-litre ÷ coverage` (£/m², or £/ft² imperial) and `qty = area`, so the user types the surface area and the line computes the cost; (2) imperial unit = **US gallons + ft²/gal** (`L_PER_USGAL = 3.785411784`); (3) **canonical storage is metric** — `qty` = litres, `cost` = £/L, `coverage_sqm` = m²/L — and the stock form converts to gallons/ft²-per-gal only when `window.units !== 'metric'`. **Schema** (migration `stock_finishing_coverage`, applied to `mhzneruvlfmhnsohfrdo`): widened `stock_items.qty` from `integer`→`numeric` (litres need decimals) and added `coverage_sqm numeric`. **Implementation:** new `Finishing` branch in `stockCatChanged`/`addStockItem`/`saveStockEdit`/`_stockAutosaveRun`/`editStockItem` mirroring the existing Edge-Banding special-case; a coverage input (`stock-fin-coverage`) + relabelled Volume/Low/Cost fields (`_setLabel`); `_fin*` conversion helpers + `_finQuoteLine(stockItem)` (shared by `_qAddStockLineFromLibrary`/`_oAddStockLineFromLibrary` in app.js, returns null for non-finishing/zero-coverage so they fall back to per-unit cost); a decimal-aware inline qty editor `setStockQtyFin` (the generic `setStockQty` strips non-digits); stock rows show coverage in the Dimensions col + derived £/m² (or £/ft²) in the Thickness col, with the qty pill/section totals in L or gal. `duplicateStockItem` carries `coverage_sqm`. `database.types.ts` updated (added `coverage_sqm`). `npm run typecheck` clean. On `feat/quote-order-overhaul-mockups`; **not pushed**.
- 2026-06-08 — **Orders become first-class shareable live links (their own `share_token`, not the origin quote's).** Reverses the earlier "order reuses the origin quote's `/q` link" decision (2026-06-05/06 entries) — an order with no shared quote could never produce a link, and the sample has 1 quote vs 4 orders. **Decision (confirmed):** the customer page now serves the order's **own** line items; `quote-public-get` resolves the token against `quotes` first, then `orders`, returning the identical response shape plus a `kind:'quote'|'order'` flag; `quote-messages` likewise resolves either and tags the customer insert with `order_id` vs `quote_id` (the thread is client-scoped, so the business side is unchanged). **Scope:** order live pages are **view-only** (items + total + two-way chat) — no customer spec-edit / line-selection / accept / pay (those settings simply aren't set on orders, and `quote-public.js` relabels "quote"→"order" via `kind` + drops the accept CTA in `rail()`). **No schema work:** `orders.share_token`/`share_settings`/`viewed_at` + the unique index already existed (migration `20260604120000`), and `order_lines` already carries the customer flags. **Frontend:** `livelink.js` made entity-generic (`_llShareQuote('order')` now returns the order itself; new `_orderLinkPanel`; `_llEnsureLines`/`_sendLiveLink`/`_generateShareLink` take `kind`); `share.js` gains `_generateOrderShareLink` (mints token + `share_settings` on the order, no per-line snapshot) and `_openLiveOrderPage` now opens the order's own token. **Out of scope (follow-up):** deposit/card payment on order pages (needs `quote-pay` token support), customer spec-edit/selection on orders, order production status on the live page. **Deployed 2026-06-09**: `quote-public-get` v6 + `quote-messages` v4 (both `verify_jwt=false`). Verified end-to-end on prod: order 46 (ORD-SAMPLE, 3 lines) minted its own token; the deployed `quote-public-get` returns `kind:'order'`, `number:'ORD-SAMPLE'`, and the 3 `order_lines` (Base 600, Wall 600, Install & fit-off); `/q?t=…` renders **"Your order · ORD-SAMPLE"** with relabelled rail, no accept/pay CTA, and the floating Message launcher. Quote regression-check passed (QUO-SAMPLE still renders as a quote). `npm run typecheck` clean. On `feat/quote-order-overhaul-mockups`; **not pushed**.
- 2026-06-09 — **Conversion funnel P0 fixes (F.1–F.4)** from the full-funnel review (PLAN.md "Conversion funnel" sections). **F.1** landing `?plan=` deep-link now survives the signup → email-confirm round trip: stashed in localStorage (`pc_pending_plan`, 48 h TTL) by the app.js init IIFE, consumed inside the signed-in auth branch — which also skips the walkthrough auto-start on that load (page is redirecting to Checkout; the tour shows on the return load). **F.2** trial-ending banner (`_renderTrialBanner` in stripe.js, hooked from `loadSubscription`): slim dismissible bar inserted after `<header>` for the last 3 trial days, re-shown once per remaining day (`pc_trial_banner_day`), "See plans" → `_wtStartCta()`; PostHog `trial_banner_shown` / `trial_banner_upgrade_clicked`. **F.3** skipping the auto-run first tour now hands off to the standalone plan-picker CTA (skippers previously never saw pricing — only completers of the 26-step tour did); guarded by new `_wtAutoRun` flag (set via `_wtStart({auto:true})` from `_wtMaybeAutoStart` only) so Help re-triggers, Pro users, the CTA-only overlay (Escape) and skips already on the pricing step don't fire it. **F.4** tour completion no longer sets `pc_hide_guide`; dashboard Getting Started card rewritten — stale "clients & projects" copy (Projects entity removed 2026-05-13) replaced with aha-path steps Set rates → Add stock → Build a cabinet → First quote, with deep-links (`switchCabTab('rates')`). `npm run typecheck` clean.
- 2026-06-09 — **Live-link workflow review — clarity & trust pass (business + customer)**. Audited the full quote→share→accept→pay→order flow (3 review agents) and fixed the verified issues. **Security (deployed):** `quote-public-get` was returning the maker's cost inputs (`unit_price`, `labour_hours`) and sensitive `business_info.abn` / `bank_details` to any unauthenticated visitor with a share link — none of which the customer page renders. Removed them from the line + business selects. `quote-pay` now rejects payment on an expired link (parity with `-get`/`-update`). `quote-public-update` accept rejects a malformed snapshot (`totals.total` must be numeric). `quote-messages` rate-limits customer posts (≤12/min per client). `quote-pay-webhook` order creation now catches the unique-index 23505 race and reuses the existing order (idempotent under concurrent Stripe deliveries); new partial unique index `orders_quote_id_unique`. All 5 edge functions redeployed (`--no-verify-jwt`). **Customer page (`q.html` / `quote-public.js`):** header now shows phone + email (more contact = more trust) + logo `alt`; "Secure & encrypted" with a tooltip; a `friendlyError()` maps raw edge-function codes to calm sentences everywhere (toggle/edit/accept/pay/boot); loading spinner + 8s slow-load reassurance; an order-link banner ("confirmed — no action needed"); CTA gained a "what this does / deposit + balance" subline and clearer button copy; pay sheet shows the deposit %/balance breakdown before charging; accepted & paid states gained a "What happens next" panel (receipt to email, balance due, maker contact); VAT line states the rate or "no VAT"; a11y (`:focus-visible`, switch `aria-label`, 16px spec inputs to stop iOS zoom, chip contrast, chat-launcher repositioned on mobile). Confirmed the page consumes only customer-safe fields, so the column removals are safe. **Business panel (`livelink.js` / `share.js`):** a live status row (Not shared → Live → Viewed → Accepted → Paid, via `_fmtLLDate`); payment toggle is Stripe-aware — if Connect can't take charges it shows a "Set up payments →" guard (`_llStripeReady` reads `_connectStatus`) instead of a toggle that would error for the customer; platform fee made explicit ("ProCabinet fee 0.7% (capped)"); deposit + customer-permission toggles relabeled in plain language; empty-quote guard (panel blocks sharing + `_llEnterLive`/`_generateShareLink` won't mint a blank link); order panel clarified as view-only + its own token (header comment corrected); send-link email number fallback (`#id`). Verified in dev preview: customer page boots clean (no token → friendly state), business panel shows the status row + Stripe guard + relabeled toggles, live preview iframe renders phone/VAT/deposit-breakdown correctly; `npm run typecheck` clean. *Deliberately skipped from the agents' lists:* removing live-link auto-create (the zero-friction design is intentional; added an empty-quote guard instead), and showing the platform fee to the customer (it's deducted from the maker, not added to the customer's charge — surfacing it would mislead).
- 2026-06-09 — **Quote/Order cards sync with live-link status (+ Supabase realtime)**. The live-link edge functions already write a richer lifecycle than the app understood — `quote-public-get` sets `viewed_at` + status `sent`→`viewed`, `quote-public-update` sets `accepted_at` + `accepted`, `quote-pay-webhook` sets `deposit_paid`/`paid` and creates the order at `confirmed` — but the quote pipeline only knew `draft/sent/approved`, so `QUOTE_STATUSES.indexOf` returned −1 and every customer-driven status silently rendered as "Draft". **Fix**: single `QUOTE_STATUS_META` map + `_quoteStatusMeta(s)` helper in `src/quotes.js` (label/badge/stage; `viewed`→stage 1 under Sent, `accepted`/`approved`/`deposit_paid`/`paid`→stage 2 under Approved). Replaced the duplicated 3-value status ternary at all 6 render sites (quotes card + sidebar editor + summary counts + filter predicate, `clients.js` client-detail list, `cabinet-render.js` builder list) with the helper; counts/tabs/filter now bucket by `stage`. Quote cards gained a live-link activity stamp in `.qc-meta` ("👁 Viewed 5 Jun" / "✓ Accepted 6 Jun" via `_fmtLLDate`, styled `.qc-live`). Order cards (`src/orders.js`) gained a live-link chip next to the status badge — "Link live" when `share_token` set, "Deposit paid"/"Paid" derived from the linked quote (`orders.quote_id`→`quotes`); **no "viewed"** for orders (payment is quote-only) and the 5-step production pipeline is untouched. **Realtime**: new `_subscribeLiveStatus()`/`_applyRealtimeRow()`/`_unsubscribeLiveStatus()` in `src/app.js` open one `_sb.channel('rt-cards')` subscribing to `postgres_changes` on `quotes` + `orders` filtered by `user_id`; the handler merges the changed row in place (preserving locally-attached `_lines`/notes) and re-renders both card lists + `_oBadge()`, so a customer viewing/accepting/paying flips the card with no reload. Subscribed after the bootstrap load, torn down on sign-out / `SIGNED_OUT`. Migration `enable_realtime_quotes_orders` adds both tables to `supabase_realtime` (RLS already scopes delivery per-user). `window._rtChannel` declared in `globals.d.ts`. **Verification**: `npm run typecheck` clean; in dev preview, fed simulated `postgres_changes` payloads through the real `_applyRealtimeRow`+render path (production-DB writes are blocked by the auto-mode classifier) and confirmed badge/pipeline/stamp for every status, plus order chips (Link live / Deposit paid / Paid) — all correct; `_quoteStatusMeta` validated across all status values.
- 2026-06-09 — **Sign-up-first auth screen + clearer funnel copy (follow-up to the 2026-06-07 funnel change).** Two messaging problems: (1) the CTA "Start 14 days of Pro, free" conflated the two "frees" (14-day Pro trial vs the free-forever tier), and (2) landing on a bare sign-in form felt abrupt. **Fixes:** **(A) Sign-up is now the default auth view** — `_authMode` initialises to `'signup'` (was `'signin'`) and the `#auth-screen` markup in `index.html` ships in sign-up state (heading "Create your account", "Create Account" button, name + marketing fields visible, toggle "Already have an account? Sign In"). So *every* logged-out entry (direct `/os`, post-sign-out, bookmarks — not just landing links) opens on account creation; returning users are one click from sign-in. The old `?signup`-param toggle in the guest branch of `onAuthStateChange` is removed (the default supersedes it; landing CTAs still link `/os?signup`, now a no-op hint). **(B) Reassurance bar** — new `#auth-reassure` ("✓ 14 days of Pro included · no card · no commitment", `.auth-reassure` green pill in `styles.css`), shown in sign-up mode only (`toggleAuthMode` hides it in sign-in), restating the CTA's promise at the moment of commitment. Used **"no commitment"** not "cancel anytime" — a no-card auto-trial has no subscription to cancel. **(C) CTA copy** — both landing amber CTAs "Start 14 days of Pro, free" → **"Create your free account"** (names the action so the click matches the form it lands on); hero micro → **"Free forever. Includes 14 days of Pro, no card needed."** (leads with the free-forever floor, Pro as a head start). Pricing section already carried the trial framing — unchanged. An annotated messaging-map mockup was reviewed with the user before building. `npm run typecheck` clean; verified in dev preview (logged-out `/os` → sign-up default + reassurance bar; Sign In toggle reverts heading/button and hides name fields + bar; CTAs/micro updated; no console errors). **Pushed to `main` `c61cfb8`.** Note: `main` advanced concurrently via a second session (`6a77c75`/`f8aec4e`/`0672713` — quote chat, DB perf, /q fix) already on origin; this push only fast-forwarded the one commit.
- 2026-06-09 — **Supabase Disk IO Budget warning — diagnosed as Nano-tier noise; added the 2 missing FK indexes on `customer_messages`.** Email from Supabase flagged `mhzneruvlfmhnsohfrdo`'s disk IO budget draining. Investigation (via Supabase MCP `pg_stat_statements` / `pg_stat_database` / `pg_statio_user_tables`): DB is 15 MB, **cluster cache-hit ratio = 100.00%** (1,628 disk reads vs 95M cache hits over 73 days), no app-side polling loop, the realtime publication has no public tables (so no logical-decoding WAL drag). The "300k SELECTs on stock_items/quotes/orders/clients" in pg_stat_statements are **73-day cumulative** (~4k/day each), normal for ~22 active users. Top WAL generators are auth-internal (`auth.refresh_tokens` UPDATE = 14 MB WAL over 73 days from hourly refresh × users), not anything app-controllable. **Verdict:** Nano-tier IO budget is conservative — a short write burst depleted the rolling budget; no fire. **Debounce audit:** all `oninput` per-keystroke writes (`cbUpdateField`, `_orderLineUpdate`, `_lineUpdate`) already debounce at 600ms through `_scheduleOrderLineUpsert` / `_scheduleLineUpsert` / `_cbScheduleAutosave` (`app.js:542,939`); non-debounced writes (`setStockQty`, `updateStockField`, `setOrderProdStart`, `_schedStepPriority`) fire on `onblur`/`onchange` only, one write per finished edit — **no change needed**. **What did ship:** migration `20260609010000_customer_messages_fk_indexes` (applied via MCP) adds `customer_messages_order_id_idx` + `customer_messages_quote_id_idx` — the 2 remaining unindexed-FK lints the advisor still showed after the 2026-05-18 FK-coverage pass. The 19 `unused_index` INFOs the advisor flagged were **deliberately left alone**: most are the FK covering indexes from `20260518150000` (kept to protect FK delete-cascade perf + future scaling once tables grow past seq-scan threshold) and the `*_tags_gin_idx` / `payments_*_idx` ones serve queries that haven't been heavily exercised yet. Advisor re-run confirms both `unindexed_foreign_keys` warnings cleared. **Recommended next step (not done):** upgrade compute add-on Nano → Micro (~$10/mo) when paid signups ramp — doubles the IO baseline and ends these notices.
- 2026-06-09 — **Stripe Connect customer payments — corrected the built charge model to the agreed Standard + direct-charge design (the shipped flow was losing money per transaction).** Pre-go-live test of `quote-pay`/`connect-onboard`/`quote-pay-webhook` + `src/connect.js`/`q.html` (all built + deployed during the overhaul) found two showstoppers: (1) it used **Express accounts + destination charges**, so per Stripe's docs the *platform* pays the ~2.9% processing fee while collecting only the application fee → **net loss on every card payment**, and destination charges carry cross-region limits that break the UK/US/CA/AU/NZ goal; (2) the **$100 fee cap was missing** and `STRIPE_PLATFORM_FEE_BPS` defaulted to **150 (1.5%)**, not the agreed **0.7%**. **Fix (revert to the locked model — confirmed with the founder):** `connect-onboard` now creates **`type:'standard'`** accounts (maker = merchant of record, carries dispute liability, pays Stripe's fee; Account Links hosted onboarding still works for Standard); `quote-pay` creates a **direct charge** (PaymentIntent on the connected account via the `{ stripeAccount }` option, no `transfer_data`) and returns `account_id`; `_shared/stripe.ts` defaults the fee to **70 bps (0.7%)** and adds a per-currency **~$100 cap** (`FEE_CAP_MINOR`: usd 10000/gbp 8000/eur 9000/cad 13500/aud 15000/nzd 16500); `src/quote-public.js` inits Stripe.js with `{ stripeAccount: pay.account_id }` so it can confirm the connected-account PaymentIntent. Verified against Stripe docs (direct charges: **connected account pays the processing fee**; Stripe.js **must** init with the connected account). `quote-pay` **v4** + `connect-onboard` **v4** redeployed ACTIVE via MCP; smoke green (OPTIONS 204; invalid-token POST → 404 `not_found`, proving the `_shared` bundle + Stripe client + DB all load). `npm run typecheck` clean. Commit `6711b61`; `quote-public.js` pushed 2026-06-09. **Pending before go-live:** (a) the `quote-pay-webhook` Stripe endpoint must be a **Connect** webhook (direct-charge `payment_intent.succeeded` fires on the connected account, not the platform) listening for `payment_intent.succeeded`/`payment_intent.payment_failed`; (b) confirm `STRIPE_PLATFORM_FEE_BPS=70` (or unset); (c) end-to-end test in test mode (onboard a Standard account → pay a `/q` deposit with a test card → verify the `payments` row, webhook, auto-created order, and the fee on the platform balance); (d) live-mode flip with the subscription Stripe S.9. `stripe_accounts`/`payments` had **0 rows** at fix time, so no Express accounts needed migrating.
- 2026-06-09 — **Stripe Connect customer payments — taken from broken to verified end-to-end (test/sandbox); go-live checklist below.** Follows the charge-model correction above; five more fixes landed. **(1) Auth token (`112972c`):** the Connect frontend (`_connectFn`) read the bearer from `_sb.auth.getSession()`, whose storage-based token goes stale on Safari / in-app webviews, so `connect-onboard`/`connect-status` 401'd "Invalid auth token"; switched to the in-memory `_dbAuthToken()` (db.js) + `apikey` header. The **same latent bug** is in `src/stripe.js` (startCheckout / openCustomerPortal / manage-pricing) and `src/accounting.js` — spun off as a separate task (revenue-affecting: Pro checkout can 401 on Safari). **(2) Connect key decoupled (`cb84b66`):** subscriptions are **LIVE** (`sk_live`) and the Connect functions shared that key via `_shared/stripe.ts`, so payments couldn't be tested without touching live billing; Connect functions now read `STRIPE_CONNECT_SECRET_KEY` / `STRIPE_CONNECT_PUBLISHABLE_KEY` (fallback to the live keys when unset), so payments run in a test sandbox while live subs keep running (sub functions read `STRIPE_SECRET_KEY` directly and never import this file). **(3) Connect enablement:** Connect was never switched on — enabled via dashboard → **"Build a platform"** (direct payments between businesses and their own customers; NOT marketplace). A stray empty **live** connected account got created during diagnosis (the app fell back to the live key before `STRIPE_CONNECT_SECRET_KEY` was set); its `stripe_accounts` row was deleted so the sandbox retry made a fresh account. **(4) customer_price stale-£0 (`57d6bb4`, supersedes null-only backfill `a01bed2`):** `customer_price` is a share-time snapshot, so a line shared/opened while still £0 (or edited after sharing) kept a wrong 0 and the customer saw £0; `_llSyncCustomerPrices` now recomputes every line from the quote's current figures on each Live-link-tab open (price-only — never touches `share_settings`), writing only changed lines. **(5) Perf (`1ae213b`):** `_generateShareLink` + `_llSyncCustomerPrices` wrote per-line `customer_price` sequentially (laggy on multi-line quotes) → now `Promise.all`. **(6) Webhook order-number:** `createOrderFromQuote` emitted `0001` (`Number('ORD-0042')`=NaN); now parses digits → max → `ORD-####`, matching `_nextOrderNumber`. Also fixed stale "deleted from project" delete-confirm copy (`de83d48`). **Verified end-to-end on prod (test/sandbox):** onboarded a Standard account (`acct_1TgQ9H…`, GB/gbp, charges_enabled) → paid a 40% deposit on `/q` (£692.39) with test card 4242 → `payments` row `succeeded`, **application_fee £4.85 (0.7%, under the £80 cap)**, `quote-pay-webhook` → 200, quote 77 → `deposit_paid`, order 57 auto-created (confirmed, £1,730.98). Agreed economics confirmed. **GO-LIVE checklist (payments still on sandbox keys):** (a) set `STRIPE_CONNECT_SECRET_KEY` / `STRIPE_CONNECT_PUBLISHABLE_KEY` to the **live** keys, or unset both to fall back to the live `STRIPE_SECRET_KEY` / `STRIPE_PUBLISHABLE_KEY`; (b) create a **live** Connect webhook (Connected accounts → `payment_intent.succeeded` / `payment_intent.payment_failed` → `…/functions/v1/quote-pay-webhook`) → `STRIPE_CONNECT_WEBHOOK_SECRET`; (c) Connect already enabled on the live account; (d) makers onboard real (live) Standard accounts. Frontend commits pushed; webhook order-number fix deployed via CLI.
- 2026-06-09 — **Live link + cabinet quote UX/UI review (pass 2) — spec-change requests persist & notify, pricing made consistent across all surfaces, customer-page polish.** Second full review of both workflows after the morning's clarity/trust pass; 14 fixes. **(1) Spec-change workflow (the big one):** a customer's "Edit" on the live page used to be client-side-only — the "Price to confirm" chip vanished on reload, `quote-pay` happily charged the STALE price (its line-sum skipped null prices silently), and the business was never told a change was requested. Now `quote-public-update` 'edit' **nulls `customer_price`** (the page derives pending state from null on boot, so it survives reloads) and **auto-inserts a `customer_messages` row** ("Requested a change to "X" — Width: 600 → 800…") so the maker gets the existing unread badge + an audit trail; `quote-pay` returns **`price_pending` (409)** when any included line is unpriced; the customer CTA disables with an explanatory hint while pending. On the business side, the Live-link tab now **force-refreshes `quote_lines`** on entry (the boot cache went stale on customer edits — quote_lines isn't in the realtime publication), `_llSyncCustomerPrices` reprices the pending lines at current rates (this IS the confirmation; only customer-editable lines count — a line merely added post-share reprices without fanfare) and the panel shows an **amber banner** naming the changed lines. Both functions **redeployed v13** via MCP; smoke green (OPTIONS 204 / bogus token 404). **(2) Pricing consistency:** the quote/order **editor totals omitted the stored `markup`** while the card (`quoteTotal`), live page (`_shareLineCustomerPrice`), PDFs, and order `value` all apply it — a Builder-created quote (markup seeded from cbSettings, default 20) showed a lower figure in the editor than everywhere else. Editors now render a read-only "Markup (N%)" row (tooltip says where it comes from) and apply it before tax; the **order editor also dropped stock materials from its subtotal entirely** (`sub = materials+labour+stockMarkupAmt` — missing `stockMat`) — fixed to match the quote side. `renderCBResults` now uses the **open quote/order's stored markup/tax/discount** (falls back to cbSettings only for a standalone builder session, labelled "(est.)"), adds a Discount row, and per-cabinet card totals use the same rates — parity-tested: card = editor = customer page to the penny on a markup+stock+discount sample. **(3) Customer page:** include/exclude switch knob was **visually backwards** (excluded showed knob in the conventional "on" position) — `[aria-pressed="true"]` now slides right; **accepted quotes render read-only** (toggles/Edit chips hidden instead of erroring "already accepted"); new **"Your discount (N%) — you save £X"** rows (discount was invisibly baked into line prices; subtotal now reads pre-discount and reconstructs via `subtotal/(1−d)`); **richer cabinet spec line** (dims · material · finish · doors · drawers — was dims+finish only); **styled `.qp-toast` replaces every native `alert()`**; **confirmation sheet before the non-payment accept** (irreversible server-side; the pay flow keeps the payment sheet as its natural confirm); spec editor gets **min–max range hints** + `*_out_of_range` / `*_not_allowed` errors map to friendly copy; "Edit ▾" chip is now a real `<button>` (it had a focus-visible style but was an unfocusable span). **(4) Business panel:** `_llAutoSave`/`_llOnSaved` now take an **explicit kind** — they inferred it from leftover `_llTab` state, so opening an order's Live tab then a quote's saved the quote panel's settings **onto the order**; stale "set the options below, then **Generate**" copy replaced (link auto-creates); preview empty-state copy updated; settings saves now reload **just the preview iframe** instead of rebuilding the preview DOM (flash). Files: `supabase/functions/quote-public-update` + `quote-pay` (deployed), `src/quote-public.js`, `q.html`, `src/livelink.js`, `src/share.js`, `src/app.js` (`_renderQuoteLineTotals`/`_renderOrderLineTotals`), `src/cabinet-render.js` (`renderCBResults`), `styles.css` (`.ll-banner`). `npm run typecheck` clean; `node --check` clean on all touched files (vite build unavailable in the review sandbox — darwin node_modules on linux). NOT pushed.
- 2026-06-10 — **Live link: bank transfers (Stripe customer_balance) + `/payment-fees` page.** New per-quote `share_settings.allow_bank_transfer` toggle (livelink.js, persisted in share.js with the take_deposit element-missing fallback so the legacy popup can't wipe it). `quote-pay` keeps `automatic_payment_methods` (so Pay-by-bank still surfaces from the maker's own Stripe dashboard) and, when the toggle is on + currency supported (gbp/usd/eur+country/jpy/mxn), additionally creates a bare Customer ON the connected account and passes `payment_method_options.customer_balance` — Stripe requires both for push bank transfers; on rejection (capability not active) it retries card-only so the customer is never blocked. Client (`quote-public.js`): `confirmPayment` now passes `return_url` (required once redirect methods exist — was a latent bug for any dashboard-enabled redirect method) and branches on the result — `display_bank_transfer_instructions` → new `bankPendingState` (re-links Stripe's `hosted_instructions_url`), `processing` → new `processingState`; `boot()` handles the redirect return (`redirect_status` param: succeeded/processing/failed, params stripped via replaceState, amount re-derived from `totals()` mirroring quote-pay's deposit-vs-full rule). Maker-facing fee transparency: new static `payment-fees.html` (privacy.html pattern; 0.7% capped table from `_shared/stripe.ts` FEE_CAP_MINOR, Stripe fees linked not hardcoded) copied in vite.config.mjs and linked from the Live-link payment row, the legacy share popup, and the Connect popup; "Accept card payment" relabelled "Accept online payment" since it now gates all methods. Webhook unchanged — bank transfers settle async via the existing `payment_intent.succeeded` path; `payment_intent.partially_funded` deliberately out of scope (fees page tells customers to send the exact amount).
- 2026-06-10 — **Growth: paid-acquisition tracking + founders push** (PLAN.md "Growth" section; Meta Ads × PostHog × Supabase funnel review found 1.29% visitor→signup, zero Meta-attributed signups despite the pixel being configured, and Stripe still test-mode). **Server-side conversions**: new `meta-capi-signup` edge function + `trg_meta_capi_signup` AFTER INSERT trigger on auth.users (pg_net, migration `meta_capi_signup_trigger`) fires CAPI `CompleteRegistration` with hashed email + fbc derived from the signup-attribution fbclid (`fb.1.<created_ms>.<fbclid>`), event_id `signup-<user_id>`; freshness-guarded (1 h) and exception-guarded so analytics can never block signup. Browser pixel passes the matching `eventID` for dedupe (`_trackSignupConversion(userId)` — app.js captures the signUp() user id). **stripe-webhook v22**: post-upsert `fireConversionSideEffects` on `checkout.session.completed` → CAPI `Subscribe`/`Purchase` (exact amount_total + currency) and PostHog `subscription_started` (distinct_id = user_id, `$set: {plan:'pro'}`) — both env-gated (`META_CAPI_ACCESS_TOKEN`/`POSTHOG_KEY`) best-effort no-ops. Decision: browser-side Meta purchase pixel REMOVED from `_trackPurchaseConversion` (the post-checkout return page is the unreliable path; one canonical server source beats dedupe risk; GA4/Google Ads stay browser-side). **Landing**: founders' offer surfaced above the fold (`.micro-founder` hero line, live seat count reusing `founderSeats()` → `#founder-hero-left`, hides when sold out). **Meta (via API, created PAUSED)**: Signups campaign `52550164607114` (OUTCOME_LEADS → OFFSITE_CONVERSIONS on COMPLETE_REGISTRATION, £15/day, winning-reel creative reused) + Founders-Retargeting shell `52550164627114` (OUTCOME_SALES, awaits custom audiences). Founders email draft + recipient list in `marketing/founders-email-2026-06-10.md`. Activation checklist (S.9 live-flip FIRST, CAPI token, audiences, flip campaigns) in PLAN.md G.5. `npm run typecheck` clean.
- 2026-06-10 — **Full-app debug sweep — three live prod DB-write bugs fixed (Sentry JAVASCRIPT-2/-5/-6) + dead-reference cleanup.** **(1) `order_lines` schema parity** (root cause of JAVASCRIPT-5, *applied to prod immediately* as migration `add_order_lines_finish_hardware_parity`): `door_finish`/`drawer_front_finish`/`drawer_box_finish`/`door_hardware`/`drawer_hardware` existed on `quote_lines` only, so `_syncCBLinesToOrder` (cabinet.js — which deletes the order's cabinet rows BEFORE the failing insert: line-loss risk) and `convertQuoteToOrder`'s `{...l}` copy 400'd with PGRST204. Decision: additive DB migration over payload-stripping — the code already treats the tables as twins and SCHEMA.md § 3.16 promises "same shape"; annotation added there. **(2)** `_qStartNewQuote` inserted legacy `subtotal`/`total` columns that don't exist on `quotes` (JAVASCRIPT-2, 4 users since 2026-05-31 — sidebar "+ quote" path broken in prod): payload slimmed to live columns. **(3)** `saveQuoteEditor`'s line flush wrote `schedule_hours: row.schedule_hours ?? null` into NOT-NULL-default-0 `quote_lines.schedule_hours` (23502, JAVASCRIPT-6) → `?? 0`; orders-side left `?? null` (order_lines.schedule_hours is nullable — intentional asymmetry, see SCHEMA.md). **(4) Unbound suggest-row handlers**: the "Start new quote QUO-X / order ORD-X" dropdown rows called nonexistent `_qNewQuoteFromInput`/`_oNewOrderFromInput` (ReferenceError, silent no-op) — implemented as wrappers reading `#pq-quote-number`/`#po-order-number` and passing the typed number via new `numOverride` param on `_qStartNewQuote`/`_oStartNewOrder` so the promised number is honoured. **(5) Stale-ID cleanup** from a defs-vs-refs audit: N-shortcut retargeted `q-client`/`o-client` → `qe-client-picker`/`oe-client-picker`, dead `panel-projects`/`pj-name` branch dropped; Escape handler `account-panel` → `account-dropdown`, dead `projects-panel` branch dropped (auth.js); dead global-kerf restore removed (app.js — kerf is per-sheet now); dead `#cl-unit-in`/`#cl-unit-mm` pill sync removed (settings.js); `_popupVal('pq-status'|'po-status')` in the two create-from-editor paths replaced with literal defaults (fields don't exist; statuses are pipeline-managed). **(6)** `src/database.types.ts` regenerated — it was several migrations stale (missing `door_type`/`door_material`/`customer_*`/`editable_specs` era columns on both line tables). Supabase advisors: `founder_seats_taken` SECURITY DEFINER warning reviewed — intentional (public landing seat counter, returns `int`, pinned search_path); leaked-password protection flagged for enabling in the dashboard; unused-index INFOs ignored (young app). `npm run typecheck` clean; build verified.
- 2026-06-10 — **Stock uncapped on the free tier.** Removed `stock` from `FREE_LIMITS` (limits.js) and both `_enforceFreeLimit('stock', …)` create guards (stock.js add + duplicate); `loadDefaultStockItems` now inserts the full default set with no cap math. Rationale: stock is the setup-investment library that feeds quotes, cut lists and orders — capping it blocked the core loop and the aha moment while contributing nothing to upgrade intent; quotes/orders/cabinet_templates/cutlists/clients stay at 5. Copy updated in lockstep: landing free card ("Unlimited stock items" + "5 saved items per other library"), FAQ visible text + FAQPage JSON-LD (kept verbatim-identical), walkthrough CTA free column, stripe.js manage popups ("5 items per library, stock unlimited"); `_openLimitHitModal` typedef/labels drop 'stock' (unreachable now). `npm run typecheck` clean.
- 2026-06-10 — **Conversion funnel P1–P3 batch** (PLAN.md "review backlog"). **Landing:** 6-question FAQ (`<details>` accordion, zero-JS) after pricing + matching FAQPage JSON-LD + footer link; "Prices in USD — checkout bills in your local currency" under the pricing header; live Founder-seats counter — `copyLandingPlugin` now also injects `__VITE_SUPABASE_URL__`/`__VITE_SUPABASE_ANON_KEY__` (publishable key) as `window.__PC_SB`, and `landing.js founderSeats()` swaps the static flag for "N of 50 left" via the public `founder_seats_taken` RPC (sold-out disables the CTA; dev no-ops); "ever **sold**" phrasing on the Founder flag/bullets (Creator Lifetime gifts exist). **Signup:** first/last-name fields removed (validation, metadata and toggle refs too — downstream tolerates nameless accounts); success copy now says the confirm link signs them in automatically. **Tour:** 26 → 10 steps (rates → builder → quote → cut list → schedule → dashboard → plans; cut Clients ×2, Quote list, Orders ×2, Stock ×2, Cabinet Library, CL Library/Layout, Schedule queue, Quick actions, Toolbar ×4); `WT_VERSION` deliberately not bumped — nothing new to teach, and re-showing to dismissers is the friction being removed. **CTA cadence:** standalone plan picker skips trial users (trial banner owns days 12–14) and runs at most once per 7 days for free users (`pc_wt_cta_last`, also stamped on tour close). **Getting Started card:** shows until the first real quote/order (stock no longer hides it) with ✓ progress ticks for stock (`stockItems`) and cabinet (`cbLibrary`). **Pricing:** annual unified at $299/yr (landing card, walkthrough CTA, stripe.js fallback, analytics conversion value — Stripe Price was already $299); launch-offer-end checklist of every hardcoded price string recorded in PLAN.md. Pending: Google OAuth (needs provider config), Founder money-back line (needs policy decision). `npm run typecheck` clean; rollup build not runnable in the work sandbox (mac-native binary) — vite.config/landing.js syntax, JSON-LD validity, placeholder injection and tag balance verified via node instead.
- 2026-06-10 — **Quote/order cards: one status language, status-wipe bug fixed, live link strictly optional.** The cards showed three overlapping status displays in two vocabularies (badge "Deposit paid" / pipeline "Approved" / meta stamp "✓ Accepted 2 Jun") and quietly fought a **critical autosave bug**: `saveQuoteEditor`/`saveOrderEditor` still read the long-removed `#pq-status`/`#po-status` selects via `_popupVal` (returns `''` for a missing element) and wrote **`status: ''` on every 600 ms autosave** — so the moment a maker touched a quote the customer had viewed/accepted/paid, the card fell back to "Draft" (and order pipelines lost their stage). That was the real source of the "statuses feel mixed up" report. **Fixes:** (1) both editors now PRESERVE the row's status — status is owned by the card pipeline + live-link events, not the editor; repaired the already-wiped prod rows via SQL (4 quotes — status inferred from `accepted_at`/`viewed_at`/`share_token` — and 5 orders → 'confirmed'). (2) **One vocabulary:** `QUOTE_STATUSES` last stage renamed Approved → **Accepted** ('approved' stays as a read alias labelled "Accepted"); `convertQuoteToOrder` and the pipeline write 'accepted'; the filter tab reads "Accepted (n)". (3) **Quote card = one status display:** the badge carries the granular label + date ("Viewed · 2 Jun", "Accepted · 2 Jun"); the duplicate `_llStamp` meta entry is gone; the pipeline's ACTIVE step shows the same granular word (Viewed/Deposit paid/Paid) so badge and pipeline can never disagree; a muted "Link live · not viewed yet" meta hint (`.qc-link-hint`) appears only when `share_token` exists and the link is unopened — makers who never share a link see a pure manual pipeline, nothing extra. (4) **Pipeline guard** (`setQuoteStatus` + `_applyQuoteStatus` + `_quoteCustomerFact`): same-stage clicks on a customer-driven status no-op (no silent viewed→sent downgrade); backward clicks from viewed/accepted/deposit_paid/paid open `_confirm` describing what the customer actually did ("The customer has paid a deposit on the live page. Move it back to Sent?") with a "payments aren't affected" note; manual-only statuses keep today's instant behaviour; forward clicks (e.g. phone-call acceptance) stay one-click. (5) **Order card separates dimensions:** the second look-alike badge (`_llChip`) is replaced by an outline `.oc-pay` pill in the META row — "✓ Paid in full" / "✓ Deposit paid · balance due" (green outline) / "Link live" (muted outline) — so production status (filled badge + pipeline, untouched) and money no longer read as two conflicting statuses. (6) **Dashboard:** Recent Quotes had its own 3-value ternary (the 2026-06-09 reconciliation missed it) — viewed/accepted/deposit_paid/paid rows showed "Draft"; now uses `_quoteStatusMeta`. Files: `src/quotes.js`, `src/orders.js`, `src/dashboard.js`, `styles.css`. `npm run typecheck` clean.
- 2026-06-10 — **Follow-up (`a3258cd`): corner badge removed from quote/order cards — the PIPELINE is the single status display** (granular word + date under the active step via `.pipe-date`); "two statuses" still felt off even with one vocabulary. Cards now show one element per dimension: pipeline (progress / production) and, on orders, the payment pill. Compact rows without pipelines (dashboard, client cards, pickers) keep their badges. Also fixed the order EDITOR pipeline clicks — `_oSetPopupStatus` wrote to the long-removed `#po-status` select and silently no-opped; it now writes through `setOrderStatus` like the card pipeline.
- 2026-06-10 — **Google sign-in (OAuth) added to the auth screen.** A "Continue with Google" button (official 4-colour G SVG, `.auth-oauth-btn`) sits above an "or" divider (`.auth-divider`) at the top of the auth card, before the email/password fields — visible in both signup and signin modes (one button serves both). New `signInWithGoogle()` (`src/app.js`, after `authSubmit`) calls `_sb.auth.signInWithOAuth({ provider: 'google', options: { redirectTo } })` with the same redirect target the email flow uses (`window.location.origin + (window._isDev ? '' : '/os')`). On success the page navigates to Google's consent screen; Google redirects back to `redirectTo`, the SDK's `detectSessionInUrl` exchanges the code, and the existing `onAuthStateChange` (SIGNED_IN) handler drives loadAllData/walkthrough/restore exactly like a password login — **no changes to the session handler were needed**. An `error` only returns synchronously if the redirect can't start (provider not configured / offline), surfaced in `#auth-msg`; a `google_signin_clicked` analytics event fires on click. **Decision/limitation:** OAuth signups can't carry the `marketing_opt_in` / first-touch `attribution` metadata that the email `signUp()` attaches — those come from Google's profile instead; acceptable since email prefs are managed in-app, but Google-originated accounts won't have paid-ads attribution stamped, and the marketing opt-in checkbox is bypassed. **Server-side config still required (dashboard, not code):** enable the Google provider in Supabase Auth → Providers with a Google Cloud OAuth client ID + secret; in Google Cloud Console set the authorized redirect URI to `https://mhzneruvlfmhnsohfrdo.supabase.co/auth/v1/callback`; ensure the prod `/os` URL + `http://localhost:3000` are in Supabase Auth → URL Configuration → Redirect URLs. Files: `index.html`, `src/app.js`, `styles.css`. `npm run typecheck` clean; dev preview confirmed the button renders correctly and the handler is wired (full OAuth round-trip needs the dashboard config above to test).
- 2026-06-10 — **Marketing opt-in recovery for OAuth signups** (follow-up to the Google sign-in entry above). The email signup form's "send me cabinetry tips" checkbox writes `user_metadata.marketing_opt_in` at `signUp()` time, but OAuth/Google signups skip that form entirely and `signInWithOAuth` can't carry `data` metadata — so those accounts landed with the flag absent and **no way to ever opt in** (there's no Settings email-preferences toggle, and building one would need an unsubscribe endpoint that doesn't exist; opt-out is already handled by Resend's per-email unsubscribe link). New **`_maybePromptMarketingOptIn(session)`** (`src/auth.js`, beside `_syncMailingList`): a one-time, opt-in-only in-app consent card ("Stay in the loop?" → "No thanks" / "Yes, send tips", `popupFadeIn`/`popupSlideIn`, no new CSS) shown only to accounts whose `marketing_opt_in` is **not** a boolean (i.e. OAuth signups + any legacy pre-checkbox account); email signups always carry true/false and are skipped. "Yes" calls `_sb.auth.updateUser({ data: { marketing_opt_in: true } })` then re-reads the session and runs `_syncMailingList`; "No"/backdrop-dismiss records false / leaves it unset (default = not subscribed, no implied consent). A per-user `localStorage['pc_mkt_asked_<uid>']` flag is set the instant it's shown, so it's truly once-per-account-per-device regardless of the answer. **Hook** (`src/app.js` `onAuthStateChange` session branch, after `_restoreAppState`): `if (!_pendingPlanNow && !window._wtActive && …) _maybePromptMarketingOptIn(session)` — gated off the first-run walkthrough (`_wtActive`) and Checkout redirects so it never stacks on the tour; for a brand-new Google user the tour runs first and the prompt surfaces on a later load instead (where the flag is still unset). **Decision:** opt-in-only rather than a full preferences toggle — keeps consent explicit and GDPR/CAN-SPAM-clean and avoids half-building an unsubscribe path. `npm run typecheck` clean; dev preview verified the card renders and both gates block correctly (already-asked → no show; existing boolean preference → no show), no console errors.
- 2026-06-10 — **Orders → cabinet line editing UX pass.** The Orders→Builder round trip had four gaps: (1) the per-row “Open in Cabinet Builder” icon ignored WHICH cabinet was clicked — user landed in the Builder and had to re-find the cabinet among the cards; (2) “Done” left the user stranded in the Cabinet tab; (3) no Discard for orders (quotes had one); (4) the editing banner only rendered when the order had ≥1 cabinet — a zero-cabinet order had no banner, so no way back at all. **Fixes:** `editOrderInCB`/`editQuoteInCB` take an optional `lineId` and pre-select that cabinet (`cbEditingLineIdx`/`cbScratchpad` set before `renderCBPanel`; new `_cbRevealSelectedCabinet()` opens the mobile editor pane + scrolls form and highlighted card into view; `switchCBMainView('results')` runs FIRST since leaving library-edit clears the selection). New `_orderLineEditCabinetRow(idx)` (app.js) threads the clicked row’s id; order cabinet rows gain the same dblclick-to-edit as quotes; new `_liDblIgnore(event)` guard on BOTH tabs’ dblclick so double-click-to-select-text inside inputs/textareas (and the ✕ cell) no longer hijacks into the Builder. `finishEditingOrder()` now returns to the order (`switchSection('orders')` + `loadOrderIntoSidebar`); banner rebuilt — always rendered (empty state too), label leads with ORD-number, buttons are “Discard changes” + “← Back to order”; new `discardOrderEdits()` restores the entry snapshot by reusing `_syncCBLinesToOrder` then returns to the order. **Latent data-risk bugs fixed while in there:** `_exitClient_cabinet`/`_cbNewClient` cleared quote-editing state but NOT `cbEditingOrderId`/`pc_cb_editing_order_id` — after exiting, standalone autosaves still routed through `_syncCBLinesToOrder` and could wipe/corrupt that order’s cabinet lines; `_loadCBLinesFromDB` now restores an interrupted order-editing session from `pc_cb_editing_order_id` (was write-only — mid-edit reload silently fell back to the standalone workspace); the quote-restore branch now filters `line_kind='cabinet'` (it loaded item/stock rows as malformed cabinets, which the next sync re-inserted as duplicate cabinet lines). Files: `src/app.js`, `src/cabinet.js`, `src/cabinet-render.js` (the order editor's row HTML lives in app.js, so orders.js itself is untouched). `npm run typecheck` + `node --check` clean; rollup build not runnable in the work sandbox (mac-native binary).
- 2026-06-11 — **Cut layout "sometimes renders tiny" fixed with a width-watching ResizeObserver**. Root cause: `drawCanvas` fits the diagram to `wrap.clientWidth` at draw time with a 200px floor, the `renderLayout` retry guard only waits ~12 frames, and nothing re-rendered on size changes — so a layout rendered while the pane was hidden (other tab active when a cutlist loads/syncs) or mid-layout stuck at the tiny floor until the next manual re-render; window resizes/rotations also kept the stale scale. Fix (`src/cutlist.js`): `_clEnsureAreaObserver()` attaches one ResizeObserver to `#results-area` (armed from `renderResults`), `renderLayout` records the width it drew for in `_clDrawnW`, and the observer re-renders when the live width deviates by >24px. The 24px dead-band exceeds scrollbar width (~15-17px) so a re-render that toggles the scrollbar can't re-trigger itself; rAF-debounced. Verified in dev preview by driving the real render path: forced a draw at the 200px floor, widened the pane, observer redrew at the correct fit and `_clDrawnW` re-anchored (no oscillation). `npm run typecheck` clean.
- 2026-06-11 — **Sentry triage → _db hardened against expired JWTs and insert races; Outlook noise filtered**. Three production Sentry issues, two real. (1) `JWT expired` 401 (PGRST303) silently dropped a `pieces` delete: the SDK's TOKEN_REFRESHED timer misses while a laptop sleeps / mobile tab freezes, so the in-memory token cache can hold a stale JWT. Fix in `src/db.js`: on any 401, `_db()` forces one `refreshSession()` (deduped across concurrent 401s via a shared in-flight promise), updates the cache, and replays the request once — headers rebuilt per attempt so the retry carries the fresh token. Safe to replay: a 401 means nothing committed. (2) `business_info` insert 409 after a "Failed to fetch": the request committed server-side, the client-side retry hit `business_info_user_id_key`. Root cause was the select-then-insert/update pattern (5 call sites: business.js ×3, migrate.js, walkthrough.js). Fix: native `upsert()` on `_DBBuilder` — POST with `Prefer: resolution=merge-duplicates` + `?on_conflict=` — and all 5 sites collapsed to single idempotent upserts. Partial payloads are safe because merge-duplicates only touches supplied columns and `name` has DB default `''` (the old "Need name (NOT NULL) — ensure baseline" insert padding was unnecessary and would have clobbered existing names if carried into upsert bodies — deliberately dropped). (3) `Object Not Found Matching Id:N, MethodName:update` — Microsoft Office/Outlook injected-script noise, added to `ignoreErrors` in main.js's Sentry init. `npm run typecheck` clean.
- 2026-06-11 — **Signup "check your email" dead ends fixed; thank-you panel replaces the missable green one-liner**. Diagnosis from auth logs + Resend delivery logs: (a) repeated signup of an already-confirmed email (`user_repeated_signup`) sends NO email by design (anti-enumeration: obfuscated user, empty `identities` array) yet the UI said "check your email" — the founder hit exactly this with adamdenney33@icloud.com (confirmed since 2026-05-17); (b) a real customer (mark@millersjoinery.co.uk) had 4 confirmation emails *delivered* by Resend but unclicked (spam suspected), retried into `over_email_send_rate_limit`, and all his links expired — `mailer_otp_exp` was only 3600s. Fixes: full `#auth-confirm-panel` (email shown, spam hint naming noreply@procabinet.app, resend w/ 60s cooldown matching `smtp_max_frequency`, go-back link); empty-identities signups flip to sign-in with "account already exists" (and no longer fire ad-conversion pixels with the obfuscated user id); `email_not_confirmed` sign-in errors route to the panel instead of a dead-end error; forgot-password flow added (resetPasswordForEmail → PASSWORD_RECOVERY popup → updateUser). Ops via management API (CLI keychain token): `mailer_otp_exp` 3600→86400; Mark's account manually confirmed (links expired, delivery proven). Commit 78d9d08; typecheck clean; states verified live in dev preview incl. a real repeated-signup round-trip.
- 2026-06-11 — **App load performance pass: boot bundle 1.24 MB → 652 KB; boot data queries start before the scripts finish**. The /os boot was a strict waterfall (module bundle → 40 classic scripts → DOMContentLoaded → INITIAL_SESSION → ~9 render-gating queries → render) with jsPDF+autotable (~450 KB) and PostHog (~350 KB + recorder) shipped eagerly despite being needed late or never. Changes: (P.1) jsPDF lazy — `window._ensureJsPDF()` dynamic-imports jspdf then jspdf-autotable on first use (warmed post-`load`+3 s); the five `_build*PDF` builders await it (all call sites fire-and-forget, so async-ifying is safe) and keep their `!window.jspdf` guards. (P.2) PostHog dynamic-imported + init after `load`; `_identifyUser` stashes `window._pendingIdentify` when it races ahead, replayed by the init; `_resetAnalytics` clears it. (P.3) gtag/fbevents script *injection* deferred to `load` — the eager stubs queue calls, so boot-window conversions (signup) still flush. (P.4) Early boot fetch: main.js reads the stored session blob (same key as `_dbAuthToken`'s fallback, skipped if expires_at < now+30 s), fires the nine render-gating REST queries immediately (`window._earlyBoot`), with one retry on TypeError network failures (stale pooled socket killed all nine with ERR_CONNECTION_CLOSED in testing); `_earlyBootOr()` (db.js) consumes each slot once, guarded on `userId === _userId`, falling back to the identical `_db()` query on miss/HTTP-error/rejection — query shapes in main.js must mirror the consumer fallbacks. (P.5) `<link rel="preconnect" %VITE_SUPABASE_URL% crossorigin>` in index.html + q.html (Vite HTML env replacement). (P.6) favicons right-sized: 512 px/137 KB PNG → 64 px (3.9 KB) + 180 px apple-touch (18.5 KB), landing.html too. Verified in dev preview: 9/9 slots consumed on a signed-in reload (data/subscription/cbSettings identical), cross-user guard leaves stale slots untouched after a user switch, dead-refresh-token boot falls back cleanly to the auth screen, stock PDF exports with autotable patched on the lazy chunk; the empty `#biz-name` input observed during testing was confirmed pre-existing via a git-stash baseline. typecheck + build clean.
- 2026-06-11 — **Walkthrough auto-start gate scoped to the account, not the browser**. Report: "walkthrough isn't triggering for new users." Diagnosis from prod data + PostHog session replay: the first-run gate read `localStorage['pc_wt_state']`, which is per-BROWSER — any browser that ever dismissed the tour (as a guest in the pre-2026-06-07 demo era, which force-showed the tour to every logged-out visitor and persisted skips to localStorage, or under another account) suppressed the auto-tour for every account created on it afterwards. The founder's own prod test (workshop@bee9.co.uk, 17:57Z) reproduced it: confirm-link landing → no tour → had to launch it via Help → User Guide (visible in the session recording's autocapture trail). Desktop signups mskalla99/carina927 (both active 06-04, guest era) fit the same pattern. Of 13 recent no-tour signups, 9 were phones (tour is desktop-only by design; mobile advisory shows instead) and 1 (roland.marshall, "Desktop Mac" in PostHog) was actually an iPad — clicked `pc-mobile-notice-ok`; iPadOS requests desktop sites so analytics device-type lies. Fix: `_wtMaybeAutoStart` ignores the localStorage entry unless `ls.user_id === _userId` (guest-era/legacy entries have no user_id → ignored; `business_info.onboarding_state` remains the per-account cross-device gate, so properly-dismissed users aren't re-shown — and affected browsers self-heal on next sign-in); `_wtPersistState` stamps `user_id` into the localStorage copy (DB copy unchanged); `_applyBizInfoFromDB` resets `window._onboardingState = {}` when the account has no business_info row, so a previous account's in-memory state can't leak across a same-tab account switch. Verified in dev preview: planted guest-era `pc_wt_state` (no user_id) + never-onboarded account → tour fires; after skip (entry now user-stamped) → reload suppresses. typecheck clean.
- 2026-06-11 — **SPEC.md body reconciled with shipped reality; § 13 re-homed as the file's final section.** §§ 1–12 had sat in unfilled template state since 2026-04-28 (placeholder author/date, unchecked boxes for shipped work, "SCHEMA.md to be drafted", Netlify hosting) while five recent log entries had been appended after the closing "How to use this spec" section — outside § 13 entirely. Fixes: boxes ticked with dates against § 13/git evidence; deviations marked explicitly instead of silently ticked (classic-script carve, not ES modules; 1,500-line file cap not met — cutlist.js 5,167/app.js 2,194/quotes.js 1,848/cabinet.js 1,825; shared-`state`-object item dropped in favour of strict types over globals; localStorage dual-write residue listed by key); constraints filled with actuals (Cloudflare Pages hosting, Vite, ~5-day actual budget); § 9 open questions all marked resolved with outcomes; § 10 phases table gains a Done column; § 13 sorted chronologically (oldest first, stable within a date) and moved to end-of-file so plain appends land inside it. Verified before ticking: localStorage.setItem grep, index.html script/style audit, src/ line counts, live cross-table SQL aggregate on prod.
- 2026-06-11 — **PLAN.md reconciled with shipped reality; S.9 (Stripe subscriptions live-mode flip) confirmed DONE via secret digests.** PLAN.md contradicted itself on S.9: the Connect go-live entry called `STRIPE_SECRET_KEY` live while the Status Snapshot + Growth G.5 still listed the flip as blocking ("nothing real can be sold"). Settled by `supabase secrets list` digest comparison: `STRIPE_SECRET_KEY` == the verified-live `STRIPE_CONNECT_SECRET_KEY`, and sha256 of the S.2 *test* Price IDs ≠ the `STRIPE_PRICE_MONTHLY`/`_ANNUAL` digests (a `STRIPE_PRICE_FOUNDER` secret also exists) → live key + replaced prices. Residuals kept under S.9: confirm Cloudflare `VITE_STRIPE_PUBLISHABLE_KEY` is pk_live (low stakes — redirect Checkout doesn't use it), one real live-Checkout smoke, decide on the 3 test-mode `subscriptions` rows. Other reconciliations, each verified against prod DB / repo before ticking: four May "🚧 In Progress" sections closed (number-format unification; mockup-J port; sidebar redesign — `orders.hours_allocated` confirmed in information_schema; multi-cutlist — `cutlists` live with 30 rows); O.3 auth email marked fully live (Resend SMTP since 2026-05-17; deliverability proven via Resend logs 2026-06-11); P.3 Sentry + C.5 PostHog accounts marked live (both in routine prod triage); O.1 closed as the Getting Started card; C.1 corrected (landing at `/`, app at `/os` — the "app stays at root" resolution was superseded ~2026-05-19/23); C.2 landing shipped; C.3 demo film rendered (`demo-video/`), embed still open; M.1–M.3 + M.5 closed by the 2026-05-23 mobile-native pass (M.4 PWA manifest genuinely open — no manifest file in repo); U.4 PDF polish closed (2026-05-14 + 2026-05-24 passes); R.1 cabinet.js carve closed (cabinet-calc/-render/-library files exist; 1,825-line residual accepted); R.2 line count corrected 2,946 → 5,167; R.4 two of three items moot (Projects entity removed F5/F6); H.1 updated (db.js:68 + main.js early-boot both hardcode the auth-token key); P.1 marked overtaken-by-events (the single Supabase project IS production); Launch Week marked window-passed (soft launch 2026-05-02, growth moved to paid ads); Stack table refreshed (42 src files; payments/email/analytics/error-logging rows current); Status Snapshot "Launch target: mid-May 2026" → launched 2026-05-02. Google OAuth status split correctly: code shipped 2026-06-10, dashboard activation still user-side.
- 2026-06-11 — **Tour pricing CTA suppressed for everyone with full Pro access — and the gate snapshotted before demo mode**. Request: "don't show the CTA screen for pro users." Two gaps: (1) the tour-end "Choose your plan" step and the skip → plan-picker hand-off gated on `isPro()` only, which excludes the automatic 14-day trial — so every fresh signup (always trialing, and labelled Pro in the UI) still ended the tour on pricing; the session CTA already excluded trialers, so the tour now matches that philosophy (`_hasProAccess`: paying OR trialing → no upsell; pricing waits for the trial-ending banner / weekly session CTA once it actually applies). (2) Deeper pre-existing bug: the tour borrows demo mode and the demo seed has `subscriptions: []`, so live `isPro()` reads false MID-TOUR even for paying users — the CTA-drop decision is made at render/skip time, meaning paying users re-running the tour still saw pricing. Fix: `_wtRunStart` snapshots `_wtNoUpsell = _hasProAccess()` over real data before flipping demo mode; `_wtLastIdx`/`_wtSkip` read the snapshot; `_wtStartCta` clears it (showing pricing is that overlay's whole job). Verified in dev preview on a paying account: mid-tour live `isPro()` false yet `_wtLastIdx()` drops the CTA step (stubbing the live gate mid-tour doesn't move it), skip produces no plan-picker hand-off, demo mode + real subscription restored after close. Guests/post-trial free users keep the CTA. Committed via selective hunk staging (`git apply --cached`) — the concurrent mobile-tour rework in the same file stays uncommitted worktree work. typecheck clean.
- 2026-06-11 — **Guided walkthrough runs on phones (and touch devices generally)**. Request: "make the walkthrough work on phones too." The tour was desktop-only by design — `_wtStart` returned early on touch and the static mobile notice showed instead. Changes (`src/walkthrough.js`, `styles.css`, `src/mobile-notice.js` comment): touch gate removed; each step declares `mv: 'list'|'editor'` and the tour drives `body[data-mv]` (mobile-nav.js) to the pane holding its target — applied AFTER the pre-click since card handlers call `_mvShowEditor` and `switchSection` resets to list; on ≤760px the tooltip pins sheet-style to the top/bottom screen edge away from the spotlight centre (side-anchoring can't fit beside full-width targets; arrow dir reuses the existing top/bottom math); touch devices get a fingertip tap-dot pointer instead of the mouse-arrow SVG (`#wt-cursor .wt-tap-dot`, tip = circle centre) plus swipe left/right navigation on the overlay (passive listeners, 60px floor + 1.5× dx:dy ratio so CTA scrolling and taps never navigate); device-aware copy via `bodyTouch` (welcome "arrow keys" → "swipe left or tap Next"; touch = `_pcIsTouchDevice`, so iPads get it too) and `bodyNarrow` (schedule gantt-bars copy → agenda copy; narrow = `_mvIsMobile`). Pre-clicks hardened: `preClickCard` is now awaited via `_wtWaitFor` (visible+sized) before clicking instead of a blind sync `querySelector().click()` — the Cut List Library grid renders async, and its synchronous "Loading…" placeholder div taught us the selector must be `#cl-lib-grid > div[onclick]`; the Cut List step now pre-opens a demo cut list from the library so the spotlit `.cl-left` shows populated sheet/piece tables on ALL form factors (previously the 26→10-step trim left it spotlighting the "no cut list open" gate). `_wtMaybeAutoStart` shows the mobile notice only when the full tour is NOT auto-starting (the tour is the welcome; returning users keep the once-per-session notice). CSS: CTA plan picker stacks to one column ≤480px (li min-height released), tour buttons ≥42px on coarse pointers. Verified in dev preview at 390×844 with forced touch: all 10 steps land on the right pane (list/editor trail confirmed), library card pre-click fires (`OPEN:` hook), sheet tooltip flips top/bottom per target half, swipe-back works and a mostly-vertical drag doesn't navigate; desktop 1280×800 re-verified unchanged (anchored tooltips, arrow cursor, keyboard copy, cut-list step now opens real data there too). typecheck clean. WT_VERSION deliberately NOT bumped — mobile users never dismissed the tour (it never ran there), and desktop users have nothing new to learn.
- 2026-06-11 — **Quote line-item autosave 400 fixed — second `schedule_hours: null` write path**. Report: "SAVE FAILED · RETRY" pill when saving a line item on prod. API log: `PATCH /quote_lines?id=eq.776` → 400; postgres log: 23502 null in `schedule_hours` (NOT NULL default 0). The Sentry JAVASCRIPT-6 fix guarded `saveQuoteEditor` (quotes.js) but missed the debounced per-line autosave `_scheduleLineUpsert` (app.js), which still sent `row.schedule_hours ?? null` — and a freshly added line has no local `schedule_hours` (the `_lineAdd`/`_qAddStockLineFromLibrary` insert literals omit it; only `id` is copied back from the insert), so the first keystroke on a new "+ Item"/stock-library line 400'd every autosave AND every RETRY (the retry closure re-sends the same null); name/qty/price edits were silently unsaved until a full editor save ran. Fix: `?? 0` in `_scheduleLineUpsert` (mirrors the saveQuoteEditor guard) + `schedule_hours: 0` seeded into both insert literals so local rows mirror the DB row. Order twin (`_scheduleOrderLineUpsert` `?? null`) deliberately untouched — `order_lines.schedule_hours` is nullable. Verification: preview session was signed out, so the replay ran at the SQL layer — stuck prod row 776 (name still '', updated_at == created_at, proving the PATCH never landed) updated with exactly the fixed payload, succeeding and healing the user's line; served-source check confirmed Vite ships the guard. typecheck clean.
- 2026-06-11 — **Pro subscribe/portal/pricing CORS preflight failure fixed — `apikey` header was never allow-listed**. Report: clicking any paid plan toasted "Load failed (mhzneruvlfmhnsohfrdo.supabase.co)" (Safari's TypeError for a failed fetch; the host suffix is Safari's, not ours — the string exists nowhere in the repo). Edge logs showed ZERO stripe-checkout requests (not even OPTIONS) while connect-status traffic flowed fine: the request died browser-side. Root cause: commit 888d1ec (2026-06-09, in-memory-token fix) switched stripe.js's three edge calls to raw `fetch` and added an `apikey` header, but stripe-checkout/stripe-portal/stripe-subscription carry hand-rolled inline CORS (predating `_shared/cors.ts`) allowing only `authorization, content-type` — so every preflight failed and Checkout has been unreachable in prod since June 9. Fix: widened all three inline lists to `authorization, content-type, apikey, x-client-info` (matching `_shared/cors.ts`); redeployed via CLI preserving each function's verify_jwt (checkout v27 + portal v20 `--no-verify-jwt`; subscription v13 verify_jwt=true — the blanket "deploy with --no-verify-jwt" memory is wrong for this one). Verified live: OPTIONS preflight from procabinet.app origin now returns the full allow-list on all three, and a POST carrying `apikey` reaches function code (401 "Invalid auth token" for a fake bearer, i.e. past CORS). Inline lists deliberately NOT refactored onto `_shared/cors.ts` mid-incident — minimal diff on live billing functions.
- 2026-06-11 — **Boot data loaded twice on every page load — fixed with a per-user guard in the auth handler (perf pass P.7)**. User report: "data takes ~15 s to load" despite the P.1–P.6 pass. Production waterfall (Playwright + Performance API on procabinet.app/os, signed in) showed the early-boot queries landing by ~0.7 s — then the ENTIRE load repeating at ~1.6 s: supabase-js emits INITIAL_SESSION and then SIGNED_IN for the same stored session on every page load (plus SIGNED_IN on tab refocus and TOKEN_REFRESHED hourly), `onAuthStateChange` ran the full `loadAllData()` + `_loadCabinetTemplatesFromDB()` on each, and supabase-js serializes async callbacks — so the two full loads ran back-to-back (~38 REST requests; the duplicate also wiped every `_lines`/`_totals` cache, re-hydrating and re-rendering everything). On a slow connection that serialized double load is the ~15 s. Fix: `_bootLoadedUserId` — the session branch bails early when `session.user.id` matches the already-loaded user (token cache is updated before the guard; realtime keeps quotes/orders live), cleared on the no-session branch so sign-out → sign-in reloads. Walkthrough exit is unaffected (it calls `loadAllData()` directly). Deliberate trade: tab-refocus SIGNED_IN no longer re-pulls cross-device edits for stock/clients/settings (quotes/orders still sync via realtime). DB exonerated first: advisors clean, biggest table 116 rows, no query > 50 ms mean. Dev-verified: every boot table fetched exactly once (18 vs 38 requests). typecheck clean.
- 2026-06-12 — **Pre-boot nav tap ReferenceError fixed (Sentry JAVASCRIPT-D) — stub + boot replay**. Prod event: iPhone Safari user tapped the Cut List nav tab on /os/ and got `ReferenceError: Can't find variable: switchSection` — the shell is plain HTML, the nav is tappable while the ~38 deferred classic scripts download, and `switchSection` lives 25th in the chain (settings.js). Fix: a one-line inline stub in the index.html head (`window.switchSection = n => window._preBootSection = n`) so the inline onclick always resolves; settings.js's top-level function declaration overwrites the stub at execute time (classic-script global binding semantics), and `restoreAppState` (persist.js) replays the stashed tap at boot with priority over the persisted last-section restore — the tap is honored, not just swallowed. Signed-out taps stash harmlessly (no boot → no replay). Verified in dev preview: stub present in served head, real switchSection after load, simulated stash + `_restoreAppState()` lands on Cut List and clears the stash. typecheck clean.
- 2026-06-12 — **Mobile tour: Finish no longer "sticks"; sheet tooltip always bottom-pinned**. Founder's phone test surfaced two issues with the 2026-06-11 mobile walkthrough. (1) Tapping Finish appeared to hang: `_wtClose` deliberately awaited the demo→real `loadAllData()` restore BEFORE removing the overlay (the no-demo-flash rule), so on a phone connection the frozen spotlight/tooltip sat there for seconds — unbounded if a fetch stalled. Fix: the tour chrome (tooltip/spotlight/cursor/dropdowns) is stripped the moment close starts and the dim mask stays up with a `.wt-restore` "Loading your data…" spinner pill, so the tap visibly responds, demo content still never flashes, and the restore is capped at 12 s via `Promise.race` (a stalled fetch can't trap the user; late responses render when they land). Guests/CTA-only closes are untouched (no `_wtTempDemo` → immediate teardown, no pill). (2) The ≤760px sheet tooltip flipped to the TOP edge when the target's centre sat in the bottom half — covering exactly the pane headers/names/first-fields the step was describing. Now always bottom-pinned (`dir='bottom'`, arrow on the sheet's top edge); panes carry their key info at the top, and `scrollIntoView(block:'center')` keeps the target's upper half clear of the sheet. Verified in dev preview at 390×844 on the real signed-in account (Pro → 9-step tour ending on Dashboard "Finish", the reported path): every spot step's tooltip bottomGap=14, and with `loadAllData` wrapped in a 3 s delay the Finish tap removed tooltip/spot/cursor and showed the pill within ~250 ms, then teardown landed with real data restored, demo off, dashboard active. typecheck clean, no console errors.
- 2026-06-12 — **Founders WhatsApp group invite wired into the funnel docs — the invite goes to seat BUYERS, not prospects.** A first pass mistakenly restructured the offer email to free users (`marketing/founders-email-2026-06-10.md`) to hand the join link to all ~16 recipients; corrected same day on founder feedback (the group is the perk people pay $299 for). Final shape: the offer email keeps its original two-item copy untouched except the stale ⚠️ pre-send warning — S.9 live-mode flip verified done 2026-06-11, so it now reads cleared and the Claim link hits live Checkout. New `marketing/founders-welcome-email-2026-06-12.md` delivers the perk after purchase: personal same-day send with thank-you + seat number (`founder_seats_taken` count), the clean invite link `https://chat.whatsapp.com/H8QI9EHNtJAE1WAlnj2dT0` (WhatsApp's `?s=cl&p=i…` share params stripped; bare form verified resolving to "ProCabinet.App Founders"), the QR as attachment (moved from the stray `WhatsApp /` drop folder to `marketing/assets/founders-whatsapp-qr.png`), the 15-min setup-call offer, and group-hosting notes (greet by name, week-one build-update, credit founders when their asks ship). Deliberately never automated — at ≤50 sends the personal note is the perk working. Buyers-only rule stated in both docs so the link stays out of public channels.
- 2026-06-12 — **Cancelled payment no longer accepts the quote — acceptance moved to post-payment, webhook backfills.** Founder repro on QUO-0001: "Accept & pay" → close the card sheet → quote locked as accepted (status `accepted`, `accepted_at` stamped, snapshot stored) with £0 taken — `payDeposit()` called `recordAccept()` right after the PaymentIntent was created, before the customer ever saw the card form. Fix across three files: (1) quote-public.js records acceptance only after `confirmPayment` resolves without error (covers card success, `processing`, and bank-transfer-committed — bankPending/processing states already stamped `accepted_at` locally, so semantics unchanged), plus in `boot()`'s `redirect_status` branch since redirect methods (Pay by bank) never return from `confirmPayment`; `recordAccept` gains an accepted guard. (2) quote-pay-webhook v13 backfills `accepted_at` (`?? now`) on `payment_intent.succeeded` so a tab closed mid-flow can't leave a paid quote unaccepted/editable (accepted_at is the public-page edit lock). (3) quote-public-update v15's accept action no longer downgrades `deposit_paid`/`paid` → `accepted` when the webhook wins the race (snapshot is skipped on 409 — informational only, order rebuilds from lines server-side). `quote-pay` never gated on accepted_at, so pay-after-accept still works. Verified on the live flow (local fixed JS + prod functions, quote 91 reset first): sheet open with real Stripe element mounted → DB still `viewed/null`; cancel + reload → "Awaiting your approval" with the pay button live, DB untouched. Success path not live-tested (would charge a real card); it's the same accept call relocated + the deployed webhook backfill. Stray artifacts of the bug: one `pending` payments row per abandoned sheet (pre-existing behaviour, PIs expire server-side).
- 2026-06-12 — **Boot loading screen — the app no longer opens as an empty shell while data loads.** New `#boot-loader` overlay (logo + spinner + "Loading your workspace…") in `index.html`, visible from first paint; cleared by `window._hideBootLoader()`. Decisions: helper + 15s failsafe defined in the head pre-boot inline script (NOT a deferred src file) so the overlay can never trap the user even if a later script fails to load; hide points are `_showAuth()` (signed-out destination) and a `finally` around `await loadAllData()` (panels rendered — or load threw); z-index 10000 = above auth screen (9999) and popups (8000, so the PASSWORD_RECOVERY popup surfaces only after load), below toasts (99999); saved `pcDark` theme now pre-applied in the same head script (try/catch for storage-blocked in-app browsers) so dark-mode users don't get a light flash — settings.js still owns the theme and re-applies it. `npm run typecheck` clean.
- 2026-06-12 — **Demo data now persists after the tour — dashboard "Remove demo data" button clears it (and itself).** New accounts used to land in an empty app the moment the walkthrough closed. Now the demo seed stays visible as a client-side overlay: `_demoOverlayInit` (called from `loadAllData` after business_info hydrates) decides once per account — empty account ⇒ `onboarding_state.demo_data='active'`, account with real content ⇒ `'removed'` sight unseen (existing users never see it) — and the flag persists via `_wtPersistState` (business_info upsert + user-stamped LS mirror). While active, `_DBBuilder.then()` (db.js) merges demo rows into selects on the 11 content tables (demo-only negative-id selects skip the network entirely), real writes pass through untouched, and writes targeting demo rows (negative id in where/payload — covers edits/deletes of demo rows AND inserts/relinks against demo parents, which would be server-side FK violations) are blocked with a debounced explainer toast. Key design choice: every demo id/FK is negated (`_demoNegateIds`) so demo rows can't collide with real bigserial ids and `id < 0` is the universal discriminator — free-tier caps count `_realCount(arr)` (excludes `id<0`/`db_id<0`) at all ~20 call sites, and `_nextQuoteNumber`/`_nextOrderNumber` skip demo rows so a user's first real quote is QUO-0001, not QUO-1047. The four boot arrays are merged in `loadAllData` itself (idempotent strip-then-concat) because the early-boot fetches bypass `_db()`. Removal = confirm dialog → flag flip → in-place reload; a session latch in `_demoOverlayInit` plus an AWAITED persist prevent the race found in testing (fire-and-forget upsert lost to loadAllData's business_info re-read, resurrecting the overlay). Nothing demo is ever written server-side, so removal can't touch real rows by construction. Verified in dev on the signed-in test account: overlay on → 30 demo rows interleaved with real ones (dashboard cards, schedule bars, revenue chart, hydrated demo quote totals), caps/numbering ignore demo, zero negative-id writes on the wire, removal cleans every table and survives reload. typecheck clean.
- 2026-06-12 — **Onboarding welcome email for every new sign-up — built, deployed, verified; goes live with the next push.** Every new account (email-confirm AND Google OAuth paths) now gets ONE founder-voiced transactional email ("Welcome to ProCabinet", from `Adam at ProCabinet <adam@procabinet.app>`, reply-to the same) with first steps, the 14-day-trial fact stated plainly, and the new Google Calendar appointment-schedule booking link (`https://calendar.app.google/3KU7rrEd8mnUu7599` — "ProCabinet setup call", 15 min, Meet, Mon–Fri 9–5 UK; created by driving the founder's signed-in Chrome, since Google exposes NO API for appointment schedules). Decisions: goes to ALL signups regardless of marketing opt-in (transactional/service email — strictly no promo content; the opt-in mailing list stays separate), copy doc `marketing/welcome-email-2026-06-12.md` in the founders-email register, greeting personalises from `user_metadata.first_name`/`full_name` when present. Architecture mirrors list-subscribe: client fire-and-forget `_sendWelcomeEmailOnce` (auth.js, beside `_syncMailingList`; cheap guards: confirmed email, `created_at ≥ 2026-06-12T00:00Z` cutoff, per-user LS flag) → JWT-verified edge function `send-welcome-email` (deployed v1, verify_jwt ON) which is the authority: re-checks the gates, then CLAIMS `app_metadata.welcome_email_sent_at` via service role BEFORE sending (claim-then-send: duplicate-send is the unrecoverable failure mode; claim rolled back on Resend failure), sends via Resend REST with `Idempotency-Key: welcome-email/<uid>` to collapse the two-tab/two-device race the last-write-wins claim can't. `app_metadata` not `user_metadata` because the browser can rewrite its own user_metadata. Verified live end-to-end on throwaway `adamdenney33+pcwelcome1@googlemail.com` (dev client + prod function): exactly one email delivered (Resend c544b827, claim 12:54:14Z), repeat-login after LS-flag wipe → server `skipped:'already sent'`, zero second sends; founder's real 05-17 account booted the new client with no invocation (cutoff). Outstanding: booking page title shows "(No title)" (Google's title input rejects synthetic events — founder types it once in Calendar → Edit), throwaway user to delete in the dashboard, and the client hook reaches prod users only on the next push to main.
- 2026-06-12 — **CSV import/export rebuilt across all six surfaces — every user-facing field now round-trips.** Audit found the CSVs exported subsets and three importers were broken outright: quotes import inserted `materials`/`labour` columns dropped in the schema normalisation (every row 400'd with PGRST204 → "0 quotes imported"), orders import read col 0 as Client while its own export wrote Order # first (a re-imported export silently put order numbers in the client field and names in the value field), clients import was a stub toast, and cut-list import dropped Material (pieces) and Grain (sheets) despite exporting them. Foundations in ui.js: `_csvParse` (RFC-4180 — quoted commas/newlines/`""` escapes; the old `split(',')` corrupted any address or note containing a comma), `_csvCol` (normalised header-name mapping with per-column aliases covering the legacy headers; returns null for headerless files → callers fall back to documented positional order), `_csvDownload`, `_csvPickFile`. Per surface: **clients** export real rows (Name/Email/Phone/Address/Notes from the `clients` array, + derived Quotes/Orders/Total Value; orphan quote/order names appended name-only) and a real importer (dedupe by name, free-cap gated). **Stock** adds Variant/Thickness/Supplier/Reorder Link/Glue/EB Width/EB Length/Coverage in both directions (columns inserted directly — DB is source of truth; accessors still used on export for LS-fallback rows); template now shows sheet+EB+finishing examples. **Orders** header-mapped both ways with Order #/Markup/Tax/Discount/Stock Markup/Priority/Production Start/Notes/Created; status accepts key or label ("In Production"→`production`); `order_number` auto-assigned via `_nextOrderNumber()` when blank. **Quotes** insert only live columns; Materials/Labour £ become two `quote_lines` (item qty 1 × £mat; labour 1 hr × £lab rate — exact value preservation through `_lineSubtotal`), uniform key set on both rows because PostgREST rejects bulk inserts with mismatched keys ("All object keys must match", caught in dev); `q._lines`/`q._totals` hydrated in-memory; export gains Quote #, Stock Markup %, Total. **Cabinet library** exports/imports the full builder spec (39 cols: type/room/carcass/door/drawer-front/drawer-box types + finishes, labour hrs/override, material cost override, 3 hardware arrays + extras as JSON cells, notes); legacy 22-col files import via header mapping. **Cut list** pieces keep Material on import (bug), gain Notes + per-side edge bands (`BandName|trim` cells matched case-insensitively against the cut list's bands); sheets keep Grain; `addPiece`/`addSheet` grew optional trailing params (all existing call sites pass fewer args). Verified in dev preview on the signed-in test account: live round-trips on all six surfaces (import → DB rows checked → export → re-import identical), comma-in-address and quote-in-notes survive, decimal finishing qty (2.5 L) lands, EB renders 22mm × 50m @ £0.45/m, edge trim flag round-trips; test rows deleted after. typecheck clean, no console errors.
- 2026-06-13 — **`send-test-email` edge function (v1, deployed) — real test sends for the Cowork email-plan artifact.** The email-planning artifact's "Send test" button now delivers drafts to the founder's own inbox immediately, from the production sender (`Adam at ProCabinet <adam@procabinet.app>` via Resend) for true rendering/DKIM fidelity. Call path is artifact → Supabase MCP `execute_sql` → `net.http_post` (pg_net, already installed) → function → Resend, because the artifact sandbox has no direct network access but the database does. Deployed `--no-verify-jwt` (pg_net carries no user JWT) with auth implemented in-function: static `x-test-key` token shared with the artifact, recipient allowlist of the founder's six own addresses (worst-case abuse = founder receives [TEST] mail), forced `[TEST]` subject prefix, single recipient, 200KB body cap. Reuses the project-wide `RESEND_API_KEY` secret; nothing app-facing touched. Source tracked at `supabase/functions/send-test-email/index.ts`.
- 2026-06-13 — **`send-test-email` v2 — inline image attachments for the QR code.** The artifact's founders' welcome cards now display the WhatsApp group QR in the email body (cropped from `marketing/assets/founders-whatsapp-qr.png` with a proper quiet zone, embedded as a data URI). Gmail strips data: URI images, so test sends swap them for `cid:` inline attachments client-side and the function gains a guarded `attachments` field (images only, 3 max, ~110KB each, base64-validated) forwarded to Resend as `content_id` inline attachments. Verified live through the pg_net chain (Resend 200, inline render).
- 2026-06-13 — **Artifact "Sync selections" — the email-plan artifact's state is now readable from chat.** The artifact's ticks/edits/order live in its sandboxed localStorage, which Claude cannot see (the founder reasonably asked why). New scratch table `cowork_email_plan_state` (SCHEMA.md § 3.27; RLS on, no policies — service-role/postgres only, invisible via PostgREST) + a Sync button that upserts the full state as one jsonb row: per-card kind/label/position/day/use/variant/angle plus final subject, plain text, and email-ready HTML (inline-styled, data-URI images already swapped for cid refs). Copy summary kept as fallback. Replaces paste-the-summary as the handoff for setting up the production sends in Resend.
- 2026-06-13 — **Production email sends set up in Resend: welcome v2 (live) + founders' welcome auto-send (live, unproven).** Founder ticked two of the email-plan artifact's emails (synced via `cowork_email_plan_state`) and approved their final copy; drip series parked unticked. (1) `send-welcome-email` redeployed v2 with the founder's "reply-first" variant — subject `Your ProCabinet account`, mission line ("ultimate operating system… the one I wish I had 15 years ago"), feedback ask, ProCabinet.App wordmark sign-off linking to the site; greeting personalisation, claim-then-send and idempotency unchanged; `marketing/welcome-email-2026-06-12.md` updated. (2) New `send-founders-welcome` edge function (v1, verify_jwt off, `x-fw-key` static auth) sends the founder-approved seat-buyer email: permanent-Pro line, 15-min onboarding-call booking link, WhatsApp invite + the group QR shown inline (cid attachment, cropped from `marketing/assets/founders-whatsapp-qr.png`), wordmark. Claim-then-send via new `founders_welcome_sends` table (email PK, SCHEMA § 3.28) + Resend Idempotency-Key = exactly-once. (3) Auto-send: `trg_founders_welcome` AFTER INSERT OR UPDATE OF plan on `subscriptions`, fires when plan→`founder` (the Stripe webhook's `recordFounderPurchase` upsert) → `notify_founder_purchase()` (security definer, exception-guarded so it can never abort the webhook write) → pg_net → the function; EXECUTE revoked from anon/authenticated per security advisor. Stripe webhook code untouched. Verified: both finals delivered to the founder through the real production path (test-mode for founders'; welcome v2 copy through send-test-email) — Resend 200s, QR renders inline. Outstanding: welcome v2 client hook reaches prod users only on the next push to `main`; the founders' auto-send is deployed/live but the first real seat sale is its first live exercise. Scratch table `cowork_email_plan_state` (SCHEMA § 3.27) carries the artifact's synced selections; both new tables are RLS-on/no-policy (service-role only).
- 2026-06-13 — **Founders' welcome QR fixed: hosted image, not inline cid (Apple Mail showed an empty box).** Inline `cid:` attachments don't render reliably across mail clients, so the QR is now served as a real https PNG by a new public `founders-qr` edge function (verify_jwt off, GET only, year-cached) and embedded with a normal `<img src>`. To avoid pasting a 42KB base64 into the function, `founders-qr` GENERATES the QR at cold start from the WhatsApp invite URL via `npm:qrcode` (a plain QR of the same URL scans to the same group as the branded screenshot in `marketing/assets/`). `send-founders-welcome` v3 drops the cid attachment + embedded base64 (now references the QR URL) and splits the Resend idempotency key: stable `founders-welcome/<email>` for real sends (exactly-once), unique `founders-welcome-test/<uuid>` for test sends (the earlier 24h idempotency key was 409-blocking corrected previews). Verified: QR endpoint returns 200 image/png with valid PNG signature; corrected founders' test delivered (Resend 200).
- 2026-06-13 — **Welcome + founder emails moved to Resend Templates (editable in-dashboard), and one-off backfill to existing users.** Both transactional emails' copy now lives in PUBLISHED Resend templates — `welcome-onboarding` (vars `GREETING`, `STATUS_LINE`) and `founder-welcome` (static) — so the founder can edit copy in the Resend dashboard without a code deploy. `send-welcome-email` and `send-founders-welcome` rewired from inline html/text to `template:{id,variables}` sends (subject now owned by the template; founder test-mode overrides subject for the `[TEST] ` prefix). Claim-then-send, idempotency keys, and the founder QR-by-URL all unchanged. **Gotcha (cost an hour):** the Resend REST API stores template variable fallbacks under snake_case `fallback_value`, but the SDK docs show `fallbackValue` — sending the camelCase key is silently dropped (stored `null`), so a go-forward send that omits `STATUS_LINE` 422s with "Missing required variable". Fixed by re-setting both vars with `fallback_value`; `STATUS_LINE`'s fallback IS the original "14 days, no card" trial sentence, so new signups are byte-identical to before. **Backfill** (founder explicitly asked to email existing users): one-off transactional welcome to the **37** trial-active (created ≤14d) confirmed non-founder accounts that hadn't been sent, GREETING personalised, trial line via fallback; the **1** existing founder got the founder email through the real production path (`send-founders-welcome` → `founders_welcome_sends` log); founder EXCLUDED from the generic welcome (the "14-day trial" line is false for permanent-Pro). The **8** trial-expired accounts were HELD — the trial copy would be false for them, and 6 of the 8 are the founder's own/bee9 test addresses (the other 2 are genuine lapsed users for a future honest re-engagement, opt-in only); they're also pre-`WELCOME_CUTOFF` so the go-forward function skips them anyway. `app_metadata.welcome_email_sent_at` (+`welcome_email_source` tag) stamped on all 38 send targets so go-forward never re-sends; reconciled 0 trial-active users left unsent. Sent via the Resend API directly (37 sends, rate-limited under 2 req/s, all 200). **Still pending:** drip steps 2+ are opted-in marketing (22 contacts in the "Mailing List" audience) and belong in Resend Broadcasts/Automations, not these transactional functions — deferred per "welcome only for now".
- 2026-06-13 — **Resend MCP key wiring fixed.** The committed `.mcp.json` resolves the key via `${RESEND_API_KEY}`, which was unset in the launch environment → MCP started with an empty key (`API key is invalid`). The live key (in gitignored `supabase/.env`) was added to the gitignored `.claude/settings.local.json` `env` block — NOT to `.mcp.json`, which is tracked/pushed and would leak the key. Bash tools pick the env up immediately; the MCP subprocess needs a full Claude Code quit-and-relaunch (a reload isn't enough). All Resend work this session was driven via the REST API directly with the `supabase/.env` key regardless, so the MCP was not on the critical path.
- 2026-06-15 — **Client address/phone/email now print on quote + order PDFs.** The "PREPARED FOR" / "BILL TO" block only rendered the client *name* — the recipient's contact details (already stored on `clients`) were missing from every customer-facing document. New shared helper `_drawDocAddressee(pdf, {label, clientName, projectName, client, M, y})` in `cutlist.js` stacks address (wrapped to the left column), phone, then email under the recipient name and returns the taller-column baseline; falls back to the prior name-only layout when no client record resolves (legacy/unlinked rows look unchanged). Wired into `_buildQuotePDF` (QUOTATION) and `_buildOrderDocPDF` (order confirmation / pro forma / invoice) by resolving `clients.find(c => c.id === row.client_id)`. The internal `_buildWorkOrderPDF` (workshop doc) deliberately left untouched — it isn't customer-facing. Verified in dev preview: helper + full quote builder render name/address/phone/email into the PDF content stream; typecheck clean.
- 2026-06-15 — **Packaging hours are now per-cabinet and cabinet-gated, not a flat per-order add.** The Packaging rate (`cbSettings.packagingHours`, set in Cabinet Builder → My Rates → Other Labour Times) was previously added **once per order** to the scheduler's hours total regardless of line content (and could be overridden per order via the never-surfaced `orders.packaging_hours` column). It's now treated like the other Other-Labour-Time rates: applied **only to cabinet lines** and scaled by their qty, so orders with no cabinets get **zero** packaging. Changed `orderHoursRequired` (`scheduler.js`) and `_orderHoursBreakdown` (`app.js`) to accumulate `packagingHours × qty` inside the `kind==='cabinet'` branch and drop the flat per-order `pack` block (the `orders.packaging_hours` per-order override is no longer read; column left in place, harmless). Packaging stays a **schedule-hours-only** quantity — it is NOT folded into `calcCBLine` labour, so it never touches the price path (matches prior behaviour where packaging never affected price). My Rates Packaging unit relabelled `hrs` → `hrs/cab` to signal the per-cabinet semantic (`cabinet-render.js`). Verified in dev preview against the freshly-loaded code: `_orderHoursBreakdown`/`orderHoursRequired` return packaging = rate×qty for cabinet lines and 0 for cabinet-free orders, the legacy per-order override is ignored, label reads `hrs/cab`; typecheck clean, no console errors. Note: the founder's current packaging rate is 0, so no existing schedule shifts. The per-order × qty scaling is a behaviour change for any non-zero rate (was once-per-order) — the rate should now be entered as a per-cabinet packing time.
- 2026-06-15 — **Follow-up: packaging is now billable per-cabinet labour (not schedule-only).** Refines the same-day entry above. Per the founder, packaging time should be charged, so it's now baked into the cabinet's labour in `calcCBLine` (added to `autoLabour` alongside the other Other-Labour-Time rates → flows into `labourHrs` AND `labourCost`/`lineSubtotal`, subject to the contingency multiplier) and folded into the Cabinet section in `calcCBSections` so the section breakdown still sums to `calcCBLine`. Because packaging now rides in via the cabinet line's `labourHrs` (cached as `_hrs`), the standalone per-cabinet packaging accumulation added to `orderHoursRequired` (scheduler.js) and `_orderHoursBreakdown` (app.js) earlier today was **reverted** to avoid double-counting; the order hours readout drops its separate "• Packaging" row and instead tags the Cabinet-labour row "incl. N% contingency + packaging". Net effect unchanged from the prior entry on the schedule side (packaging still cabinet-only, per-cabinet, ×qty, no per-order override), but it now also appears in the price. Verified in dev preview against freshly-loaded code: at 0.5 hrs/cab, contingency 5%, rate £65 → labourHrs +0.525h, labourCost +£34.125, lineSubtotal +£34.125 per cabinet; `calcCBSections` sum still equals `calcCBLine` (356.4787 = 356.4787); breakdown has no packaging field; typecheck clean; no console errors. Founder's current rate is 0, so no live price/schedule moves until set.
- 2026-06-18 — **Cabinet Builder dimensions now honour the imperial/metric unit setting; currency made consistent between the in-app PDF/print and the public live link.** Two customer-reported bugs. (1) **Units:** the Builder hardcoded `(mm)` labels and rendered raw stored values, ignoring `window.units` — an imperial user saw mm everywhere. Cabinet dims are *canonically stored in mm* (`w/h/d`, `quote_lines.w_mm`, `default_w_mm`) and the whole calc/cut-list engine assumes mm, so storage was left untouched and only the **display/input layer** was made unit-aware. New `units.js` helpers convert mm↔active-unit on the way out/in: `dimDisplayFromMM(mm)` (= `formatDim(units==='imperial' ? mm/25.4 : mm)`, so it honours decimal/fractional/feet-inches + cm/m format modes for free), `dimInputToMM(str)` (= `parseDim(str) × (imperial?25.4:1)`), and `dimsLabelFromMM(w,h,d)` ("W × H × D <unitLabel()>", blank dims dropped). Wired into the Builder editor W/H/D inputs (now `type=text inputmode=decimal`, `onchange` not `oninput` so partial fraction text isn't reparsed mid-entry; `cbUpdateField` routes `w/h/d` through `dimInputToMM`), the live section-header chip + `_refreshCBLiveCosts`, the cabinet line cards, the library grid + sidebar cards, the copy-summary string, and the shared `_lineDisplay` (so the in-app quote/order **PDF + print HTML** match the Builder). (2) **Currency:** the in-app PDF/print read `window.currency` (localStorage `pcCurrency`, default `$`) while the public live link reads `business_info.default_currency` server-side (default `£`); `setCurrency` only ever wrote localStorage and `default_currency` was set ONLY at the one-time migration, so the two drifted (customer saw `$` on the quote, `£` on the live link). Made the DB the source of truth, mirroring how `unit_format` already overlays: `setCurrency(c, persistDB=true)` now fire-and-forget upserts `{user_id, default_currency, updated_at}` (subset upsert preserves other columns; no-ops when not signed in), and `_applyBizInfoFromDB` overlays `b.default_currency` onto `window.currency` via `setCurrency(val, false)` on boot — so the in-app PDF can no longer drift from the live link, and a currency change in Settings now actually propagates to the live link. **Verified** end-to-end in dev preview on the founder's real (metric/£) account: a 600×720×560mm cabinet card renders "600.0 × 720.0 × 560.0 mm" (metric, honouring decimals=1), "23.6 × 28.3 × 22.0 in" (imperial decimal), and "23 5/8 × 28 3/8 × 22 1/16 in" (imperial 16ths); `dimInputToMM('24')`→609.6, `'23 5/8'`→600.075; `setCurrency` round-trips without error; founder's settings restored exactly afterward; typecheck clean, no console errors. **Known remaining gap (not reported, deferred):** the public live link still prints cabinet dims in mm — `quote-public.js` has its own dim code (not `_lineDisplay`) and the `quote-public-get` edge function's business select omits `default_units`/`unit_format`, so fixing live-link dims needs an edge-function field add + redeploy. So an imperial user's Builder/PDF now show inches while the live link still shows mm; flagged for a follow-up.
- 2026-06-18 — **Follow-up: the public live link now prints cabinet dimensions in the maker's unit (closes the gap flagged in the prior entry).** The standalone `quote-public.js` had its own mm-hardcoded dim code (read-only spec line + the optional spec editor), separate from `_lineDisplay`, and the `quote-public-get` edge function's business select omitted the unit fields — so an imperial maker's live link still read mm even after the in-app/PDF fix. (1) **Edge fn:** added `default_units, unit_format` to the `business_info` select (presentation prefs, safe to expose unlike abn/bank_details); redeployed `quote-public-get` via CLI **with `--no-verify-jwt`** (it was v17 verify_jwt=false — a plain deploy would have flipped it true and 401'd the public page) → now v18, smoke-tested reachable without a JWT. (2) **Page:** since the page is a standalone ES module that deliberately doesn't load `units.js`, ported compact self-contained helpers (`fmtDim`/`unitLbl`/`dimsLabel`/`parseDimToMm`, faithful copies of the units.js fraction/feet-inches/parse math) driven by page-local `unitSys`/`unitFmt` set from `D.business`. Read-only spec dims use `dimsLabel`; the spec **editor** dim rows became text inputs showing the maker's unit, and `setField` converts `w_mm/h_mm/d_mm` back to mm via `parseDimToMm` before buffering (mm stays the canonical stored/sent value). **Non-obvious gotcha that changed the design:** `business_info.default_units` is UNRELIABLE — it froze at the one-time migration (rows show `default_units:'mm'` while the live `unit_format.mode:'decimal'`), the same drift currency had. So `unitSys` is derived from `unit_format.mode` (`decimal|fractional|feetInches`→imperial, `mm|cm|m`→metric), with `default_units` only a last-resort fallback. `unit_format` is reliable because `saveBizInfo` keeps syncing it. **Verified end-to-end on real prod data** via the deployed edge fn: QUO-0105 (a maker whose `default_units` is the stale 'mm' but `unit_format.mode='decimal'`) now renders "19.7 × 94.5 × 22.0 in …" across its cabinet lines (was mm), 9 rows, no console errors; ported helper math re-checked for metric mm/cm + imperial decimal/fractional/feet-inches incl. round-trip parse; typecheck clean. **Latent, not addressed (pre-existing, cross-device):** the authed app still takes `window.units` from localStorage `pcUnits` while `unit_format` comes from the DB overlay, so on a second device where `pcUnits` was never set the two can disagree (formatDim keys off `window.units` first). Out of scope for the reported bug; would need boot to derive `window.units` from `unit_format.mode` too.
- 2026-06-18 — **Cross-device fix: the imperial/metric SYSTEM flag now follows the synced `unit_format`, not just device localStorage.** Follow-up to the same-day unit/currency work. `window.units` (imperial|metric) was sourced ONLY from localStorage `pcUnits` (device-local, never DB-synced), while `window.unitFormat` (mode/decimals/denominator) overlays from `business_info.unit_format` on boot. So on a second device — or after a localStorage clear — `pcUnits` could disagree with the DB: a maker who'd chosen imperial showed up as `units:'metric'` (locale default) while the overlay set `unit_format.mode:'decimal'`. `formatDim` branches on `window.units` FIRST, so the builder/cut-list rendered mm despite the imperial mode. Root cause: two representations of the unit system — `pcUnits` (live, device-local) and `unit_format.mode` (live, DB-synced) — with nothing reconciling them (and `default_units` frozen at migration, useless as a tiebreak; this is the same drift that broke the live link, see the 4c4384c entry). Fix: make the DB-synced `unit_format.mode` the source of truth for the system too. `_applyBizInfoFromDB` now derives `_sys` from `uf.mode` (decimal/fractional/feetInches→imperial; mm/cm/m→metric) and, when it differs from `window.units`, calls the new `setUnits(sys, {fromDB:true})`. The **fromDB flag is critical**: it SKIPS setUnits' mm↔inch conversion of cut-list sheets/pieces — those values are stored in the maker's *current* unit (the cut list writes `p.w` straight to the `w_mm` column with no conversion, so they're already correct on the true device unit; only the local flag was wrong) — and skips re-persisting a value just read from the DB. setUnits also now persists genuine USER toggles to `business_info.default_units` (`_syncUnitsToDB`, mm|inches, mirrors `_syncCurrencyToDB`) so the column stops being stale going forward, and re-renders the Cabinet Builder (`renderCBPanel`) on a unit change. Net: the authed app and the live link now both treat `unit_format` as the single source of truth for system + format, so an imperial maker reads inches consistently across devices, the builder, the PDF, and the live link. **Verified** in dev preview on the founder's real metric account: forcing the mismatch (`unit_format.mode='decimal'`, `units='metric'`) and running the overlay path flips `window.units`→'imperial' with the format mode untouched and the pills updated, while in-memory pieces/sheets stay byte-identical (no conversion); the derivation table maps all six modes correctly; founder state (metric/mm, pcUnits) fully restored with cut-list data intact; the fromDB path triggers no DB write; typecheck clean, no console errors. No-op for consistent users (the common case): when `pcUnits` already agrees with `unit_format.mode`, no flip, no conversion, no write.
- 2026-06-18 — **Markup is now hidden on all client documents (quote + order PDF and print HTML), folded into the line prices.** Customer request: "I don't want clients to see the markup costs." The PDF/print itemised a "Markup (X%)" line (and a "Stock markup" line); the public live link already hid markup (it shows the per-line `customer_price`, which `_shareLineCustomerPrice` computes as cost×(1+markup), summed → subtotal → tax → total, no markup line). So the docs were inconsistent with the live link. Founder chose "hide for everyone" (no per-account toggle). Implementation keeps the final total identical and only changes presentation: a per-document `lineScale = afterMarkup / sub` is applied to every displayed line price/amount so the lines now sum to the marked-up subtotal, the "Subtotal" row shows `afterMarkup` (markup baked in), and the Stock-markup + Markup rows are dropped; Tax and order Discount rows + the Total are unchanged. Uniform scaling distributes both general and stock markup proportionally across lines — per-line attribution shifts slightly but the document reconciles exactly and no margin figure is exposed. Wired in three shared paths: `_drawDocLineItems` (cutlist.js) gained an optional `priceScale` (default 1, multiplies `d.unitPrice`/`d.total`); `_buildQuotePDF` + `_buildOrderDocPDF` (cutlist.js) pass it and emit the reduced totals block (Subtotal row now gated on tax|discount, since with markup baked in subtotal==total when neither applies); the quote print-HTML in `printQuote` (quotes.js) scales `d.total` and drops the markup rows the same way. **Verified** in dev preview on a real quote (QUO-0062, markup 20% / tax 13%): raw subtotal 197.21 → new subtotal 236.66 (markup 39.44 folded in), Σ(scaled line amounts)=236.66 (reconciles), final total 267.42 unchanged; the captured print-HTML has no "Markup"/"Stock markup" line, shows Subtotal 236.66 + a Tax line + Total 267; typecheck clean, no console errors. Order docs (confirmation / pro-forma / invoice) get the same treatment; the internal work-order PDF is unaffected (not customer-facing). No toggle/setting added — if a maker ever wants markup itemised, that's a future `priceScale=1` + restored-rows branch behind a flag.
- 2026-06-19 — **Added a per-cabinet Installation time rate (mirrors Packaging), moved both Packaging + Installation into Core Rates, and renamed Contingency → "Labour Time Contingency".** Customer request. New `cbSettings.installationHours` + `business_info.default_installation_hours` (migration `20260619120000_add_installation_hours.sql`, additive `numeric not null default 0`; applied to prod, `database.types.ts` regenerated — the regen also picked up two previously-stale tables, `cowork_email_plan_state` + `founders_welcome_sends`). `calcCBLine` folds `installationHours` into `autoLabour` right after `packagingHours`, so it flows into both `labourHrs` (schedule) and `labourCost`/`lineSubtotal` (price) subject to the contingency multiplier, scaling with qty — identical billing semantics to packaging (see the 2026-06-15 follow-up entry). Persisted via `_syncCBSettingsToDB` (business.js) + `_applyBizInfoFromDB` (app.js). In My Rates the `coreItems` array gained Packaging + Installation (`hrs/cab`, `step 0.05`; the core row template now honours an optional `item.step`) and the Contingency label became "Labour Time Contingency"; Packaging was dropped from `labourItems` (Other Labour Times 6→5 rates, Core 5→7). The two `renderCBResults` breakdown rows and the order-hours `incl.` tag (`_renderOrderHoursBreakdown`, app.js) were relabelled to match (tag now lists "packaging" + "installation" + "N% labour time contingency"). No double-count: the order hours breakdown reads cabinet labour via cached `calcCBLine` `_hrs`, which already includes installation. **Verified** in dev preview on the founder's real account: Core Rates renders 7 rows incl. Installation (`hrs/cab`, step 0.05) and Labour Time Contingency, Other Labour Times shows 5 (no Packaging); `calcCBLine` at 0.5 hrs/cab install + 5% contingency adds exactly 0.525 h to labourHrs (= 0.5×1.05), restored to 0 without saving; both breakdowns read "Labour Time Contingency (5%)"; typecheck clean. Founder's install + packaging rates are both 0, so no live price/schedule moves until set. **Note:** the cabinet-render.js half of this (Core Rates UI + rename) was swept into commit `9496f22` by a parallel session; the remaining wiring (state/save/load/calc + migration + types + schema) is in `01da7a3`.
- 2026-06-19 — **Stock items can be flagged "Visible to customers in live link", offered as selectable materials on the customer's live page.** Founder request. New `stock_items.customer_visible boolean not null default false` (migration `20260619130000_stock_customer_visible.sql`, additive; applied to prod, `database.types.ts` hand-patched Row/Insert/Update). A share-style `.mini-toggle` ("Visible to customers" + "Offer this item as a selectable material on live link quotes") was added to the Stock editor sidebar form (`index.html`, after Reorder Link, before the submit button), wired via `_stockCustVis()`/`_stockSetCustVis()` helpers into `addStockItem`/`saveStockEdit`/`_stockAutosaveRun`/`editStockItem`/`cancelStockEdit`/`duplicateStockItem` (stock.js); the toggle's inline `onclick` flips aria-pressed (`_shTgl`) and schedules autosave, so editing an existing item persists on toggle and a fresh Add reads it on submit. **Non-obvious decision:** the customer's live-link spec-editor material dropdown is fed by the `materials` array from the `quote-public-get` edge function, which historically merged only `catalog_items(type='material')` + used line materials. Since `catalog_items` is deprecated and `stock_items` is now the source of truth for materials, the fix was server-side only — the edge fn now also selects `stock_items` where `customer_visible=true` for the deal's owner and merges their names into `materials`. `q.html`/`quote-public.js` needed **no change** (they already render `D.materials` into the carcass/door/drawer dropdowns). Redeployed `quote-public-get` via CLI **with `--no-verify-jwt`** (was v18 verify_jwt=false; a plain deploy would flip it true and 401 the public page) → now v19, confirmed verify_jwt=false. **Verified** end-to-end in dev preview on the founder's real account: editing stock item 50 ("Blum 110° Hinge"), toggling on autosaves `customer_visible=true` to the DB (re-fetched to confirm), the editor reloads the flag on reopen, `cancelStockEdit` resets it off, and toggling back off persisted `false` (test data reverted, no residue on real data); typecheck clean, no console errors. Defaults `false` so no maker's live link changes until they flag an item.
- 2026-06-19 — **Restored the Cut List parts CSV Import/Export, dropped the Clear button.** Audit of every CSV import/export path found the five live surfaces (Clients, Stock, Orders, Quotes, Cabinet Library) all round-trip correctly — every export heading maps back to an import alias (incl. `Coverage (m²/L)`→`coverageml`) and column counts match. The one gap: the Cut List parts Import/Export/Clear buttons were dropped from `index.html` in `d0ba3d9` (library-pattern refresh), orphaning `exportCSV`/`triggerImportCSV`/`downloadTemplate`/`clearCutList` + the hidden `csv-import-input`. The functions were intact and correct, so re-exposed Export/Import in the **Cut List Library** filter row (`renderCLCutListLibraryView`) using the standard `btn btn-outline lib-filter-btn` style for tab-wide consistency with Clients/Stock/Orders/Quotes — they act on the open cut list's parts. The Clear button was deleted along with its now-dead `clearCutList` + `_doClearAll` code (per founder request). Verified end-to-end (export→serialize→parse→re-map round-trips; embedded commas preserved). `downloadStockTemplate` remains intentionally orphaned (templates removed in `ea6bb3f`).
- 2026-06-19 — **Added the project's first automated tests: a Playwright smoke suite wired into the CI deploy gate.** Founder asked how to reduce customer-surfaced bugs. Gap analysis: the project already has static typing (`tsc`), a CI gate (`deploy.yml` typecheck→build→deploy), and live Sentry, but **no behavioural tests** — a change that compiles and builds but breaks a flow at runtime deploys straight to customers. Filled the gap with smoke tests (`tests/e2e/`, `tests/README.md`). Two layers: `smoke.public.spec.js` (logged-OUT, no creds — app boots, every classic `src/*.js` loads or the test names the 404, no uncaught boot error, auth toggle + empty-field validation) and `smoke.app.spec.js` (logged-IN via the existing `window._signInForTesting()` dev helper — dashboard + all 8 sections + quote/order editor forms render without throwing). Logged-in tests are **read-only by design** (the test account is the founder's real account and Stripe is live, so they open screens but never write/charge) and **auto-skip** when the helper is absent (prod build / CI without creds), so the suite never falsely fails. `playwright.config.js` auto-starts the Vite dev server, runs serial/single-worker (logged-in tests share one real session). npm scripts: `test:e2e` (full, local pre-push), `test:e2e:ui`, `test:smoke` (logged-out only). **CI gate:** `deploy.yml` runs `npm run test:smoke` after typecheck, before build — a red smoke test blocks the production deploy; needs only the publishable Supabase env (no secrets). **Verified:** full suite green (7 tests, real login + all screens), and the boot test proven non-trivial by temporarily hiding `src/auth.js` → test went red naming the file → restored (git-clean). **Outstanding (not blocking):** a dedicated throwaway test account would let the logged-in suite also run in CI (today it's the local pre-push check); then add its creds as GitHub secrets and switch the CI step to `test:e2e`. New habit: add a read-only test for each customer-reported bug as it's fixed, so it can't silently return.
- 2026-06-19 — **Extensive debug audit: verified the app is healthy, fixed a small set of real bugs (mostly a recurring "debounced autosave reads a torn-down DOM" pattern) + two prod-DB hardening items.** Method: `tsc` (clean), a live dev-preview boot (no console errors across all 40 classic scripts), Supabase security+performance advisors, two waves of parallel file audits, and line-by-line verification of every high-severity claim. The audits over-reported heavily; **the following ~14 were read down to source and ruled out** — chat XSS (escaped via `_escHtml`), free-tier cap bypass (all sites use `_realCount()`), quote-total order-of-ops (`quoteTotal` == the PDF math), cut-list edge-band "double-trim" (trimmed = the intended cut size, documented at cutlist.js:1988), boot-load dedup race (`_bootLoadedUserId` set before the first `await`), cabinet-calc override (final-cost by design), demo-overlay limit (re-sorts before slicing), public accept null-price snapshot (`confirmAccept` blocks pending), Stripe/Connect/accounting token staleness (all on in-memory `_dbAuthToken()`), schedule `'created'` sort (ascending=FIFO, consistent), dashboard "Recent Quotes" array, early-boot single-consumption. **Real fixes shipped:** (A) debounced-autosave teardown guards — `_generateShareLink` (share.js) bails when the Live-link settings DOM is gone (a stray `_llAutoSave` was resetting a configured deposit % → 0, and would also flip the payment/selection toggles + per-line flags off); `_oAutoSaveTimer`/`_qAutoSaveTimer` are now cleared on every editor close/switch and `saveOrderEditor`/`saveQuoteEditor` bail when the editor isn't on screen (prevents clobbering order_number/name/tax/etc. with null/0 or spawning a junk record). (B) `_loadCBClientById` (cabinet.js) now detaches `cbEditingOrderId` like its siblings — picking a client mid-order-edit was routing the new quote's cabinets into the old order (the autosave sync gives `cbEditingOrderId` precedence). (C) robustness — `parseDim` guards fraction div-by-zero (`'1/0'`→0 not Infinity), `_loadUnitFormat` `JSON.parse(... || '{}')`, `exportQuotesCSV` awaits `_hydrateQuoteTotals` so it can't emit £0. (C1) **schema:** the Builder's carcass-type + drawer-box-type choices (both drive labour) had no `quote_lines`/`order_lines` column, so the `_cbLineToRow`/`_quoteLineRowToCB` converters dropped them and labour reset to default ref-hours on reload — added `carcass_type`/`drawer_box_type` to both tables (migrations `20260619140100`*; applied to prod; `database.types.ts` regenerated, which also backfilled several columns the stale types file was missing) + mapped both directions in migrate.js. (S1) **security:** `notify_meta_capi_signup()` (a SECURITY DEFINER *trigger* fn) was EXECUTE-able via `/rest/v1/rpc` because `PUBLIC` held the grant — revoked from PUBLIC/anon/authenticated (migration `20260619140000`*; `postgres`/`service_role` retain it, trigger fires as definer regardless; grants verified). Verified: typecheck clean throughout; dev-preview console clean after every reload; `parseDim` + the C1 converter round-trip confirmed live via the pure functions (the dev server points at prod + is signed in, so mutating flows were code-reviewed, not eval'd, to avoid writing real data). Left intentionally: `founder_seats_taken()` anon access (landing seat counter), the two RLS-no-policy tables (deny-by-default, service-role only), the 24h OTP expiry (deliberate for the confirmation UX). **Dashboard-only follow-up (not code):** enable Supabase Auth leaked-password protection (HaveIBeenPwned). 5 commits; not pushed.
- 2026-06-19 — **Fixed the smoke gate failing the deploy on its first CI run ("webServer was not able to start. Exit code: 143").** Playwright's `webServer.command` was `npm run dev`, which carries a `predev` hook (`pkill -f 'node_modules/.bin/vite'`) that frees port 3000 for local dev. That pkill is a footgun under Playwright: killing a Vite process cascades to kill its parent `npm run dev` — the exact server Playwright is waiting on — surfacing as a SIGTERM (exit 143) before Vite even prints its banner. Added a dedicated hook-free `dev:test` script (plain `vite`) and pointed `webServer.command` at it; the local `npm run dev` convenience is left untouched. Verified locally with `CI=1 npm run test:smoke` (3 passed; server boots + tears down cleanly under Playwright). Note: the exact CI SIGTERM source couldn't be reproduced on macOS (local passed even pre-fix), but the `predev` cascade is the only signal-sending command in the repo and its failure signature matches exactly; CI confirmation lands on the next push to `main`.
- 2026-06-19 — **Smoke suite now runs logged-in in CI via a dedicated test account; fixed the Playwright webServer cold-start on CI.** Two follow-ups to the smoke-suite landing. (1) **CI cold-start fix** (`374c1fd`): the first CI run failed with "webServer was not able to start. Exit code: 143" — the Playwright webServer launched `npm run dev`, whose `predev` hook `pkill -f 'node_modules/.bin/vite'` SIGTERMs the very server Playwright just started (on Linux the vite process path literally contains `node_modules/.bin/vite`; locally it passed only because Playwright reused an already-running dev server and never cold-started). Added a hook-free `dev:test` script (plain `vite`) and pointed `playwright.config.js` webServer at `npm run dev:test`; `npm run dev` (with its port-clearing pkill) is left untouched for local use. The gate worked as designed throughout — the broken smoke step blocked Build+Deploy. (2) **Logged-in CI coverage** (`4749b11`): created a dedicated throwaway account `adamdenney33+e2e@googlemail.com` (gotrue signup via anon key, then `email_confirmed_at` set via SQL so it can sign in — id `68deaa14-…`), NOT the founder's personal account. Its password lives in `.env.local` (gitignored) + GitHub secrets `TEST_EMAIL`/`TEST_PASSWORD`; `deploy.yml`'s test step now passes them and runs the full `npm run test:e2e` (logged-out + logged-in). Vite exposes the publishable Supabase URL/key from the step `env:` to `import.meta.env`, and `dev:test` runs in dev mode so `window._signInForTesting` exists. **Verified:** CI green with all 7 tests passing (the 4 logged-in tests ran, not skipped — 18s), deploy succeeded. One stray signup-confirmation email reaches the founder's Gmail (ignorable; account confirmed via SQL); welcome email on first sign-in is DB-idempotent so it won't repeat. Tests remain read-only (live Stripe + prod Supabase).
- 2026-06-19 — **Stock "Visible to customers" now defaults ON, and the Edit Material sidebar was re-sectioned + the toggle's "outside the boundary" misalignment fixed.** Founder follow-up to the `customer_visible` feature shipped earlier today. (1) **Default ON:** column default flipped `false`→`true` (migration `stock_items_customer_visible_default_true`); the static toggle and the `cancelStockEdit()` reset now start a fresh Add at `aria-pressed="true"`; and a **one-time global `UPDATE` flipped all 239 existing rows (27 users) to visible** — the founder's deliberate product decision, understood to override any items other users had hidden on their own live links. A precise per-id rollback snapshot was captured *before* the flip (`~/.claude/plans/stock-customer-visible-flip-rollback.sql`, kept outside the repo as it holds user_ids). `editStockItem()` is untouched, so editing an existing item still loads its saved value. (2) **Alignment fix:** `.share-toggle-row` (and the submit button) were direct `.form-section` children that missed the stock-panel 14px horizontal-pad rule (which only targeted `.form-group`/`.form-row`/the dim wrappers), so the right-aligned mini-toggle hung past the field column. Extended the pad rule to the toggle row, the section titles, and the qty wrappers; the submit button insets via inline `margin:8px 14px 0;width:calc(100% - 28px)` (padding on a `width:100%` button only pads its interior, it doesn't inset the box). (3) **Full sectioning:** added `.form-section-title` dividers — Details / Dimensions / Stock & pricing / Supplier / Visibility — reusing the existing style; the Dimensions title is conditional (`#stock-spec-title`, driven in `stockCatChanged()`: "Dimensions" for sheet/timber/edge-banding, "Coverage" for Finishing, hidden for Hardware/Other). Cost/Unit was lifted out of the cramped 3-across Qty/Low/Cost row onto its own row — `#stock-qty-fields` and `#stock-eb-qty-fields` became wrapper divs (IDs kept, so the category show/hide and the Finishing relabel still work), and the "Visibility" title now provides the divider so the toggle's `border-top` is dropped inside `#panel-stock`. **Verified** in dev preview (logged-in, real account): Add mode shows the toggle ON + Cost on its own row + all five headings + an inset submit button; the 6-category sweep confirmed the spec-heading text/visibility, generic-vs-EB qty-row toggling, and the Finishing Volume/Cost-per-L relabel; toggle content aligns at x=14 with every label and its right edge no longer overflows (desktop + 375px mobile); typecheck clean, no console errors. DB re-checked post-flip: default=`true`, 239 visible / 0 hidden. `database.types.ts` needed no change (the column already had a default, so `customer_visible` was already optional in the Insert type).
- 2026-06-19 — **Live link tab: pinned the `.ll-tabs` builder/Live-link bar (mobile "no way back to the quote" fix).** On the Live link sub-tab the only navigation back to the builder is the Quote/Order **builder** tab in `.ll-tabs` — the builder's own back arrow (`.ph-back`) lives inside `#qb-body`, which is `display:none` on the Live tab. `.ll-tabs` was `position:static` and the first child of the `overflow-y:auto` scroll container (`#quote-editor-host` / `#order-editor-host`), so it scrolled away with the content. On mobile (full-screen single pane, `data-mv=editor`) a long controls panel — payment + customer toggles + per-line spec controls — scrolled the tab bar off-screen and stranded the maker with no way back to the quote. Fix is one CSS rule: `.ll-tabs { position:sticky; top:0; z-index:5; background:var(--surface) }` so the tab is always reachable (solid bg + z-index so scrolled content passes behind it); also keeps the tabs visible while scrolling the desktop sidebar. **Verified** in dev preview at 375px (logged-in, demo quote): scrolled a 1666px panel down 600px and the bar stayed pinned at the top; tapping "Quote builder" returned to the builder (its back arrow re-appeared); no console errors. CSS-only — no JS/types touched.
- 2026-06-19 — **Email ↔ in-app messages bridge (architecture decisions).** Made the client-scoped `customer_messages` chat reachable by email both ways. Key non-obvious choices: (1) **Outbound via a DB AFTER-INSERT trigger** (`trg_message_notify`, founders-welcome pattern) rather than per-call-site `fetch` — there are four insert paths (business supabase-js, `quote-messages`, the `quote-public-update` change-note, and the new inbound fn) and the trigger covers all without bypass. (2) **Reply routing = a per-client `clients.reply_token` (uuid) + a role prefix in the local part**: `c-<token>@` / `b-<token>@reply.procabinet.app`. The `c`/`b` prefix records the *expected* sender so we never infer direction from the spoofable `From`; chosen over reusing `share_token` (per-quote, rotatable, null for clients with no share link). (3) **Loop break:** the inbound fn writes `via='email'`; the trigger's first guard skips those, so an emailed reply is never re-emailed (app-origin messages are the only thing that triggers email — documented limitation: an email→email relay is intentionally not done). (4) **Inbound webhook is metadata-only** — `email.received` carries no body, so `messages-inbound` fetches it from Resend's `GET /emails/receiving/{id}` after Svix-verifying the raw body. (5) **Sender attribution accepts unverified** (product decision): the reply-to token is a delivered secret, so a reply arriving at it is treated as legitimate and inserted; a `From` match against the address on file just upgrades `email_verified` true (a mismatch is flagged "⚠ via email · unverified" in the UI, not dropped). (6) **Idempotency** via `inbound_emails.message_id` PK claim + a `customer_messages.outbound_status` null→'sending' claim-flip + Resend `Idempotency-Key`. `MSG_BRIDGE_KEY` is inlined in the trigger + edge fn (matching `x-fw-key`); `RESEND_WEBHOOK_SECRET` is a Supabase secret. Full design: `~/.claude/plans/i-would-like-messages-reactive-eagle.md`. Code complete + typecheck clean + render verified in preview; activates on deploy + DNS (MX on `reply.procabinet.app`, Resend Receiving + webhook).
- 2026-06-19 — **QuickBooks Online accounting integration switched on + verified live end-to-end** (the credentials-pending tail of the 2026-05-30 build). Intuit granted production API access (App Partner Program, App ID `b8d805f3…`, Accounting scope `com.intuit.quickbooks.accounting`). Switch-on was **config only — no code change, no redeploy**: registered the redirect URI `https://mhzneruvlfmhnsohfrdo.supabase.co/functions/v1/accounting-oauth-callback` in the Intuit app's Keys & credentials (Production key set), and set five Supabase secrets — `ACCOUNTING_TOKEN_KEY` (base64 of 32 random bytes; `crypto.ts` throws if not exactly 32), `ACCOUNTING_STATE_SECRET`, `QBO_CLIENT_ID`, `QBO_CLIENT_SECRET`, `QBO_ENVIRONMENT=production` (it defaults to `sandbox` in `providers.ts`, so production must be set explicitly). **Verified end-to-end** on the `adamdenney33@googlemail.com` Pro account: OAuth connect → Intuit authorize → callback token-exchange → AES-GCM encrypt → store created a `connected` row for the real company **"Pro Cabinet"** (realm `9341457311095122`, access+refresh tokens encrypted), and **pushing ORD-0001 created a DRAFT invoice with its four line items (≈£2,150) in production QuickBooks** (confirmed in Sales → Invoices). **Setup gotchas worth recording:** (a) don't paste the doc's literal `<placeholder>` into a secret — `QBO_CLIENT_ID="<production Client ID>"` stored verbatim and Intuit rejected the connect with `client_id=<production Client ID>`; (b) copy/paste then appended trailing newlines to the client id (`client_id=AB…DgV%0A%0A` in the Intuit error URL) — Intuit matches `client_id` exactly, so it must be set with **no trailing whitespace**; (c) the founder's shell is **zsh**, where `read -rp` errors `read: -p: no coprocess` (that's a bash flag) — use `read "var?prompt"` / `read -s "var?prompt"`. **False alarm:** the first look at QuickBooks showed an empty "Invoice 1001" — that was a *stray new unsaved invoice* (QBO numbers new ones from 1001), **not** the pushed draft, which had the line items all along (the Sales → Invoices list disambiguated it). **Open follow-up:** the connection's `default_tax_code` is **null** — `pickDefaultTaxCode` found no usable tax code in the new "Pro Cabinet" company at connect time, so pushed invoices are **tax-free (no VAT)**. It's picked once at connect and there's no UI to edit it, so to get 20% VAT: set VAT up in QuickBooks, then Disconnect → reconnect (re-runs the pick), or add a tax-code picker / manual override (future). Xero remains connected (org "ProCabinet.App", since 2026-06-03). Integration code unchanged since `1b1eab9` — this was credentials/config only.
- 2026-06-21 — **Live link now shows the business logo (self-heal of `business_info.logo_url` from the device).** The customer live link (`q.html` → `quote-public-get`) renders `business_info.logo_url` server-side, but the logo frequently lived only in `localStorage` (`pc_biz_logo`) — added before the one-time migration, before the upload→DB sync shipped, or on a run where the cloud upload failed ("saved on this device, but cloud sync failed") — so it showed on PDFs (which read localStorage via `getBizLogo()`) yet the live link fell back to the name initial. A DB sweep confirmed `logo_url` was null for nearly every account, incl. the founder's. Added `_healLogoToDB()` (`business.js`): when signed in with a localStorage logo, upload it via the `image-upload` function and upsert `{user_id, logo_url, updated_at}`; called from `_applyBizInfoFromDB` (`app.js`) when the DB has no `logo_url` but localStorage does — mirrors the existing currency self-heal (device wins), runs once per session, silent/background. `removeLogo()` is now async and also nulls the synced URL so removing a logo propagates to the live link. **Verified** on the e2e account: seeded a localStorage logo → boot heal uploaded to storage + set `logo_url` → the customer page rendered the `<img>` logo instead of the initial badge (also confirmed the heal auto-fires on boot); test artifacts cleaned up afterward. **Reverse gap (also fixed, same day):** a second device with `logo_url` set but empty localStorage rendered PDFs without the logo (the PDF header reads only `getBizLogo()`/localStorage). Added `_hydrateLogoToLS(url)` (`business.js`), called from the `b.logo_url` branch of `_applyBizInfoFromDB` when localStorage is empty: fetches the public logo and caches it as a data URL in `pc_biz_logo` so PDFs render it on any synced device (business-assets is public, so the cross-origin GET is allowed; non-fatal on failure). The two helpers now reconcile localStorage ⇄ `business_info.logo_url` on boot in both directions. Verified in preview (CORS GET from the dev origin cached a `data:image/png` into `pc_biz_logo`). Orphan test object from verification removed via `supabase storage rm` (service role; SQL/user-JWT deletes are blocked — see memory).
- 2026-06-21 — **Fixed the Remotion Reels Tracker artifact's dead reel/Studio links + made rendered reels playable inline.** The artifact's "Studio: <comp>" links pointed at `http://localhost:3000/<comp>`, but the Vite dev server owns :3000, so clicking them opened the cabinet app instead of Remotion Studio. Two-part fix. (1) **Per-entry Studio ports** (package.json): `studio:ig-reel`→`--port 3031`, `studio:ig`→3032, `studio:video`→3033, plus new `studio:demo` (`remotion studio demo-video/index.ts --no-open --port 3034`) — now all four studios can run at once and none clash with Vite, and each composition's link resolves to its own port. (2) **Inline playback** — new `scripts/serve-videos.mjs` + `serve:videos` npm script: a tiny range-capable static server (:3050, override via `VIDEO_PORT`) that serves ONLY media under `out/`, `marketing/`, `demo-video/` (403s everything else incl. path traversal; emits HTTP 206 with `Content-Range` so `<video>` can seek; `Access-Control-Allow-Origin:*`; bound to 127.0.0.1). The tracker artifact (`Documents/Claude/Artifacts/remotion-reels-tracker`) was **rebuilt and re-published via the cowork artifact tool** — its folder is a protected/unmountable location so it couldn't be patched in place — and now: plays each rendered reel inline from :3050 (lightbox `<video>` with a "start the server" fallback), points Studio links at the per-entry ports with a copy-command button, shows a live video-server status pill, and sources card thumbnails from posters auto-generated from each reel's first frame (ffmpeg → gitignored `*/thumbs/`). localStorage publish-tracking (`remotion-tracker-v1`) is preserved across the rebuild. **Verified:** server returns 206 with correct `Content-Range` for `out/instagram/reel.mp4`, 403s `package.json` + `out/../package.json`; rebuilt artifact JS passes `node --check`. Note: `out/` + `marketing/videos/` (and nested `demo-video/out/`) are gitignored, so the reels/posters aren't committed — they live on the founder's machine; a fresh clone regenerates posters by running the renders + `serve:videos`.
- 2026-06-21 — **Reel video server now auto-starts at login (zero terminal for the user).** Per founder request ("I don't want to run anything in the terminal"), installed a user LaunchAgent `~/Library/LaunchAgents/app.procabinet.reelserver.plist` (`RunAtLoad`+`KeepAlive`, `ThrottleInterval` 10) that runs `/opt/homebrew/bin/node scripts/serve-videos.mjs` on :3050 — so the Remotion Reels Tracker artifact plays reels inline with nothing to launch. `serve-videos.mjs` now derives its repo root from the script's own path (`fileURLToPath(import.meta.url)/..`) instead of `process.cwd()`, so it works regardless of launchd's working dir. Installed via osascript/computer-control (no Terminal). Verified live: `__ping`=200, a `Range` request returns 206 (`bytes 0-1023/5880419`), and after `kill` the agent auto-respawned (pid 74835→74992, ping 200). The plist is machine-specific (absolute `/Users/adamdenney/...` paths) so it is **not** committed. To remove: `launchctl bootout gui/$(id -u)/app.procabinet.reelserver && rm ~/Library/LaunchAgents/app.procabinet.reelserver.plist`. Studio (editing) still needs the Studio program running — launched on demand via computer-control, not auto-started.
- 2026-06-21 — **Sentry triage → boot self-heal for dropped domain scripts + third-party noise filtered (via the Sentry MCP).** Pulled the 16 unresolved `pro-desktop/javascript` issues and confirmed **none is a reproducible bug on `main`**. The three newest (`JAVASCRIPT-N/M/P`) were the interesting class: the 18 classic `<script defer>` domain files share one global scope, so when a single file's request drops on a flaky mobile network, **every global it defines silently vanishes** and the first unguarded caller throws. Proof from the event data: `JAVASCRIPT-N` (`_hydrateQuoteTotals`) and `-M` (`renderQuoteMain`) were the **same Android session, same trace, 5s apart, release `6ea19ad`** — both functions live in `quotes.js`, so that one script never loaded; `JAVASCRIPT-P` (`window._hideBootLoader`) came from release `d4281c7` (~2 weeks old) = a long-lived stale tab; `JAVASCRIPT-4` (`toggleAccount`/settings.js, 15d ago) is the same dropped-script class. Caching was already correct (`_headers`: `/os/` `must-revalidate`, `/src/*` `?v=` content-stamped), so this is runtime network flakiness, not stale cache. **Fix (two layers).** (1) **`src/main.js` boot self-heal:** after `load`, probe one sentinel global per critical domain file (`loadAllData`/`renderStockMain`/`renderQuoteMain`/`renderOrdersMain`/`toggleAccount`/`_loadCutList`); if any stay absent through a ~3s grace poll (250ms×12 — avoids false-positives from Vite dev serving classic scripts after `DOMContentLoaded`, and from a genuinely slow CDN), a script genuinely dropped, so reload **exactly once per tab** (a failed `defer` request isn't cached, so the reload re-fetches it), guarded by a `sessionStorage` flag against loops; if still missing after the one reload, or storage is blocked, it stops and `captureMessage`s instead of looping. (2) **`src/app.js` guards:** wrapped the boot render calls in `loadAllData` and at top-level (`renderStockMain/QuoteMain/OrdersMain`, `loadBizInfo/loadLogoPreview`) in `try/catch`, matching the existing pattern at app.js:1694/1765, so one missing module degrades instead of aborting the rest of boot. (3) **`ignoreErrors` += three noise patterns:** `/Java object is gone/` (Android in-app-webview bridge, `JAVASCRIPT-J`) and two `@supabase/auth-js` Web-Locks-contention messages (`JAVASCRIPT-E/-3` — benign, the refresh retries). **Deliberately left alone:** the `_db` write-failure `captureMessage` warnings (`JAVASCRIPT-2/5/6/7/8/F/G/H/K`) — low-volume, already mitigated by the 2026-06-11 `_db` hardening, and genuine signal that a real customer write failed (downgrading to breadcrumbs would blind us). **Verified:** `npm run typecheck` clean; in the preview the healthy boot does **not** spurious-reload (the post-fix document lived 29s past its own 3s grace window with the heal flag `null` and all sentinels present — the earlier `DOMContentLoaded`-timing false-positive is gone). **Sentry hygiene (via MCP `update_issue`):** resolved `JAVASCRIPT-P` (stale client) and `-4/-N/-M` (fixed by the self-heal), ignored-forever `-J/-E/-3` (now filtered client-side), each with an explanatory activity-feed comment. Ships on next push (Cloudflare auto-deploy).
- 2026-06-21 — **My Rates → "Extra Panels" is now user-extensible (custom panel types, full material + labour + cut-list).** The Extra Panels rates section was the only one of its kind hardcoded to 5 fixed rows (Fixed Shelf / Adj. Shelf Holes / Loose Shelf / Partition / End Panel), each a labour-hours key on `cbSettings.labourTimes`; the Carcass/Door/Drawer/Base sections were already `+ Add` arrays. Added `cbSettings.extraPanelTypes` (array of `{id, name, hrs, basis}`) rendered below the 5 built-ins with add/rename/remove + a basis dropdown, wired end-to-end so a custom type behaves exactly like a built-in shelf/end-panel. **Non-obvious decisions:** (1) **`basis` reuses the cabinet's own W/H/D rather than per-instance dimensions** — three options `WD` (shelf-like = innerW×(D−T)), `HD` (side/partition-like = H×D, the default for gables/dividers/fillers), `WH` (back/face-like = W×H), mirroring the exact geometry the 5 built-ins already use, so material cost and cut-list sizes are correct with zero extra input. The basis→area and basis→cut-dims maps live in one place (`_extraPanelArea` / `_extraPanelCutDims` in `cabinet-calc.js`, shared by the metre-scale cost path and the mm-scale cut list). (2) **Per-cabinet quantities live on `line.extraPanels` keyed by stable `id`, not name** — renaming a type never orphans a line's quantity; a quantity whose type was deleted is silently ignored (only current `extraPanelTypes` are iterated). (3) **Folded into the existing "Shelves & Partitions" editor section and the `shelves` cost bucket** rather than a new section — `_extraPanelTotals()` returns raw `{matRaw, hrs}` injected before the existing markup/contingency multiplies so the section breakdown still sums to `calcCBLine`. (4) **Material = carcass `line.material`, no edge-banding** — matches partitions/end panels (only fixed+adj shelves add edging). New handlers `addCBExtraPanel` / `cbStepExtraPanel` / `cbUpdateExtraPanel` (`cabinet.js`) mirror the flat-field steppers but write the nested map; `cbSettings.extraPanelTypes` is absent for existing users and read everywhere as `|| []` (lazily created on first add). **Verified in preview:** an `HD` custom panel with `hrs = endPanel` rate is byte-identical to a built-in end panel (material 35.90, labour 1.2506 h, total 98.43 — `totalsMatch: true`); all three bases compute the right area; the cut list emits a sized row (720×320, qty 1); add/edit/remove persist to `pc_cq_settings`; the per-cabinet stepper raises labour by `hrs × contingency` and updates the live section total; `npm run typecheck` clean; no console errors; test data reverted afterward.
- 2026-06-21 — **Loose shelves + partitions were missing their material cost (priced as labour-only despite being real cut panels) — fixed.** Reported as "Loose Shelf missing from adjustable['s] specs": adjustable shelves got material cost but loose shelves did not, and the same gap hit partitions. Root cause: `calcCBLine` and `calcCBSections` summed shelf material as `(shelves + adjShelves) × shelfArea` and panel material as `endPanels × H×D` only — **loose shelves and partitions were omitted from material entirely** (they got labour + a cut-list part, so every cabinet with them was under-priced by their sheet material). The section comment even claimed "material + labour for **all** shelf/partition/end-panel kinds." Fix: loose shelves fold into the shelf-area term (they're the identical "Shelf" cut part as fixed/adj — same `innerW×(D−T)`), partitions fold into the H×D term beside end panels — mirroring exactly what `_cabinetPartsList` already cuts. Applied identically to **both** calc functions so the section breakdown still sums to `calcCBLine` (verified invariant). **Edge banding** also extended to loose shelves (front edge, same as fixed/adj — partitions/end panels stay unbanded as before). **Other "missing specs" swept:** (a) `_openCabinetPopup` (app.js) — a legacy quick-edit popup that lists doors/drawers/shelves/adj/partitions/end panels but **omitted Loose Shelf**; added the field + save (note: this popup is currently **unreferenced** — cabinets drill into the sidebar editor via `cbSelectLine`, which already had all five — but fixed for correctness if revived). (b) Library card caption (cabinet-render.js) listed only shelves; now also shows partitions / end panels / custom panels, matching the cabinet card. The live sidebar editor (desktop + mobile) already had Loose Shelf, so no change there. **Verified in preview** (Birch Ply 18mm): a loose shelf now costs material+edging **identical** to a fixed shelf (Δ £4.12), a partition identical to an end panel (Δ £5.57), both previously £0 material; section-sum invariant intact (£158.04 = £158.04); typecheck clean, no console errors.
- 2026-06-21 — **Live-link editable specs only exposed *fixed* shelves — added adjustable shelves, loose shelves, partitions & end panels (full stack).** The customer-facing quote page (`q.html` / `quote-public.js`) lets the maker unlock per-line specs the customer can request changes to; the Storage group only offered "Shelves" (`fixed_shelves`). Reported as "Loose Shelf … wasn't visible under editable specs on the live link page" (+ check for others). Added four spec keys — `adjShelves`/`looseShelves`/`partitions`/`endPanels` → columns `adj_shelves`/`loose_shelves`/`partitions`/`end_panels` — mirroring the existing `shelves`→`fixed_shelves` mapping across **three layers** that all key off the same `editable_specs` string array: (1) **`livelink.js _llSpecsFor`** — the business-side "Editable specs" checklist (with `used` badges from the line's counts; "Shelves" relabelled "Fixed shelves"); (2) **`quote-public.js specEditor`** — the customer-side number inputs (0–12) under a "Shelves & panels" group, plus the four columns added to the `toggleSpec` `originalValues` snapshot so Cancel reverts them; (3) **`quote-public-update` edge fn** — `SPEC_LABELS` (change-note text), the line `SELECT` (old values for the note), and the validation allowlist (`clamp(0,12)` + `allows(key)`, one guard each, mirroring `fixed_shelves`). **`quote-public-get` already returned all four columns** (lines 75/80) so no read-side change was needed. Reprice model unchanged: any spec edit nulls `customer_price` → "Price to confirm" → maker re-prices in-app, with a change-note dropped into the client chat. **Verified** business-side on QUO-1044 (real account): `_llSpecsFor` returns all 21 keys; enabling "request changes" renders the five storage options (Fixed/Adjustable/Loose shelves · Partitions · End panels) per line; toggle reverted, no `editable_specs` left set, no console errors; `npm run typecheck` clean. **Deploy required:** client ships via push→Cloudflare; the `quote-public-update` edge fn must be deployed (backward-compatible, additive) **before/with** the client push so a customer editing the new fields isn't silently ignored by the old allowlist.
- 2026-06-21 — **Live link "Auto-accept changes" — server-side re-pricing of customer spec edits.** Until now a customer dims/spec edit on `/q` nulled `customer_price` → "Price to confirm" → the maker re-priced in-app (the public page deliberately never receives cost inputs, and the costing engine ran only in the maker's browser). Added a Pro-gated Live-link toggle that, when on, prices the edit **server-side** so the customer sees the new price instantly and can accept/pay with no maker step. **Why it needed a real build, not a flag:** `calcCBLine` (~440 lines) isn't pure over `cbSettings` — `mp()`/`hwp()` do a **stock-first** lookup into the maker's stock library (`stockItems` + `_scGet`), so the server can't just reload `business_info`. **Approach — resolved-rate snapshot + thin port (chosen over a shared single engine to avoid destabilising the live, money-critical calculator):** (1) **`quotes.rate_card jsonb`** (migration `20260621160000`, service-role only, **never** selected by `quote-public-get`) holds a snapshot the maker's browser writes via **`_buildRateCard`** (`share.js`) — `matPerM2`/`hwUnit`/`finishPerM2` resolved through the SAME stock-first functions (extracted to module scope as `_matPricePerM2`/`_hwUnitPrice` in `cabinet-calc.js`, so resolution stays single-sourced) plus the scalars/type-arrays + quote markup/discount/stock_markup; written in `_generateShareLink` (resilient if the column isn't migrated yet) and refreshed on every Live-tab open (`_llSyncCustomerPrices` — the reconciliation point for rate drift). (2) **`supabase/functions/_shared/costing.ts`** — a statement-order-faithful Deno port of the geometry→cost math taking an explicit `rateCard` (only the math is duplicated; price resolution is not). `priceCabinetLine` returns null (→ "to confirm") for non-cabinet rows **and** when the snapshot doesn't cover a referenced rate, so a maker who adds a material after sharing can never under-charge. (3) **`quote-public-update`** action `edit` prices via the core when `auto_accept_edits` && `rate_card` && cabinet (quote-level wrapper taken fresh from the row), sets `customer_price`, posts an "auto-priced" chat note, and returns the figure; any failure → null + "to confirm" (never a guess). (4) **`quote-public.js`** `applyEdit` uses the echoed price (skips `_pending`) so price + totals update live; one summary toast worded per auto-priced vs awaiting-confirmation (also fixed `applyEdit`'s missing success-return that left the edit buffer uncleared). (5) **`livelink.js`** "Auto-accept changes" toggle under "Let customers request changes" (`data-needs="edit"`, default off, live caption swap). **Correctness backstop:** `costing.test.mjs` (`npm run test:costing`) captures golden customer prices from the LIVE browser engine against a real rate card and asserts the Deno port matches **to the penny** (6 diverse cabinets) + the null fallbacks. **Verified:** parity test green, `npm run typecheck` clean, toggle render + handler verified in preview. **Deploy (ordering matters):** apply the migration + regen types **first**, **then** deploy `quote-public-update` (it selects `rate_card`); the frontend is deploy-order-safe. Detail: `PLAN.md` → "Live link — auto-accept edits".
- 2026-06-22 — **Live link "Send live link" button split into "Send via email" + "Send via messages" (with optional copy to self).** The single mailto button on the quote/order Live-link panels (`livelink.js`) became two. **Send via email** is the unchanged pre-filled `mailto:`. **Send via messages** opens a new "Send live link via message" dialogue (`_sendLiveLinkMsg`/`_sendLiveLinkMsgConfirm`): an editable, link-prefilled message + a "Send me a copy" checkbox. **Key reuse decision:** sending just inserts a business `customer_messages` row (owner RLS, tagged `quote_id`/`order_id`) — which makes it appear on the customer's live page AND, via the existing `customer_messages → messages-notify` Resend bridge (sender='business' ⇒ emails the client), emails them the same text. So "it also emails the customer" is a free side effect of posting the message, not a second send path. **The copy needed new infra:** the bridge only emails the *customer* for business messages and there's no generic "email me" function (`send-test-email` is allowlisted to Adam's own addresses), so a new edge fn **`send-live-link-copy`** (JWT-verified, deployed v1) emails the business their copy — recipient resolved **server-side from the caller's JWT** (`business_info.email`, fallback auth email), **never** from the request body, so it can't be used as an open relay. **Other decisions:** (1) "Send via messages" requires a client on the record (the thread is client-scoped) and notes when the client has no email (message still posts to the live page); (2) the copy checkbox is disabled with a hint when the business has no email set; (3) the message carries the raw share URL as plain text — email clients auto-linkify it, in-chat it renders as a bubble. **Files:** `livelink.js` (buttons + dialogue + `_llBizEmail`/`_llSendCopyEmail`), `styles.css` (`.ll-copy-row`, `.ll-link-actions` flex-wrap for the 3rd button), new `supabase/functions/send-live-link-copy/index.ts` (reuses `_shared/auth.ts`+`cors.ts`). **Verified:** `npm run typecheck` clean; dialogue render confirmed in preview (link-prefilled message, customer-email note, copy checkbox); deployed fn health-checked (200 CORS preflight / 401 unauth). **Deploy:** edge fn already deployed to prod (verify_jwt on); client ships via push→Cloudflare. **Commit note:** the `livelink.js` changes were swept into `1bc5a32` (the concurrent auto-accept commit) by a parallel session's broad `git add`; the CSS + edge-fn source are in `11580e9` — so `git log -- src/livelink.js` attributes the buttons to the auto-accept commit.
- 2026-06-22 — **Fix: "Send via messages" never emailed the customer — the email↔messages bridge isn't applied in prod, so it now sends DIRECTLY.** Bug report: the customer didn't receive the live-link email. Root cause (confirmed via the Supabase MCP): prod `public.customer_messages` has only `id/user_id/client_id/quote_id/order_id/sender/body/read_at/created_at` — the **`email_message_bridge` migration was never applied**, so the `via`/`outbound_status`/etc. columns and the `trg_message_notify` trigger don't exist, and `messages-notify` is never fired. The previous entry's "posting a business message auto-emails the customer" assumption was therefore false in prod (the chat row inserts fine — that part worked — but no email ever goes out). **Fix:** replaced `send-live-link-copy` with a generalised **`send-live-link`** edge fn (JWT-verified) that emails the customer **directly** via Resend (and, if "Send me a copy" is ticked, the business too) — both recipients resolved **server-side**: the customer must be one of the caller's own `clients` (`clients.user_id = caller`), the copy goes to the caller's own business email; nothing is taken from request address fields (no open relay). The customer email is `from: "<biz> via ProCabinet" <messages@procabinet.app>` with **`reply_to` = the business email** (so replies reach the maker directly — no inbound-routing infra needed), and URLs in the body are linkified. The client (`_sendLiveLinkMsgConfirm`) **still inserts the chat row** because the live page reads it via `quote-messages` (which selects only `sender,body,created_at` — existing columns — so the link does show on `/q`); it then calls `send-live-link` for the actual email(s) and toasts per `{customer:'sent'|'skipped'|'failed', copy?}`. `send-live-link-copy` deleted from the project. **Verified:** `npm run typecheck` clean; `send-live-link` deployed via CLI + health-checked (200 preflight / 401 unauth); end-to-end Resend delivery not self-tested (preview was logged-out, no token) but uses the same verified-domain path as `messages-notify`/`send-welcome-email`. **Separate pre-existing bug noted (NOT fixed here):** the business-side chat in `clients-chat.js` selects `_CM_COLS` (incl. the missing `via`), so every load 400s → caught → the business sees an **empty** thread in prod; the live-page side is unaffected. The real long-term fix is to apply the `email_message_bridge` migration (adds the columns + trigger + inbound routing) — deferred as a larger, separate piece of work.
- 2026-06-22 — **Fixed that business-side empty-chat bug: `clients-chat.js` `_CM_COLS` is now `'*'`.** The explicit column list named the four bridge columns that don't exist in prod, so every thread `.select()` 400'd and the caller's `catch` left the thread empty — the business never saw customer replies (confirmed: client_id 24 had 13 unseen customer messages). Switched `_CM_COLS` to `'*'`, which returns whatever columns exist, so it works pre- and post-migration with no future edit; the bridge UI (`_ccEmailBadge`) already no-ops when `via` is absent, and the realtime merge only reads existing fields. **Verified:** `npm run typecheck` clean; prod has real conversations (client 24: 15 msgs) that now load instead of 400-ing; not preview-tested live (preview was logged-out). Supersedes the "deferred" note above for the *display* bug; applying the full `email_message_bridge` migration (for inbound-email replies) remains separate.
- 2026-06-22 — **Customer live link (`/q`) redesigned as a "document" matching the app's generated PDF, + customer-side PDF download.** The customer-facing page now reads like the maker's invoice/quote PDF (`_buildOrderDocPDF`): Helvetica, heavy `#111` header rule, `PREPARED FOR / ISSUED` block, **grouped** line-item table (`CABINETS`/`LABOUR`/…), red discount, and a **black total pill** — replacing the prior gold/SaaS card-and-rail look (the old design read "very AI"). Approved via 3-then-2 static mockups (`livelink-redesign-mockups.html`); the final is **one identity at two breakpoints** — desktop = document sheet + sticky monochrome checkout rail; mobile (≤880px) = same document edge-to-edge with a **sticky bottom pay bar** (Qty/Price columns drop, Amount stays). The existing **floating Message launcher kept** (restyled gold→dark pill; on mobile it sits above the pay bar). **`q.html`:** the page palette is pinned on `body.qp` (var overrides → always light "paper", ignores OS dark mode, Helvetica); kept overlays (pay/confirm sheet, spec editor, photo-viewer lightbox, chat, toast, state cards) restyled to the doc; **removed `#qp-top`/`.qp-wrap`** (brand now lives in the document header). **`quote-public.js`:** `renderTop()` deleted; new building blocks `docHead()/addrBlock()/greet()/itemsHtml()/closingHtml()` + `docTotals()/ctaState()/ctaHtml()/rail()/actionBar()/updateSummaries()`; `row()` is now a table row with an **include checkbox** for optional items (was an iOS-style toggle) and Qty/Price/Amount columns; success/error states carry a `stateBrand()` line (replacing the removed top bar). Every handler preserved — toggle (now syncs **all three** summary surfaces via `updateSummaries()`), spec edit + edit-mode CTA, accept/pay (Stripe), chat, photo lightbox. **PDF download** (`__qp.downloadPdf()` in the rail + mobile bar): lazy `import('jspdf')` (stays **code-split** — not in first paint) → `buildQuotePdf()` redraws the document with jsPDF, mirroring `_buildOrderDocPDF` from the **public payload only**. **Decisions:** (1) **Logo on the page + in the PDF** (added 2026-06-22 after first shipping name-only): the document header shows `business.logo_url` as an `<img>` (caption mode — logo over the name + contact); `buildQuotePdf()` is now async and embeds it via `loadImageDataUrl()` — a crossOrigin `<canvas>`→PNG data URL — sized to ≤40×18mm, with a **graceful name-banner fallback** if the canvas is CORS-tainted (Supabase public buckets send `ACAO:*`, so it normally embeds). (2) **No "Made with ProCabinet.app" branding** on the customer page or PDF — `quote-public-get` exposes no tier flag, so the page can't tell free from Pro; stamping a paying maker's customer document is undesirable. Footer = business name + date + "Secured by Stripe". (3) **PREPARED FOR shows the customer's full details** — name + address/phone/email (added 2026-06-22; was name-only at first launch). `quote-public-get` now `select`s `email, phone, address` from the *single* client tied to the deal (additive, **needs deploy**) — it's the customer's own info on their own link, like a printed invoice's BILL TO. No project column (quotes aren't tied to a project); right column shows Issued/Created + "Valid until" from `settings.expires_at`. (4) Discount shown back as a line (page line prices are post-discount; `preDiscount = subtotal/(1−d)`), matching the prior rail logic. **Dev aid:** `/q.html?mock=1` under `npm run dev` renders the doc from an inline sample payload (DEV-gated, dead-code-eliminated from the prod build) so the layout/PDF can be worked on without a live token. **Verified (mock):** desktop + mobile render; optional toggle updates doc pill + rail pill in sync (£4,032→£6,612); spec-editor + Send-edits/Cancel edit mode; chat opens; **valid 9.6 KB `application/pdf` generated** with no errors; `npm run typecheck` + `npm run build` clean (jsPDF lazy chunk confirmed). **Not yet verified — needs a live token post-deploy:** edge-fn round-trips (toggle/edit persistence, real line photos), the Stripe pay sheet, and the redirect-payment outcome states. **Follow-ups (2026-06-22, same day, by request):** (a) **logo added** to the page header + PDF (`loadImageDataUrl` crossOrigin-canvas, name-banner fallback), then **doubled** — page `.qpd-logo` ≤330×112px (≤260×88 mobile), PDF caption logo ≤76×34mm; (b) **customer details restored** to PREPARED FOR (see decision 3 — `quote-public-get` change, **must deploy**); (c) **logo upload cap raised 500KB→2MB** in `business.js handleLogoUpload` (the `image-upload` edge fn already hard-caps at 16MB; the localStorage data-URL cache write is now `try`-guarded so a large logo can't break the upload). **Deploy:** client via push→Cloudflare **+ deploy `quote-public-get`** (additive column select — back/forward compatible, so order doesn't matter; until deployed the page/PDF just show the customer name with no contact lines).
- 2026-06-22 — **Email ↔ messages bridge ACTIVATED in prod (outbound notifications + inbound email replies).** The `email_message_bridge` migration (written 2026-06-19) had never been applied and both bridge functions were undeployed — so chat messages emailed nobody. Activated the whole thing: applied the migration (`clients.reply_token`, `customer_messages.via/email_verified/inbound_email_id/outbound_*`, `inbound_emails` table, `business_info.email_bridge_enabled`, realtime publication, the `trg_message_notify` AFTER-INSERT trigger), deployed `messages-notify` + `messages-inbound` (`--no-verify-jwt` — pg_net carries no JWT so notify uses a static `x-msg-key`; Resend carries a Svix sig so inbound verifies that). Now every `customer_messages` insert emails the opposite party via the trigger → `messages-notify` (pg_net), and email replies to `c-/b-<token>@reply.procabinet.app` flow back via the `messages-inbound` Resend webhook as `via='email'` rows (the trigger skips them → no loop). **Resend (via MCP):** registered `reply.procabinet.app` receiving-only (eu-west-1) + inbound webhook → `messages-inbound` (email.received/delivered/bounced/complained); signing secret stored as the `RESEND_WEBHOOK_SECRET` edge secret. **Cloudflare DNS (founder):** MX `reply → inbound-smtp.eu-west-1.amazonaws.com` (pri 10, verified) + DKIM TXT `resend._domainkey.reply`. **`send-live-link` reverted to copy-only:** the trigger now emails the customer for the "Send via messages" business message, so the function no longer emails the customer (would double) — it only sends the optional "Send me a copy" to the business, and `_sendLiveLinkMsgConfirm` calls it only when the box is ticked (toast infers the customer-email status from the client having an email). Supersedes the earlier 2026-06-22 "send the customer email directly (bridge not in prod)" entry. **MCP fix that unblocked the Resend work:** `.mcp.json` overrode the inherited `RESEND_API_KEY` with an unexpanded `${RESEND_API_KEY}` → the Resend MCP got a junk key; removed the override so `resend-mcp` inherits the real exported key (file is git-tracked → no hardcode). Needed a Claude Code restart. **Verified end-to-end:** a test business message to the self-client (id 24, `adam.denney@hotmail.com`) reached `outbound_status='delivered'` with a Resend id — proving trigger → notify → Resend → delivered AND that the inbound webhook + Svix verification work (the `email.delivered` event updated the row through `messages-inbound`). A real inbound *reply* parse wasn't self-tested (needs an actual reply) but rides the same verified webhook path. `npm run typecheck` clean.
- 2026-06-22 — **Fix: live-link auto-accept silently rejected every customer finish/material change — `catalog_items` is a dead table, so the public edge fns now source finish/material names from the quote's `rate_card` snapshot.** Bug report: "auto accept edits doesn't work on the livelink." Root cause (confirmed via the Supabase MCP): `public.catalog_items` is **empty across the entire prod DB (0 rows, 0 users)** — its only writer, `business.js _syncCatalogToDB`, is **defined but never called**; the catalogue actually lives in `cbSettings` (→ the `rate_card` snapshot) and `stock_items`. But both public functions still treated `catalog_items` as the source of truth: `quote-public-get` built the customer's **finish** dropdown from it (empty → only the line's own finish selectable), and `quote-public-update`'s `catNames()` validated the customer's pick against it (empty set → `422 finish_not_allowed` / `material_not_allowed`). So any finish/material edit was rejected *before* pricing ran; worse, `quote-public-get` *offered* materials from `stock_items` that `quote-public-update` then always rejected. Only dims/count/shelf edits (no name validation) auto-priced — which is why it "sometimes" worked. **Fix:** the `rate_card` snapshot is the authoritative, **guaranteed-priceable** name set (built in `share.js _buildRateCard` from `cbSettings` + customer-visible stock + line names), so both functions now read it. (1) `_buildRateCard` additionally stores typed `materialNames` / `finishNames` arrays (materials = `cbSettings.materials` + `customer_visible` stock + line mats; finishes = `cbSettings.finishes` + line finishes, minus `'None'`). (2) `quote-public-update` replaces `catNames` with `allowedNames(kind)` → prefers `rate_card.{materialNames,finishNames}`, falls back to the snapshot's cost-map keys (`matPerM2`/`finishPerM2` — already keys every priceable name, so **existing shared quotes are unblocked immediately, no re-share**), then to `catalog_items` for pre-`rate_card` quotes; `rateCard` is now read once at the top of the edit block and reused by the auto-accept re-price. (3) `quote-public-get` selects `rate_card` and offers `finishes`/`materials` from the typed lists when present (else the legacy catalog_items+stock fallback). (4) `_shared/costing.ts` `RateCard` gains optional `materialNames?`/`finishNames?` (unused by the math → costing parity unchanged). **Result:** material edits work on existing quotes the moment the fns deploy (validation falls back to the cost-map keys); finish dropdowns fully populate once the maker next opens the Live-link tab or re-shares (`_llSyncCustomerPrices` rewrites `rate_card`, now with the typed lists). **Files:** `src/share.js`, `supabase/functions/_shared/costing.ts`, `supabase/functions/quote-public-{get,update}/index.ts`. **Verified:** `npm run typecheck` clean; `npm run test:costing` 3/3 (parity to the penny). **NOT yet deployed** — `quote-public-get` + `quote-public-update` are a real-money path; edge deploy held for explicit confirmation. **Dead code noted (not removed here):** `business.js _syncCatalogToDB` + the `catalog_items` table are now fully unused by the live-link path.
- 2026-06-24 — **Stock Library: delete (row + sidebar), bulk select/actions, and sortable lists incl. manual drag-and-drop.** Three user-requested additions to `src/stock.js` + `styles.css` (`index.html` for the sidebar button). (1) **Delete** — a trash icon per row (next to Duplicate) and a "Delete Material" button in the sidebar editor (shown only in edit mode, hidden in "Add" mode); both route through `_confirm`-gated `deleteStockItem(id)` → the existing `removeStock` (now surfaces DB errors via toast, toasts success, and returns the editor to Add mode if the open item was deleted). (2) **Bulk** — a checkbox per row + a select-all checkbox per category header (rendered only when the section is expanded); when any row is selected a toolbar shows above the lists with **Delete** (one confirm, `.in('id', ids)`), **Show/Hide to customers** (`customer_visible` via one `.in()` update), and **Clear**. Selection is in-memory (`window._stockSelected` Set), cleared on reload. (3) **Sort** — a dropdown in the filter bar (Low-stock-first [default] · Name A–Z/Z–A · Qty · Value · **Manual**), preference saved to `localStorage pc_stock_sort`; sorting is applied to the filtered list so it orders **within** each category group. **Manual** mode makes rows `draggable` with a drag handle (reuses cutlist's `DRAG_HANDLE`) and reorders within a category, persisting via a new **`stock_items.sort_order int`** column (migration `20260624120000_add_stock_sort_order.sql`, applied to prod; types hand-edited in `database.types.ts`). Order persistence follows the established **DB-column-first + localStorage-fallback** dual-write (`_soGet`/`_soSet`/`_soMap` via `_stockUpdateCols`, key `pc_stock_order`), so it syncs across devices when logged in and still works for local/guest mode; `_stockManualReorder` rewrites a clean `0..n` sequence for the whole group on each drop. **Verified** in preview (logged-out guest data): all sort modes order correctly, manual rows draggable + handles render, a programmatic reorder moved an item to the front and persisted to the fallback map; `npm run typecheck` clean; no console errors. **Deploy:** the migration is already applied to prod (additive nullable column, backward-compatible); client ships via push→Cloudflare.
- 2026-06-24 — **R.5 — deprecated `catalog_items` table fully retired (client + edge fns + table dropped).** Follow-through on the 2026-06-22 bug diagnosis (the live-link finish/material rejection traced to the dead `catalog_items` fallback). Done in two passes. **Pass 1 (client dead-code, commit `efe651a`):** removed `_applyCatalogFromDB` (`app.js`), `_syncCatalogToDB` + `_catalogSyncTimer` (`business.js`), and the `catalog_items` early-boot prefetch (the unused `cat` slot in `app.js loadAllData` + the `_ebGet` prefetch in `main.js`) — all written/fetched but never read. **Pass 2 (table retirement):** re-audited prod — `catalog_items` was **empty (0 rows / 0 makers)**, so the edge-fn fallback for the 12 shared pre-`rate_card` quotes was already a no-op (their dropdowns come from `stock_items`); **no `rate_card` backfill needed**. Removed the `catalog_items` fallback branches from `quote-public-get` (finish + material name lists; material list still merges customer-visible stock) and `quote-public-update` (`allowedNames` now returns the snapshot's typed lists / cost-map keys, else an empty set — identical behaviour to the empty table), redeployed both `--no-verify-jwt` (get v31, update v27), smoke-tested (bogus token → clean 404). Removed `_migrateCatalog` + its orchestrator entry (`migrate.js`) and the `catalog_items: []` demo seed (`demo.js`). Re-confirmed 0 rows / 0 inbound FKs / 0 dependent views, then `drop table public.catalog_items` (the 4 RLS policies drop with it) — migration `20260624130000_drop_deprecated_catalog_items.sql` — and regenerated `database.types.ts`. **Kept:** `cbSettings.materials/hardware/finishes` (the live in-memory pricing fallback in `cabinet-calc.js`) and the `rate_card` snapshot path — those are unrelated to the dropped table. `npm run typecheck` + `npm run build` clean. PLAN.md R.5 marked ✅.
- 2026-06-25 — **Meta Conversions API: server-side signup `CompleteRegistration` (closes the two Events Manager pixel recommendations).** Events Manager was flagging weak event-match-quality + "add web-only Conversions API" on the Signups pixel (`1913344152250764`). Root cause was NOT a missing function — **the signup server CAPI already existed in prod and was wrongly assumed absent** (the repo had no source, so a grep found "nothing"). Reality: trigger `trg_meta_capi_signup` on `auth.users` → `notify_meta_capi_signup()` → pg_net POST `{user_id}` → deployed `meta-capi-signup` fn (v11), firing for **every** signup incl. Google OAuth, with a 1-hour freshness guard, matching on SHA-256 email + an `fbc` reconstructed from the stored `fbclid` (`fb.1.<ts>.<fbclid>`). So the only real gap was the higher-quality signals a DB trigger can't see. **Merge (not rebuild)** — `supabase/functions/meta-capi-signup/index.ts` brings v11 into the repo and **extends** it: preserves the trigger's `{user_id}` path (email + reconstructed fbc, OAuth coverage, freshness guard, UUID validation), and now ALSO accepts `{fbc,fbp,event_source_url}` from a browser caller + reads `client_ip_address`/`client_user_agent` from headers (real `_fbc` overrides the reconstructed one). Same `event_id signup-<user_id>` dedupes the browser pixel + both server callers (Meta keeps the richest match). No-ops without `META_CAPI_ACCESS_TOKEN` (the same secret the stripe-webhook Purchase CAPI uses). **Client wiring** (`src/analytics.js`): `_trackSignupConversion()` now also `fetch`-POSTs the function fire-and-forget (`keepalive`, no-op without `window._SBURL`/`_SBKEY` or a user id), plus a `_readCookie()` helper for `_fbc`/`_fbp`; comments corrected to describe the dual server path (the first-draft "client-initiated, not a trigger" framing was wrong — both fire, and the client call is purely the match-quality boost on top of the trigger). **Deploy:** `--no-verify-jwt` (fires pre-email-confirmation, no session JWT). `npm run typecheck` clean. **NOT yet deployed / verified** — needs the edge deploy + `META_CAPI_ACCESS_TOKEN` confirmed set + an Events Manager check (deduped event with a Processed server copy + rising EMQ). **Follow-up (same commit-set): Purchase CAPI match quality.** `sendMetaPurchase` (`stripe-webhook`) matched on hashed email only (a `// NOTE` flagged it). Now the client (`src/stripe.js startCheckout`) forwards `_fbc`/`_fbp` to `stripe-checkout`, which stamps them into the Checkout **session metadata** (founder + subscription paths, ≤400 chars, omitted when empty), and `sendMetaPurchase` reads `session.metadata.fbc/fbp` into `user_data`. No client IP/UA on this one — Stripe, not the browser, calls the webhook. `_readCookie` added to `globals.d.ts` (now cross-file). `META_CAPI_ACCESS_TOKEN` confirmed set in prod (digest seen via `supabase secrets list`).
- 2026-07-03 — **SEO + AEO build: technical fixes + `/blog` pipeline + 5 launch articles (PLAN C.4 ✅).** Live-site audit found the site invisible to AI assistants and leaky to Google: Cloudflare 403-blocks every AI crawler (GPTBot/ClaudeBot/PerplexityBot/ChatGPT-User/Claude-User; Googlebot fine), no robots.txt or sitemap (both fell through to HTML, with Cloudflare's managed content-signals prepended onto the robots response), soft-404s (any unknown path → 200 + landing = duplicate content), no canonicals anywhere (`/landing.html` deployed as a byte-identical dup of `/`), relative og:image, no Organization/SoftwareApplication schema, and `/os` + `/q` (private customer quotes!) indexable. **Decisions:** allow ALL AI crawlers incl. training bots (public pages are marketing copy; the app is behind auth) · blog lives at `/blog` in-repo (markdown → static HTML at build) · canonical founder bio = "ran his own cabinetry workshop for over 10 years" (brand-voice.md's "14 years" corrected). **Code:** new `robots.txt` (allow-all + `Content-Signal: search=yes, ai-input=yes, ai-train=yes` + sitemap pointer; deliberately NO Disallow on `/os`//`/q` — crawlers must fetch them to see the noindex), `llms.txt` (llmstxt.org format; blog section auto-appended at build), branded `404.html` (its presence flips Cloudflare Pages off SPA-fallback → real 404s; all prod routes verified as real files, ads audited — all campaigns paused, sole final URL is `/`), canonicals + og:url/site_name + absolute og:image on landing + legal pages, `noindex` meta on `index.html`(app)+`q.html` with belt-and-braces `X-Robots-Tag` in `_headers` (plus missing must-revalidate rules for `/q`, `/payment-fees`), landing gets an `@graph` JSON-LD (Organization `#org` + WebSite + SoftwareApplication with the 4 real offers at on-page launch prices, NO aggregateRating — update in the same commit as any pricing change). **Blog:** `scripts/blog.mjs` (single source of truth: flat-frontmatter parser (fail-loud), marked (devDep) → HTML, FAQ auto-extraction from `## Common questions` → FAQPage JSON-LD, BlogPosting+Person+ItemList schema, byline box, one CTA with `utm_campaign=<slug>`, same PostHog/GA4/Meta wiring as landing with empty-env no-op in dev) + `blog.css` (privacy.html's document look) + `blogPlugin(env)` in vite.config (closeBundle writes `dist/blog/**`; dev middleware renders the same templates live) + `seoFilesPlugin()` (copies root SEO files, appends blog list to llms.txt, generates `sitemap.xml` from `loadPosts()` — `/os`/`/q`/`/landing.html` can never enter it). **Articles (`content/blog/`):** best-cut-list-software (comparison, web-verified competitor facts "as of July 2026", ItemList), how-to-price-cabinet-jobs (formula + worked kitchen table), reduce-plywood-waste-nesting, how-i-stopped-underpricing-my-work (E-E-A-T anchor, pure founder story), cabinet-software-or-spreadsheets — all first-person Adam, British English, no fabricated stats, written+adversarially verified+cross-checked by a multi-agent workflow. **Tests:** `tests/e2e/smoke.blog.spec.js` (read-only: index renders, first post has byline/FAQ, no pageerrors). **Manual steps pending (ordered checklist in `docs/SEO.md`):** Cloudflare AI Crawl Control → allow all + "Block AI bots" off + managed robots.txt OFF (else it keeps prepending `ai-train=no`), GSC domain-verify via Cloudflare TXT + submit sitemap, Bing import. Until those flip, AI crawlers keep getting 403s regardless of this deploy.
- 2026-07-03 — **Refactor: file-size completion pass — every source file back under the SPEC § 7 1,500-line target (backlog R.2 + R.4, tracked as R.6).** Phase E + R.1 never finished the <1,500 cap and `app.js`/`cutlist.js` had regrown. 12 mechanical carve commits, each verified (`npm run typecheck` src-clean + `npm run build` + Playwright public boot smoke; the R.4 commit also ran the signed-in smoke) and individually bisectable (app boots green after every one). **Design:** three plan agents + an adversarial integration review cross-checked the whole set against the tree before any edit — verified no symbol claimed by two target files, all insertions/zero tag reorders, and that the only hard load-order constraint (app.js's parse-time boot block calls into the carved render/editor fns, so every new file loads before app.js) holds for all slots. **`cutlist.js` 5,233 → 923** — `cutlist-pdf.js` (print/PDF + 5 doc builders), `cutlist-layout.js` (guillotine + nested packers + canvas), `cutlist-edge.js` (edge-band UI + column visibility), `cutlist-render.js` (sheet/piece CRUD, keyboard nav, CSV, library view); residual keeps module state, state machine, DXF export, CRUD orchestration. **`app.js` 2,274 → 348** (now purely bootstrap/orchestration: `_requireAuth`, `_userId`, `loadAllData`, `onAuthStateChange`, boot block, beforeunload) — line editors + stock/cabinet popups → new `line-editor.js`; auth screens/session flows → `auth.js`; realtime status sync → `livelink.js`; `_applyBizInfoFromDB` → `business.js`; card pipeline-hover + `trunc` → `ui.js`; landing `?plan=` pending-plan stash → `stripe.js`; dead `hexRgba` deleted. **`quotes.js` 1,939 → 787** — sidebar editor + print/doc dispatch → new `quote-editor.js` (`duplicateQuote` stays as lifecycle CRUD). **`stock.js` 1,554 → 1,399** — the localStorage-map/DB dual-write persistence layer (`_stockUpdateCols` + `_sc*/_ss*/_sv*/_so*/_oq*/_on*`) → new `stock-persist.js`; the two supplier UI actions stay. **R.4:** `let clients` moved from `stock.js` to the top of `clients.js` (its conceptual home); `projects` stays (legacy). TDZ audit: clients.js loads after app.js, but every parse-time reach for `clients` in earlier files sits behind a null-drill short-circuit (`renderQuoteMain`/`renderOrdersMain` map empty boot arrays; `_qpState`/`_opState.clientId` null), so its binding is never accessed while in TDZ; clients.js's own trailing `renderDashboard()` init runs after the declaration. Verified via the signed-in smoke (dashboard + every section incl. Clients render without throwing). **Classic-script carve, not ES modules** (per the standing 2026-04-30 deviation): new files are `<script defer>` tags in index.html (vite auto-discovers `src/*.js`); cross-file symbols resolve through the shared global lexical environment; no `globals.d.ts` additions needed. Seven new files; 47 classic-script tags total. `cabinet.js` (1,884) left as-is — residual accepted in R.1. Deferred follow-ups logged in PLAN.md: R.3 shadow-name unification; dead-code sweep (`_smartClientSuggest`/`updateQuoteField`/`deductStockFromCutList`/`quoteFromCutList` rode along with the quote-editor carve, no live callers); the inert `window._qpState/_opState/_cbDirty` beforeunload guard.
- 2026-07-03 — **Cleared the three deferred follow-ups from the file-size pass (dead-code sweep · unsaved-changes guards · R.3 shadow-name unification).** (1) **Dead-code sweep** — removed five functions with no live callers repo-wide that rode along with the quote-editor carve: `_smartClientSuggest` (superseded by `_smartQClientSuggest`), `updateQuoteField` + `_qFieldDebounceTimers` (old aggregate quote-field editor), `deductStockFromCutList` / `quoteFromCutList` (legacy cut-list→quote bridges); quote-editor.js 1,189 → 1,100. (2) **Unsaved-changes guards now actually fire** — the `beforeunload` guard (app.js) and the pre-walkthrough confirm (walkthrough.js) read the dirty flags as `window._cbDirty/_clDirty/_qpState/_opState`, but those are top-level `let` globals in cabinet.js/cutlist.js/line-editor.js and never attach to window, so both dirty checks were silently inert (only the genuine `window._saveInFlight` Set worked). Switched to the bare globals; dropped the now-unused window-prop declarations from globals.d.ts (kept `_saveInFlight`). The flags already have a set-on-edit/clear-on-save lifecycle (they power the in-app "Discard unsaved changes?" dialogs), so no false positives. Verified in a headless browser: the guard blocks for each dirty surface (quote/order/cabinet/cut-list) + in-flight save, and stays silent when clean. (3) **R.3 — cut-list shadow-name unification** — the "~30 sites" note was stale: dimension consumers had already migrated to the `thickness_mm/width_mm/length_m` columns via `_svGet`/`_svSet`, leaving the short-name shadow (`thickness/width/length` on `stockItems`) as a redundant parallel copy hydrated from the `_mm` columns by a load-time map in `loadAllData` (+ a twin in the demo overlay). Removed both hydration maps, the shadow intersection type on `stockItems`, and every shadow read/write — redundant `?? item.thickness` fallbacks after `vd.X ??` (kept the real `item.h`/`item.w` fallbacks) in edit-populate / stock-list display / cabinet-library edge picker; the one shadow read without a `_svGet` fallback (edge-picker meta) now reads `item.thickness_mm` directly; and shadow writes in the seed loader, duplicate, add-new, both edit-save paths, and the +Add-new-stock popup (each already had an adjacent `_svSet`). `thick` was already dead. Dropping the intersection type made any missed shadow read a typecheck error — none surfaced. Verified end-to-end in a headless browser: an edge-band row's dims flow through `_svGet` (from the `_mm`/`_m` columns), render correctly in the stock list (`22mm × 50m`, `2mm`), and feed the cut-list add path, with no shadow properties on the row. All three: `npm run typecheck` src-clean + `npm run build` + full Playwright smoke green. PLAN.md R.3 marked ✅.
- 2026-07-03 — **Cabinet Builder "edit in place" UI rework (Variant 2, commits `e2d4e32` + `525a73b`).** User picked from three mockups. **Part A (editor sidebar):** `renderCBEditor` went from 8 always-expanded accordion sections to a pinned sticky header (name + W/H/D/Qty grid + live total), six section rows (Box / Doors / Drawers / Interior / Hardware / Notes & Extras) whose common controls live inline in the header (count steppers, `.cb-sec-pill` type selects, carcass-material smart input, `.cb-chip` mini-steppers), and a sticky action footer (Duplicate / To Library / Delete; library-mode variant Add-to-Quote / Duplicate / Delete). Sections start **collapsed** — bodies keep the long tail (finishes, coverage sliders, hardware lists, extras). Section keys: `drawerFronts`+`drawerBoxes` merged into `drawers` (calcCBSections already had the combined bucket), Extras folded into `notes`, new `hw` section groups all three hardware scopes. `_refreshCBLiveCosts` rewritten for the new badge ids + chip counts + header/footer totals. Qty is now editable in the editor (clamped ≥1 in `cbUpdateField`). All smart-suggest id contracts preserved — zero `cabinet-library.js` changes. **Part B (results panel):** per-cabinet detail cards replaced by a quote-editor-style line table (`.cb-li-table`: Cabinet | Size | Qty | Each | Total + hover actions incl. the intact `._cbct-btn` cut-list widget); totals moved to a right-aligned `.pf-totals` block with the All-Cabinets card's math verbatim (markup/tax/discount/contingency parity); new pinned footer bar `#cb-results-footer` (index.html flex sibling — pins without `position:sticky`) carries counts + total + context action (order → "Back to order", quote → "Go to Quote", standalone → Send to Quote/Order). Footer explicitly hidden on all three empty-state returns and on Library switch (stale-total guard). **Key decisions:** cloned the table skin as `.cb-li-*` instead of reusing `.editor-li-table` (its ≤760px block force-converts rows into quote-specific editable cards); row Total = pre-markup `lineSubtotal` so rows sum to the Subtotal; mobile ≤760px turns rows into a 2-row grid (name+total / qty+actions, actions always visible). Selector migrations: `walkthrough.js` preClickCard + `_cbRevealSelectedCabinet` → `.cb-li-row`; `cbEditCabinetFromOutput` now re-renders results (highlight follows selection) and scrolls the editor to its TOP. Dead CSS purged (`.cb-section-*`, `.cb-cab-*` card/sections, `.cb-summary*`, `.cb-grand-total` + media refs). Verified: typing focus retention, sticky header/footer in the nested-scroller sidebar, suggest dropdown from the sticky header, library round-trip, totals parity (£99/+£20/£119), mobile 375px both panes, dark-mode-safe tokens, e2e smoke 4/4. **Follow-up:** wiki clips for build-and-price-a-cabinet + set-up-your-rates reference `.cb-cab-card` in their drive scripts — update to `.cb-li-row` when re-recording.
- 2026-07-03 (later) — **Cabinet Builder editor v2: always-open spec cards (commit `09d8f0a`).** The morning's edit-in-place editor (collapsed sections + inline header controls) didn't meet the real goal — the user wants the ENTIRE spec visible with nothing hidden. After two mockup rounds injected into the live app (3 layout designs, then 3 card-grid variants), the user picked "3A with the styling and gaps from My Rates": a 2-col grid of always-open cards styled like the My Rates section boxes, with the ORIGINAL section groupings restored (Cabinet incl. dims/hardware, Doors, Drawer Fronts, Drawer Boxes, Shelves & Partitions, Extras, Notes — the v1 Drawers merge and Notes-absorbs-Extras are gone). Real controls throughout (smart inputs, rates-scale selects, steppers, pct sliders); hardware = always-visible chips + tap-to-expand hwListUI editor (`cbHwEditOpen`/`cbToggleHwEdit`); card badges include their section's hardware bucket so cards sum to the unit total. Removed `cbOpenSections`/`toggleCBSection`/`cbExpandAll`/`cbCollapseAll` + `.cb-sec-*`/`.cb-chip` CSS. **Gotcha recorded:** rules inside the early ≤760px media block lose same-specificity ties to base rules later in the file — the editor's mobile overrides are scoped `#cb-cab-editor …` to win. Mockup workflow note: designs were prototyped by injecting styled DOM into the running app via the preview (real stylesheet/data), with a switcher bar to flip variants live — far higher fidelity than standalone mockups. Results-table (Part B) untouched.
- 2026-07-03 (later) — **Hardware on every physical-component card: 2 new hardware buckets (commit `536e0b1`).** Cabinet Builder now shows a Hardware line on all five component cards. Added scopes `shelf`→`shelfHwItems` and `drawerFront`→`drawerFrontHwItems` alongside cabinet/door/drawer. **Cross-stack, parity-verified** (`npm run test:costing` penny-exact, fixture extended with shelf+drawer-front hardware 613.45→643.69): migration `add_shelf_drawerfront_hardware_to_lines` adds additive jsonb `shelf_hardware`/`drawer_front_hardware` to `quote_lines`+`order_lines` (default '[]', **applied to prod**); `cbDefaultLine`/`_hwListKey` (cabinet.js); `calcCBSections` new buckets into `hardware` + `calcCBLine.hwCost` sums all 5 scopes (cabinet-calc.js); server `_shared/costing.ts` reads the 2 columns, sums in hwCost, and `coversLine()` validates them; `_cbLineToRow`/`_cbRowToLine` round-trip (shared mapper → quotes+orders); library templates round-trip free via the `default_specs` jsonb blob; `share.js` snapshots the new hardware names for live-link rate cards; CSV export/import extended; editor `hwLine('shelf')`/`hwLine('drawerFront')` + badges + `_refreshCBLiveCosts`; types regen; SCHEMA.md updated. **Deploy note (memory: migration→edge fn→frontend):** migration applied; the quote-pay/quote-pay-webhook edge fns (which import `_shared/costing.ts`) must be redeployed before the frontend push so live-link customer edits price the new hardware — held for explicit go-ahead. Verified end-to-end in preview: all 5 cards show hardware, adds price into the quote total and persist across reload with no PGRST errors.
- 2026-07-05 — **Cabinet Builder results-row polish + selected-row expanded view (several small commits).** A run of interactive UI fixes on the `.cb-li-table` results panel: (1) the selected/`.editing` row's amber highlight — the fix converged through three wrong guesses to the real cause. In the compact **card layout** (`@container cbres (max-width:660px)`) the row is a CSS grid with gaps and BOTH the `tr` and each `td` carried the translucent `--accent-dim`; because it's semi-transparent the cells double-painted over the row, so name/qty/total/actions each rendered as a *darker* amber box ("localised highlights of each section"). Fix: in card mode carry the highlight on the ROW only and force every cell transparent; the inner qty-stepper/cut-list-pill controls (surface2 fills, teal count/add chips, borders) are also flattened to transparent so only the row highlight shows. Desktop table mode never had it (cells butt together, no gaps, no row-level bg). (2) Zebra striping restored in the card layout, painted on the `tr` (not cells) so it composes with hover/editing without reintroducing the double-paint; all row-bg rules scoped to `.cb-li-row` so the expand row is exempt. (3) Results `.pf-totals` promoted to a white card (`--surface`/`--border`/`--radius`/`--shadow`, scoped `#cb-results`) — the base surface2 fill vanished against the grey results panel. (4) **New: selected cabinet shows an expanded view.** For the active row, `renderCBResults` emits a sibling `<tr class="cb-li-xrow">` (colspan 6, amber-tinted to merge with the `.editing` row) holding a two-panel `.cb-li-expand`: **Specification** (dims, carcass·finish, back panel, construction, doors, drawers, interior parts, hardware across all 5 scopes, room, notes — each row omitted when empty) + **Cost breakdown** (per-section subtotals from `calcCBSections` mirroring the editor badges, a Unit-cost sub-row, a ×qty Line-total when qty>1, and a Materials/Labour-hrs/Hardware note). **The cost-breakdown panel was removed immediately after per user request — the expanded view is now spec-only**, its key/value list flowing into as many columns as the panel width allows (`repeat(auto-fill, minmax(240px,1fr))`, so 2 cols on desktop, 1 on a phone). The expand `<tr>` shifts `nth-child` parity for rows below the selection (accepted — zebra is ~0.024 alpha, imperceptible). Verified live in both layouts (typecheck clean); test-only door/drawer/shelf edits reverted off the e2e quote.
- 2026-07-05 (later) — **Results row: dims moved under the name, Size column dropped.** Per user request the desktop row's material sub-line (`.cb-li-sub-desktop`, was `line.material`) now shows the dimensions, and the redundant standalone **Size** column (`.cb-col-size` th + td) was removed entirely — dims previously showed in both places once the sub-line changed. Material still lives in the expanded spec view + the mobile sub-line (`dims · material`, unchanged). Expand-row colspan 6→5; the container-query card breakpoint dropped 660→560px since the table now needs ~112px less width. `.cb-col-size` CSS removed. typecheck clean; verified in both table and card layouts.
- 2026-07-05 (later) — **"Add to Library" moved to the results row; actions row restyled as buttons.** Per user request the `Add to Library` action moved out of the editor footer (`.cb-ed-foot` now just Duplicate / Delete) onto each results row. It sits as a **text** button to the **left of the cut-list** widget in `.cb-li-actions`, and the whole actions row was restyled to read as a set of buttons like the original footer — shared `.cb-act-btn` chrome (white `--surface` fill, `--border`, rounded; text for Add-to-Library, 28px squares for the ⧉/× icons). The cut-list pill's earlier flat/transparent treatment (from the highlight fix) was changed to a **white** button surface + border so it matches — white (not the old surface2 grey, which read as a highlight on the amber selected row); its inner count/add chips stay flat. Actions are now always visible (was hover-reveal). `.cb-col-act` widened 150→236px to fit the button row, and the card breakpoint raised 560→640px accordingly. `.cb-del-btn`/`.cb-dup-btn` restyle is scoped to `.cb-li-actions` (the editor's hardware/extras `.cb-del-btn` glyphs must stay bare). Verified in both table and card layouts incl. the amber selected row.
- 2026-07-05 (later) — **All quantity controls unified as −/+ steppers.** Two mismatched quantity inputs brought in line with the editor's count steppers (`.cb-rr .cl-stepper`): (1) the results-row qty stepper — previously flattened to transparent (from the highlight fix) — restyled to the same bordered surface2 shell with a white input; (2) the Extras qty field, a plain number input, converted to a real `−/+` stepper (new `cbStepExtra(lineId, idx, dir)` in cabinet.js, min 1, delegates to `cbUpdateExtra` which re-renders). The Extras stepper inherits the editor `.cb-rr .cl-stepper` styling automatically. Verified live: both match the Doors/Drawers/Shelves count steppers and the Extras +/− increments correctly.
- 2026-07-05 (later) — **Card layout: dims + material back to 2 separate lines.** The compact card's sub-line combined dimensions and material into one string joined by " · " (present since the original Part B build) — long material names got truncated, hiding either the dims or the material. Split into two separate `.cb-li-sub-mobile` spans (each its own line, each independently truncated only if that one value is too long) instead of one joined-and-truncated line.
- 2026-07-05 (later) — **Reverted the card 2-line split (a0bf552) — user wants the buttons row directly below one info line, as before.** Splitting dims/material into two separate lines pushed the actions row down a line; reverted to the single " · "-joined sub-line so the button row sits immediately beneath the name, matching the original card layout.
- 2026-07-05 (later) — **Fixed the card layout's stretched qty stepper.** Root cause: the card grid used 2 columns (`minmax(0,1fr) auto`) with areas `"name total" / "qty act"` — qty shared the wide 1fr column with name, so the stepper stretched to fill it (and the button row, squeezed into the narrow auto column, got clipped/pushed off-screen). Restructured to 3 columns (`auto minmax(0,1fr) auto`) with areas `"name name total" / "qty act act"` — name spans col1+col2 (still flexible, absorbed by the 1fr track), qty sits alone in col1 (auto-sized to the stepper's own width), and act spans col2+col3 (gets the flexible space for the button row). Verified: stepper now ~66px (was stretching to fill ~380px+), Add to Library + icons all fit on the line; desktop table mode (outside the container query) unaffected.
- 2026-07-05 (later) — **Card layout: action buttons back on their own bottom line (3-row card).** The prior fix (b72a814) kept the qty stepper and the button row on one line; the user wanted them split. Card grid is now 3 rows — `"name total" / "qty qty" / "act act"` (cols `minmax(0,1fr) auto`): info+total on top, the compact qty stepper on its own line, the action buttons (Add to Library · cut-list · dup · del) left-aligned + wrapping on the bottom line. `.cb-col-qty` gets `justify-self:start` + the stepper `width:max-content` so it shrinks to content instead of filling the full-width row (block-level flex would otherwise stretch it — same root cause as "too wide"). Desktop table mode is outside the container query and unaffected.
