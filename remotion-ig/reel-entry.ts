// Remotion entry for the 9:16 product reel. Named *-entry (root: ReelRoot.tsx)
// per Remotion's convention.
import { registerRoot } from 'remotion';
import { RemotionRoot } from './ReelRoot';

registerRoot(RemotionRoot);
